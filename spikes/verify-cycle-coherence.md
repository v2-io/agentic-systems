# Verification: closing independent coherence check of the accumulation-type-confound canon-integration cycle

*2026-05-19. Independent closing verification. Not canon. Does not edit any segment, any other agent's spike, or any staged/landed file — the only channel is the finding below; a corrective pass is separate and decided with Joseph. Fresh context by design: I did not continue or reuse any prior agent's working state. I read the committed canon files in full and re-derived the load-bearing structural claims (the singular-parameter coincidence and the homogeneity-degree obstruction) independently rather than accepting any artifact's summary, including the brief's and the adjudication's.*

*Author: independent verifier (Opus 4.7, 1M). Commits verified: `5158ed5`, `2f46f20`, `da3c6b4`, `aabcf8e`, `639e7e2` on `main`.*

---

## 0. Verdict

**Landing (b): corrigible findings.** The landed canon is mathematically correct, internally coherent, honestly tiered, and the load-bearing two-operator split is independently re-derivable — I re-derived it and concur. There is **no false canon and no incoherence in any segment body**. The findings are one genuine durability defect (F-1, the cited history layer is not in the committed tree), one notation-discipline lapse in a tracked decision aid (F-2), and one judgment call resolved in favor of the landed choice with a recorded vigilance seam (F-3, the predictive-side $\eta_k$). F-1 is the one that matters; it is breadcrumb-durability, not truth.

The cycle did the hard structural work correctly. The split that was executed — `#disc-m-preservation` was housing two distinct targets/operators ($\mathcal A_D$ predictive-sufficiency and $\mathcal A_{\text{refl}}$ identity-continuity), at opposite ends of one singular contraction parameter, neither superseding the other — is the structurally right answer, and the over-claim an earlier pass made (Lindley-is-the-linearization / subsumes) was caught, withdrawn, and is **absent from every landed canon body**. Seam #1 stayed corrected: it is flagged open / test-not-assert in `der-turnover`'s Working Notes, not asserted.

---

## 1. What I re-derived independently (so the concurrence is earned, not relayed)

The decisive structural claim the whole cycle rests on is that $\mathcal A_D$ (the SDPI-affine destroy-and-reconstruct recursion) and $\mathcal A_{\text{refl}}$ (the reflected Lindley/Loynes identity-gap walk) are irreducibly distinct operators, not one read in two ways — specifically that the reflected form is not the small-loss linearization of the affine form. I did not take this from the adjudication (which is itself one artifact in the chain and whose independence I cannot fully audit). I re-derived it from the established machinery.

The singular-parameter coincidence, re-derived: the affine recursion $I_{k+1}\le\eta_k I_k+a_k$ has accumulation resolvent $\mathcal A_D=\sum_{j\ge0}\bar\eta^{\,j}(\cdot)$ with operator norm $1/(1-\bar\eta)$, which diverges as $\bar\eta\to1^-$ — a genuine pole at $\bar\eta=1$. The reflected walk $g_{k+1}=(g_k+\xi_k)_+$ has Loynes representation $g_\infty\stackrel{d}{=}\sup_{n\ge0}S_n$ with $S_n/n\to\mu$ by Birkhoff, finite a.s. iff $\mu\lt0$; its entire load-bearing content is the boundary $\mu=0$ (Atkinson recurrence plus non-degeneracy gives $\sup_n S_n=+\infty$ a.s., no finite stationary law). The affine/Lindley correspondence maps contraction modulus to drift sign, so $\mu\to0$ is exactly $\bar\eta\to1$ is exactly $\lVert\mathcal A_D\rVert\to\infty$. A linearization is a small-perturbation approximation valid near a regular point; the $\mu=0$ boundary is not a regular point of $\mathcal A_D$, it is its pole — one cannot represent an object whose entire content lives at a parameter value as the linearization of an object that is singular at that same value. The "Lindley is the linearization / supersedes" framing inverts the actual relationship. This holds, and the landed bodies state precisely this and only this.

Second independent witness, the homogeneity-degree obstruction, re-derived and checked against canon: Model D ultimate bound $\propto1/\alpha$ (degree $-1$ in $\alpha$), Model S $\propto1/\sqrt\alpha$ (degree $-1/2$). A change of homogeneity *degree* cannot arise from a choice of norm on a fixed operator (norms are degree-preserving up to constants), so they are different functionals. I verified this is not a free-floating assertion: `01-aat-core/src/deriv-sector-condition.md` line 258 carries "Corollary A.1S.1: Disturbance-Model Containment Dichotomy" stating the first-exit probability is exactly the two-point set $\{0,1\}$, fixed by the disturbance model's support structure, $\alpha$-invariant. The `NOTATION.md` §"Accumulation typing" claim that the accumulation operator "is the bounded linear resolvent with norm $\sim\nu/\alpha$ under Model D … a structurally different mean-square functional under Model S … not one operator in several norms (verified: the $1/\alpha$ vs $1/\sqrt\alpha$ homogeneity change and the categorical Cor-A.1S.1 containment dichotomy)" is faithful to the canonical corollary, not invented for the convention.

Conclusion of §1: the two-operator split is structurally true, independently of every artifact in the chain. The cycle's central claim is sound.

---

## 2. Pressure-test results, item by item

**(1) No residual subsumption ghost anywhere in canon — CLEAN.** I grepped every touched body (`der-turnover`, `disc-m-preservation`, `der-identity-continuity-threshold`, `disc-stability-certificate`, `result-sector-persistence-template`, `form-composition-closure`, `hyp-the-three-deaths`) for `linearization` / `supersed` / `one normalization` / `same object` / `small-loss`. Every occurrence in a segment body is in *negation* — explicitly stating they are not a linearization or normalization of each other and neither supersedes the other. The one `superseded` in `der-turnover`:117 is precisely scoped ("superseded **for the predictive-sufficiency accumulation question only**") and immediately followed by the explicit distinct-operator/distinct-target statement; that is correct present truth, not a ghost. The der-turnover excisions and the disc-m-preservation omission-fix (the brief's highest-risk loci) are clean: `disc-m-preservation`'s body carries no embedded falsehood; its accumulation paragraph is $\mathcal A_D$-faithful and the cross-reference to the identity operator is present and correct.

**(2) integration-is-replacement integrity — CLEAN in bodies.** The deleted additive inequality in `#disc-m-preservation` ($\epsilon^{(n)}=\sum_k\Delta\epsilon_k$ with the break-even $\mathbb E[\Delta\epsilon_k]\le\mathbb E[\Delta I_k]$) is gone from the body; the history sits in Working Notes under an explicit "History (not present truth):" marker. The re-typed `#form-composition-closure` Open rows are present truth in the table and Discussion, with the false "decides whether very large teams are representable as single AAT agents at all" line *explicitly withdrawn* in the body text ("**withdrawn** — it was the dissolved mis-frame, not a live open question"), and history relegated to Working Notes with "History (not present truth)" markers. No softened pointer-ghosts in any body. (The one durability gap is at the *spike history layer*, not the segment bodies — F-1.)

**(3) Honest tiers — CLEAN.** `der-identity-continuity-threshold` is `status: conditional` with an exact core, and explicitly says a flat-"exact" landing "would itself be an integration-is-replacement failure (sharp-but-mis-tiered replacing soft-but-honestly-tiered)"; (M-ADD)/(M-FREE)/(C-S) are carried at channel-independence prominence, exactly as required. The `#form-composition-closure` strategy-DAG row's Q1 is a *named* sub-open ("a named sub-open, not a reopening"), stated in the table, the Discussion divider, and the Working Note — not a silent reopening. Seam #1 (whether the $\mathcal A_D$ R1/R2 no-go is genuinely novel vs a bridge-lemma-resolvent instance) is flagged in `der-turnover`'s Working Notes verbatim as "genuinely open — orthogonal to this landing and **not asserted from structural feel (the trigger to test, not assert)**," matching `RECONCILIATION.md` §4. I traced the over-claim's history: an earlier pass (RESULT.md §5, embedded Review) did assert the subsumption; it was caught by RECONCILIATION, withdrawn there, and the withdrawn claim does **not** appear in any landed canon body. It stayed corrected. ✓

**(4) Cross-ref + volume coherence — CLEAN.** All `#…` refs resolve to the right landed segments (verified by slug lookup across all four `src/` trees). F-ADJ-2 holds exactly: `der-turnover` (Volume III) `depends:` is precisely `result-sector-persistence-template`, `obs-context-turnover`, `form-information-bottleneck` — **no III→IV `depends:` edge**; the `#der-identity-continuity-threshold` reference is prose-only. `der-identity-continuity-threshold` is Volume IV with IV→IV and IV→I deps only. `#obs-context-turnover` is Volume III (`03-llm-core/src/`). The OUTLINE rows in both `03-llm-core/OUTLINE.md` and `04-eli-core/OUTLINE.md` are accurate to the segment content; the `hyp-the-three-deaths` IV→III prose cross-ref correctly points at `#der-turnover-information-recursion` and frames CONSORTIA/MEMORATA as the provably-necessary forcing channel, faithful to R1/R2.

**(5) The $\eta$ / $\varrho_{\text{rg}}$ situation — verified, with one judgment call (F-3).** The rename to $\varrho_{\text{rg}}$ is contained to `der-identity-continuity-threshold`'s scope with an explicit Symbol-note; the projected-floor disturbance keeps $\rho_k$. `#result-persistence-condition` was *not* crossed: its last commit is the pre-cycle AAD→AAT rename and the cycle diff against it is empty; its line-61 per-dimension $\eta_k$ is intact and untouched. The independent judgment call (F-3 below) is whether the *predictive-side* SDPI coefficient $\eta_k$ in `der-turnover` / `disc-m-preservation` is a residual collision with that line-61 $\eta_k$.

**(6) Lint of added content — CLEAN; pre-existing debt confirmed genuinely pre-existing.** All cycle-added content lints clean (`der-turnover`, `der-identity-continuity-threshold`, `disc-m-preservation`, `disc-stability-certificate`, and the NOTATION §Accumulation-typing block at L206-212 all return "All clean"). Every lint hit in a touched file is in a line-span the cycle did not touch, verified by diff and `git log -L`: `result-sector-persistence-template` L75/L130 were last touched by the AAD→AAT rename commit `08b6f40` (pre-cycle); `form-composition-closure` L216/230/246/299 are outside the cycle's added rows; `hyp-the-three-deaths` L46 is pre-existing (cycle added L76-77); `NOTATION.md` L19/L20 is the standing 2026-05-18 $\rho$ drift caveat (cycle added L206-212). This matches the brief's tracked-seam list exactly. The discipline (line-span distinction, not global count) is satisfied.

**(7) The tracked-seam list — present and durable, with one exception (F-1).** `msc/for-joseph-acc-confound.md` is tracked (committed), 20 KB, and carries the D1-D4 decision structure including the $\eta\to\varrho_{\text{rg}}$ hard precondition and the conditional-with-exact-core tier requirement. The named reasoning-trail spikes (`spike-accumulation-type-confound`, `spike-strategy-dag-composition`, `spike-composition-scaling-N`, `verify-A-restatement-model-DS`, `verify-cdmp-corrected-statement`, `adjudicate-disc-m-preservation-operator`, `continuity-persistence/99-verdict`) are all committed. The INDEX/CHANGELOG-prose reconciliation to the two-operator frame is explicitly logged as a tracked follow-on in the CHANGELOG 2026-05-19 "Seam flagged for the verifying agent" paragraph — that seam is genuinely tracked, not buried. The exception is F-1.

---

## 3. Findings

### F-1 (corrigible; the one that matters) — the committed canon cites a history layer that is not in the committed tree

The committed `CHANGELOG.md` 2026-05-19 (line 40) states: "**RECONCILIATION.md** (the continuity steward's withdrawal of the symmetric-dual subsumption) **is the artifact whose position survived structural scrutiny**," and (line 42) cites "`RECONCILIATION.md` §4's *open* question" as the authority for the Seam #1 test-not-assert flag. Both landed segments' Working-Notes / Landing-context cite it the same way (`der-turnover`:129-130, `der-identity-continuity-threshold`:133). But `spikes/continuity-persistence/RECONCILIATION.md` is **untracked** (`git ls-files` does not list it; it exists only in the working tree), and `spikes/continuity-persistence/RESULT.md` has **uncommitted modifications**: the §5 over-unification *withdrawal callout itself is not committed* — `git show HEAD:…/RESULT.md` has zero matches for the withdrawal text, so the committed `RESULT.md` still carries the refuted "Lindley/Loynes is the small-contraction linearization … subsumes" claim as unmarked present-tense spike text with no obstruction marker.

Why this is corrigible, not false canon: the segment bodies are mathematically correct, self-contained, and do not structurally depend on the spike content (the math lives in the segments, correctly, per math-lives-in-segments). The two-operator split is independently re-derivable (§1). The defect is breadcrumb durability, not truth — exactly the class the project's own `durability-claims-by-tool-action` and `spike-references-only-in-Working-Notes` disciplines target. But it is a real integration-is-replacement gap *at the history layer the canon points to*: a `git clone` reproduces canon that (a) names a position-of-record file absent from the tree, and (b) points readers at a committed `RESULT.md` whose surviving text asserts a claim the cycle refuted, with the withdrawal living only in an uncommitted working-tree edit. The "if I dropped dead at end of turn" test fails for the cited history layer.

Precise corrective (separate pass, with Joseph): commit `RECONCILIATION.md` and the `RESULT.md` §5-withdrawal modification (and verify `RESULT.md`'s embedded Review over-reach — the one the adjudication §4 named — is dominated by the §5 callout in the committed state, or annotate it so the committed spike trail does not present a refuted claim as live). Scope is the spike history layer only; no segment body needs to change.

Note on self-disclosure: the older D1 sub-entry of the same CHANGELOG date self-discloses an uncommitted state ("at time of writing," "pending the commit-when-asked gate"), and the two-operator sub-entry says the segment work was "uncommitted … pending the commit-when-asked gate" — but the segments *did* land in `da3c6b4`, so that prose is now stale relative to its own commit (it describes its content as uncommitted while committing it). That staleness is cosmetic and self-referential; it does not change F-1, which is about the *trail* files, not the segments.

### F-2 (corrigible; low stakes) — the tracked decision aid violates the math-formatting discipline this cycle codified

`msc/for-joseph-acc-confound.md` is tracked and is written in bare Unicode math and hard-wrapped paragraphs (e.g. `ℛ`, `𝒜_refl`, `[contraction]/[critical]/[absorbing]`, `≤`, `η_k` outside `$…$`). The CLAUDE.md change *landed in this very cycle* (commit `aabcf8e`, item 6 self-reminder) names exactly this trap — an `msc/` decision memo shipping Unicode-math because the register feels exempt — and cites the 2026-05-18 `msc/for-joseph.md` instance. The memo predates the same-cycle discipline-tightening by hours, so it is not strictly a regression, but the seam-tracking aid itself instantiates the trap the cycle was codifying against. Low stakes (delete-at-any-time `msc/` working doc), surfaced for honesty and because it is the live decision aid Joseph is meant to read. Corrective: re-render to delimited LaTeX + one-logical-line paragraphs in a separate pass, or accept as transient scratch and let it expire with the thread.

### F-3 (judgment call; resolved in favor of the landed choice, vigilance seam recorded) — the predictive-side $\eta_k$

`disc-m-preservation` `depends:` on `result-persistence-condition` and (via prose + the `der-turnover` cross-ref) uses $\eta_k$ for the SDPI contraction coefficient in $I_{k+1}\le\eta_k I_k+a_k$. `result-persistence-condition` line 61 uses $\eta_k$ for the per-dimension Model-S persistence threshold quantity. My independent call: this is **acceptable standard notation disambiguated by context, not a residual collision requiring a rename** — but it is a genuine vigilance seam worth recording, not dismissing. Reasoning: (a) $\eta$ for the SDPI / contraction coefficient is the field-standard symbol (Ahlswede-Gács, Polyanskiy-Wu, both cited in `der-turnover`), so renaming it would *introduce* non-standard notation to avoid an in-repo clash — the opposite tradeoff from the identity side, where $\varrho_{\text{rg}}$ had no field-standard claim and collided with both $\eta_k$ *and* $\eta^\ast$ *and* was newly introduced (the identity-side rename was correctly made for those reasons; this side's reasons do not transfer); (b) `result-persistence-condition`'s $\eta_k$ does not surface anywhere in `disc-m-preservation`'s body — its line-61 per-dimension form points onward to a *further* segment (`#result-per-dimension-persistence`) and is not invoked in `disc-m-preservation`'s reasoning, which uses only the $\alpha\gt\rho/R$ headline; (c) the type distinction is sharp and self-evident in context (a coefficient $\in[0,1]$ multiplying a state vs a rate on the greater-side of a persistence inequality), and the two $\eta_k$'s never co-occur in a single segment's exposition. The landed choice (rename identity side only) is the right one. Recorded vigilance seam: if any future segment ever pulls both per-dimension persistence and turnover-SDPI into one scope, the predictive-side $\eta_k$ should be revisited then.

---

## 4. One-paragraph summary for the corrective-pass decision

The cycle is structurally correct and honestly tiered; I re-derived the load-bearing singular-parameter coincidence and the homogeneity-degree obstruction independently and concur — the two-operator split is true, not artifact-relayed, and the earlier subsumption over-claim was caught, withdrawn, and is absent from every landed canon body. No false canon, no incoherence in any segment. One corrective is worth a separate gated pass: F-1 — `spikes/continuity-persistence/RECONCILIATION.md` is untracked and `RESULT.md`'s §5-withdrawal is uncommitted, while the committed CHANGELOG and both new segments cite them as the position-of-record and the Seam #1 authority, so the committed canon points at a history layer not reproducible from a clean clone and the committed `RESULT.md` still carries the refuted subsumption unmarked. F-2 (the tracked decision aid violates the same-cycle-codified math-formatting discipline) and F-3 (predictive-side $\eta_k$, judgment-called acceptable with a recorded vigilance seam) are lower stakes. The segment bodies need no change; the corrective is committing the spike history layer truthfully.
