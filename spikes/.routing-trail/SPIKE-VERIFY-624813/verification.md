# SPIKE-VERIFY-624813 — Independent-verify gate: S1 `integrated-misfiled` items

*Verifier: independent confirmer ≠ S1 adjudicator (SPIKE-WORKING-417303). Slice =
the two items the S1 adjudication dispositioned `integrated-misfiled`:
`spike-bridge-lemma-nonlinear-strengthening-2026-04-24` (§7.1 only) and
`spike-fenchel-bregman-reframe-additive-coordinate-forcing-2026-04-24` (both halves).
Decisive test per `doc/spike-routing.md` §2/§7: load-bearing content present in a
`src/` segment, **verified first-hand by opening the actual loci** — not the INDEX
label, not the adjudication's own summary. Every locus below was opened directly in
this pass; git provenance used as the §7-sanctioned non-destructive instrument where
un-poisoned. No moves / edits / commits / segment changes — read/verify only; the
durable batch is the parent's.*

---

## Headline

**Both `integrated-misfiled` dispositions CONFIRMED.** Each spike's load-bearing
content is genuinely present in the named `src/` loci, first-hand verified, and
genuinely covers what the spike carried (in the fenchel-bregman case, more fully
than INDEX records). The S1 adjudication's two named claims hold under independent
re-opening. The §7.1-vs-§7.2 boundary on the bridge-lemma spike is drawn correctly:
§7.1 is in canon; §7.2 is genuinely absent (the residual CL-1 orphan strand) — the
adjudication's boundary is right and should not be merged.

No refute. Two corroborating side-findings the parent should act on at cycle-close
(both already flagged by S1; independently re-confirmed here): two distinct
stale-INDEX entries pointing in the *dangerous* (true-landing-recorded-as-unlanded)
direction.

| Spike | S1 disposition | Independent verdict | Loci re-opened first-hand |
|---|---|---|---|
| bridge-lemma (§7.1 only) | `integrated-misfiled` | **CONFIRMED** | `result-contraction-template.md` (full), `form-composition-closure.md:141-294`, `deriv-sector-condition.md:84-96`, `impl-cooperative-adversarial.md` (full) |
| fenchel-bregman (both halves) | `integrated-misfiled` (INDEX understated) | **CONFIRMED — incl. the understated-INDEX claim** | `deriv-strategy-cost-regret-bound.md:172-200`, `disc-additive-coordinate-forcing.md:1-90`, `spikes/INDEX.md:106` |

---

## 1. `spike-bridge-lemma-nonlinear-strengthening-2026-04-24` — §7.1 only

**S1's load-bearing claim:** the §7.1-minimal content (Route 2 §3 DA2'-inc ≡
(CT2)-at-$M=I$ Euclidean-metric-α₁ reclassification; the N1 Lipschitz-floor no-go;
the N2 adversarial three-obstruction no-go) is fully in `src/`; §7.2 (passivity /
dissipativity route + Class-1/2/3 port-structure) is a *separate* orphan strand,
nowhere in `src/`. Disposition therefore `integrated-misfiled` for the file (its
primary §7.1 purpose served), with §7.2 held as the CL-1 orphan.

### 1.1 §7.1 strand A — DA2'-inc ≡ (CT2)-at-$M=I$ + Euclidean-α₁ reclassification

**CONFIRMED, first-hand.** The equivalence and the theorem-import→AAT-internal
reclassification are present in *both* named segments, and `form-composition-closure`
carries *more* than the spike's §7.1 asked for (the spike requested one Discussion
paragraph; canon has a dedicated `*[Derived, status: exact]*` equivalence block):

- `result-contraction-template.md` — opened in full (222 lines). The S1-cited lines
  check out: **L75** (incremental Euclidean sector condition "equivalently
  **DA2'-inc**"), **L77** ("(CT2) at $M=I$ … matching DA2'-inc … collapses (T2) and
  DA2'-inc into one condition"), **L83** ("Sub-scope metric-α₁ … **AAT-internally
  derived via DA2'-inc ≡ (CT2) with M=I**"), **L130** ("The Jacobian-level
  observation … AAT has been carrying the Jacobian-level Euclidean contraction
  condition … all along"), **L132** ("**AAT-internally derived via DA2'-inc
  equivalence** rather than theorem-imported"), **L197** (Related-Work row naming the
  "Rockafellar-Wets / Nesterov equivalence (DA2'-inc ⇔ Jacobian-symmetric-part PD ⇔
  (CT2)-at-$M=I$) … the standard monotone-operator identity AAT uses to make the
  Euclidean metric-α₁ case AAT-internally derived"). S1's remaining cited lines
  (170/176/186/188/212) are the bridge-lemma tier-carryover, meta-pattern
  restatement, Findings, and Working-Notes landing-context — all present and
  consistent; none load-bearing on their own but none misattributed.
- `form-composition-closure.md` — opened L141–294. **L210** "The Jacobian-level
  observation" + **L220–230** a full `*[Derived, status: exact]*` block
  **"DA2'-inc ≡ (CT2) at $M=I$ — structural-transparency equivalence"** with the
  three-way (a)/(b)/(c) equivalence and the exact spike §3.2 citation chain
  (Rockafellar & Wets 1998 *Variational Analysis* Cor 12.4; Nesterov 2004 §2.1.3
  Thm 2.1.9). **L230** "Implication for AAT-internal derivability" converts the
  Euclidean sub-scope "from 'theorem-imported from Lohmiller-Slotine' to
  'AAT-internally derived via a standard monotone-operator identity' … no new axiom
  required" — verbatim-in-spirit to spike §3.3 / §7.1.
- Operator-family cross-side also present at `deriv-sector-condition.md:84` (A2'/DA2'
  α/β as Rockafellar-1970 / Bauschke-Combettes one-point vs two-point strong
  monotonicity) — consistent, not the load-bearing locus but corroborating.

**Git provenance (un-poisoned, decisive-supporting):** `git log -S"DA2'-inc ≡ (CT2)
at" -- form-composition-closure.md` → **`b76ee67` "2026-04-24 pressure-point cycle:
Tier 1 landing"** — the spike's *own cycle commit* (spike dated 2026-04-24, Gemini
pressure point #1). The §7.1 content entered `src/` in the spike's cycle; it is not
a sweep artifact. (The other two pickaxe probes land on AAD→AAT rename commits
`08b6f40`/`9745397` = sweep-poisoned recency, exactly as `doc/spike-routing.md` §7
warns; content-presence already first-hand-confirmed, so this does not weaken the
verdict.)

### 1.2 §7.1 strand B — N1 Lipschitz-floor no-go

**CONFIRMED, first-hand.** `deriv-sector-condition.md:96` is precisely the spike's
§5.1: `*[Derived, status: exact scope-exit statement]*`; "structural scope-exit for
contraction-based bridge-lemma analysis"; the *exact* spike counterexample
($F(\delta)=\alpha\delta$ for $|\delta|\lt1$, $F(\delta)=\alpha\delta+\mathrm{sign}(\delta)$
for $|\delta|\ge1$; $f_c$ jumps by $\eta^\ast$ at $|X|=1$); the van der Schaft &
Schumacher 2000 + Di Bernardo-Liuzza-Russo 2014 external apparatus; and the
`#disc-identifiability-floor` Instance-2 regime-C composition — i.e. the spike's
§5.1 "Composition with `#disc-identifiability-floor`" landed too. Textbook 5A/5B
no-go landing, faithful.

### 1.3 §7.1 strand C — N2 adversarial three-obstruction no-go

**CONFIRMED, first-hand.** `result-contraction-template.md:144-152` ("Strategic /
adversarial equilibria — three independent convergent obstructions") itemizes
exactly the spike §5.2 obstructions: (i) Slotine 2003 §III saddle-point, (ii) Khalil
2002 ch.6 Thm 6.4 passivity-universality, (iii) Daskalakis et al. 2018 last-iterate
non-convergence; L152 hands off to `#deriv-strategic-composition` as the spike
recommended. Findings Related-Work row L201 records it as "*contradicted by this
finding* (in the sense of correctly bounding scope)"; Search-Log L207 records the
spike provenance. Chapter-level surfacing independently confirmed at
`impl-cooperative-adversarial.md:47-55` ("Three adversarial obstructions to
contraction — the equilibrium hand-off"), catalogued at its L68 as finding #24.

### 1.4 §7.1/§7.2 boundary — independently confirmed correct

The adjudication's split (file → `integrated-misfiled` on §7.1; §7.2 = separate
CL-1 orphan, do **not** merge) is right:

- `ls 01-aat-core/src/ | grep -i dissipat\|passiv` → **empty**: no
  `#dissipativity-template` segment exists.
- `grep -ni "willems|clean port|leaky port|storage function|port-structure|positive-real"
  der-directed-separation.md` → **empty**: the §7.2 Class-1/2/3 port-structure /
  Willems-storage reading is genuinely absent.

So §7.2 (the constructive heterogeneous Kalman+PID-on-positive-real-plant
$\mathcal L_2$-stability route + the novel Class-1/2/3 port reading) is genuinely
not in canon. The only passivity mentions in canon are the **N2 negative** use
(passivity-universality *fails* adversarially) — already counted under §7.1, not the
§7.2 constructive result. The boundary is at the right place; merging §7.2 into the
misfiled disposition would double-orphan the port-structure observation (which is
*novel*, not just textbook-composition). S1's instruction — `git mv` only **after**
the §7.2 orphan is captured, co-land §7.2 with sibling `spike-passivity-composition`
— is sound and should be preserved (this is the CL-1 cluster in the tracker).

**Verdict 1: CONFIRM `integrated-misfiled` for the §7.1-minimal content.** Boundary
correct; §7.2 remains the CL-1 orphan. Safe for the parent's durable `git mv` →
`.integrated/` once §7.2 is captured per the CL-1 plan.

---

## 2. `spike-fenchel-bregman-reframe-additive-coordinate-forcing-2026-04-24`

**S1's load-bearing claim (the stronger one):** *both* halves landed — §6.3
Bregman-Fenchel dual-pair identification **and** the full §7.1 meta-segment reframe
(Reframe A: "one geometric object / four layer-specific manifestations /
Legendre-Fenchel geometry", incl. the axiom-independence guard) — and INDEX line
106 is **understated** (records the meta-reframe as an *unlanded* Tier-3
architectural proposal when it is in fact canon). Disposition `integrated-misfiled`.

### 2.1 §6.3 Bregman-Fenchel dual-pair identification

**CONFIRMED, first-hand.** `deriv-strategy-cost-regret-bound.md:182-196`
(S1 said 180–196; the §6.3 block proper is 182–196 — L180 is the
Čencov/Morozova-Chentsov background; trivially-off-by-two start line, substance
exactly where claimed and complete): heading "§6.3 — Bregman-Fenchel
identification: reverse-KL and log-odds as dual coordinates"; tag *[Derived
(bregman-fenchel-dual-pair, exact; standard Legendre-Fenchel)]*; negative-entropy
potential / log-partition Fenchel conjugate / softmax primal-dual / log-odds ratio /
reverse-KL Bregman identity with full $\square$ derivation = spike §1.2 verified
computation; L194 "Identification with `#deriv-edge-update-natural-parameter`" (the
log-odds as Fenchel-dual natural parameter — the spike's central claim);
Amari & Nagaoka 2000 §3.5 / Bregman 1967 / Beck & Teboulle 2003 citations.

### 2.2 §7.1 full meta-segment reframe (Reframe A) — the stronger half

**CONFIRMED, first-hand, essentially in full.** `disc-additive-coordinate-forcing.md`
opened L1–90:

- **L14** "**these four layers are layer-specific manifestations of a single
  geometric object**: the exponential-family Legendre-Fenchel structure" — Reframe A
  verbatim; same line explicitly retires the old framing ("previously appeared as a
  '1-anchor-plus-3-theorem' structure").
- **L20–30** "The underlying geometric object" section — negative-entropy potential
  / log-partition conjugate / softmax / reverse-KL Bregman / Fisher = Hessian of
  dual potential, with Amari & Nagaoka 2000 §3.5 / Bregman 1967 / Rockafellar 1970 /
  Bauschke-Combettes 2017 = spike §7.3 deliverable #2.
- **L32–44** recast four-instance table (Layer / AAT-internal axiom / Uniqueness
  mechanism / Forced coordinate / Relation to exponential-family geometry / Source
  segment) = spike §7.3 deliverable #3.
- **L50** "**The convergence across independent axioms is itself the meta-pattern's
  substance — not a byproduct to be compressed into a single axiom.**" — the spike's
  §7.1 axiom-independence "do not over-unify" guard, landed **verbatim**. This is
  the specific load-bearing guard S1 flagged; present word-for-word.
- **L52–56** Čencov-Fenchel relationship + scope dependency = §7.3 #5.
- **L58–78** adjacent-cases reclassification (Lyapunov = Euclidean-potential
  Bregman; IB = imported-provenance; (AV) = squared-norm potential) + the diagnostic
  filter = §7.3 #4 + the §8 candidate-filter.
- **L80–88** complementarity-with-other-meta-segments section = §7.3 #6.

That is all seven of the spike's §7.3 deliverables. The spike's remaining spike-only
items (Gaussian-case walkthrough §9.2; the speculative second parallel meta-pattern
for (AV) §8.1; Amari-Nagaoka theorem-number PDF-granularity §9.2) are honest
follow-ons explicitly flagged in the spike's *own* §9.2/§9.3 caveats — not orphaned
load-bearing content. Reframe A is in canon, faithfully and essentially complete.

### 2.3 The understated-INDEX claim — CONFIRMED, dangerous direction

`spikes/INDEX.md:106` reads: "Local identification LANDED Tier 1 … **Full
meta-segment reframe → Tier 3 architectural proposal: … Seven-item meta-segment
rewrite per spike §7.3 deserves architectural-proposal entry.**" First-hand
refutation: the meta-reframe **did land** (§2.2 above), so INDEX records a
fully-landed canonical result as an *unlanded* proposal — the rarer, more dangerous
INDEX error (would invite a future agent to re-open and re-litigate closed canon).
Independently corroborated by INDEX's *own* internal inconsistency: the bottom
"2026-04-23 Gemini Gap A/B — PROMOTIONS LANDED" cycle-summary already lists
`#additive-coordinate-forcing`'s 4th-primary-instance landing + satellite
cross-references, contradicting L106's "Tier 3 unlanded." **Flag for the parent
(cycle-close, per S1):** reconcile INDEX L106 to "fully landed (both §6.3 and the
meta-reframe)". This is the second worked instance of the `doc/spike-routing.md`
Refinement-1 scar ("the convenience-label is unreliable in *both* directions");
sub-shape = true-landing-recorded-as-unlanded (distinct from the pilot's
external-block-encoding sub-shape).

**Git provenance (sweep-poisoned — *not* the decisive instrument here):** all three
pickaxe probes (`single geometric object`, the axiom-independence guard,
the §6.3 heading) land on **`9745397` "Rename AAD→AAT … Stage 2a"** (2026-05-15).
The rename sweep rewrote these segments wholesale (slug/dir rewrite forced
content rewrite), so pickaxe cannot see through it to the original landing commit.
This is the recency-poisoning `doc/spike-routing.md` §7 explicitly warns about — and
exactly why §2/§7 make **content-in-`src/` first-hand** the decisive test, *not*
git. The content is unambiguously present and faithful; the verdict rests on the
first-hand read, not provenance.

**Verdict 2: CONFIRM `integrated-misfiled` (both halves landed; INDEX L106
understated in the dangerous direction).** Safe for the parent's durable `git mv` →
`.integrated/`, with the L106 reconciliation flagged for cycle-close.

---

## 3. Side-findings for the parent (no action taken here)

1. **Two distinct dangerous-direction stale-INDEX entries** in S1's slice, both
   true-landing-recorded-as-unlanded: (a) INDEX L106 fenchel-bregman ("Full
   meta-segment reframe → Tier 3 architectural proposal" — landed); (b) the
   bridge-lemma §7.1 row at INDEX L106-block is *correctly* "LANDED Tier 1" — that
   one is accurate; the dangerous error is specifically the fenchel-bregman row.
   Recommend reconciling the fenchel-bregman row at cycle-close, as S1 already
   flagged.
2. **§7.1/§7.2 boundary preservation is load-bearing.** The bridge-lemma `git mv`
   must not fire before the §7.2 port-structure observation is captured into the
   CL-1 landing plan — it is the only durable home of that *novel* (not merely
   textbook-composition) Class-1/2/3 port reading. S1's "move only after capture"
   instruction is correct; re-confirmed here.
3. **Git was poisoned for fenchel-bregman, un-poisoned for bridge-lemma.** Data
   point for the §7 evidence-hierarchy: within a *single* slice, pickaxe was
   decisive-supporting for one spike (the cycle-commit `b76ee67` survived) and fully
   poisoned for the other (rename sweep `9745397` ate the provenance). The
   first-hand content read was the only instrument that worked for *both* — i.e. the
   §2/§7 "content-in-src first-hand is decisive, git is supporting-only" ordering
   held exactly. No frame change recommended; offered as a corroborating data point
   for the existing scar.

---

*No segment files, spikes, INDEX entries, MANIFEST entries, moves, or commits were
made. Read/verify only, per the independent-verify gate (`doc/audit-routing-instructions.md`
§8). The durable batch is the parent's.*
