---
spike: strategic-composition-class-3-attempt
file: 05-INTEGRATION-PLAN
parent: 00-FRAMING.md
prior: 01-STRENGTHEN-ATTEMPTS, 02-REFRAME-INSIGHT, 03-DYNAMIC-REGIME-AXIS, 04-MATURITY-CHECK, 99-VERDICT
purpose: tentative integration plan — everything this spike has surfaced, with conditional paths for the two background spikes (06-PILLAR-PRIOR-ART and spike-identifiability-floor-instance-6) and the Joseph-reserved decisions
status: tentative — to be refined once background-spike returns land
---

# Tentative integration plan — the whole kebab

This file consolidates everything the strategic-composition-class-3-attempt spike has surfaced into a single ordered landing plan with explicit conditionals on the two background spikes and on Joseph-reserved decisions. It does not commit to any path — it lays out the path-set and the gates.

## §I.1 What this spike has surfaced (recap)

Across five files (00–04 + 99), this spike has produced:

**(S1) Two corrective findings on existing canon.** From the strengthen-first attempt — the Cournot witness against the unconditional Class 3 claim, the conflation diagnosis on the current Class 2 claim, and the GUC-rename-residue discovery in `#impl-strategic-composition`. These land regardless of what else happens.

**(S2) The dynamic-regime axis surfacing.** §B four-tier ladder (R0/R1/R2/R3) formalized as a structural classification of composite agents by joint-dynamics fixed-point structure plus Lyapunov certificate available. §C derivable content (the macro-state-type / regime identity for finite $N$, the regime-transition asymmetry no-go, the Lyapunov-machinery transfer R0 → R1). §F mapping showing the axis lives implicitly across ten+ existing segments.

**(S3) Cross-axis observation.** §G — two-axis picture (Axis A architectural class × Axis B dynamic regime) is independent, full cross-product is meaningful. Wrapping construction is regime-independent; alignment work is architectural-class-independent. The two-axis observation has a *parallel-pattern* shape (each axis admits a three-operation modularity-like dynamics — alignment-strengthening / strategic-divergence / adversarial-divergence on Axis B parallel to truthification / strategic-self-coupling / adversarial-coupling-pressure on Axis A).

**(S4) Two candidate floor instances (sketched, not derived).** §H.5 — Instance 6 candidate (regime-identification from component marginals). The §C.3 transition no-go could itself be a separate floor instance (regime-ascent impossibility from purely-dynamical operations).

**(S5) The maturity assessment.** Three landing standards (segment / meta-segment / standalone paper) with explicit blockers named for each. The §M.6 one-line recommendation: schedule Pillar prior-art search; conditional on its result, draft `disc-dynamic-regime-axis` as discussion-grade; defer meta-segment status until Instance 6 floor derivation and `#disc-modularity-state-dynamics` both land.

## §I.2 Background spikes in flight

Two spikes launched 2026-05-21 in parallel:

**(BG1) Pillar prior-art search.** Output → `spikes/strategic-composition-class-3-attempt-2026-05-21/06-PILLAR-PRIOR-ART.md`. Targets the four-tier classification, the macro-state-type identity, the regime-transition asymmetry no-go, and the candidate Instance 6 against five adjacent literatures (control theory, game theory, multi-agent learning, evolutionary game theory, mean-field games). Output is a per-target Pillar verdict with bibliography.

**(BG2) Identifiability-floor Instance 6 derivation.** Output → `spikes/spike-identifiability-floor-instance-6-2026-05-21.md`. Attempts to derive the candidate Instance 6 per the schema of Instances 1–4, with the Liberzon 2003 coupling-sign-bit unidentifiability as the closest precedent. Either succeeds with a structural escape characterization, or returns a no-go on the candidate honestly.

Both are pure spike work — no canon changes. Both feed §I.4 decision branches below.

## §I.3 Decision tree

```
                        [Spike content as of 2026-05-21]
                                     │
                                     ▼
                         (S1) Corrective findings ─────────────────────────┐
                                     │                                     │
                                     │ (always land)                       │
                                     ▼                                     │
                        Phase 1: Foundation fixes                          │
                        (resolves contradiction;                           │
                         non-discretionary)                                │
                                     │                                     │
                                     ▼                                     │
                  ┌──────────────────┴──────────────────┐                  │
                  │                                     │                  │
            [BG1 returns]                         [BG2 returns]            │
            Pillar verdict                        Instance 6 outcome       │
                  │                                     │                  │
        ┌─────────┴─────────┐                  ┌────────┴───────┐          │
        │                   │                  │                │          │
   "Novel/Adj"        "Substantial         "Derived"        "No-go"        │
   (proceed)          overlap"              (proceed)       (document)     │
        │             (re-scope)                │                │         │
        ▼                   │                   ▼                ▼         │
  Phase 2a:                 ▼              Phase 3a:        Phase 3b:      │
  Draft segment       Phase 2b: Pull   Land floor     Document            │
  `#disc-dynamic-     back to axis-    Instance 6     no-go in spike      │
   regime-axis`       internal to                                          │
                       AAT only                                            │
        │                   │                   │                │         │
        └───────┬───────────┴───────────┬───────┴────────┬──────┘         │
                │                       │                │                 │
                ▼                       ▼                ▼                 ▼
        Phase 4: Truthful-fix cascade across canon (the §V.2 (C2)–(C7) work,
        plus axis-surfacing per the §F mapping table, plus §I.1 (S1) corrections
        in their final form)
                                        │
                                        ▼
                    Phase 5: Cross-segment ripple — annotate ten+ segments per §F
                                        │
                ┌───────────────────────┼───────────────────────┐
                │                       │                       │
                ▼                       ▼                       ▼
        [BG2 derived]         [`#disc-modularity-       [BG1 = "Wholly Novel"
        AND                    state-dynamics`           or "Substantial
        meta-segment           landed]                   Novelty"]
        complementarity        AND                       AND
        unlocked               parallel-pattern          standalone-paper
                               observation               candidacy reachable
                               nameable                       │
                │                       │                       │
                └───────────┬───────────┘                       │
                            │                                   │
                            ▼                                   ▼
            Phase 6: Meta-segment promotion          Phase 7: Standalone-paper
            (`#disc-dynamic-regime-axis`             trajectory (B-N-Dyn portfolio
             elevated to peer of                     entry; Joseph-reserved)
             `#disc-separability-pattern` etc.)
```

## §I.4 Phase-by-phase plan

### Phase 1 — Foundation fixes (non-discretionary; can proceed immediately)

**Scope:** resolve the inter-segment contradiction documented in `00-FRAMING.md` §1 and `99-VERDICT.md` (F4). This is independent of all background spikes and Joseph-reserved decisions — the contradiction exists in current canon regardless of what the dynamic-regime axis becomes.

**Two options** (per `99-VERDICT.md` (C1)):

- **(1a) Conservative fix.** Swap "Class 3 (Coupled)" → "Class 2 (Partial)" in the four occurrences in `#impl-strategic-composition` (§"Strategic composition lifts contraction to equilibrium" three times; Working Notes finding-`#23` reference once). Add the 2026-05-09 GUC migration note to the segment's Working Notes. This preserves the current canon position (Class 2 claim from `#deriv-strategic-composition`) while resolving the literal contradiction.

- **(1b) Truthful fix.** Defer Phase 1 to Phase 4 — do the full architectural-class pullback together with the axis surfacing, replacing architectural-class language with dynamic-regime language across all affected segments at once.

**Recommendation:** (1a) executes in a single sub-cycle and is reversible; (1b) is the integration-is-replacement landing per the math but is larger scope. **If Phase 4 lands within a week of this spike, (1b) is cleaner.** If Phase 4 takes longer (waiting on Pillar / Instance 6), execute (1a) as an interim fix so canon is not internally contradictory for the gap period.

**Estimated effort:** (1a) = single edit cycle (~10 minutes of authoring + commit). (1b) = bundled into Phase 4.

**Pre-commit hygiene:** none of Phase 1's changes touch the canon-modifying-spike scope ("canon" here = `01-aat-core/src/impl-strategic-composition.md` — yes that *is* canon). Per the CLAUDE.md *Working Conventions* discipline `feedback_commit_before_canon_modifying_spike`, commit any prior canon-touching work before starting Phase 1. Current `git status` shows clean working tree, so nothing to commit first.

### Phase 2 — Pillar-conditional segment authoring

**Gate:** BG1 returns. Decision branches on the Pillar verdict.

**(2a) "Wholly Novel" or "Substantial Novelty with Adjacent Literature" outcome.**

Proceed to draft `#disc-dynamic-regime-axis` as a discussion-grade segment in `01-aat-core/src/`. Content:

- §B four-tier formal definition (R0/R1/R2/R3).
- §C derivable content (macro-state-type identity, Lyapunov-machinery transfer, transition no-go shape).
- §F mapping summary (which existing segments instantiate the axis).
- §G cross-axis interactions (Axis A × Axis B independence, wrapping/alignment-work independence).
- `## Findings` with Brief at draft Feynman-criterion bar.
- `## Working Notes` flagging the meta-segment promotion path conditional on §I.6 below.

Depends list: `scope-composite-agent`, `deriv-strategic-composition`, `form-composition-closure`, `result-sector-persistence-template`, `deriv-critical-mass-composition`, `der-team-persistence`, `hyp-symbiogenic-composition`, `disc-separability-pattern`. Long but precise.

Cross-references inbound from segments touched in Phase 5.

**Estimated effort:** 1 cycle (segment authoring + Findings draft + cross-reference scaffolding).

**(2b) "Substantial Overlap with Named Prior Art" outcome.**

The four-tier classification (or an isomorphic one) is already named in one of the adjacent literatures. In that case, AAT's contribution becomes *integration* per the prior-art-integration discipline:

- Adopt the prior-art name and citation directly (don't NIH; rename our R0/R1/R2/R3 to the established vocabulary).
- Draft `#disc-dynamic-regime-axis` (or whatever the natural name becomes) as adopting the prior-art classification with the AAT-specific instantiations of the §C derivable content (macro-state-type identity in AAT vocabulary; Lyapunov-transfer in AAT vocabulary; etc.).
- §C.1's structural identity may or may not be in the prior-art literature; this is a sub-question to revisit per the Pillar verdict's per-target breakdown.

The structural shape of the segment is similar to (2a); the framing is different — adoption rather than invention.

**Estimated effort:** 1 cycle, slightly different content.

**(2c) Re-scope outcome.**

If Pillar finds the four-tier classification *and* §C content *and* §H.5 candidate all substantially overlap with named prior art, the segment landing is reduced to a cross-reference paragraph in `#deriv-strategic-composition` and/or `#disc-separability-pattern` rather than its own segment. The axis-surfacing happens implicitly via adoption + cross-references, not as a new segment.

**Estimated effort:** 0.5 cycle.

### Phase 3 — Instance 6-conditional floor work

**Gate:** BG2 returns. Decision branches on derivation outcome.

**(3a) Derived outcome.**

Instance 6 is derived as a structural no-go with primary-source-verified anchor and characterized structural escape. Two landing options:

- **Light:** Add Instance 6 paragraph to `#disc-identifiability-floor` referencing the spike for the full derivation.
- **Full:** Promote Instance 6 to a full segment-like treatment within `#disc-identifiability-floor`, parallel to the existing Instance 1/2/3/4 treatments.

The light landing is sufficient for unlocking the meta-segment-status path (Phase 6) — what matters there is that the floor instance exists, not its presentation depth.

**Estimated effort:** light = 0.5 cycle; full = 1 cycle.

**(3b) No-go outcome.**

The Instance 6 candidate doesn't derive cleanly. The spike documents what was tried and why it didn't work. This is itself a finding (per the precedent of `spike-4th-identifiability-floor-instance-2026-05-20` Outcome C).

Consequence for the integration plan: meta-segment status (Phase 6) loses its planned complementarity unlock via Instance 6. Three responses:

- Defer meta-segment status indefinitely (segment-only landing is the terminal state for the dynamic-regime axis).
- Find a different floor instance to serve as complementarity. Candidate: the §C.3 regime-transition asymmetry no-go could itself be derived as a floor instance — different mechanism, different no-go shape, but same axis.
- Proceed to meta-segment status without complementarity, breaking the `#disc-separability-pattern` ↔ `#disc-identifiability-floor` precedent. *Not recommended* — the complementarity is what gives the meta-pattern structural integrity.

**Estimated effort:** the no-go documentation is already in BG2's output; the response decision is Joseph-reserved.

### Phase 4 — Truthful-fix cascade across canon

**Gate:** at least Phase 2 (any branch) completes. Phase 3 not strictly required but recommended.

This is the substantive integration-is-replacement work per `99-VERDICT.md` §V.2.

**(4a) `#deriv-strategic-composition` revisions** per `99-VERDICT.md` (C2):

- Pull back the "Class 2 (Partial) composite from Class 1 (Separated) sub-agents" claim in Discussion §"Class-2-(Partial)-composite-from-Class-1-(Separated)-sub-agents" and in the Findings-style table row.
- Replace with the dynamic-regime-axis language (R0 → R1 transition; macro-state-type change; no architectural-class change without additional conditions).
- Add cross-reference to `#disc-dynamic-regime-axis` (or the equivalent landing per Phase 2's branch).
- Update Working Notes to reflect the cycle.

**(4b) `#der-directed-separation` revisions** per `99-VERDICT.md` (C3):

- Pull back the "Class 2 (Partial) composite" claim in Discussion §"Composite-level class inheritance".
- Replace with the corrected composite-class-inheritance content (per §I.4 (4d) below).
- Add cross-reference to `#disc-dynamic-regime-axis`.

**(4c) `#impl-strategic-composition` revisions** per `99-VERDICT.md` (C7):

- Replace the four "Class 3 (Coupled) composite" claims with dynamic-regime language (per Phase 1 if executed conservatively, this is already done; per Phase 1 deferred, this is where it happens).
- Re-derive the §"Modular safety architectures fail under goal divergence" implication using the regime-axis mechanism (R0 → R1 transition; saddle-Nash; multi-equilibrium; last-iterate non-convergence). This is the §V.2 (C7) "preserve the conclusion, fix the mechanism" landing.
- Add 2026-05-09 GUC migration note in Working Notes.

**(4d) `#hyp-directed-separation-under-composition` revisions** per `99-VERDICT.md` (C6):

The architectural-class composite-inheritance table from `02-REFRAME-INSIGHT.md` §7.3 (B5) needs a home. Three options:

- Promote `#hyp-directed-separation-under-composition` to derived along the routing-structure axis; the table becomes its new Formal Expression content.
- Land a new derivation segment `#der-composite-class-inheritance` carrying the table.
- Keep as a Discussion paragraph in `#der-directed-separation`.

The first option requires the (R4) shared-substrate mechanism from `01-STRENGTHEN-ATTEMPTS.md` §5 to be fully derived (currently sketched). If pursued, would also need a focused follow-on spike on the shared-substrate analysis (see §I.7 (J4) below).

**Recommendation:** start with the third option (Discussion paragraph in `#der-directed-separation`); upgrade to second option (new segment) if/when the (R4) work is done; consider first option (hypothesis promotion) only after both.

**(4e) `#disc-separability-pattern` cleanup** per `99-VERDICT.md` (C4):

- Audit row 3 ("Class 2 (Partial) — directed separation holds for identified submodules") for which sense of "Class 2" is meant. Likely it's a structured-repair-tier-vocabulary use, not architectural-class. If so, rename to avoid ambiguity with the architectural Class 2.
- Add the dynamic-regime axis as an 8th row (R0 separable core / R1 structured repair / R2 + R3 general open) if Phase 2 branch (2a) or (2b) — i.e., the axis lands as a segment somewhere.

**(4f) `#scope-composite-agent` Working Notes update.**

Resolve the first Working Notes bullet "Whether a common scalar exists across the alignment routes" in the negative: the four scope routes (C-i/ii/iii/iv) correspond to regime-tier entry conditions on the dynamic-regime axis, not to a continuous spectrum on a single scalar. The dynamic-regime axis is the natural classification, not a scalar reduction of alignment routes.

**Estimated effort:** 1–2 cycles for the full cascade. Can be batched by segment-touch (open multiple files in one cycle and apply consistent changes).

### Phase 5 — Cross-segment ripple

**Gate:** Phase 4 completes.

Per `03-DYNAMIC-REGIME-AXIS.md` §F mapping table, ten+ segments need annotation:

- Light annotations (cross-reference to `#disc-dynamic-regime-axis`): `#form-composition-closure`, `#result-sector-persistence-template`, `#deriv-critical-mass-composition`, `#der-team-persistence`, `#hyp-symbiogenic-composition`, `#disc-separability-pattern` (Contraction ladder row).
- Substantive annotations (axis-tier vocabulary surfacing): `#deriv-strategic-composition` ($\alpha'/\beta'$ as R1/R2 sub-tiers), `#impl-strategic-composition` (regime-transition-as-hand-off framing per Phase 4 (4c)).
- Mechanical annotations: any segment using "Class 2 composite" / "Class 3 composite" vocabulary inherited from the pre-spike framing.

A targeted grep across `01-aat-core/src/` for "Class 2 composite", "Class 3 composite", "partially-opposing objectives", "strategic composition" would surface the full ripple set.

**Estimated effort:** 1 cycle (mostly mechanical with the grep-driven approach).

### Phase 6 — Meta-segment promotion

**Gate:** BG2 returned with derivation (Phase 3a), `#disc-modularity-state-dynamics` has landed (currently queued), and Phase 4 + Phase 5 complete.

Promote `#disc-dynamic-regime-axis` from segment to peer-meta-segment status alongside `#disc-separability-pattern` / `#disc-identifiability-floor` / `#disc-additive-coordinate-forcing` / `#disc-modularity-state-dynamics`.

Content additions for the promotion:

- Complementarity statement with `#disc-identifiability-floor` Instance 6 (per Phase 3a's derivation).
- Parallel-pattern observation with `#disc-modularity-state-dynamics` (the three-operation parallel across Axis A and Axis B, per §G).
- Status upgrade from `discussion-grade` to whatever the peer meta-segments carry.

Plus the meta-pattern cross-references that the existing meta-segments have to each other.

**Estimated effort:** 1 cycle.

### Phase 7 — Standalone-paper trajectory

**Gate:** BG1 returns favorable Pillar verdict (substantial novelty or wholly novel on the four-tier classification or one of the §C contents); Phase 6 completes; Joseph reserves the trajectory decision.

The B-N-Dyn portfolio entry (parallel to the B-N-Sep entry currently in `#disc-separability-pattern` Working Notes) would carry the paper plan:

- Cross-domain integration of dynamic-regime classification (control / game theory / multi-agent learning / evolutionary games / mean-field games).
- AAT-specific contribution layer (the macro-state-type identity, the regime-transition asymmetry no-go, the Instance 6 floor).
- Venue analysis, prior-art landscape, cite-and-extend anchors per the B-N-Sep template.

**Joseph-reserved.** No work proceeds without explicit go.

## §I.5 Cycle estimate (favorable path)

Assuming BG1 = "Substantial novelty," BG2 = "Derived," `#disc-modularity-state-dynamics` lands in parallel within a few weeks:

| Phase | Cycles | Cumulative |
|---|---|---|
| Phase 1 (foundation, conservative) | 0.2 | 0.2 |
| Phase 2 (segment authoring, branch 2a) | 1.0 | 1.2 |
| Phase 3 (Instance 6 light landing) | 0.5 | 1.7 |
| Phase 4 (truthful-fix cascade) | 1.5 | 3.2 |
| Phase 5 (cross-segment ripple) | 1.0 | 4.2 |
| Phase 6 (meta-segment promotion, gated on `#disc-modularity-state-dynamics`) | 1.0 + wait | 5.2 + wait |
| Phase 7 (standalone-paper trajectory) | Joseph-reserved | — |

**Total to meta-segment promotion (favorable path):** approximately 5 cycles plus wait time on `#disc-modularity-state-dynamics`.

**Total to segment landing only (without meta-segment promotion):** approximately 4 cycles.

## §I.6 Cycle estimate (less favorable paths)

If BG1 = "Substantial overlap" (Phase 2b instead of 2a): same total, slightly different content.

If BG1 = "Re-scope" (Phase 2c): total drops to ~3 cycles; meta-segment promotion path closes.

If BG2 = "No-go" (Phase 3b): total drops to ~4 cycles; meta-segment promotion path is blocked unless §C.3 transition no-go is derived as alternative complementarity.

If both BG1 = "Re-scope" and BG2 = "No-go": only Phases 1 + 4 (limited to (4a)–(4c)) + Phase 5 (limited cross-references) land. The dynamic-regime axis becomes implicit cross-reference content rather than a new segment. Total ~2 cycles.

## §I.7 Joseph-reserved decisions (consolidated)

From `99-VERDICT.md` §V.3 plus new items from this integration plan:

**(J1)** Conservative-fix vs truthful-fix for Phase 1. *Recommendation: conservative if Phase 4 will lag; truthful if Phase 4 is imminent.*

**(J2)** Light-touch vs full meta-segment for the dynamic-regime axis. *Recommendation: defer to Phase 6 gate; if `#disc-modularity-state-dynamics` lands and BG2 derives, meta-segment is mature.*

**(J3)** The placement of the architectural-class composite-inheritance table (Phase 4 (4d)). *Recommendation: third option (Discussion paragraph) initially; upgrade later.*

**(J4)** Whether to file a follow-on spike for the shared-substrate (R4) machinery. *Recommendation: defer unless multi-LLM-system analysis becomes load-bearing for `03-llm-core/`. Could land as `spike-shared-substrate-class-3-2026-XX-XX/`.*

**(J5)** Whether the dynamic-regime axis should also produce a `disc-modularity-state-dynamics` interaction segment (covering the parallel-pattern observation). *Recommendation: defer to after both `#disc-modularity-state-dynamics` and `#disc-dynamic-regime-axis` are stable; the parallel-pattern observation becomes nameable then.*

**(J6) New from this plan:** ordering of Phase 1 vs Phase 4. *Recommendation: (1a) immediately + Phase 4 later is operationally safest (canon is never internally contradictory). (1b) postponing Phase 1 into Phase 4 is cleanest if the cycle is short.*

**(J7) New from this plan:** Standalone-paper trajectory (Phase 7). *Joseph-reserved per `99-VERDICT.md`.*

**(J8) New from this plan:** Whether to proactively file `spike-instance-6-alternate-framing-2026-XX-XX/` if BG2 returns no-go, to explore the §C.3 regime-transition asymmetry as a candidate alternate floor instance. *Recommendation: defer until BG2 returns and the no-go pattern is visible.*

## §I.8 What to commit when

The CLAUDE.md *Working Conventions* `feedback_commit_before_canon_modifying_spike` discipline (Joseph 2026-05-20) requires commits before canon-modifying work, so the commit boundaries seam the spike's diff for review/revert/attribution.

| Action | Commit before? | Commit after? | Notes |
|---|---|---|---|
| Background spikes BG1 + BG2 complete | Already at clean tree | Yes, separate per-spike commits with the spike file + the spike subdirectory contents | Pure spike work — no canon touched |
| Phase 1 (1a) conservative fix | Commit prior work if any | Yes — single commit with `#impl-strategic-composition` swap + migration-note add | Canon-modifying |
| Phase 2 segment authoring | Yes — commit Phase 1 first | Yes — single commit with new segment + its `## Findings` + cross-references | Canon-modifying |
| Phase 3 Instance 6 landing | Yes — commit Phase 2 first | Yes — single commit with `#disc-identifiability-floor` Instance 6 addition + spike archival to `.integrated/` | Canon-modifying |
| Phase 4 truthful-fix cascade | Yes — commit Phase 3 first | Multiple commits — one per segment touched, batched as the cycle proceeds | Canon-modifying, multi-segment |
| Phase 5 cross-segment ripple | Yes | Multiple commits — batch by segment-family | Canon-modifying, multi-segment |
| Phase 6 meta-segment promotion | Yes | Single commit with status upgrade + complementarity + parallel-pattern additions | Canon-modifying |
| Phase 7 standalone-paper trajectory | Yes — gated on Joseph | (per Joseph's ops cycle) | Out of agentic-systems |

## §I.9 Risk register

**(R-Reg-1) BG2 returns no-go and §C.3 transition no-go also fails to derive as alternate.** Phase 6 meta-segment promotion permanently blocked. Mitigation: segment-only landing (Phases 1–5) is still sufficient for the framework; meta-segment status was an upside, not a requirement.

**(R-Reg-2) BG1 returns substantial-overlap and the prior-art naming is awkward.** AAT adopts the awkward name per the prior-art-integration discipline. Mitigation: where AAT-internal vocabulary (R0/R1/R2/R3) is more compositional than the imported name, keep the AAT vocabulary as an alias while citing the prior-art name as the canonical external reference. Precedent: `#deriv-strategic-composition`'s sub-scopes $\alpha'/\beta'$ alongside Monderer-Shapley / Rosen / Hart-Mas-Colell external citations.

**(R-Reg-3) Phase 4 cascade introduces new internal contradictions across the ten+ segments touched.** Mitigation: explicit cross-segment consistency audit at Phase 5 close — verify the regime-axis language used in `#deriv-strategic-composition`, `#der-directed-separation`, `#impl-strategic-composition`, `#hyp-directed-separation-under-composition`, and `#disc-dynamic-regime-axis` matches across all five. The §F mapping table in `03-DYNAMIC-REGIME-AXIS.md` is the consistency reference.

**(R-Reg-4) The cycle takes longer than expected and `#impl-strategic-composition` carries the contradiction in the meantime.** Mitigation: Phase 1 (1a) immediately (low-cost conservative fix), then Phase 4 (1b)-shaped revision later. Canon is never in a contradictory state.

**(R-Reg-5) The parallel-pattern observation (§G) doesn't survive scrutiny once `#disc-modularity-state-dynamics` lands** — i.e., the queued meta-segment's actual content doesn't align with the dynamic-regime three-operation analogy in the way §G sketches. Mitigation: §G is observational, not derived. Phase 6 meta-segment promotion would still happen via the complementarity unlock (Phase 3a → meta-segment status) without depending on the parallelism.

## §I.11 BG1 Pillar return — Phase 2 branch resolved (2026-05-21)

The Pillar prior-art search (BG1) completed. Full report at `06-PILLAR-PRIOR-ART.md`. Per-target verdicts:

| Target | Verdict | Confidence |
|---|---|---|
| T1 — Four-tier classification as unifying construct | **Substantial Overlap with Named Prior Art** | High |
| T2 — Macro-state-type / regime structural identity (§C.1) | **Substantial Novelty with Adjacent Literature** | Medium-High |
| T3 — Regime-transition asymmetry no-go (§C.3) | **Substantial Novelty with Adjacent Literature** | Medium |
| T4 — Identifiability floor for regime from marginals (§H.5) | **Substantial Novelty with Adjacent Literature** | Medium |

**The most consequential prior-art finding.** Candogan, Menache, Ozdaglar & Parrilo 2011 (*Math. of OR* 36:474–503) — Hodge-style strategic decomposition of finite normal-form games into potential + harmonic + nonstrategic components — gives a *coordinate-free* organizing structure for the R1-vs-R2 boundary that is cleaner than the AAT tier-wise partition. Letcher-Balduzzi 2019 *Differentiable Game Mechanics* extends this to the dynamics side via potential / Hamiltonian component decomposition. The four-tier ladder *as a classification* is recognized prior art at the foundational level across these works plus Sandholm 2010, Hofbauer-Sigmund 1998, Omidshafiei et al. 2019 $\alpha$-Rank, and Papadimitriou-Piliouras 2019.

### Decision-tree resolution

Per §I.3 the Phase 2 branch was conditional on BG1's verdict. T1 = "Substantial Overlap" resolves Phase 2 to **branch (2b)** — segment authoring proceeds with prior-art integration framing rather than invention framing. The other three targets (T2 / T3 / T4) hold Medium to Medium-High novelty independently and are the natural locus of AAT's contribution.

### Phase 2 (2b) — specified content for the segment authoring

`#disc-dynamic-regime-axis` draft must:

- **Open with the integration framing.** State explicitly that the four-tier partition is a consolidation of prior-art classifications under AAT-internal vocabulary (R0/R1/R2/R3 mapped to potential / non-potential / cyclic / mean-field with the named-prior-art anchors cited). Per `feedback_prior_art_integration` discipline: adopt the established concepts, cite the original names, surface what AAT adds.
- **Carry the citation set.** Six required citations per BG1's executive table: Candogan-Menache-Ozdaglar-Parrilo 2011 (Hodge decomposition); Letcher-Balduzzi 2019 (Hamiltonian dynamics decomposition); Sandholm 2010 *Population Games* (taxonomy); Hofbauer-Sigmund 1998 *Evolutionary Games* (regime classification via phase-portrait); Omidshafiei et al. 2019 $\alpha$-Rank; Papadimitriou-Piliouras 2019 (cycles-as-meaning). Additional cites for T2/T3/T4 per the report.
- **Locate the AAT-specific contribution narrowly** — the three loci where Medium+ novelty holds: §C.1 macro-state-type / regime structural identity for finite $N$ with R3 as boundary case; §C.3 regime-transition cost-asymmetry framing tied to mechanism-design impossibility; §H.5 candidate identifiability-floor for regime from component marginals (subject to Phase 3 outcome).
- **Carry the §F mapping table** as the integration-internal-to-AAT layer — what the axis lets the framework consolidate that the prior art does not (the ten+ existing segments where it lives implicitly).
- **Status: discussion-grade.** Per BG1's recommendation and the prior-art-integration discipline — the axis-as-classification is recognized; the AAT contribution lands at the level of internal-vocabulary consolidation plus the three narrower-novelty items. This is a discussion-grade contribution, not a derived-grade theorem segment.

### Implications for Phases 6 and 7

**Phase 6 (meta-segment promotion).** Unchanged in structure. Still gated on Instance 6 derivation (BG2) and `#disc-modularity-state-dynamics` landing. The integration framing does not preclude meta-segment status — `#disc-separability-pattern` itself is a meta-pattern that consolidates a structure recognized elsewhere (Hintikka 1991, Bareinboim 2022, Robins-Richardson-Shpitser 2020 per its Working Notes), with AAT's contribution being the cross-domain meta-pattern recognition and the specific seven-ladder enumeration. The dynamic-regime axis can land at the same kind of meta-segment status if the §C.1 + §C.3 + §H.5 contributions plus the parallel-pattern observation cohere.

**Phase 7 (standalone-paper trajectory).** **Constraint added.** BG1 explicitly: "the Pillar T1 overlap makes the standalone-paper trajectory require additional novel content beyond the current spike — a fully-derived §C.3 cost-asymmetry tied to a structural-distance metric, and the derived §H.5 floor." Standalone-paper candidacy is now harder than the maturity check estimated: the four-tier classification is not the publishable contribution; the AAT-specific identities (§C.1 + §C.3 + §H.5) are. A paper would need to:

- Derive §C.3 with a structural-distance metric (currently sketched at the argumentation level, not derived with a metric). This is a separate spike — call it `spike-regime-transition-cost-asymmetry-derivation-2026-XX-XX/`.
- Derive §H.5 fully (BG2's job; if it succeeds, this constraint is satisfied for §H.5).
- Possibly augment §C.1 with a sharper formulation that goes beyond "macro-state type collapses with regime for finite $N$" into a more theorem-grade statement.

This raises Phase 7 from "Joseph-reserved trajectory decision" to "Joseph-reserved trajectory decision *plus at least one and possibly two new spike cycles*." Add a new Joseph-reserved decision (J9) below.

### A new recommended spike — T4 second-pass Undermind

BG1's T4 verdict is "least-searched of the four; second-pass Undermind search recommended before promotion." This is independent of BG2 (which is *deriving* Instance 6, not searching for prior art on it). Recommended structure: a focused second-pass search targeting the inverse-game-theory literature (Kuleshov-Schrijvers 2015; Ling et al.; LQ-game ID work) and the Liberzon-2003-adjacent switched-system identifiability literature for any *negative* result on regime-from-marginals identification. This would run *after* BG2 returns so the search can target the specific no-go statement BG2 produces (or against the no-go's framing if BG2 finds no clean derivation).

Action label: **A4 — Second-pass Undermind for §H.5 floor**, gated on BG2 returning. Estimated 0.5 cycle.

### Updated decision tree

The §I.3 decision tree updates: Phase 2 resolves to (2b). Phase 3 still pending BG2. Phase 6 unchanged in structure. Phase 7 picks up the additional spike-cycle requirement (J9 + possible follow-on spikes).

```
                        [BG1 returned: Phase 2 → (2b)]
                                     │
                                     ▼
                         Phase 2(b) authoring
                         (segment with prior-art
                          integration framing,
                          status discussion-grade)
                                     │
                                     ▼
                            [BG2 still pending]
                                     │
                  ┌──────────────────┴──────────────────┐
                  │                                     │
            (3a) Derived                        (3b) No-go documented
                  │                                     │
                  ▼                                     ▼
            A4: Second-pass                        Decision: spike
            Undermind for T4                       alternate floor
                  │                                 (§C.3 as candidate)?
                  ▼
            Phase 4–5 (truthful-fix cascade
            + cross-segment ripple)
                  │
                  ▼
            Phase 6 (meta-segment, gated on
            BG2 + `#disc-modularity-state-dynamics`)
                  │
                  ▼
            Phase 7 (standalone paper, gated on
            §C.3 derivation spike + Joseph go)
```

### Updated cycle estimate (favorable BG1 + favorable BG2 path)

| Phase | Cycles | Cumulative |
|---|---|---|
| Phase 1 (foundation, conservative) | 0.2 | 0.2 |
| Phase 2(b) (segment, integration framing) | 1.2 | 1.4 |
| Phase 3(a) light landing | 0.5 | 1.9 |
| A4 second-pass Undermind for T4 | 0.5 | 2.4 |
| Phase 4 (truthful-fix cascade) | 1.5 | 3.9 |
| Phase 5 (cross-segment ripple) | 1.0 | 4.9 |
| Phase 6 (meta-segment, wait-gated) | 1.0 + wait | 5.9 + wait |
| Phase 7 (paper, +§C.3 derivation spike) | 2.0 + Joseph-reserve | n/a (Joseph) |

The Phase 2 increment is +0.2 cycles (integration framing requires more careful citation work than a pure-invention framing). The A4 second-pass is +0.5 cycles. Phase 7 picks up +2.0 cycles minimum if Joseph greenlights the paper trajectory (a §C.3 derivation spike plus the paper authoring).

### Joseph-reserved decision (J9) — new

**(J9) Whether to schedule the §C.3 derivation spike** (a `spike-regime-transition-cost-asymmetry-derivation-2026-XX-XX/`) in service of Phase 7 paper candidacy. The spike would derive §C.3's cost-asymmetry no-go with a structural-distance metric in regime space — turning the currently-sketched argumentation-level claim into a derived theorem with explicit ascent-cost lower bound and descent-direction default characterization. This is the load-bearing additional novel content BG1 names as necessary for standalone-paper status. Without it, the segment lands fine, the meta-segment landing is reachable, but standalone-paper candidacy stays out of reach.

## §I.12 BG2 Instance 6 return — Phase 3 resolved on an unanticipated branch (2026-05-21)

The Instance 6 derivation spike (BG2) completed. Full spike at `spikes/spike-identifiability-floor-instance-6-2026-05-21.md` (151 lines, lint clean). The outcome lands on a *third branch* the §I.3 decision tree did not anticipate.

### What BG2 actually found

The derivation **succeeded** at the math level: an exact no-go via cross-regime witness construction (R0 shared-objective vs R1 Cournot with parameter-matched marginal mean+variance; R0 vs R2 matching-pennies under aggregation). The mechanism: Sylvester-at-one-remove rank-collapse on the marginal-Fisher information for the topology coordinate, with the same Liberzon 2003 anchor Instance 3 uses.

The agent then ran the M1 distinctness check — "is this a separate floor instance or a scope-extension of an existing one?" — and found **three of four distinctness tests fail**:

- Not an independent external theorem (same Liberzon 2003 anchor as Instance 3).
- Not an independent mechanism family (same Sylvester-rank-collapse-on-coupling-coordinate mechanism as Instance 3, just expanded to broader topology bits).
- Not an independent escape menu (four of five escapes inherit verbatim from Instance 3 — interventional access on coupling topology / matched Tier at composite level / passivity certificate / common contraction metric — plus *one new escape*: composite-level convergence-rate-class observation).

**Honest landing per integration-is-replacement:** the no-go is a **scope-extension of Instance 3**, not a new sixth instance. Instance 3's current scope ("coupling-sign bit unidentifiability") was narrower than the math actually supports — the same mechanism gives unidentifiability of the broader **coupling-topology bit** (sign + potential-existence + game-structure class), which spans R0/R1/R2 regime distinguishability rather than just cooperative-vs-adversarial.

This is a stronger outcome than either of the §I.3 branches (3a derived-as-new-instance / 3b no-go-on-candidate) anticipated. The agent followed strengthen-first correctly: derived the math, then verified what *kind* of contribution it is, then routed honestly. Treating it as a new sixth instance would have been the inflated-novelty error per the math-novelty-recognition discipline.

### Decision tree resolution — branch (3c)

Per §I.3 Phase 3 resolves to a **new branch (3c) — derived-as-scope-extension-of-existing-instance**:

```
Phase 3:
  [BG2 returned]
       │
  ┌────┼────┐
  │    │    │
 (3a)(3b)(3c) ← BG2 lands here
  │    │    │
  │    │    └─ Derived as scope-extension of Instance 3
  │    │       (broadens the existing instance's scope,
  │    │        does NOT add a new instance)
  │    │
  │    └─── No-go on candidate (not what happened)
  │
  └────── Derived as new Instance 6 (not what happened)
```

### Phase 3(c) — specified content

**(3c.1) Broaden `#disc-identifiability-floor` Instance 3.** Modify the existing Instance 3 statement from "coupling-sign bit" unidentifiability to "coupling-topology bit (sign + potential-existence + game-structure class)" unidentifiability. The mathematical anchor (Liberzon 2003) is unchanged; the scope of the no-go is broadened to match what the same anchor actually supports. This is integration-is-replacement at the meta-segment scope level — the broader claim replaces the narrower one because the narrower one was under-characterizing what the floor mechanism gives.

**(3c.2) Add escape (e) to Instance 3's escape menu.** Composite-level convergence-rate-class observation — passive distinguishability of R2 from {R0, R1} via exponential-vs-polynomial convergence rate (R0/R1 give exponential near equilibrium; R2 gives polynomial $O(1/\sqrt T)$). R0-vs-R1 still requires one of the inherited four escapes. The new escape is a *passive* observation — does not require intervention or composite-level coupling-topology access — and is therefore operationally lighter than the four inherited escapes.

**(3c.3) Add strengthened consequence to Instance 3.** Promote `#deriv-strategic-composition`'s sub-scope $\alpha'/\beta'$ partition to the *unique broadly-available* R1-vs-R2 regime classifier under escape (e). This is parallel in form to Instance 1's "separable core under observable sibling covariance" and Instance 2's "structured repair under observable $C$" — each floor instance has a positive-counterpart claim that names what works under the escape.

**(3c.4) Land the §3.3 cross-regime witness math as a new appendix segment.** Recommended location: `01-aat-core/src/deriv-regime-marginal-indistinguishability.md` as a small derivation segment carrying the witness constructions explicitly. The math-lives-in-segments discipline says good math derived in a spike does not reside only in the spike. The witness is currently in the spike at `spikes/spike-identifiability-floor-instance-6-2026-05-21.md` §3.3; it needs a canon home. Alternative location: extend `#deriv-critical-mass-composition`'s asymmetric-limit appendix to include the witness as an instantiation. The new-segment option is cleaner — the witness is about marginal-distinguishability across regimes, which is a different topic from critical-mass dynamics.

### Implications for Phase 6 (meta-segment promotion)

**The complementarity unlock is achieved without adding an instance.** Phase 6's gate per §I.4 was "BG2 derived + `#disc-modularity-state-dynamics` landed." BG2 derived a *scope-extension* of Instance 3 (not a new instance), but the complementarity Phase 6 needs comes from `#disc-identifiability-floor`'s broadened Instance 3 specifically referencing the regime-axis content. The broadened Instance 3 *now names* the regime-distinguishability bits as the things unidentifiable from marginals — that *is* the complementarity for `#disc-dynamic-regime-axis`.

This is actually a *cleaner* meta-segment landing than the originally-planned new-Instance-6 landing would have been:

- The meta-pattern structure (separability ↔ identifiability) is preserved without instance-count inflation.
- `#disc-identifiability-floor` Instance 3 (broadened) becomes the negative-half complementarity for *both* `#disc-separability-pattern` Contraction ladder *and* `#disc-dynamic-regime-axis` four-tier ladder. The instance does double duty cleanly.
- The strengthened-consequence (sub-scope $\alpha'/\beta'$ as unique broadly-available R1-vs-R2 classifier under escape (e)) is the bridge between `#deriv-strategic-composition` and `#disc-dynamic-regime-axis` — naming what the regime-axis vocabulary buys operationally that the sub-scope vocabulary alone did not name.

**Phase 6 gate revised:** "Phase 3(c) broadened-Instance-3 lands + `#disc-modularity-state-dynamics` lands." The first half is in this cycle's scope; the second remains the queued segment.

### Implications for A4 second-pass Undermind for T4

BG1's recommendation was a second-pass Undermind for T4 (the candidate Instance 6 floor) before promotion. The candidate is now resolved as scope-extension-of-Instance-3 rather than new instance. The second-pass Undermind's target therefore changes:

- **Original A4 target:** prior-art for new regime-from-marginals floor.
- **Revised A4 target:** prior-art for the broadened Instance 3 scope (the coupling-topology bit beyond coupling-sign bit) and prior-art for the new passive escape (e) (convergence-rate-class as regime classifier). Less novel-novel-novel checking, more sanity check on the scope-extension.

Estimated effort drops to 0.3 cycles from 0.5.

### Implications for Phase 4 — truthful-fix cascade

**Phase 4 adds two items per (3c):**

- **(4g)** Broaden `#disc-identifiability-floor` Instance 3 per (3c.1)+(3c.2)+(3c.3). Migration note: "Instance 3 scope broadened 2026-05-XX from coupling-sign bit to coupling-topology bit per `spikes/spike-identifiability-floor-instance-6-2026-05-21.md`. Old scope is a strict subset of new scope; downstream cross-references remain valid."
- **(4h)** Land `#deriv-regime-marginal-indistinguishability` new appendix segment carrying the §3.3 witness math. Add to `01-aat-core/OUTLINE.md` Appendix section.

Estimated effort for Phase 4 increases by 1.0 cycle (from 1.5 to 2.5) — the new appendix segment authoring is non-trivial since it carries the witness math at derived-grade.

### Implications for Phase 5 — cross-segment ripple

Two additional segments touched per (3c):

- `#deriv-strategic-composition` — sub-scope $\alpha'/\beta'$ paragraph annotated with the cross-reference to Instance 3's new strengthened-consequence (R1-vs-R2 classifier under escape (e)).
- `#impl-strategic-composition` — already touched by Phase 4 (4c); add a cross-reference to escape (e) as an operational diagnostic for the modular-safety failure mode.

No additional cycle increment; folds into existing Phase 5 work.

### Updated decision tree (full, both backgrounds returned)

```
                        [BG1 returned + BG2 returned]
                                     │
                                     ▼
                  Phase 2(b) — segment authoring with
                  prior-art integration framing
                                     │
                                     ▼
                  Phase 3(c) — broadened Instance 3
                  + new appendix `deriv-regime-marginal-
                    indistinguishability`
                                     │
                                     ▼
                  A4 (revised) — second-pass Undermind
                  for broadened-scope + new escape
                                     │
                                     ▼
                  Phase 1 (foundation, conservative) — if
                  not already done; or rolled into Phase 4
                                     │
                                     ▼
                  Phase 4 (truthful-fix cascade) +
                  Phase 5 (cross-segment ripple) —
                  now including (4g) Instance 3 broadening
                  and (4h) new appendix segment
                                     │
                                     ▼
                  Phase 6 (meta-segment) — gated only on
                  `#disc-modularity-state-dynamics`
                  (the BG2-side complementarity is achieved)
                                     │
                                     ▼
                  Phase 7 — Joseph-reserved (J9: schedule
                  the §C.3 derivation spike?)
```

### Updated cycle estimate (post-BG1 + post-BG2)

| Phase | Cycles | Cumulative |
|---|---|---|
| Phase 1 (foundation, conservative) | 0.2 | 0.2 |
| Phase 2(b) (segment, integration framing) | 1.2 | 1.4 |
| Phase 3(c) (broaden Instance 3 + new appendix) | 1.0 | 2.4 |
| A4 second-pass Undermind (revised target) | 0.3 | 2.7 |
| Phase 4 (truthful-fix cascade + 4g + 4h) | 2.5 | 5.2 |
| Phase 5 (cross-segment ripple) | 1.0 | 6.2 |
| Phase 6 (meta-segment, wait-gated on `#disc-modularity-state-dynamics`) | 1.0 + wait | 7.2 + wait |
| Phase 7 (paper, +§C.3 derivation spike, Joseph) | 2.0 + Joseph | n/a |

Net change from post-BG1 estimate: +1.3 cycles to meta-segment (mostly from Phase 3(c) appendix + Phase 4 (4g)+(4h)). The increase is real but is paying for *more truth content* — the broadened Instance 3 plus the new appendix segment carry math that wasn't previously in canon.

### Risk register update

**(R-Reg-1) — superseded.** The risk that BG2 returns no-go and §C.3 also fails to derive, blocking meta-segment promotion, is moot. BG2 returned with content; the broadened Instance 3 provides the meta-segment complementarity directly.

**(R-Reg-6) — new.** Broadening Instance 3's scope is itself an integration-is-replacement move in a meta-segment, which means any segment currently cross-referencing Instance 3 in its narrower form picks up the broader meaning. Audit needed at Phase 4 (4g): which segments cross-reference Instance 3, and does the broadening change their downstream reasoning? Likely affected: `#deriv-critical-mass-composition` (Instance 3's source instance), `#scope-composite-agent` (Instance 3 enabling-condition language), `#impl-strategic-composition` (Instance 3 operationalization). Mitigation: explicit cross-reference audit at Phase 4 (4g) close.

### Joseph-reserved decision (J10) — new

**(J10) Whether the new appendix segment `#deriv-regime-marginal-indistinguishability` lands as its own segment or as an extension to `#deriv-critical-mass-composition`'s asymmetric-limit appendix.** Per BG2's recommendation, the new-segment option is cleaner (the witness is about marginal-distinguishability across regimes, distinct topic from critical-mass dynamics). Joseph's call on framework architecture.

## §I.10 One-paragraph summary

The strategic-composition-class-3-attempt spike has surfaced two corrective findings (Cournot witness against unconditional Class 3; conflation diagnosis on current Class 2; GUC-rename residue in `#impl-strategic-composition`), one new axis (dynamic regime, four-tier ladder R0/R1/R2/R3, lives implicitly across ten+ existing segments), one cross-axis observation (Axis A architectural × Axis B dynamic-regime are independent; parallel three-operation modularity-like dynamics on each), and two candidate floor instances (regime-identification-from-marginals; regime-transition asymmetry). Two background spikes are in flight to mature two of these (Pillar prior-art for the axis; Instance 6 derivation for the floor). The integration plan stages the work in seven phases — Foundation (immediate, non-discretionary), two Pillar-conditional drafts, one Instance 6-conditional floor landing, the truthful-fix cascade across affected segments, the cross-segment ripple per the §F mapping, meta-segment promotion (gated on Instance 6 + `#disc-modularity-state-dynamics`), and standalone-paper trajectory (Joseph-reserved). Favorable-path cycle estimate is ~5 cycles to meta-segment promotion plus wait time on the queued segment; less-favorable paths range from 2 to 4 cycles. Eight Joseph-reserved decisions are consolidated in §I.7. Five risks are registered in §I.9 with mitigations. The kebab is not committed yet — the plan is the plan.
