# INTEGRATION — Canon Landing Record + Follow-On Plan

**Status (2026-05-22):** typology landed in canon at `draft` stage. Two new segments:

- `#disc-partial-coupling-pathways` (type: `discussion`, status: `discussion-grade`) — the (stage × source × form) parameterization, form-determines-wrappability, stage-cascade propagation, composition with leakage locus, wrapping-regime correspondence.
- `#der-belief-strategy-attractor` (type: `derived`, status: `conditional`) — the source-asymmetry result with linearized stability analysis of belief-strategy attractors under $\Sigma_t$-source coupling.

OUTLINE.md updated with both rows in the Section A meta-segment region (adjacent to `#disc-modularity-state-dynamics`). Working-Notes pointers added to four upstream segments (`#der-directed-separation`, `#disc-adversarial-coupling-pressure`, `#der-class-coercion-via-wrapping`, `#def-agent-spectrum`).

This file remains as the *landing record* and the *follow-on plan*; the section labels below reflect both the original pre-landing recommendation structure (preserved for traceability) and the post-landing state.

---

## 0. The honest top-level verdict (revised post-correction)

**Original verdict (2026-05-22 first pass).** "The typology is substantive enough to warrant canon pointers, but not yet ready for direct segment promotion." This deferred-promotion stance was too conservative — Joseph's methodology correction 2026-05-22: *"Our epistemology allows for more than just derivation and high proof standard parts. Expository narratives that help feel out the landscape for some of the more 'fuzzy' areas like this are absolutely possible as segments — just with the epistemology properly grounded. We really don't like working theory living in spikes instead of the main body."*

**Revised landing.** The typology has landed in canon at `draft` stage with properly-marked epistemic tier (discussion-grade for the framing segment; conditional for the derived attractor segment). Two follow-on sub-spikes are queued to upgrade tier:

(a) **Result 4's posited $K(\Sigma)$ form.** The multiplicative-gain shape of sunk-cost coupling is plausible and matches operational practice, but is not derived from a more primitive AAT mechanism. A sub-spike that derives $K(\Sigma)$ from utility-cost analysis of belief-revision-under-strategic-commitment would close this gap and upgrade `#der-belief-strategy-attractor` from `conditional` toward `exact` under the linearization.

(b) **Pillar-style prior-art search is incomplete on the cognitive-psychology side.** The active-inference / Friston / Pearl-blanket territory has been searched (`ref/Prior_art_for_AAT_directed_separation.md`); the motivated-reasoning / identity-economics / dual-process / self-deception literatures have not. The typology's novelty claims (especially Result 4 as a structural theorem about belief-strategy attractors) cannot be promoted past *intuition-only* novelty status until that search completes. Each landed segment's Findings block carries an honest `intuition-only` Search Log entry — these will be appended with the Pillar verdict when the search lands.

Both gaps are *closeable*. The segments are in canon at `draft` with honest tiers; sub-spike work advances them toward higher stages.

## 1. Recommended canon touches (this cycle)

### 1.1 Working-Notes pointers in existing segments

These are minimal, low-risk touches that surface the typology's existence so future agents can find it. No claims promoted to body content; pointers only.

**`#der-directed-separation` — Working Notes addition.**
Add a bullet near the existing "scope condition" Working Notes:

> - **Sub-typology of Class 2 (Partial Coupling).** The scalar $\kappa_{\text{processing}}$ aggregates over a structurally rich sub-space. A (stage × source × form) parameterization within Class 2 distinguishes content-form (additively biased, post-hoc-debiaseable, W₂-compatible) from process-form (non-separable, requires stage replacement or W₁) coupling; objective-source from strategy-source coupling (strategy-source admits self-stabilizing belief-strategy attractors per `spikes/spike-class2-typology-2026-05-22/03-derivations.md` Result 4, objective-source does not under orient-cascade exogeneity); and four cascade-propagating stages (featurization, likelihood, aggregation, consolidation). The structure-vs-behavior refinement at the Class 1 cell is the agent-level shadow of the form distinction at Class 2. Spike: `spikes/spike-class2-typology-2026-05-22/`. Not yet promoted; sub-spikes on $K(\Sigma)$ derivation and prior-art search are required before segment-promotion.

**`#disc-adversarial-coupling-pressure` — Working Notes addition.**
Add a bullet near "Three operations on modularity state":

> - **Stage × source × form structure within the adversarial mechanisms.** The mechanism table (identity-binding / affect-urgency / sunk-cost-engineering) is a projection of the broader Class 2 sub-typology onto specific (stage, source) cells: identity-binding → P1+P2 / $O$ / content-to-process (form varies with strength); sunk-cost → P3 / $\Sigma$ / process (the cell that admits belief-strategy attractors per the typology's Result 4); affect/urgency → cascade-bypass (outside the (stage × source × form) parameterization; a separate failure mode). Spike: `spikes/spike-class2-typology-2026-05-22/`. The typology gives the structural reason these three mechanisms have distinct repair characters.

**`#der-class-coercion-via-wrapping` — Working Notes addition.**
Add a bullet on sub-type-aware wrapping regime selection:

> - **Sub-type-aware wrapping regime selection.** Knowing only that a component is Class 2 (Partial) is insufficient to choose W₁ vs W₂. The right wrapping regime depends on the un-wrapped component's *form* of coupling: content-form sub-types admit W₂ (post-hoc response structuring); process-form sub-types require W₁ (structural goal-blind query path) or are not coercible to Class 1 at all if pipeline access is unavailable. Additionally, $\Sigma$-source coupling can undermine W₁'s structural commitment via strategy-context leakage in stateful components — a $\Sigma$-channel-suppressed W₁ may be required. Spike: `spikes/spike-class2-typology-2026-05-22/`.

**`spikes/spike-leakage-locus-2026-05-18.md` — cross-reference addition.**
The leakage-locus spike's INTEGRATION note recommends an appendix segment. When that segment is written, it should include a §"Composition with Class 2 sub-types" subsection citing this typology spike. The composition: locus is universal ($\ker\mathcal I_\tau$); typology determines functional form within the locus. See `spikes/spike-class2-typology-2026-05-22/03-derivations.md` Result 5.

**`#def-agent-spectrum` Working Notes — minor.**
The current Working Note ("The one genuinely-new thread: where the dangerous coupling concentrates") points at the leakage-locus result. A small additional clarification: the leakage-locus result tells you *where* the coupling acts (in $\ker\mathcal I_\tau$); the Class 2 sub-typology tells you *through which stage* the coupling enters and *what functional form* it takes within the locus. The two are complementary, not competing. (One-line addition; optional.)

### 1.2 TODO.md additions

Three follow-on items, each scoped as a sub-spike:

**TODO item — Sub-spike: $K(\Sigma)$ derivation from utility-cost analysis.**
Derive the multiplicative-gain shape of sunk-cost-style $\Sigma$-source coupling from a utility-cost analysis of belief-revision-under-strategic-commitment. Specifically: model the agent's cost of revising $M_t$ as bundled with the cost of abandoning $\Sigma_t$, and show the effective belief-update dynamics reduce to a multiplicative-gain modulation in the linearized regime. If successful, Result 4 in the typology spike upgrades to *exact* tier under the conditional. If unsuccessful (the effective form is not multiplicative), Result 4 stays *robust qualitative* and needs a refined statement.

**TODO item — Sub-spike: Pillar prior-art search on cognitive-psychology side of the typology.**
Pillar-style targeted search across motivated-reasoning (Kunda, Kahan), identity economics (Bénabou-Tirole, Akerlof-Kranton), dual-process theory (Evans, Kahneman, Stanovich), and self-deception philosophy (Mele, von Hippel) literatures. Scope: §7.2 of the typology spike. Verdict-likely *Wholly Novel* on integrated typology + Result 4; *Substantial Overlap* on individual axes. Required before any promotion past `draft` stage.

**TODO item — Sub-spike: Composite-level sub-type inheritance under composition.**
Extend the typology to composites: given Class 2 sub-agents with sub-types $(S_i, R_i, F_i)$, derive the composite's sub-type as a function of routing structure and substrate-sharing. This extends `#hyp-directed-separation-under-composition` to track sub-type, not just class. Most substantial follow-on; not on the critical path for promoting the static (single-agent) typology.

### 1.3 `msc/modularity-cycle-plan-2026-05-09.md` annotation

The M4 modularity-state-dynamics meta-segment is scoped but not landed. The typology gives M4 a static-structural complement (M4 names the *operations*; the typology names the *state space* on which they act). When M4 lands, the two segments should be located near each other in OUTLINE and cross-referenced.

Recommended: add a paragraph to the M4 cycle plan noting the typology as a related artifact and a candidate landing partner. (Optional; M4 is independent and can land first if Joseph prefers.)

### 1.4 `spikes/PROPOSED.md` entry

Add a row to PROPOSED:

> | spike-class2-typology-2026-05-22 | Spike for sub-typology of Class 2 (Partial Coupling) by (stage × source × form). Status: math pushed; canon pointers recommended this cycle; promotion to `disc-class-2-sub-typology` deferred pending two sub-spikes ($K(\Sigma)$ derivation; Pillar prior-art). | Active |

This makes the spike discoverable to future agents who run sub-typology questions.

## 2. Conditional second-cycle plan (if sub-spikes succeed)

If both follow-on sub-spikes complete favorably ($K(\Sigma)$ derived → Result 4 exact; Pillar search → typology novel), a `disc-class-2-sub-typology.md` segment becomes promotable. Recommended shape:

### 2.1 Proposed segment outline

**Slug:** `disc-class-2-sub-typology`.

**Type:** discussion.

**Status:** *robust qualitative* initially; *derived* after Result 4 promotes to exact.

**Depends on:** `der-directed-separation`, `disc-adversarial-coupling-pressure`, `def-agent-spectrum`, and the (then-landed) appendix segment for the leakage-locus lemma.

**Sections:**

1. **Pipeline decomposition.** The four-stage $f_M = \tau \circ \alpha \circ \lambda \circ \phi$ structure, with mapping to existing AAT machinery (mismatch signal, update gain, etc.). Posited as canonical for Bayesian-style updaters; the segment is honest that monolithic architectures lose precision.

2. **The (stage × source × form) parameterization.** Formal definitions; recovery of Class 1 / 3 at endpoints; statement that the scalar $\kappa_{\text{processing}}$ is the projection onto magnitude.

3. **Form determines wrappability.** Result 2 from the spike, with the identifiability argument. The structure-vs-behavior refinement at Class 1 is the form distinction at Class 2 read backward — unification noted.

4. **Source asymmetry — belief-strategy attractors.** Result 4 from the spike, ideally promoted to exact tier after the sub-spike on $K(\Sigma)$.

5. **Composition with the leakage locus.** Result 5 — the locus is universal; the typology determines functional form within it.

6. **Operational consequences.** Refined behavioral estimator (probe along $\ker\mathcal I_\tau$, distinguish mean from covariance shifts, distinguish exogenously-bounded from autocorrelated). Wrapping-regime correspondence (W₂ ↔ content; W₁ ↔ process-with-access).

7. **Honest scope.** Most precise for modular Class 2 architectures; interpretive lens for monolithic Class 3. Trajectory coupling is orthogonal and separately treated. The form classification depends on probing protocol; content-form wrapping gives Class-1-by-behavior, not honesty.

### 2.2 Placement in OUTLINE

Two candidate placements:

- **(A) As a refinement-discussion segment under Section II (directly downstream of `#der-directed-separation`).** Reads as "having stated the architectural classification, here is the sub-structure of the middle cell." Cleanest narrative; readers encountering the architectural classification then encounter its refinement.

- **(B) As a meta-segment alongside `#disc-identifiability-floor`, `#disc-separability-pattern`, `#disc-additive-coordinate-forcing` (and the unlanded `#disc-modularity-state-dynamics`).** Reads as "here is another cross-sectional structural pattern of AAT." Fits the meta-segment shape (cuts across multiple chapters, surfaces structure rather than deriving new results).

Joseph's call. Recommendation: (B), if and when the M4 segment lands — they pair naturally (operations on modularity-state ↔ sub-structure of the modularity-state-space). If M4 stays unlanded, (A) is the safer placement.

### 2.3 Findings entry

A single `## Findings` block with:

- **Brief:** Plain-language version reaching toward Feynman criterion. Candidate analog: the difference between (a) an engineer who reads instrument data and writes a goal-blind report, then a separate engineer who reads the report and decides what to do, versus (b) a single engineer who reads instrument data with the decision already in mind, where what they notice depends on what they're trying to accomplish. The first is Class 1 (Separated); the second is Class 3 (Coupled); Class 2 is the messy middle — and the typology says the messy middle has structure: it matters *which* part of the data-reading process is being shaped by the goal, *whether* it's shaped by what the engineer wants vs by what plan they're committed to, and *whether* the shaping is a measurable bias (subtractable) or a wholesale reframing (not subtractable). This Brief is below the bar at present; the bathtub-equivalent has not been written. The current paragraph is an attempt that needs sharpening.

- **Impact:** Refines the operational meaning of the Class 2 label from "uncomfortable middle category" to "structured sub-space with specific repair regimes per cell." Connects to the wrapping construction, the adversarial-coupling-pressure mechanism table, the unlanded M4 meta-segment, and the leakage-locus appendix.

- **Novelty Claim:** *Claim recognition* of the (stage × source × form) sub-typology as the structural complement to Class 1's structure-vs-behavior refinement. *Claim differentiation* on the source-asymmetry result (Result 4): strategy-source coupling produces self-stabilizing belief-strategy attractors that objective-source coupling does not, under the orient cascade's exogeneity assumption.

- **Related Work:** Per the to-be-completed Pillar search (§7 of the spike). Likely entries: Friston et al. (active inference, already-cited adjacent literature); Bénabou-Tirole (identity economics — likely *adjacent literature*); Kunda, Kahan (motivated reasoning — likely *empirical instantiation supporting*); Kruglanski (lay epistemics — possibly *formal antecedent* on the source dimension).

- **Search Log:** Local AAT-internal ref/ scan completed 2026-05-22 (§7.4 of spike); cognitive-psychology Pillar search recommended as sub-spike, not yet performed.

## 3. Items that do NOT get promoted (this cycle or next)

For honesty:

- **The pipeline decomposition itself** is not a standalone result. It's apparatus; it lives only as the formal setup for the typology.
- **Result 1 (stage-localization of repair)** is a basic functional-composition observation; not promotion-grade as a standalone.
- **Result 3 (cascade propagation)** is similarly a composition-of-functions argument; not standalone-promotable.
- **The wrapping-regime correspondence (Result 6)** is a structural mapping; lives in the typology segment if it lands, not as a separate segment.
- **The full 670-cell parameterization** is structural completeness; the segment promotes the 30-cell operational coarsening, with the full parameterization referenced as the ambient quotient.

## 4. Risks and how to mitigate

**Risk: typology too dependent on the posited pipeline decomposition.**
Mitigation: explicit scope statement in any promoted segment that the decomposition is canonical for Bayesian-style updaters and serves as an interpretive lens for monolithic architectures. Honest about precision-loss in monolithic cases.

**Risk: form distinction (content vs process) feels operational rather than fundamental.**
Mitigation: state explicitly that form is *probing-protocol-dependent*, analogous to the existing $\hat\kappa_{\text{processing}}$ estimator's dependence on probes. The typology inherits the same epistemic stance — not a worse one.

**Risk: source asymmetry result depends on $K(\Sigma)$ being multiplicative.**
Mitigation: this is the sub-spike to launch first. If $K(\Sigma)$ derives cleanly from utility-cost analysis, the result strengthens to exact. If it derives to a different form, Result 4's claim updates.

**Risk: prior-art search reveals substantial overlap with cognitive-psychology / economics literatures.**
Mitigation: per `feedback_prior_art_integration.md`, AAT adopts established concepts with citation. If overlap is substantial, the typology's contribution narrows to the *AAT-internal integration* (mapping to wrapping construction, composition with leakage locus, source-asymmetry theorem), with adopted naming from prior art for the axes.

## 5. Decision points for Joseph

The recommended path requires Joseph's call on:

1. **Adopt the canon-pointer touches in §1.1?** (Low-risk; surfaces typology without committing canon.)
2. **Add the three TODO items in §1.2?** (Sub-spike scoping; commits no work but reserves the slots.)
3. **Launch one or both sub-spikes now, or defer?** (The $K(\Sigma)$ derivation is the higher-priority of the two; the Pillar search is more total work.)
4. **If sub-spikes succeed, promote to a `disc-` segment per §2?** (Future decision; not needed now.)
5. **Placement preference (A) vs (B) per §2.2?** (Future decision; informs the M4 cycle plan.)

Optimal path if budget permits: (1) yes, (2) yes, (3) launch the $K(\Sigma)$ sub-spike first (smaller-scope, can promote Result 4 to exact tier if successful), defer the Pillar search to the cycle that decides on promotion. This maintains forward progress without front-loading the heavier prior-art work.

## 6. Why not do more in this spike

A note to future agents picking up this work: the spike was deliberately stopped at the point where the structural carve is *visible* and *substantively examined* but not yet *exhaustively defended*. Pushing further would have crossed into territory that requires either (a) the $K(\Sigma)$ sub-spike or (b) the Pillar search — both of which are scoped follow-ons, and both of which produce work better landed *with* their results (in canon or in a refined typology segment) than as appended sections to this spike.

The discipline is `feedback_segment_voice_not_diff_voice.md` applied to spike work: the spike is the reasoning trail; landing the math belongs in segments, not in larger and larger spike documents. When the sub-spikes complete, the typology segment can be written cleanly; the spike will then be archaeology pointing at the segment.
