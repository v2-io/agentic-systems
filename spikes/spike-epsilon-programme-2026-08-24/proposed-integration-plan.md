# Proposed integration plan — ε-programme spike (suggestions with reasons, not a work order)

*For the integration agent, after independent verification. The spike's results are in `README.md`; verification report expected at `de-novo-feedback-1.md`. Everything below is a suggestion; the integrators' judgment governs.*

## What I'd suggest landing, and where

1. **Theorem E3 + Lemma E4 (degradation bound + κ-bridge)** — the strongest candidate for canon: it supplies the degradation-norm bound for the carving cluster and the temporal-compounding form `#impl-orient-cascade` flags open. It must land *wired into* the existing ε-machinery, not beside it: cross-cite `#deriv-observation-ambiguity-bias-bound` (the landed per-step ε(κ) model; E4's Pinsker route parallels its Fisher–Rao track and should say so), `#form-composition-closure` (Δ(π) as the single-agent TV instance of the ε* closure-defect idea), `#result-section-ii-survival`, and `#disc-approximation-tiering` (the window structure instantiates (AT1)–(AT4): parameter ε, monotone, graceful, with δ(P)-vs-required-precision as the ascension diagnostic). Natural home: a new segment near `#deriv-decomposition-uniqueness` (e.g. `deriv-epsilon-belief-degradation`), depends: `der-directed-separation`, `deriv-decomposition-uniqueness`, `deriv-observation-ambiguity-bias-bound`. Suggested tier: `exact` for the upper bounds and the ceiling sharpness; the marginal-form linear-rate sharpness is robust-qualitative and should be labeled so (README §4.3 states the split precisely — the §4.3 self-correction is part of the record, keep it in WN only per segment-voice discipline).
2. **Rigidity (Theorem E1) + the prior gauge (Theorem E2(iii))** — either the same segment or a sibling. The (P-push) swap counterexample should ride along (it constrains future operationalizations). The "gauge is a prior" observation connects to the weak-lumpability open line already in the distributional spike's §6; if that spike lands, cross-wire.
3. **The window structure (§5)** — Discussion-grade material for whichever segment carries E3, plus a candidate correction *to the two unverified sibling spikes' framing* if/when they integrate: annihilation and splintering should be presented as sub-coupling-tolerance phenomena (scale-relative), not as unconditional facts about coupled agents. This is a suggested change to other parts of the theory, with reasoning at §5.1–5.3.
4. **The |𝓜|=4 complementarity witness (§6)** — supersedes the genericity spike's "constructible at |𝓜|=6" estimate; if the genericity spike integrates, its §6 line should be replaced (integration-is-replacement), citing this construction. The character-group observation is a flagged open, not a result.
5. **FAST-paper relevance:** the paper's exact-only caveats on the trichotomy can, if verification holds, be upgraded to "exact below coupling-tolerance; at tolerance ≥ κ-scale the canonical picture holds with linear degradation (Theorem E3)" — a materially better story than caveat-everything. Verification must precede any paper use.

## Do-not-inherit list (interpretive language I suspect is mine, not the math's)

- "The ε-theory *prices* the trade" / "priced at δ" — economic metaphor, mine.
- "Splintering is what you see when you demand precision finer than the coupling itself" — a gloss; the theorem-form is the window statement in §5.2.
- "The gauge is a prior" as a slogan — the theorem is the diameter bound; the weak-lumpability connection is motivation, not derivation.
- The Fourier/character framing in §6 — the verified content is the three-carving check; "organized by the character group" is a pattern-observation from one example.
- Any novelty impression (§7 last bullet) — Michel & Siegle 2024 (lit-scan.md) may already contain E3's shape; must be read before any novelty claim about the degradation bound.

## Dependencies and epistemic couplings

- The spike's core theorems (E1–E4) depend only on canon (`#deriv-decomposition-uniqueness`, `#der-directed-separation`) and first principles — they do **not** require the two unverified sibling spikes to be right.
- §5 and §6 *interpret* the siblings' witnesses; if verification refutes a sibling witness, §5.2's window statement stands (it quantifies over whatever exact carvings exist) but the illustrative content changes.
- The four completion-states reading: E1/E2/E3/E4 are strengthened-to; §4.3 marginal-form sharpness is an honest strengthen-incomplete with the obstruction named; §6 contains a demonstrated self-refutation converted to a stronger result.
