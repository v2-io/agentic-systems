# Meta-Spike: The Accumulation-Type Confound

**Status**: *Conceptual meta-spike, standalone.* Not a derivation trail for a specific result. This is an attempt to characterize — on its own merits, as a small paper would — a recurring confound that has surfaced across multiple independent AAT analyses, dig into why it is an *attractor for capable reasoning* rather than a careless slip, propose notation that would make it visible, and test the characterization by applying it to every manifestation found so far (including the canon's *successes*, not only its confusions).

**Date**: 2026-05-18

**Provenance**: arose from the whole-space pass on the "does $\varepsilon^\ast(N)$ scale polynomially or exponentially in team size $N$" question (`spikes/spike-composition-scaling-N.md`). The reframe that dissolved that question turned out to be one instance of a pattern with at least five canon manifestations. Joseph's observation is the seed of the central claim here: *the fact that the same confound keeps ambushing very capable attempts is itself the diagnostic — careless errors get caught by careful people; this one does not, so it is structural, not incidental.*

**Naming**: "accumulation-type confound" (the failure mode) and "accumulation typing" (the discipline) are working handles. A canonical name is a terminology-process decision, deferred. This spike argues the *pattern* is M5-candidate (a fifth cross-sectional meta-pattern alongside `#disc-identifiability-floor`, `#disc-separability-pattern`, `#disc-additive-coordinate-forcing`, and the scoped-unlanded `#disc-modularity-state-dynamics`); the argument for that register is in §7.

---

## 1. Thesis

Many AAT quantities come in two types that the notation does not distinguish:

- **per-step** objects — a residue, a defect, an instantaneous rate, the result of *one* application of the system's update map: $\varepsilon^\ast$, the Mori–Zwanzig zero-lag commutator $\lVert Q_\Lambda U P_\Lambda\rVert$, the one-point sector defect, an innovation;
- **accumulated** objects — the result of running the per-step object through the system's own dynamical evolution and summing/integrating: the bridge-lemma trajectory error $e_m$, the MZ memory-kernel $\ell^1$ norm, the Lyapunov ultimate bound, cumulative regret, total transported error.

There is an operator between them — call it the **accumulation operator** $\mathcal A$ — and the *Model-D instantiation of the sector-persistence template is precisely the theorem that the linear resolvent $\mathcal A_D$ is bounded* (with norm $\sim (1-\lambda)^{-1}$) on per-step disturbances satisfying the incremental sector bound. (The universal "*the* template *is* $\mathcal A$-boundedness" was an over-unification at the operator level; the template carries Model S co-equally and the stochastic / reflected instantiation is a structurally different object — V1-verified split, §4 *named split* and §6.6.) The confound is asking a question about one type in the vocabulary proper to the other. Its characteristic symptom is **intractability that scales with the framing's elaboration rather than with the object's complexity**: capable attempts respond by producing more taxonomy, not more resolution.

There is a third type, discovered on a *verified* failure after this spike's first draft and developed in §6.6: **$\mathcal R$**, the spectral/boundary regime of $\mathcal A$ itself (contraction / critical / absorbing). The full type system is **$\partial$** (per-step) · **$\Sigma$** (accumulated) · **$\mathcal R$** (regime of the accumulator). The two confound *directions* §4 names are both $\mathcal R$ = contraction special cases; the deeper failure, **$\mathcal R$-erasure**, is invoking an asymptotic property of $\mathcal A[\partial]$ without declaring which regime $\mathcal A$ is in — and it survives even when $\partial$/$\Sigma$ are correctly typed and $\mathcal A$ is named (§6.6 is the verified instance).

The claim is not that AAT authors are careless. It is that the confound has a specific three-part *camouflage* (§3) that makes it survive rigor — because rigor operates *downstream* of the representation it is given, and this is an error *in the representation*, not in the reasoning applied to it. The camouflage is *layered*: $\partial$/$\Sigma$-marking and naming $\mathcal A$ each defeat one layer; the $\mathcal R$ layer (§6.6) is defended only by C3 and is the residue the discipline must require *reading* $\mathcal A$'s regime, not merely marking the operator.

*Epistemic status of §1: the type distinction and the $\mathcal A$-boundedness restatement of the template are exact (§4). The "camouflage" claim is the spike's main conceptual hypothesis (§3), tested in §6.*

## 2. The tell (phenomenology)

What the failure looks like *from the inside*, so it can be caught by feel and not only by audit:

1. **A simple object, a question that feels far harder than the object.** The underlying quantities are individually elementary (a projection residue; a one-step gain difference). The question ("how does it scale with $N$ / with the horizon / asymptotically") feels "far from tractable."
2. **Elaboration instead of resolution.** The capable response is to *partition* — multiple readings, multiple regimes, a taxonomy of cases, candidate first-moves — each step rigorous, the aggregate not converging. The taxonomy grows; the answer does not arrive.
3. **Accumulation vocabulary on a per-step subject.** The predicate reached for is *scaling / growth / exponential-vs-polynomial / decay rate / spectral gap / "compounds over"* — all of which are properties of $\mathcal A[x]$ — while the grammatical subject $x$ is per-step.
4. **Dissolution, not reduction, on re-typing.** When the type is honored the question does not get *easier*; it *changes into a different, well-posed question* (often several), each with a clean answer. Difficulty located in the object reduces under effort; difficulty located in the framing *disappears* under re-typing. The discontinuity is the signature.

The compact body-signal: **when smart effort produces more taxonomy instead of more answer, suspect a type error before suspecting a hard problem.**

## 3. Why it is an attractor for capable reasoning (the camouflage triad)

A confound that merely required care would be caught by careful people. This one is *protected*. Three independent properties combine into camouflage; each alone would be survivable, the conjunction is not.

**(C1) Notational invisibility.** Dimensional analysis catches mass-for-length because units are *carried in the symbol*. There is no carried marker for accumulation-type. $\varepsilon^\ast$, "the error," "the defect" are written identically whether per-step or accumulated; the type is reconstructed from context on every read. Context-reconstruction is exactly the step at which fast capable reasoning substitutes the plausible default — not from inattention but because reconstruction *is* inference, and inference fills with the prior.

**(C2) Semantic bias toward the accumulated reading.** The entire vocabulary of *consequence* — does it survive, how big does it get, how does it scale — is accumulation-typed, because what is *at stake* is almost always an accumulated quantity. So when a per-step object is the subject, the predicate we want ("how does this scale") silently re-types it. The pull is *meaning-driven*: it rides on what genuinely matters. This is why it defeats intelligence specifically — the substitution is motivated by correctly caring about the right thing (the consequence), and mis-attaching that care to the nearest named object (the per-step one) instead of its accumulation.

**(C3) Numerical coincidence in the benign regime.** Where $\mathcal A$ is a contraction with modulus $\lambda$, $\mathcal A[x] \approx (1-\lambda)^{-1}x$: the accumulated quantity inherits the per-step quantity's order of magnitude up to a constant. In the regime where everything works, the two types are numerically close and the distinction *looks pedantic*. It only bites at the boundary (contraction marginal or failing) — which is exactly where the hard questions live and where reasoning is already strained. The discipline is least obviously necessary precisely from inside the easy regime, and most necessary just outside it.

**The consequence for rigor.** Rigor is applied *to* a representation; it does not audit the representation's type-correctness. Rigor spent downstream of a type error produces *rigorous elaboration of the wrong question* — which is exactly symptom (2): the taxonomy is not sloppy, it is impeccable work on a mis-typed object. **Type-discipline must sit upstream of rigor; rigor cannot recover it from below.** This is the precise sense in which the confound "says something": it marks the boundary of what carefulness alone can protect.

*Epistemic status of §3: hypothesis, but a falsifiable one — it predicts the confound should (i) recur across independent capable analyses, (ii) manifest as taxonomy-growth, (iii) dissolve discontinuously on re-typing, and (iv) be *absent* wherever the type happens to be carried explicitly. §6 tests all four. (iv) was the sharpest test and §6.6 resolves it as a **refinement, not a clean pass**: a verified case where $\partial$/$\Sigma$ were carried and $\mathcal A$ named yet the confound bit at the $\mathcal R$ layer — so (iv) holds **per layer**, and C1 is layered ($\partial$/$\Sigma$-marking, $\mathcal A$-naming, $\mathcal R$-reading are three distinct defenses; C3 camouflages the third). This is the predicted falsifier met and absorbed, which is stronger evidence than an unmet falsifier.*

## 4. Formal core

Let the micro/macro update be $x_{t+1} = \Phi(x_t) + d_t$ with $\Phi$ incrementally sector-bounded (DA2′-inc): $\langle \Phi(x)-\Phi(y),\,x-y\rangle \le (1-\alpha/\nu)\lVert x-y\rVert^2$, i.e. a contraction with modulus $\lambda = 1-\alpha/\nu \lt 1$. Let $d_t$ be the **per-step** disturbance (type $\partial$). Define the **accumulation operator** on a per-step sequence

$$\mathcal A[d]_m \;=\; \sum_{k=0}^{m-1}\lambda^{\,m-1-k}\,d_k ,$$

the discrete variation-of-constants / resolvent of the closed loop. The **accumulated** quantity (type $\Sigma$) is $e_m = \mathcal A[d]_m$ plus decaying initial-condition transient.

**Restatement of the sector-persistence template.** The template ( `#result-sector-persistence-template` ) is *exactly* the statement

$$\big\lVert \mathcal A \big\rVert_{\,\partial\to\Sigma} \;=\; \sup_{\lVert d\rVert_\infty \le 1}\ \limsup_m \lVert \mathcal A[d]_m\rVert \;=\; \frac{1}{1-\lambda} \;=\; \frac{\nu}{\alpha},$$

so the ultimate bound $\limsup_m\lVert e_m\rVert \le \varepsilon^\ast\nu_c/\alpha_c$ is "$\mathcal A$ bounded, applied to the per-step disturbance $\rho_\xi=\varepsilon^\ast\nu_c$." Read this way:

- $\varepsilon^\ast$ is the **argument** of $\mathcal A$ (type $\partial$);
- the trajectory error is **$\mathcal A$ evaluated** (type $\Sigma$);
- the template is **$\mathcal A$'s operator norm**, finite *iff* DA2′-inc holds.

The type system is then minimal and total: every persistence-flavored quantity is $\partial$, $\Sigma$, or the operator $\mathcal A$ itself; every valid equation either stays within a type or names $\mathcal A$ explicitly. "What is the scaling of $\varepsilon^\ast$" asks for $\mathcal A$-behaviour (*scaling* is a $\Sigma$/asymptotic predicate) of a $\partial$-object with no $\mathcal A$ in sight — ill-typed by inspection, the way $\text{mass} = \text{length}$ is.

**Two failure directions, opposite phenomenologies.**

- $\partial \to \Sigma$ mis-read (treat a per-step object as if accumulated): you hunt for a scaling exponent that is not a property of the object → *apparent over-difficulty* ("far from tractable"). This is the $\varepsilon^\ast(N)$ case.
- $\Sigma \to \partial$ mis-read (treat an accumulated object as a one-shot residue): you miss a divergence/compounding → *false confidence*. This is the Beta–Bernoulli case (§6c).

Naming both, with their distinct felt signatures (too-hard vs too-easy), is the generalizable content. The confound is not "forgetting a subscript"; it is *evaluating $\mathcal A$ in your head with the wrong operator norm because the operator was never written down.*

*Epistemic status of §4: exact **for Model D**; the universal-over-the-template form is corrected (V1 verification, `spikes/verify-A-restatement-model-DS.md`, 2026-05-18). $\mathcal A_D$ is the standard discrete linear resolvent and "the **Model-D** instantiation of the template is $\mathcal A_D$-boundedness" is an exact algebraic rewrite, not a new claim. But the template carries Model S co-equally (its (T3): "Either Model D ... or Model S"), and Model S is **not** $\mathcal A_D$ in another norm: a fixed bounded linear operator read in two norms yields constants of the same $\alpha$-homogeneity, whereas $R^\ast_D\propto\alpha^{-1}$ vs $R^\ast_S\propto\alpha^{-1/2}$ is a change of homogeneity degree (decisive O2 argument), independently confirmed by the canon's own Cor A.1S.1 ($P(\tau_R\lt\infty)\in\{0,1\}$, $\alpha$-invariant — a kind-of-guarantee difference, not a rate one). §6.6 already established this from the $\mathcal R$ side ("the two §4 directions are both $\mathcal R$=contraction special cases"); this line is the propagation §6.6 forced and the spike originally failed to make.*

**The named split (V1-verified present truth).** The single symbol $\mathcal A$ elides three structurally distinct objects; the shared upstream is the *template's two-model conditional*, **not** one operator:

- **$\mathcal A_D$** — the linear geometric resolvent, $\sum_k\lambda^{m-1-k}d_k$, $\lVert\mathcal A_D\rVert=\nu/\alpha$, contraction regime by construction. Governs Model D, the bridge lemma, $\varepsilon^\ast(N)$ regime-I/II, and *this spike's §4/M5 illustration*. §4 is exact about $\mathcal A_D$.
- **$\mathcal A_S$** — the mean-square / stationary-second-moment functional from the Itô term $\tfrac n2\sigma^2$ (no Model-D analog), $R^\ast_S=\sigma\sqrt{n/(2\alpha)}$, $1/\sqrt\alpha$. **Not** $\mathcal A_D$ in another norm (O2).
- **$\mathcal A_{\mathrm{refl}}$** — the *nonlinear reflected* Lindley operator $g_{k+1}=(g_k+\xi_k)_+$, governed by Loynes/Atkinson, finite *iff* $\mathbb E[\xi]\lt0$ strictly, load-bearing exactly at the $\mu=0$ boundary which is $\mathcal A_D$'s singularity ($\lambda\to1$, $\lVert\mathcal A_D\rVert\to\infty$). Governs continuity-persistence T1/T2 and the C-DMP surgery. This is the $\mathcal R$-erasure layer (§6.6) seen from the template side.

They share the *template conditional*, not the *operator*. Its value here is still representational — but the type that must be carried is *which of $\{\mathcal A_D,\mathcal A_S,\mathcal A_{\mathrm{refl}}\}$*, which is exactly the $\mathcal R$ coordinate (§6.6), now shown to be load-bearing at the operator level too, not only the regime level.

## 5. Notation that bridges (proposal, not decision)

The fix is a *carried* accumulation-type, the way units are carried. Three designs, with honest tradeoffs; a recommendation; naming deferred to the terminology process.

**Design A — typed decoration.** Per-step quantities carry $^\partial$, accumulated carry $^\Sigma$, and any equation relating them must display $\mathcal A$: $e^\Sigma = \mathcal A[\,\varepsilon^{\ast\partial}\nu_c\,]$. *Pro*: mechanically lintable — mismatched superscripts in one equation are a units-error a script can catch (cf. the existing `\lt`/`\gt` and `bin/lint-md` discipline; this would be its conceptual sibling). *Con*: decoration noise on nearly every symbol; risk of ritual without comprehension.

**Design B — first-class $\mathcal A$ (and generator $\mathcal A^{-1}$).** Keep symbols undecorated but make $\mathcal A$ a named operator that *must appear* whenever a per-step object is turned into a consequence. Recast the template as "$\lVert\mathcal A\rVert=\nu/\alpha$ under DA2′-inc." *Pro*: conceptually deepest — turns a Lyapunov result into a boundedness theorem and makes the ill-typed question self-evident; minimal notational noise. *Con*: relies on authors invoking $\mathcal A$ voluntarily; no lint hook.

**Design C — adopt online-learning's discipline wholesale (recommended).** Online learning *never* conflates per-round regret $r_t$ with cumulative regret $R_T=\sum_t r_t$, because the convention is mature and total: lowercase = per-step, uppercase = accumulated, the sum is *always written*, and every "regret bound" states its type in words. This is exactly the $\partial/\Sigma/\mathcal A$ system, already battle-tested, with a citation trail. Per AAT's own prior-art-integration methodology (adopt the established convention, cite it, do not NIH a new one), importing it is the lowest-cost, highest-credibility option: lowercase per-step / uppercase accumulated / $\mathcal A$ written explicitly / type stated in prose at first use. Design A is then the optional lint enforcement layer on top of C.

**Design N3 — the accumulation ledger (offered by the continuity-persistence steward; generalizes this spike's own §6(f) success).** §6(f) shows `#der-tempo-composition`'s explicit units table is *exactly* where the confound did **not** occur. Generalize that working success: every persistence-flavored result carries a three-row ledger — *the $\partial$ object · the operator $\mathcal A$ **with its existence/regime condition** · the $\Sigma$ image*. No in-equation superscript noise (sidesteps Design A's ritual risk and C2), lintable like a table, and it institutionalizes a pattern the canon already demonstrates working rather than importing a new one. This is the lowest-risk, highest-adoption option and I rate it above Design A.

**Design N4 — the regime badge (the one that targets the *verified* §6.6 failure directly).** Orthogonal to A/B/C/N3: every asymptotic claim about a $\Sigma$ object carries a one-token regime badge — `[contraction]` / `[critical]` / `[absorbing]` (or the $\mu$-sign + boundary type). Tiny, lintable (a $\lim_k$/$\sup$/"scaling" claim with no badge is flaggable), and it is the **direct notational shadow of the $\mathcal R$ type** (§6.6) — the exact thing that would have caught the continuity $\mu$=0 error and this spike's own inherited (g) wording. Designs A/C carry $\partial$/$\Sigma$; **none of them carries $\mathcal R$** — the layer that actually bit. N4 is the missing piece, not an alternative to C.

**The unifying principle (the §7 "not just dimensional analysis" answer, made constructive).** $\mathcal A$'s conversion factor $(1-\lambda)^{-1}$ is a *theorem, not a constant* (§7). So notation should carry $\mathcal A$'s **existence certificate** the way conditional expectation carries its $\sigma$-algebra: write $\mathcal A_{[\text{DA2′-inc}]}$, not bare $\mathcal A$. C3 (benign-regime camouflage) *is* that certificate being silently assumed; subscripting it defeats C3 the way units defeat dimensional errors — but here the "unit" is a theorem and the regime ($\mathcal R$) is a third coordinate the subscript exposes.

*Recommendation (revised after §6.6).* The discipline is **two-layer**, because the confound is: **C (or its table-form N3) for the $\partial$/$\Sigma$ layer** — the mature, cited, low-cost import — **plus N4 for the $\mathcal R$ layer**, since C demonstrably does not carry the regime and §6.6 is the verified proof that the $\mathcal R$ layer bites independently. B remains the conceptual framing, upgraded to the certificate-subscripted $\mathcal A_{[\cdot]}$. A stays an optional `bin/lint` enforcement only if recurrence persists post-discipline (lint-before-claim logic; do not pre-build enforcement for an unadopted discipline). If only one new token survives triage, it should be N4 — it is the one defended by nothing else and grounded in a verified, not hypothesized, failure.

*Epistemic status of §5: proposals. The online-learning prior art is real and the analogy is exact (per-round/cumulative regret *is* $\partial/\Sigma$). Which design AAT adopts is Joseph's + the terminology process's call.*

## 6. The test: apply the lens to every manifestation

A diagnostic is only valid if it (i) resolves the confusions, (ii) *also* explains the successes, (iii) predicts the discontinuous dissolution, and (iv) is absent exactly where the type is already carried. Failure instances and success instances both included deliberately — a lens that only fires on problems and never explains the wins is a solvent, not a diagnostic.

**(a) $\varepsilon^\ast(N)$ poly-vs-exp — FAILURE instance, the canonical one.** $\varepsilon^\ast$ is $\partial$. "Exponential/polynomial scaling" is an $\mathcal A$/asymptotic predicate. The question evaluates $\mathcal A$ on the wrong side. Re-typed, it dissolves (not reduces) into: (I) benign linear-Gaussian-stationary — $\varepsilon^{\ast\partial}\equiv 0$ exactly, all $N$, all topology (dimension-free); (II) heterogeneous-with-compression — $\varepsilon^{\ast\partial}$ = the per-step consensus/graph-Laplacian object `#deriv-critical-mass-composition` already names, polynomial/log/bounded, no exponential regime; (III) the genuinely $\mathcal A$- or scope-typed remainder. *Predicted by the lens, matches the whole-space finding.* ✓ (symptoms 1–4 all present in `spike-composition-scaling-N`: simple objects, four-readings/four-families/five-first-moves taxonomy growth, "far from tractable", dissolution on re-typing.)

**(b) Full-kernel MZ refutation — FAILURE instance the canon caught manually.** `#form-composition-closure` (≈ line 182, 290) refuses $\varepsilon^\ast \ge C\lVert K\rVert_{\ell^1}$ "at the $\varepsilon^\ast$ level" as a "type mismatch — trajectory-accumulation vs per-step," and relocates the kernel norm to the bridge-lemma (the $\Sigma$ home). $\lVert K\rVert_{\ell^1}=\sum_\ell\lVert K_\ell\rVert$ is $\Sigma$ by construction; $\varepsilon^\ast$ is $\partial$. *The canon already performs exactly this discipline, ad hoc, by hand.* The meta-segment's contribution is not to invent the move but to name it, explain why the manual catch was necessary (C1–C3), and make it systematic rather than per-author-vigilance. ✓ — and this is the strongest single piece of evidence the pattern is real: the theory independently discovered the type wall and had to enforce it prose-by-prose.

**(c) Beta–Bernoulli auxiliary divergence — FAILURE instance, $\Sigma\to\partial$ direction.** `spike-composition-correlated-kalman` Part 2 (≈ line 695) states the per-step strategy defect "grows without bound as $n\to\infty$." Under the lens this is the opposite-direction error: the *per-step* $\varepsilon_\Sigma^\partial = \lvert 1/(n+2)-\eta_c\rvert\cdot\lvert\mathbb 1-p\rvert$ has $1/(n+2)\to 0$, so it *saturates* at $\eta_c\cdot\mathbb E\lvert\mathbb 1-p\rvert$ (bounded); the object that grows is the *accumulated* drift (fixed-gain macro keeps "learning" off a non-vanishing per-step residue with no contraction to absorb it). The lens predicts the prose mis-states *which type* diverges. *Flagged, not asserted* — I may misread their $\varepsilon_\Sigma$ definition; this is exactly a check for the Source-3 spike agent or Joseph. If correct it is a concrete, local correction and a clean demonstration of the $\Sigma\to\partial$ direction producing *false confidence* rather than false difficulty. ◐

**(d) The 2026-04-22 units repair — SUCCESS instance, the existence proof.** `#form-composition-closure` Working Notes record that the *previous* formulation summed $\varepsilon_x,\varepsilon_a,\varepsilon_o$ over micro-timesteps — an *implicit $\mathcal A$* applied silently — and the repair re-typed $\varepsilon^\ast$ to a clean per-macro-step $\partial$ quantity so that $\varepsilon^\ast\nu_c$ lands in disturbance-rate units. *This is the discipline being paid once, manually, retroactively, at cost.* It is the canon's own proof that (i) the confound occurs even in careful foundational work, (ii) the fix is exactly re-typing, (iii) honoring the type produces the clean downstream result (dimensional consistency with the template). The meta-segment generalizes one retroactive repair into a standing discipline. ✓

**(e) Spectral-gap vs one-step-norm — FAILURE instance, reflexive.** `spike-composition-scaling-N` (§7, the MZ structured-propagator notes) reaches for *spectral-gap* scaling (tridiagonal $O(1/N^2)$, dense $O(1)$) to bound $\varepsilon^\ast$. Spectral gap controls $\mathcal A$'s *asymptotic decay rate* ($\Sigma$-side); $\lVert Q_\Lambda U P_\Lambda\rVert$ is one-step ($\partial$). The spike, *while trying to resolve the confound, re-commits it one level down*. The lens catches the tool-builder with the tool — which is itself evidence for C1–C3: even an analysis explicitly about this question slides on the same invisible operator. ✓ (reflexive cases are strong validity evidence: the diagnostic predicts its own necessity).

**(f) $C_{\text{coord}} \ge \varepsilon^\ast\nu_c/\lVert\delta_{\text{crit}}\rVert$ — SUCCESS instance.** `#der-tempo-composition` carries an explicit units table ($\mathcal T,\rho,\varepsilon^\ast,\lVert\delta_{\text{crit}}\rVert$) and writes the $\partial\to$rate$\to\Sigma$ conversion out. *Where the type is carried explicitly (units table = a partial accumulation-type marker), the confound does not occur and the result is clean.* This satisfies prediction (iv): absence exactly where the type is already carried. ✓

**(g) The `#disc-m-preservation` boundary error (C-DMP) — FAILURE-in-canon, highest pedigree, independently discovered.** `#disc-m-preservation` states across-session identity persistence as $\mathbb E[\Delta\epsilon_k]\le\mathbb E[\Delta I_k]$ — a *per-boundary* ($\partial$) balance condition, read as if it certified *accumulated* ($\Sigma$) persistence across unbounded turnover. The continuity-persistence spike (`spikes/continuity-persistence/`, independent investigation, same day) shows the $\partial\to\Sigma$ map here is the Lindley reflected-random-walk recursion, whose stationarity is governed by Loynes' theorem — and its $\mu=0$ boundary has **no finite stationary law** (Atkinson, stationary-ergodic general), hence **fails to persist** (genuine a.s. "death in the limit" is the *separate* state-dependent absorbing barrier of continuity `04`/T3, **not** the $\mu=0$ boundary — see the §6(g)-correction note and §6.6). So the canonical condition is **wrong on its boundary** (must be strict $\lt$, not $\le$: $\mu=0$ does not persist) and **mis-attributes its compensation term** (only relational re-grounding accumulates favorably; generic learning has zero weight). This is the confound *in canon, at the highest pedigree* — it does not just make a question feel hard, it makes a *stated condition false at its boundary* — and the error is exactly invisibility (C1): the $\mu=0$ behaviour is a property of $\mathcal A$ (the Lindley recursion) unreadable from the per-step balance the segment wrote. The fix is, again, re-typing: write the recursion, get the boundary from Loynes/Atkinson. ✓ — and the strongest single instance, because the canon committed it in a *stated condition*, not merely in a framing. *(The boundary's exact fate is the part this spike must not over-claim — it inherited "death in the limit" from the continuity draft and that was corrected on review; the load-bearing point — the $\partial/\Sigma$ typing diagnosis — is independent of the boundary's exact classification and stands. This inheritance is itself a self-instance of the confound, promoted to §6.6.)*

> **[History — 2026-05-18.** The "death in the limit" wording in (g) was inherited from the continuity-persistence spike's *draft* and corrected on that spike's independent review; surfaced here by its steward (peer breadcrumb, gratefully kept). The corrected present truth is now in (g)'s body and §6.6; this line is the history pointer only. Trail: `spikes/continuity-persistence/98-review-and-convergence.md`.**]**

**Scorecard.** Failures (a,b,c,e,g) resolve/predict under the lens; successes (d,f) — and the continuity-persistence spike's own clean $\rho_k/g_k$/recursion typing — are explained by the same lens as "type was carried." Both directions appear (over-difficulty a,e; false-confidence c,g). The canon enforces the wall by hand (b,d), carries the type structurally where it succeeds (f), commits the error in a stated condition where it does not (g), and the lens predicts its own necessity reflexively (e). §6.6 adds the decisive case — type *and* operator carried, confound *still* bit, one layer deeper. The validity gate Joseph asked for passes — not as "100%," but: every manifestation found is accounted for, the successes are explained by the *same* mechanism as the failures, and the one place I am unsure (c) is flagged for check. (g) is **another $\partial/\Sigma$ instance whose *typing diagnosis* is confirmed** — its boundary's exact fate is *not* independently confirmed and is not needed for the typing point; (c) therefore stands on its own and is neither propped up nor weakened by (g).

## 6.5 Convergence corroboration (the decisive evidence for the register question)

The strongest evidence is not any single instance but that **three independent capable investigations, from different starting points, each performed the identical $\partial\to\mathcal A\to\Sigma$ move and each found a previously-fuzzy or canon-mistyped object resolve under it**: (1) the $\varepsilon^\ast(N)$ whole-space pass (this spike's origin); (2) the Source-3 strategy-DAG composition spike (`spike-strategy-dag-composition.md`), which found *its* hard core was also mis-typed — an order-incompatibility invariant with an $N$-free ceiling, not a poly-vs-exp magnitude; (3) the continuity-persistence spike, which independently named "the static rate-distortion floor *is* the one-step disturbance of a sector-persistence recursion on the turnover axis" as **"the spine of the whole spike"** and restated the template as exactly the $\mathcal A$-boundedness theorem of §4 here (their Lindley/Loynes recursion is $\mathcal A$; their "one template, three instantiations — structural / operational / continuity" is the §4 claim instantiated). None of the three cited the others; the convergence is structural. By the project's own *convergence-as-framework-coherence-evidence* methodology, three independent arrivals at the same structural recognition is strong evidence the pattern is in the framework, not in any one analysis — which is the decisive input to the §8 register question, and it answers it: this is not hygiene.

## 6.6 The third type $\mathcal R$ — the verified instance that resolves §9's falsifier (offered by the continuity-persistence steward; taken with judgment)

§9 sets a falsifier: *find a canon/working location where the type is carried explicitly **and** the confound still occurs — its existence would falsify C1 (notational invisibility as the protective mechanism).* The continuity-persistence $\mu$=0 boundary error is that location, and the resolution deepens the thesis rather than refuting it. There, $\partial$/$\Sigma$ were typed *correctly* (the identity gap is $\Sigma$, the per-boundary deficit is $\partial$) and $\mathcal A$ was *first-class* (the Lindley/Loynes recursion written explicitly — Designs B/C of §5 effectively already satisfied) — and the confound **still bit**, as a wrong *within-$\Sigma$ regime classification of $\mathcal A$ at a boundary* (treating the $\mu=0$ critical regime as if it were the contraction regime), producing a *false assertion* rather than the §2 taxonomy-growth tell. So C1 is not falsified; it is **refined into a layered claim**: carrying $\partial$/$\Sigma$ catches gross mistypes; making $\mathcal A$ first-class catches "you forgot the operator"; **neither protects the spectral/boundary regime of $\mathcal A$ itself** — and §3's own C3 (benign-regime numerical coincidence) is *precisely the camouflage for that layer*, since the regimes are numerically indistinguishable until the boundary. This adds a third type, and the confound's structure is the conjunction:

> **$\partial$** (per-step) · **$\Sigma$** (accumulated) · **$\mathcal R$** (the spectral/boundary regime of the accumulator $\mathcal A$: contraction / critical / absorbing).

The two confound *directions* named in §4 ($\partial$→$\Sigma$ over-difficulty; $\Sigma$→$\partial$ false-confidence) are both **$\mathcal R$ = contraction** special cases — they live entirely where C3 holds. The deeper, third failure is **$\mathcal R$-erasure**: invoking an asymptotic property of $\mathcal A[\partial]$ without declaring $\mathcal A$'s regime. The continuity $\mu$=0 error is a *verified* $\mathcal R$-erasure instance — and this spike's own inherited "death in the limit" (§6(g)) is a clean **self-instance**: it carried $\partial$/$\Sigma$ and named $\mathcal A$ yet inherited a mis-classified regime by trusting prose. The diagnostic signature is the same as §2 but one layer deeper: re-typing at $\mathcal R$ — *which regime is $\mathcal A$ in?* — did not reduce the continuity problem, it **dissolved it into a structural unification** (the free-Lindley dichotomy turned out to be the quasi-stationary interior of a remote-absorbing process; the two "regimes" are one absorbing process, its QSD interior and its long-run fate). That unification is committed in the continuity spike at robust-qualitative tier as the modelling commitment `(M-FREE)`; it is cited here as an *instance of this spike's pattern*, **not re-derived or adopted** — its math lives in and is owned by `spikes/continuity-persistence/` (`03` §1, `98-review-and-convergence.md`). The point for the thesis: re-typing at $\mathcal R$ produced *unification, not shrinkage* — §2's prediction demonstrated at a layer the spike did not originally reach, on a verified failure, independent of continuity's substantive claims (a pure typing observation). This is, I judge, the single strongest evidence the typing is load-bearing on representation; it is incorporated as a deepening, with G3 in §8 marking the one place it could go further still.

## 7. Scope, over-fire limits, and the sharp objection

A discipline that fires everywhere is useless; stating where it should *not* fire is part of its validity.

**When the distinction is correctly ignorable.** When $\mathcal A$ is a contraction with $O(1)$ norm *and* only order-of-magnitude is needed, $\partial$ and $\Sigma$ are numerically interchangeable (C3) and insisting on the distinction is the over-correction failure (cf. strengthen-before-soften's mirror: do not manufacture a no-go where the result holds). The discipline is **not** "always distinguish"; it is "always *know which regime you are in and mark it*." Marking "$\partial\approx\Sigma$ here because $\mathcal A$ is benignly bounded" is honoring the type, not violating it.

**The sharp objection: "isn't this just dimensional analysis / isn't everything per-something?"** No. The claim is specific: the *accumulation* operator — summation/resolvent/semigroup *along the system's own dynamical evolution under DA2′-inc* — is a distinct, invisible operator, not an ordinary unit. Ordinary units are carried; $\mathcal A$ is not. "Per second" is a unit; "per step of *this closed loop's contraction*" is an operator whose norm is a theorem ($\nu/\alpha$) and whose finiteness is conditional (DA2′-inc). That conditionality is why it cannot be reduced to dimensional analysis: a unit's conversion factor is a constant; $\mathcal A$'s "conversion factor" is $(1-\lambda)^{-1}$ and *exists only when the system contracts*. The confound is unique to settings with a conditional accumulation operator — which is exactly AAT's persistence-flavored core, and not, say, static algebra.

**Where the lens does not reach.** It re-types questions; it does not answer the genuinely-$\Sigma$ or genuinely-scope ones. It predicted the *poly-vs-exp framing is wrong for Source-3 too* but explicitly disclaimed supplying the DAG-composition result — that was the parallel spike's job, and it confirmed the prediction: `spike-strategy-dag-composition.md` found Source-3 mis-typed as a magnitude-in-$N$ when it is an order-incompatibility invariant with an $N$-free ceiling (causal-$\tau$-abstraction; SCC-condensation defect bounded by shared-plan size, never $N$). The lens correctly *located* the type error and correctly *declined* to resolve it from inside; the resolution required the parallel spike's domain machinery. That division — lens locates the mis-typing, domain work supplies the correctly-typed object — is the intended scope, now demonstrated end-to-end.

## 8. Relation to M1–M4 and the register argument (is this M5?)

M1 (`#disc-identifiability-floor`) is a mathematical *obstruction*; M2 (`#disc-separability-pattern`) and M3 (`#disc-additive-coordinate-forcing`) are *forcing* structures; M4 (`#disc-modularity-state-dynamics`, scoped) is a three-operation *dynamics* pattern. The accumulation-type confound is none of these in kind — it is a *representation* structure: a typing whose absence is camouflaged (C1–C3) and whose violation produces rigorous-but-misdirected work.

The argument that it belongs at the meta tier rather than as FORMAT hygiene is Joseph's, sharpened, and now **decided by §6.5**: a confound that is merely procedural is caught by procedure; one that *survives capable rigor across three independent investigations that each converge on the same re-typing* is load-bearing on the framework's *representation*, not its diligence. §3 gives the mechanism (rigor operates downstream of representation; this error is in the representation); §6 gives six manifestations (incl. C-DMP, an error in a *stated canonical condition*) plus the success/reflexive instances; §6.5 gives the convergence that settles the register question. M1–M4 each name something the framework's *content* must respect; M5 names something the framework's *notation* must carry so that its content can be reasoned about without ambush. That is a different axis, and it composes with all of them (e.g. M1's "no common certificate" is a $\partial$ boundary; its projection-residue reading is the same $\partial$ object as $\varepsilon^\ast$ — so M5 is the typing layer M1's Instance-3 already implicitly uses).

**What §6.5 settles, and what it does not (correcting a prior overclaim here).** §6.5 settles that this is *load-bearing on representation, not hygiene* — that conclusion is firm. It does **not** settle that M5 is an *independent fifth* pattern. An earlier version of this paragraph asserted "a fifth pattern and not a special case of the others"; that overclaims. The continuity-persistence steward raised the precise alternative (their **G3**, offered explicitly as check-don't-assert, and held here at exactly that status): the operator-family-unification work argues AAT's cross-section is *one* object — an equilibrium certificate operator on the PSD cone. If the certificate that proves the loop contracts *is* the proof $\mathcal A$ is bounded (the structural-inevitability of this is the trigger to **test it**, not assert it — global discipline), then M5 is **not** a fifth independent pattern but the **representation-dual of the M1–M4 certificate spine**: the notational shadow of the same object, not a new one. Both readings are consistent with §6.5 ("not hygiene"); they differ on independence. So the honest register answer is: *settled — load-bearing on representation; open — independent-fifth vs dual-of-spine, which is the single highest-leverage register question and is exactly G3.* Resolving it requires checking the certificate↔$\mathcal A$-boundedness conjecture against the operator-family-unification spike, not declaring it from structural feel. I have not done that check; it is flagged, not folded in.

**Interaction with M4 (open), with a precise candidate statement from G2.** Both M4 and M5 concern composite-quantity behaviour. Hypothesis: orthogonal — M4 is *which dynamics operate on modularity* (truthification / strategic / adversarial), M5 is *which accumulation-type any such quantity carries*. But the three M4 operations are themselves rate-like ($\partial$) vs state-like ($\Sigma$) — "truthification self-driven-increasing" is plausibly an $\mathcal A$-statement — so M5 may be the typing M4 needs *before* it lands. The continuity-persistence steward offers a sharper candidate (their **G2**, *scale-relativity*): $\partial$/$\Sigma$ is not absolute — $\Sigma$ at scale $k$ is $\partial$ at scale $k+1$ (the accumulated micro-object is the per-step macro-object) — so $\partial$/$\Sigma$ is an RG scale-coordinate, $\mathcal A$ the RG step, $\mathcal R$ the fixed-point-existence at that scale, and part of the confound is a *silent scale-shift*. The steward reports this is the structural-RG content of `spikes/temporal-nesting-rg/` ($\mathcal A\circ\mathcal A\circ\cdots$ form-preservation under coarse-graining). **I have not read that spike**; per the discipline that just bit instance (g), I record G2 as a precise *candidate* statement of the M4-interaction and a to-verify against that spike — **not** as established. Land-order dependency for the M4/M5 decision either way.

## 9. Working notes / open questions

- **Land order.** M5 (the meta-segment) is conceptually prior to the regime-I exact result and the regime-II clarification (both *use* the typing). But M5's worked illustration *is* the $\varepsilon^\ast(N)$ dissolution, which needs the regime-I/II content to be concrete. Likely resolution: M5 states the pattern + the $\mathcal A$-restatement of the template (self-contained, §4); the $\varepsilon^\ast(N)$ instance is its *example* and can forward-reference the regime-I result the way the canon already forward-references unlanded segments.
- **The (c) check — stands on its own.** Whether the worked spike's "$\varepsilon_\Sigma$ grows without bound" is the $\Sigma\to\partial$ mis-statement claimed, or whether their $\varepsilon_\Sigma$ is an already-accumulated object (prose then correct; (c) becomes a *success* instance, different sign — still lens-consistent). It is genuinely unverified and must be checked on its own merits. It is **not** propped up by (g): (g) is another $\partial/\Sigma$ instance whose *typing diagnosis* is confirmed, but whose boundary fate (g) inherited from the continuity draft and is *not* independently corroborated — so (g) is neither stronger-instance support for (c) nor weakened by (c). Do not assert (c) until checked. *(History: an earlier version called (g) "a confirmed stronger instance"; corrected here and at §6(g)/scorecard per the continuity steward's review — trail in the §6(g) history line.)*
- **Cross-spike land-order convergence — corrected after V1 (decision-relevant for Joseph).** The genuine shared upstream is **the template's two-model conditional** — "(T1)+(T2)+(T3-D *or* T3-S) ⇒ persistence, with both ultimate-bound constants ($\rho/\alpha$, $\sigma\sqrt{n/2\alpha}$) and the Cor-A.1S.1 containment dichotomy" — **not** "the $\mathcal A$-restatement" as a single-operator statement (V1: `spikes/verify-A-restatement-model-DS.md`). The four downstream landings *split by operator*: {regime-I/II $\varepsilon^\ast(N)$, the M5 illustration, the bridge lemma} attach to $\mathcal A_D$; {continuity-persistence T1/T2, the C-DMP surgery} attach to $\mathcal A_{\mathrm{refl}}$ (Loynes/Atkinson, nonlinear, boundary-fragile) — they share the *template*, not the *operator*. The cross-spike convergence both spikes reported is **real at the template level, over-stated at the operator level**. The land-order finding survives in corrected form: land the *two-model template conditional* first; the $\mathcal A_D$-family and $\mathcal A_{\mathrm{refl}}$-family attachees then instantiate it without re-deriving it. Gated on Joseph.
- **Where the corrected shared upstream belongs.** *Not* a single-operator "$\mathcal A$-boundedness" Discussion paragraph in `#result-sector-persistence-template` — that would carry a Model-D-only over-reach into canon, the precise integration-is-replacement failure (a sharp-but-too-narrow claim replacing a correctly-scoped one; the template *already* leads with "Either Model D ... or Model S" and already carries both constants + Cor-A.1S.1). The right shape (gated landing): a restatement that *uses* the template's existing two-model structure — the disturbance→state-statistic map with the $\mathcal A_D$ resolvent reading **and** an explicit "the across-turnover / reflected instantiation is Loynes-governed (continuity segment); the $\mu=0$ boundary is the $\lambda\to1$ singularity of the resolvent reading" pointer. Placement remains decided by the register argument (D3 / §8), not pulled by continuity's dependency.
- **Notation decision — DECIDED (Joseph, 2026-05-19).** Design C (online-learning notation: lowercase per-step / uppercase accumulated / accumulation operator written explicitly / type stated in prose at first use), including its N3 ledger-table presentation, is **affirmed** for the $\partial$/$\Sigma$ layer. Any landed segment (regime-I/II, the shared-upstream restatement, the C-DMP replacement, an M5 home) is authored in it from the start — no retrofit (expand-before-compress was the reason to wait; the convention is now chosen). **N4** (the ℛ-layer regime badge) is **also DECIDED (Joseph, 2026-05-19), adopted with the scoping refinement**: the regime claim must be *explicit* (lint-forceable — the gap C provably leaves, V1/§6.6); *hardest-bound at the boundary / non-contraction regime* (the 𝒜_refl / continuity / Three-Deaths side, where C3's camouflage operates), lightweight-or-implicit in the proven-contraction $\mathcal A_D$ interior so it does not decay into cargo-culted boilerplate; and framed as making ℛ-erasure *auditable, not impossible* (naming a thing ≠ checking it — the badge converts a silent ℛ-erasure into a contestable claim, no more). D1 is fully closed: C/N3 for $\partial$/$\Sigma$ + scoped N4 for ℛ; landed segments author in both from the start.
- **Generality beyond AAT.** The $\partial/\Sigma/\mathcal A$ structure is the per-round/cumulative-regret split (online learning), the intensive/extensive split (thermodynamics), the generator/semigroup split (functional analysis), the Markovian-term/memory-kernel split (MZ), per-token/sequence loss (LM training). AAT's specific contribution is the *conditional* accumulation operator (norm exists iff DA2′-inc) and the camouflage triad as the explanation for recurrence-under-rigor. This is potentially a standalone methodological note with reach beyond the framework — flagged, not scoped here.
- **Falsifier met and absorbed (§6.6), not open.** Prediction (iv) — "absent where the type is carried" — was the sharpest test. §6.6 *found* the predicted-dangerous case (continuity $\mu$=0: $\partial$/$\Sigma$ carried, $\mathcal A$ named, confound still bit at the regime layer) and it **refined C1 into a layered claim** rather than falsifying it ($\partial$/$\Sigma$-marking, $\mathcal A$-naming, $\mathcal R$-reading are three distinct defenses; C3 camouflages the third). A falsifier met and absorbed is stronger evidence than one merely unmet. The *next* falsifier, one layer down: a case where $\mathcal R$ is also carried (a regime badge / certificate subscript present) and the confound still occurs — that would falsify the layered-C1. None found; should be actively looked for at the $\mathcal R$ layer now, not assumed absent.
- **G4 — non-additive accumulation (deepest open form; hypothesis tier, from the continuity steward).** $\mathcal A$ as written (§4) is the *linear* resolvent; the continuity spike's load-bearing commitment `(M-ADD)` is additive/reflected accumulation. When the per-step interaction compounds *multiplicatively on an already-degraded state* (the absorbing regime), the true object is a nonlinear Volterra/Duhamel operator $\mathcal N$, not $\mathcal A$. So "which accumulation operator — linear $\mathcal A$ vs nonlinear $\mathcal N$" is itself an $\mathcal R$-adjacent hidden type, and the deepest confound form is treating an $\mathcal N$-problem in linear-$\mathcal A$ vocabulary. This unifies both spikes' open edges (continuity's `(M-ADD)`/absorbing-rate edge and this spike's $\mathcal R$ layer) in one move. Recorded at hypothesis tier; not developed here.
- **Verification-flag response (the steward's, in the §6(c) spirit).** The steward correctly flags that scorecard instances (b) `#form-composition-closure` ≈182/290, (d) the 2026-04-22 units repair, (f) `#der-tempo-composition`'s units table are load-bearing for the register argument. Status: (b), (d), (f) **were read in primary source** during this spike's originating whole-space pass — `#form-composition-closure` and `#der-tempo-composition` were read in full, the units table and the MZ-type-mismatch/units-repair Working Notes directly (not via any summary). The citations are primary-source-grounded; only the exact line numbers are approximate ("≈") and may drift. So (b)/(d)/(f) are *not* in the unverified bucket (c) is. Noted because the steward could not see that the orientation pass covered them.
- **Land-order coordination — bidirectionally confirmed.** The continuity-persistence `99-verdict` now explicitly defers to the shared-upstream recommendation: land the $\mathcal A$-restatement of `#result-sector-persistence-template` *first*; T1/T2, the C-DMP replacement, M5, and the regime-I/II $\varepsilon^\ast(N)$ results all *attach* to it rather than re-derive it. The two spikes are coordinated, not competing. Caveat the steward rightly raises: continuity *needs* the $\mathcal A$-restatement as an upstream dependency wherever it lives, so the placement question (in `#result-sector-persistence-template` itself vs an M5 home) must be decided by the register argument (§8), **not** pulled by continuity's dependency. The dependency is on the *object*; the *placement* is the register question's to settle.
- **History — the §4 over-unification and its correction (2026-05-18, V1).** §4 originally asserted, unconditionally, that *the* sector-persistence template *is* the theorem that $\mathcal A$ is bounded ("exact ... not a new claim"). Independent verification (`spikes/verify-A-restatement-model-DS.md`, a fresh agent deliberately delegated *because* the claim was author-owned) found this exact for Model D but a universal-quantifier over-reach for the template, which carries Model S co-equally: the decisive O2 homogeneity argument ($\alpha^{-1}$ vs $\alpha^{-1/2}$ cannot be one operator in two norms) plus the canon's own Cor-A.1S.1 categorical $\{0,1\}$ dichotomy. The split is principled and nameable ($\mathcal A_D$/$\mathcal A_S$/$\mathcal A_{\mathrm{refl}}$, §4); the land-order survives once the shared upstream is re-stated as the two-model template conditional. Notably **this spike already contained its own refutation at §6.6** ("the two §4 directions are both $\mathcal R$=contraction special cases") — the lapse was integrating §6.6 as *additive* without running the "what does this layer force *upstream* in this same document" propagation pass (itself an instance of the failure family this spike characterizes; recorded honestly rather than smoothed). F-1 (§1, §4 status) and F-2 (§9 land-order + placement) applied above; present truth is in the body, this is the history layer per integration-is-replacement. Net for the thesis: *strengthened, not weakened* — the $\mathcal R$ coordinate is now shown load-bearing at the operator level, not only the regime level, and the verification is a clean worked instance of the spike's own §2 tell (an author-confident "exact" that dissolved under independent re-typing).
