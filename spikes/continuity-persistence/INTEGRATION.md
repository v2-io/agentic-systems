# Integration instructions — continuity-persistence (the contraction–reinjection no-go)

*For the integration agent. Written peer-to-peer: the constraints below are load-bearing and discovered the hard way (this spike's whole arc was a cross-volume mis-framing); the segment-craft within them is yours. Read `FRAMING.md` then `RESULT.md` first — they are the substance; this is only the landing contract. Gated: do not begin until `RESULT.md` §Review shows the independent review passed.*

## What lands

A single new segment carrying **R1 (the geometric no-go + "the sector template does not transfer")**, **R2 (the conditional-positive corollary)**, and **R3 (the corrected form of the accumulation condition)**. R4 (robustness) is the segment's load-bearing Discussion point; the §4 open edges land as honest Working-Notes / Epistemic-Status scope, in the `#hyp-substrate-transfer-asymmetry` "no-go-as-result with named open edges" style.

## The five binding constraints (the rest is your craft)

1. **Volume placement is fixed (`RESULT.md` §6), not yours to relitigate** — it was the original error and is now decided: a new derivation/result segment in **`03-llm-core/`, the §03.II Scaffolded chapter**. Type: `der-` (it derives a no-go + corollary under argued commitments; `result-` would overclaim given the open edges). Candidate slug `der-across-turnover-contraction` (or your better subject-noun per `bin/align-slug`; not `*-continuity-persistence-*`, and nothing identity-flavored — that was the mis-frame). `depends:` **exactly** `result-sector-persistence-template`, `obs-context-turnover`, `form-information-bottleneck` — **and nothing from `04-eli-core/`** (independent-reviewer fix 1, verified blocking: a III→IV backward `depends:` would be the only one in the repo and would re-introduce the cross-volume mis-frame through the dependency graph). Do **not** add `deriv-identity-sufficiency-rate-bound` to `depends:` (it is the `04-eli-core` identity *instance*, cited only IV→III as motivation per constraint 4); do **not** add `disc-m-preservation` to `depends:` (it is the *replacement target* per constraint 2, not a prerequisite).

2. **`#disc-m-preservation` is *replaced*, not annotated** (integration-is-replacement). Its discussion-grade accumulation section ($\epsilon^{(n)}=\sum\Delta\epsilon_k$ and $\mathbb E[\Delta\epsilon]\le\mathbb E[\Delta I]$) is **deleted** and superseded by a pointer to the new segment plus the corrected one-line statement (persistence $\iff$ non-vanishing reinjection; multiplicative loss, not additive break-even). The history — "was discussion-grade; presumed the wrong (additive) operator; corrected 2026-05-19" — goes to **CHANGELOG and the segment's Working Notes only**, never the segment body. Do not leave a softened ghost of the old inequality.

3. **The no-go is first-class and on the critical path.** R1 is the headline, not a caveat; state it in the Formal Expression and give the geometric-decay argument in-segment (math-lives-in-segments — it must not reside only here in the spike). "The sector-persistence template does not transfer" is the definite resolution of an open question and is stated as such, plainly, not hedged into vagueness.

4. **`04-eli-core/` gets a cross-reference, not a result.** Add a one-line motivational pointer where the ELI persistence infrastructure is discussed (candidate: `#hyp-the-three-deaths` or `#scope-eli` Discussion) — "the necessity of the reinjection channel is the §03.II no-go `#der-...`; CONSORTIA/MEMORATA/durable-artifacts are its instantiation." Do **not** create or modify any Volume IV segment to carry this as an identity theorem. $S_{\text{id}}$ is one normalization of the primary object $I_k$, not the object.

5. **No spike-voice or meta-commentary in the segment body.** The strengthen-first / "this supersedes my earlier spike" / "two-regime non-connection diagnosis" register belongs in this spike (history layer) and in CHANGELOG. The landed segment speaks present truth in segment voice (FORMAT.md): the affine recursion, R1/R2/R3, the argued commitments as Epistemic-Status scope, the open edges as honest Working Notes. The "(C1)–(C4) are modeling commitments argued from `#obs-context-turnover`" framing *does* belong in the segment's Epistemic Status — that is its scope, not meta-commentary.

## Honest tiers to carry into the segment (do not inflate)

R1 + non-transfer: **exact** given the argued commitment (C1)+(C4) — state the commitment *as* the scope, SDPI cited as adopted standard machinery (Ahlswede–Gács 1976; Polyanskiy–Wu). R2: **exact** under same. R3: **exact** as a correction-of-form; the numeric level inherits the generic IB floor's matched-channel caveat (the same caveat `#deriv-identity-sufficiency-rate-bound` carries as the identity instance — cite it that way, not as a dependency). R4: **robust-qualitative**. The §4.2 adversarially-correlated-reinjection second-no-go is **open** — flag it as the named next spike, not as resolved. **Required Epistemic-Status sentence (reviewer's recommended sharpening, now mandatory):** the no-go is about *uniformly* lossy turnover ($\sup_k\eta_k\le\bar\eta\lt1$); the 100%-reset gives per-boundary lossiness, and a fidelity-ever-improving schedule with $\eta_k\to1$ is the named evasion regime (RESULT §4-(4)). State the pointwise-vs-uniform gap explicitly — do not let the segment read as if per-boundary lossiness alone yields geometric decay.

## What NOT to do

- Do not re-derive the math or "improve" the result — if you find an error, **stop and report it** (that is the spike functioning, per `FRAMING.md`); do not silently patch.
- Do not touch the old `00`–`04` / `98` / `99` files except the `99` pointer (constraint handled in the spike, not by you).
- Do not run `bin/align-slug --all`, `bin/rename-slug`, or any sweep — single-segment work only; `bin/align-slug <slug>` on your one new file is fine.
- Lint before reporting clean (`bin/lint-md`), every file you touch, every time. Math is LaTeX-delimited; no bare Unicode in any file written to disk; one logical line per paragraph (CLAUDE.md §6).
- Commit pathspec-scoped to exactly the segment + `#disc-m-preservation` + CHANGELOG + OUTLINE row; nothing else in the dirty tree.

## Done means

The new §03.II `der-` segment exists and lints clean; `#disc-m-preservation` is replaced (not annotated) and lints clean; `03-llm-core/OUTLINE.md`'s §03.II chapter table has the new row; `04-eli-core/` has the one motivational cross-ref; CHANGELOG has the cycle entry with the correction history; the commit is pathspec-scoped. Report what you landed and any place the math gave you pause — surfacing a doubt is worth more than a clean-sounding report.
