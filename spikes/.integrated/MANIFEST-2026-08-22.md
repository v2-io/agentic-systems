# .integrated MANIFEST — 2026-08-22 (spikes-corpus drain)

Forward-cycle filing under `spikes.sop.md` §2-bis / §2a / §5. INDEX `LANDED` labels treated as hypotheses; each move is first-hand content-in-`src/` plus exclusion-pickaxe on the refuted form. Parent independent-verifies via the uncommitted diff before commit.

The `git mv` of the eight filed spikes landed in `85b711f` (same-day). This MANIFEST records the content-and-regression check that justified those moves, and the follow-on Working-Notes drain (resolved items deleted; narrative in the CHANGELOG draft, not as in-segment "Discharged" breadcrumbs).

## Filed (`integrated-misfiled` → `.integrated/`)

- `spike-multi-timescale-stacking-2026-06-10.md` — two-timescale stacked-persistence theorem (Model D, premises (S0)–(S5), threshold $\varepsilon_{\max}=\Delta\rho^\ast/(L_h v^{\max})$) first-hand in `#der-multi-timescale-stability` (`exact`, draft). Exclusion: `sketch-multi-timescale-stability` renamed at `de56b9a`; current segment is the post-promotion form.
- `spike-mood-timescale-matching-2026-06-17.md` — $\tau^\ast=\sqrt{\tau_{\mathrm{env}}\, r/(2\sigma_\theta^2)}$ first-hand in `#der-mood-timescale`; `#def-mood` Epistemic Status states "not matching it." Exclusion: `"matches the autocorrelation"` is present only as the *refuted* slogan (now a regression-guard), not as a live claim.
- `spike-captured-objective-2026-06-17.md` — capture-as-input-leg of (D3), $\alpha\to 0$, dual to severed-actuation, first-hand in `#der-captured-objective-dynamics` + `#def-death-as-factor-loss`. No refuted prior form.
- `spike-severed-actuation-dynamics-2026-06-11.md` — $\Omega$-kernel severing, do-sector split, five results at mixed tiers, first-hand in `#der-severed-actuation-dynamics`. Exclusion: `"correction tempo zero"` (first-draft error) never entered `src/` — empty pickaxe.
- `spike-adaptive-tempo-redundancy-penalty-2026-07-15.md` — signed $\Delta$, common-source $R_s$ + saturation, two no-gos, first-hand in `#deriv-tempo-additivity` + `#def-adaptive-tempo` Discussion. INDEX (and a `#def-adaptive-tempo` WN) still said ripples were unexecuted: **false** — first-hand `#disc-independence-audit` §3, `#der-team-persistence`, `#def-strategic-tempo` Epistemic Status all carry the two-sided repair (2026-07-16). Exclusion: `"equality iff informationally independent"` never in current `src/` as a live claim.
- `spike-composite-tempo-synergy-2026-07-16.md` — $\mathcal T_c\le\mathcal T_{\text{joint}}$, signed additive-baseline, closure-exact super-additive witness $1/(1+\rho)$, first-hand in `#der-tempo-composition` Formal Expression. INDEX "pending confirmer≠author" was stale (spike §7 records the pass). Canon-body spike pointer in Formal Expression + Epistemic Status reduced (math is in-segment). Exclusion: unconditional $\mathcal T_c\le\sum_i\mathcal T_i$ retired at `b1f37c9`; current body states it only as the independence-scoped historical form.
- `spike-l1-bias-sim-rerun-2026-07-16.md` — restated Monte Carlo block + AND-root + preserved script, first-hand in `#deriv-l1-update-bias` §Monte Carlo verification. Exclusion: confabulated `"OR-cooperative"` / `$\rho\in\{0.1,\ldots,0.9\}$` deleted at `cdca343`; current block uses the feasible $\rho\le 1/4$ grid. Script stays in the spike (the reason this file exists).
- `spike-rho-factorization.md` — first filed on "the no-go is in canon", **reversed** on the lead's first-hand read (the positive content was the item `#internal-external-decomposition` marked "under separate investigation"), then **landed** as `#deriv-mismatch-budget-attribution` and **filed** (`9d2e0c9`, verifier ≠ author). The test that was mis-applied the first time: "the headline result is in canon" is not "the content is in canon".

## Held this cycle (not a filing op)

- `spike-rho-additive-variance-strengthening-2026-04-24.md` — **orphaned payload.** Recommended landing `#rho-decomposition` was never created (`ls */src/*rho*` returns only `internal-external-decomposition` and `result-mismatch-decomposition`). AV theorem, MC/LD/PID sub-regimes, rate-domain no-go live only in the spike. Heavy landing, not auto-filed. Cross-refs inside the spike updated to the moved predecessor.
- `spike-neutral-drift-endogenous-coupling-strengthening-2026-04-24.md` — §8/§10.1 in `#der-architecture-noidentifiability`; remainder (A–F, §§1–7, 10.2–10.5) still spike-resident per the spike's own header. Held.

## Working-Notes drain (same cycle, follow-on)

Resolved "Discharged / Resolved / Provenance / Reasoning trail" items were **deleted**, not annotated or path-repointed. Remaining WN content is forward pointers, regression-guards, or dead-end warnings. Narrative of what resolved is in the CHANGELOG draft, not in the segments.

## Canon-body reduce-not-repoint (same cycle)

- `#der-tempo-composition` Formal Expression + Epistemic Status: spike path removed (was sending a reader to the spike to understand canon).
- `#disc-independence-audit` §3: "ripple audit tracked in the 2026-07-15 spike" deleted (ripples executed; remaining pointer was a category error).

Generated snapshots (`CURRENT-VOL1.md`, `mono/aat-v0.4.0.md`) still carry the old Formal-Expression spike sentence; they are rebuilt, not patched.
