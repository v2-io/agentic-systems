# Spike: Resolving the Contested Instance 4 of `#disc-identifiability-floor` — Independent Re-derivation

> [!note]
> **Integration trail (2026-05-21):** verdict integrated into canon per `spikes/identifiability-floor-integration-plan.md`. Object B landed as new appendix segment `#der-architecture-noidentifiability` (Kalman-Ho similarity-orbit no-go with the math-gate repairs applied: moment-claim restricted to similarity-invariant summaries; Lyapunov sign matched to canon convention $F\Pi + \Pi F^\top = \sigma_w\sigma_w^\top$; exact / robust-qualitative tier boundary carried consistently). `#disc-identifiability-floor` Instance 4 reinstalled as architecture-noidentifiability with the Sylvester-mechanism taxonomy repaired (rank-collapse trio {I1, I2, I4}, projection {I3}). Object A explicitly absorbed in `#disc-additive-coordinate-forcing` §"Downstream consequences of the (PI) commitment" with the floor-vs-coordinate-forcing distinction articulated. The CL-2 heavy refinement (Regime-C confound) discharged as the same object projected onto the disturbance-statistic coordinate (§7 of this spike). PROPOSALS §D.9 CLOSED. CHANGELOG 2026-05-21. The Fano finite-sample refinement remains honestly open as a separate follow-on direction (§4).

*Started 2026-05-18. Research spike. Not canon. Truth-establishing only;
routing / `.integrated` moves / segment edits are a separate parent call
(this spike does not move or edit segments). Per `doc/spike-routing.md` §0:
truth re-derived on constitutive structure is the only arbiter; canon, the
triage, the source segment, the navigator, and the sibling spikes are all
drifting proxies, interrogated below, not obeyed — mine included.*

**Mandate (one sentence).** Settle, by re-derived mathematics, whether the
contested "Instance 4" of `#disc-identifiability-floor` is (a) a genuine
identifiability-floor instance to be sharpened to exact-with-conditions, or
(b) a downstream theorem of `#disc-additive-coordinate-forcing`'s 4th
instance — meaning canon committed a §4.1-shaped category error — and/or (c)
whether the genuine fourth floor is the agent-internal-architecture /
Regime-C-confound one (Fano-anchored, the rho-recheck's conditional-
separation confound), with conditions and tier; a rigorous no-go is as much
a result as a success.

---

## 0. Verdict up front (then the work)

The contest is between four documents that disagree, and **the
disagreement dissolves once the question is split correctly** — the
documents are not answering the same question, and one of them (canon)
conflates two genuinely different objects under one number.

**There are TWO distinct candidate "fourth things," and canon put the
wrong one in the Instance-4 slot.**

- **Object A — "universal-$C$ under non-(PI) norms" (`#deriv-observation-
  ambiguity-bias-bound` §4 / triage Candidate 3 / canon's Instance 4
  body).** This is **NOT a floor instance.** It is a true no-go but of a
  *different kind* than Instances 1–3: it is a **downstream uniqueness
  theorem of the (PI) axiomatic commitment**, exactly as the triage and the
  source segment's own §4 both already say. Canon's `disc-identifiability-
  floor.md` placing it as "Instance 4" is a **category error — a §4.1-shaped
  canon defect** (a segment asserting an epistemic classification that its
  own cited source segment contradicts in writing). *Tier of this verdict:
  exact* — it follows in a few elementary steps from the constitutive
  status of (PI) (`#scope-agent-identity`, verified first-hand: (PI) is "a
  genuine axiomatic choice ... not directly forced") plus the five-element
  test's E4 (≥2 structurally-distinct escapes), which Object A provably
  fails with exactly **one** escape, and the Sylvester-mechanism boundary
  *already stated in canon's own Discussion* which silently excludes it.

- **Object B — "architecturally-distinct, behaviorally-identical agents
  unidentifiable from on-policy summary data" (neutral-drift spike §8 /
  triage Candidate 2 / rho-recheck Regime-C confound).** This **IS a genuine
  floor instance** and the three independent documents that reach it
  (neutral-drift from the agent side, the triage from the taxonomy side,
  the rho-recheck from the disturbance side) are converging on a real
  object. But — strengthen-before-soften applied honestly — **it does not
  reach *exact* at the general level; it reaches *exact* in a precisely
  characterized sub-scope (linear-Gaussian / Kalman) and *robust
  qualitative* in general, and a precise no-go bounds why it cannot do
  better.** It does **not** inherit the Sylvester rank-collapse mechanism
  (the mandate's warning is correct and load-bearing); it has its own
  obstruction, which I derive: it is a **Fisher-information rank-deficiency
  of the *channel-restricted* score, identical in mechanism to Instance 2,
  NOT a new mechanism** — and that recognition is itself the sharpening.

- **The Regime-C confound (rho-recheck) and Object B are the same object,
  and the rho-recheck's "exact" two-term identity is the *escape
  boundary*, not a competitor.** The conditional M/π/cross split being
  confounded under Regime C *is* Object B projected onto the disturbance-
  statistic coordinate. This confirms the mandate's "same object" linkage
  and CL-2's heavy-reserved refinement at the mathematical level.

So the unified answer is: **(b) for Object A (category error, mark it) AND
(a)-in-sub-scope + (c)-precise-boundary for Object B (genuine floor;
exact in the Kalman sub-scope, robust-qualitative + a derived no-go
in general).** Outcomes (a), (b), and (c) of the mandate *all* obtain,
on different objects — which is the resolution, not a hedge.

**On the framing.** The mandate asked "is the contested Instance 4 genuine
or a category error, and/or is the genuine one the agent-internal one." The
framing's "and/or" is exactly right and the answer uses both arms: it is a
category error *and* the genuine fourth floor is the agent-internal one.
The one place I'd sharpen the framing: the mandate (following canon and the
triage) treats "Instance 4" as one contested slot. The mathematics says
there were always **two** candidate-fours conflated by a shared ordinal;
the category error is precisely the conflation, not just the mislabel.

Confidence (epistemic ladder): **exact** for "Object A is not a floor
instance / canon's Instance 4 is a category error" (§2–§3); **exact** for
"Object B's obstruction is Instance-2-mechanism rank-deficiency, not
Sylvester-for-free and not a new mechanism" (§5); **exact-in-sub-scope /
robust-qualitative-in-general** for Object B as a floor instance (§4, §6);
**exact** for the Regime-C ≡ Object-B identification (§7).

---

## 1. The four documents, and what each is actually a proxy for

Read first-hand (`src/` and the spikes), logged as proxies to test, not
authority (§0 of the routing doc):

| Document | Status | Claims about "Instance 4" | What it is a proxy for |
|---|---|---|---|
| `disc-identifiability-floor.md` | `discussion-grade` | Lists **Object A** as "Instance 4 — Universal Information-to-Distance Constant under Non-(PI) Norms", tier *exact (counter-example-grade)* | The *current asserted classification* — drifted: it contradicts its own cited source |
| `deriv-observation-ambiguity-bias-bound.md` §4 | `conditional` | Says Object A "**does not match the five-element test** ... is a **downstream theorem of the (PI) commitment, not a new floor instance**" | The *source-of-truth derivation* — agrees with the triage against canon |
| `spike-identifiability-floor-instance-triage-2026-04-24.md` | self-labels *robust-qualitative judgment*, "five-element test ... Discussion-grade" | Object A (its Candidate 3) "**fails criterion E4** ... redirect to `#additive-coordinate-forcing`"; Object B (its Candidate 2) "**genuine sibling**, Instance 4" | A *judgment triage* — correct in direction, but explicitly not a re-derivation; needs the math underneath it |
| `spike-neutral-drift-…-2026-04-24.md` §8/§10.1 | self-labels §8 a *sketch* | Proposes **Object B** as genuine fourth instance, Fano / CHT / Kalman-Ho anchors, "**not yet** a crisp closed-form no-go" | A *proposed-not-derived* floor — the thing this spike must actually derive |
| `spike-rho-structure-recheck-2026-05-18.md` §3 | self: *exact* (2T), *conditional* (3-way) | The conditional M/π/cross split is "confounded under Regime C — `#disc-identifiability-floor` Instance, exactly" | Independent arrival at Object B *from the disturbance side* — convergence evidence |

**The single sharpest fact, surfaced by reading the source segment
first-hand (not the triage's summary of it):** `deriv-observation-
ambiguity-bias-bound.md` line 127 — a `src/` segment, the *cited source*
for canon's Instance 4 — states in its own canon body that Object A "does
not match the five-element test ... The honest position: this no-go is a
**downstream theorem of the (PI) commitment**, not a new floor instance."
Canon's `disc-identifiability-floor.md` line 92 lists it *as* Instance 4.
**A segment and the segment it cites as its source contradict each other on
an epistemic classification.** That is the §4.1 invariant violated
(audit-routing §4.1: "a segment must never lie about its own status") —
located, not yet settled. §0 says locate-with-a-proxy, settle-with-math.
The math is §2–§3.

---

## 2. Object A is not a floor instance — the constitutive argument (exact)

I do not lean on the triage's E4 verdict or the source segment's
self-assessment (both proxies). I re-derive it from what (PI) constitutively
*is*, read first-hand from `#scope-agent-identity` (`status: robust-
qualitative`, line 29):

> "(PI) is a genuine axiomatic choice — its cost is that AAT carries an
> additional invariance commitment at the scope layer ... consistent with
> (but **not directly forced by**) the three consequences above."

**Constitutive fact P (verified first-hand, `#scope-agent-identity`):**
(PI) is an *independently-posited AAT-internal axiom*. It is not derived
from the loop, not derived from chronica, not derived from any
identifiability obstruction. Its companion uniqueness machinery is Čencov
1982: *(PI) + Čencov ⟹ Fisher-Rao is the unique invariant metric on
statistical-manifold sub-cases of $M_t$.* This is, verbatim, the **fourth
primary instance of `#disc-additive-coordinate-forcing`** (verified
first-hand: `disc-additive-coordinate-forcing.md` line 44, the "Metric"
row, "(PI) on singular trajectories → Čencov-invariance → Fisher
information metric").

**The five-element shape of a floor instance (read off Instances 1–3,
canon `disc-identifiability-floor.md`):** setting → *external,
independent-of-AAT* impossibility theorem → no-go → **≥2 structurally
distinct boundary escapes each mapped to AAT machinery** → strengthened
consequence (elevates *new* machinery from useful to load-bearing).

Now the re-derivation, in elementary steps:

**Step 1 — Object A's "external theorem" is not external to AAT in the
sense Instances 1–3 require.** Instances 1–3 invoke CHT (Bareinboim et al.
2022), Cramér-Rao (Cramér 1946), common-Lyapunov-nonexistence (Liberzon
2003) — each a published theorem *whose statement and truth are entirely
independent of any AAT axiom*. Object A's "no-go" is: *without (PI),
$\lVert\Delta M\rVert$ vs. $I$ is coordinate-dependent; with (PI)+Čencov,
$C=\sqrt2$.* The obstruction (coordinate-dependence of a Euclidean-parameter
norm) and its removal (Čencov-forced Fisher-Rao) are **both internal to
AAT's own axiom choice**. The "external theorem" Object A cites — Čencov
1982 — is the *escape's* uniqueness machinery, **the very same theorem that
already defines `#disc-additive-coordinate-forcing`'s 4th instance.** A
floor instance's external theorem *forbids*; Object A's external theorem
*forces the escape*. These are opposite roles. *This is an exact structural
distinction, not a judgment call.*

**Step 2 — Object A has exactly one escape; E4 requires ≥2 structurally
distinct ones.** Re-derive the escape count from the math, not the triage.
The no-go is "$\exists$ no universal $C$ under an arbitrary parameter
norm." The displacement-to-information ratio under a parameter norm
$\lVert\cdot\rVert_S$ (chart $S$) transforms as $\lVert\Delta M\rVert_S /
\sqrt I$, while $I$ (mutual information) is chart-invariant. The ratio is
unbounded over the orbit $\{S\}$ (the heteroscedastic-normal witness
$\{N(0,\sigma^2)\}$: $d_{FR}$ fixed, Euclidean-$\sigma$ displacement
$\propto\sigma\to\infty$ — I re-checked this: under $ds^2 = 2\,d\sigma^2/
\sigma^2$, a fixed $d_{FR}=\sqrt2$ gives $\Delta\sigma\approx\sigma$, so
sup over $\sigma$ is $+\infty$; correct). To bound the ratio you must
**quotient out the chart freedom by fixing a canonical metric**. Čencov:
the *unique* invariant Riemannian metric on a statistical manifold is
Fisher-Rao (up to scale). So the canonical-metric choice is **forced and
unique**: there is exactly one escape, "adopt (PI)+Fisher-Rao," and *no
alternative escape can exist* because Čencov says the canonical invariant
metric is unique. This is not "the triage counted one escape"; it is
"uniqueness of the invariant metric *mathematically forbids a second
structurally distinct escape*." A floor instance's ≥2-escape structure
carves an *operational space* (intervene / instrument / observe-jointly /
witness — Instances 1–3 each have 3–5, genuinely distinct in what the
agent must *do*). Object A has a *forced point*, not a space. **Exact: a
uniqueness theorem on the escape side is logically incompatible with the
≥2-distinct-escapes structure that defines a floor instance.**

**Step 3 — Object A's "strengthened consequence" is re-use, not
elevation.** A floor instance elevates *new* machinery to load-bearing
(Instance 1: `#der-loop-interventional-access`; Instance 2: observability-
as-augmentation). Object A "elevates (PI)." But (PI) is *already*
load-bearing — it is *already* `#disc-additive-coordinate-forcing`'s 4th
primary instance (verified first-hand). Object A does not surface a new
load-bearing role; it *consumes* an existing one. The bias-bound theorem
$C_{FR}=\sqrt2$ is a *client* of the (PI)-forced Fisher-Rao metric, exactly
as `disc-additive-coordinate-forcing.md` line 56 already states ("the bias
bound ... relies explicitly on the Fisher-Rao metric"). A client of a
forced coordinate is a **downstream theorem of the coordinate-forcing
pattern**, by the definition of that pattern. *Exact, from the definitions
of the two meta-patterns.*

**Conclusion (§2, exact).** Object A fails the floor shape on all three
load-bearing elements (external-theorem role inverted; single forced
escape, not ≥2; consequence is re-use, not elevation). It *is* a true
no-go — "no universal $C$ in arbitrary parameter norms" is correctly
proved counter-example-grade in `#deriv-observation-ambiguity-bias-bound`
§4 — but it is the no-go *that motivates the (PI) scope-gate of a
downstream bias bound*, structurally a member of the **additive-
coordinate-forcing** family (the "downstream consequences of the (PI)
commitment" already named in `disc-additive-coordinate-forcing.md` §"The
Čencov-Fenchel relationship"), not of the **identifiability-floor**
family. **Canon's `disc-identifiability-floor.md` Instance 4 is a category
error.** This is the (b) outcome of the mandate, reached by re-derivation,
and it independently *agrees with the source segment's own canon text* —
the canon defect is localized entirely in `disc-identifiability-floor.md`,
not in `deriv-observation-ambiguity-bias-bound.md` (whose §4 is already
correct).

---

## 3. Cross-check against canon's own Sylvester boundary (independent confirmation, exact)

The mandate flagged: "the proven Sylvester's-law mechanism covers only the
rank-collapse subclass — Instances 1–2 — so a 4th instance does not inherit
it for free." This is correct and gives an *independent* exact confirmation
of §2, from a different direction.

Canon's own Discussion (`disc-identifiability-floor.md` line 150, the
Sylvester-mechanism paragraph, plus the "boundary facet" Finding line 191)
partitions the floors by mechanism:

- **Rank-collapse subclass (Instances 1, 2):** the obstruction is a
  rank-deficient *information operator*; irreducibility = Sylvester's law
  of inertia (congruence preserves the count of zero eigenvalues; the
  agent's only freedom is reparameterization = congruence; so no
  coordinate change escapes).
- **Composition (Instance 3):** a *different* obstruction — composition is
  a non-invertible projection, not a congruence; the no-go is a
  Schur-complement / memory-kernel statement.

**The diagnostic, re-derived:** canon's Sylvester paragraph *never places
Object A in this taxonomy at all.* It addresses "Instance 2 cleanly,
Instance 1 structurally" (rank-collapse) and "Instance 3 (composition) is a
*different* obstruction" — and stops. The Findings line 166 says "**four**
independently-derived results" and the Related-Work table line 179 lists
Object A as the fourth, *yet the load-bearing mechanism Discussion that
explains why each floor is irreducible has only three entries and
explicitly says "the floors do not share one mechanism — Sylvester for
rank-collapse, a projection/closure obstruction for composition."* Object
A has **no irreducibility mechanism in the taxonomy** — because its
irreducibility is not an inertia/projection statement at all: it is the
*uniqueness* of the Čencov metric (a statement about the escape being
unique, §2 Step 2), which is categorically not a "no representation
escapes a degeneracy" statement. **A genuine floor instance must have an
irreducibility mechanism of the floor-family kind (why no representational
move escapes the no-go). Object A's "irreducibility" is the uniqueness of
its escape — the opposite structural role.** Canon's own Discussion,
read carefully, *already silently excludes Object A from the floor
mechanism taxonomy*; the Findings/Related-Work tables that count it as
"the fourth" were never reconciled with the mechanism Discussion. This is
the internal incoherence made precise — and it is exactly the §4.1 shape:
the segment asserts (in its instance list + Findings count) a status its
own mechanism analysis contradicts.

*Tier: exact.* This is a consistency check internal to canon, resolved by
reading what the mechanism Discussion does and does not cover; it requires
no new mathematics, only the recognition that "uniqueness of the escape" ≠
"irreducibility of the floor."

---

## 4. Object B is a genuine floor instance — strengthen-before-soften, then the precise boundary

Now the harder, more valuable half. The mandate: do not certify a floor
instance unless the Cramér-Rao / Fano / rank-collapse argument *actually
closes*; a 4th instance does not inherit Sylvester for free. I attempt the
strongest true statement first.

**Setting (re-stated precisely from the neutral-drift spike §8, the
rho-recheck §3, and the triage Candidate 2, unified).** Two agents $A, A'$
on the same regime-restricted trajectory, with identical sector-condition
summary $(\alpha, R)$ (equivalently: identical ultimate-deviation
statistics on $\lVert\delta\rVert$) in that regime. Observer has *on-policy,
in-regime, summary-only* data. Question: can $A$ be distinguished from
$A'$?

**Strengthening attempt (try to make "identifiable" true under the
strongest assumptions).** Suppose the agents are linear-Gaussian (sub-scope
$\alpha_1$ / Kalman): $\delta$ obeys $d\delta = -F\delta\,dt +
\sigma_w\,dW_t$ with $F$ the (linearized) correction operator,
realization-dependent. The observer sees the *innovation process*
statistics. Can the realization $(F, \sigma_w, \text{state-space basis})$
be recovered from the innovation second-order statistics?

This is **exactly the Kalman / Ho-Kalman canonical-form question**, and it
is classically *settled in the negative*: the innovation-sequence spectral
density (equivalently, the steady-state covariance + $\alpha$ + $R$)
determines the transfer function only up to **similarity transformation
and pole-zero canonical form** (Kalman 1963; Ho & Kalman 1966; Anderson &
Moore 1979 *Optimal Filtering* §10.4 — minimal realizations sharing an
innovation spectrum form a similarity-equivalence class). So the strongest
honest statement is *not* "identifiable" — strengthening fails here, and
its failure is itself a sharp classical theorem, not a hand-wave. **This
is the (c)-outcome material: the strengthening attempt closes into a
precise no-go, which is the result.**

**The no-go, made crisp (this is what the neutral-drift spike §8.3 said it
did NOT yet have; I supply it now).**

> **Claim (Object B no-go, exact in the linear-Gaussian sub-scope).** Let
> $A, A'$ be two AAT agents whose linearized closed-loop residual
> dynamics are minimal state-space realizations $(F, \sigma_w)$ and
> $(F', \sigma_w')$ related by an invertible similarity $F' = T F T^{-1}$,
> $\sigma_w' \sigma_w'^\top = T \sigma_w\sigma_w^\top T^\top$. Then the
> stationary innovation process — hence every $(\alpha, R)$-summary, every
> moment of $\lVert\delta\rVert$, and the entire on-policy observation law
> — is **identical** for $A$ and $A'$. No on-policy, in-regime,
> summary-restricted statistic distinguishes them. *Proof:* the
> innovation's stationary law is determined by the pair $(F,
> \sigma_w\sigma_w^\top)$ only through similarity-invariant quantities
> (eigenvalues of $F$ = the $\alpha$-spectrum; the solution $\Pi$ of the
> Lyapunov equation $F\Pi + \Pi F^\top + \sigma_w\sigma_w^\top = 0$
> transforms as $\Pi' = T\Pi T^\top$, so $\mathbb E\lVert\delta\rVert^2 =
> \operatorname{tr}\Pi$ is *not even similarity-invariant in general* —
> but the observable is the innovation *spectrum*, which is
> $\det(i\omega I - F)$-determined, similarity-invariant). The architectural
> content (the basis $T$, i.e. *which* internal coordinates implement the
> correction) lives entirely in the similarity orbit and is annihilated by
> the spectrum map. $\square$ (Classical: Kalman canonical-form
> non-uniqueness; the AAT-specific content is the recognition that the
> *architectural* degree of freedom AAT cares about — `#der-agent-opacity`'s
> "which internal mechanism" — *is* the similarity orbit, hence invisible
> in the on-policy summary.)

**External theorem doing the work:** Kalman 1963 / Ho-Kalman 1966
canonical-form non-uniqueness (linear-Gaussian sub-scope, the *tight*
anchor). General sub-scope: Bareinboim et al. 2022 CHT at the agent-as-SCM
layer (two SCMs over the agent's state space agreeing on Level-1
observation data, disagreeing on Level-2). **Dual-anchor**, exactly as the
neutral-drift spike §8.3 suspected — and the dual-anchor is *honest*, not a
weakness: it is the same dual structure Instance 1 has (CHT general /
explicit construction sharp).

**The Fano arm — does it close?** The mandate flags Fano as the neutral-
drift spike's proposed anchor (§B.5/§10.1: "Fano's inequality at the
observer-side prediction task"). I tested whether Fano *adds* a sharper
no-go than Kalman-Ho. It does *not* close into a sharper statement, and
honesty requires saying so: Fano gives a lower bound on prediction-error
probability *given* a bound on the channel mutual information $I(A; \text{
obs})$ between the architectural variable and the observation. But in the
similarity-orbit construction above, $I(A;\text{obs}) = 0$ *exactly* (the
observation law is *identical*, not merely close), so Fano degenerates to
the trivial bound "error probability $\ge$ prior" — true but vacuous, and
strictly weaker than the *exact* Kalman-Ho "they are indistinguishable,
full stop." **Fano is the right tool for the *approximate* / finite-sample
version (architectures that are close-but-not-equal in innovation spectrum,
where $I\gt 0$ small); it is the wrong tool for the *exact* population no-go,
where the Kalman-Ho similarity argument is strictly sharper.** This is a
genuine correction to the neutral-drift spike's proposed anchoring: *the
exact no-go is Kalman-Ho, not Fano; Fano is the finite-sample refinement,
not the floor.* Reported as a no-go on the proposed Fano anchoring itself —
demonstrated (the $I=0$ degeneration), not asserted.

---

## 5. The obstruction mechanism for Object B — NOT Sylvester, NOT new: it is Instance-2 rank-deficiency (exact)

The mandate's sharpest technical warning: a 4th instance "does not inherit
[Sylvester] for free." Correct. I derive what Object B's irreducibility
mechanism *actually is*, because canon's mechanism taxonomy (§3) has a hole
exactly here and getting this right is the load-bearing sharpening.

**It is not Sylvester.** Sylvester (canon's Discussion) governs the
rank-collapse subclass: the obstruction is a rank-deficient *information
operator*, and the agent's representational freedom is *congruence*
($\mathcal G \mapsto S^\top\mathcal G S$, the Fisher reparameterization
law), under which inertia (zero-eigenvalue count) is invariant. For Object
B, the relevant transformation between $A$ and $A'$ is **state-space
similarity** $F\mapsto TFT^{-1}$, *not* metric congruence on a Fisher
operator. Similarity and congruence are different group actions
($T(\cdot)T^{-1}$ vs. $S^\top(\cdot)S$); Sylvester's law is a statement
about the congruence action specifically. So Object B does **not** inherit
the Sylvester mechanism — the mandate's caution is mathematically exactly
right, and a spike that claimed Sylvester here would be overclaiming.

**It is not a genuinely new mechanism either — and recognizing that is the
sharpening.** Attempt the strengthen-first move: is there a *single*
information-operator rank statement underneath the similarity-orbit no-go?
Yes. Map Object B to the Fisher geometry: parameterize the agent by
$\theta = (\text{similarity-invariants}, \text{orbit-coordinates})$ —
formally, the realization manifold fibered over the transfer-function
manifold by the similarity group $GL(n)$. The observation law depends on
$\theta$ **only through the similarity-invariant base** (the spectrum /
transfer function), by the §4 proof. Therefore the **Fisher information of
the on-policy observation channel, as a form on the full realization
manifold, is rank-deficient: it is identically zero on the entire
tangent distribution of the similarity fiber.** That is *exactly* the
Instance-2 structure — "the Fisher matrix admits a null space; perturbations
along the indeterminacy manifold are unobservable" (canon Instance 2,
verified first-hand: the rank-1 factorization with a 2-dim null space along
the mixture-indeterminacy manifold). Object B is the **same mechanism as
Instance 2** — a *Fisher-information null space along a structurally-forced
indeterminacy manifold* — with the manifold being the similarity orbit
instead of the mixture-indeterminacy manifold.

**And therefore it *does* fall under canon's Sylvester boundary after
all — at one remove, and this is the precise correction to canon's
mechanism taxonomy.** Once Object B is recognized as a Fisher-null-space
floor, *its* irreducibility under the agent's *metric/coordinate* freedom
(distinct from the similarity freedom that *generates* the
indistinguishable pair) *is* Sylvester, exactly as for Instance 2: no
reparameterization of the observation model refills a rank-deficient
observed-Fisher. The clean statement:

> Object B's floor has **two** group actions, and conflating them is the
> trap: (i) the *similarity* action $GL(n)$ that *generates* the
> indistinguishable architectural pair (this is what makes it a floor at
> all, and it is *not* Sylvester — it is the fiber of the realization-to-
> transfer-function bundle); (ii) the *congruence* action on the
> observed-Fisher that the agent could try to use to *escape* by
> reparameterization, which *is* Sylvester (and forbids the escape,
> exactly as Instance 2). Object B is therefore **mechanistically a member
> of the rank-collapse subclass** (escape-irreducibility = Sylvester, same
> as Instances 1–2), with the *additional* structure that the rank-deficient
> direction is a Lie-group fiber rather than a single indeterminacy
> manifold. It is the *closest sibling of Instance 2*, not a new mechanism
> and not Instance 3's projection/closure obstruction.

*Tier: exact.* The reduction (observation law factors through
similarity-invariants ⟹ observed-Fisher null on the fiber ⟹ Instance-2-
form rank-deficiency ⟹ Sylvester for escape-irreducibility) is a chain of
elementary, forced steps. **This is the sharpening the neutral-drift spike
§8 could not supply ("does NOT yet have a crisp closed-form ... and the
mechanism") and the triage explicitly deferred ("counterexample
construction informal ... not yet written").** It also *repairs* canon's
Sylvester-boundary Discussion: the correct taxonomy is **rank-collapse
{Instance 1, Instance 2, Object B} via Sylvester; composition {Instance 3}
via Schur/projection** — three rank-collapse members, not two, and Object A
*not in the taxonomy at all* (§3).

---

## 6. Object B's escape structure (E4) — does it have ≥2 genuinely distinct escapes? (exact for the count, robust-qualitative for the mappings)

A floor instance needs ≥2 structurally distinct escapes, each mapped to
AAT machinery. The neutral-drift spike proposed four (loop-interventional /
horizon-extended / higher-moment / architecture-instrumented). I
re-examine whether they are *genuinely distinct* or collapse — the triage's
honest worry (§4.2 of the triage: "is (b) genuinely distinct from (a)?").

Re-derived from the §4/§5 structure:

- **(a) Loop-interventional access** (`#der-loop-interventional-access`).
  An intervention $do(\cdot)$ on the agent's input perturbs *along a
  similarity-fiber direction* the on-policy data never excites. The
  intervention generates Level-2 data; the §4 no-go is Level-1-only.
  *Genuinely escapes:* the similarity orbit is invisible to the
  *observation* map but the *interventional response* map (correction-
  function Jacobian probed off the on-policy manifold) is **not**
  similarity-invariant — $TFT^{-1}$ and $F$ respond differently to a probe
  in a fixed external basis. Exact that this breaks the orbit
  degeneracy.
- **(b) Higher-moment / out-of-regime observation.** In the *nonlinear*
  sub-scope (AAT's $\beta$), two agents matched at $(\alpha,R)$ and second-
  order generally differ at moments $\ge 3$ (the neutral-drift spike §3 /
  `#der-interaction-channel-classification` Case 4 kurtosis diagnostic). In
  the linear-Gaussian sub-scope this escape **provably collapses**: a
  Gaussian innovation has no information beyond second order, so
  higher-moment observation adds nothing — the orbit stays invisible. *So
  (b) is a genuinely distinct escape only outside the linear-Gaussian
  sub-scope, and is provably void inside it.* This is a sharp scope
  statement, not a hedge — and it is exactly the kind of "the escape exists
  here, provably not there" boundary that makes a floor instance
  informative.
- **(c) Architecture instrumentation** (direct read of the update rule /
  internal state). Trivially escapes by breaking the black-box scope; it is
  the "you must instrument the latent" escape, structurally the *same kind*
  as Instance 2's escape (i) (instrument $C$). Genuinely distinct from
  (a)/(b) in *what the observer must have* (white-box access vs.
  interventional vs. moment).
- **(d) Horizon extension under the original policy** — the triage's
  suspected collapse. Re-derived: passive observation at longer horizons
  under the *same* policy generates *more samples of the same observation
  law*. The §4 no-go is a statement about the *law*, not the sample size:
  the similarity orbit is annihilated by the law, so no amount of *passive*
  data under the on-policy law escapes it. **(d) collapses into (a):**
  horizon extension escapes *only if* the extended horizon takes the agent
  *out of the current regime* (a regime change is an exogenous
  intervention-like event) — i.e. (d) is either void (same regime) or a
  special case of (a)/(b) (regime change = out-of-regime data). **The
  triage's worry is correct; I resolve it: (d) is not an independent
  escape.** Honest count: **three** genuinely distinct escapes
  (interventional / higher-moment-in-nonlinear-scope / white-box), not
  four. Three ≥ 2: **E4 passes, with the spurious fourth escape removed.**

*Tier: exact* for "(d) collapses, count is 3 not 4" and "(b) is void in
linear-Gaussian sub-scope"; *robust-qualitative* for the precise machinery
mappings (they depend on `#der-loop-interventional-access` /
`#der-agent-opacity` Discussion content that is itself robust-qualitative).

**E5 (strengthened consequence):** Object B elevates `#der-loop-
interventional-access` to a load-bearing role at the *agent-internal*
layer — and this is the **third semantically-distinct mode** the triage §4
correctly insisted must be stated honestly, not glossed (mode 1: agent-self-
intervention, Instance 1; mode 2: observer-on-sub-agent, Instance 3; mode
3: observer-on-agent-input, Object B). I confirm the triage's
disambiguation is mathematically right: the three "interventions" are
different group-theoretic objects (the §4/§5 similarity-orbit framing makes
this precise — Instance 1 breaks a *causal-sufficiency* degeneracy,
Instance 3 a *coupling-sign* degeneracy, Object B a *similarity-orbit*
degeneracy; the shared content is "Level-2 data breaks a Level-1
degeneracy," the distinct content is *which* degeneracy). E5 passes,
*robust-qualitative* (the three-mode claim is a real pattern; the per-mode
mappings inherit the robust-qualitative tier of the cited segments).

---

## 7. The Regime-C confound IS Object B (exact) — settling the CL-2 linkage

The mandate: "Your verdict also settles CL-2's heavy reserved refinement
(same object) — note the linkage but stay in the math." I derive the
identity rather than assert it.

The rho-recheck (`spike-rho-structure-recheck-2026-05-18.md` §3, verified
first-hand and re-derived in §1 of *this* spike via the same constitutive
C1/C3) establishes, *exact* tier:

$$\rho^2 = \rho_\star^2(\text{env};\pi) + \Delta_{\text{agent}}^2(\mathcal M;\pi,\text{env}) \quad\text{(2T, exact, GA-1 only)}$$

and that the *finer* split $\Delta_{\text{agent}}^2 = \Delta_{\mathcal
M}^2 + \Delta_\pi^2 + 2\chi$ is **conditional**: identified under Regime A
(interventional) or Regime B (functional-form), **confounded under Regime
C**.

**Claim (identity, exact).** "The M/π/cross split is confounded under
Regime C" *is* Object B's no-go projected onto the disturbance-statistic
coordinate. *Derivation:* $\Delta_{\text{agent}}^2$ is, by C3 (verified
first-hand, `#result-mismatch-decomposition` §Derivation step 3, `status:
exact`), $\nu\,\mathbb E\lVert\hat o_t - \bar o_t\rVert^2$ — a functional
of *the model's predictive mean*, i.e. of the agent's architecture
$(F,\sigma_w,\text{basis})$. Splitting it into a "model-class part" and a
"policy part" is asking: *from on-policy summary disturbance statistics,
can you attribute the reducible disturbance to architecture vs. policy?*
Under Regime C (the rho-recheck's "no interventional and no functional-
form assumption" — i.e. on-policy, summary-only) the split is confounded
**for exactly the reason §4 derives**: the architectural content lives in
the similarity orbit, which the on-policy law annihilates; you cannot
attribute disturbance to "this architecture" vs. "this policy" because the
architecture is only identified up to the orbit, and the orbit is what
$\Delta_{\mathcal M}^2$ would need to be pinned to. **Regime A escapes =
Object B escape (a) interventional. Regime B escapes = a functional-form
restriction that quotients the orbit (the §6 (b)/(c) family).** Same
no-go, same escapes, same mechanism (§5 Fisher-null-on-the-orbit) — viewed
once from the agent side (neutral-drift), once from the disturbance side
(rho-recheck). $\square$

**This confirms, mathematically, the mandate's "same object" linkage and
the navigator's CL-2 heavy-reserved scoping** (PROPOSALS §D.9 / TODO:147:
"the conditional M/π/cross split (Regime-C confound), which is provably the
same object as the identifiability-floor 4th-instance question"). It is
provable, and now proved here, not asserted. The convergence of three
independent derivations (neutral-drift agent-side, triage taxonomy-side,
rho-recheck disturbance-side) onto one object is — per the convergence-as-
coherence discipline — strong evidence the object is in the framework, not
in any one agent's head. *I treated the convergence as a locating proxy
(§0) and then settled it by the §4–§7 re-derivation; it is the
re-derivation, not the convergence, that certifies it.*

---

## 8. Verdict, conditions, tier, confidence

**The unified question, settled:**

> The contested "Instance 4" was always **two objects under one ordinal**.
>
> **Object A** (universal-$C$ under non-(PI) norms, canon's stated Instance
> 4): **NOT a floor instance — a category error in
> `disc-identifiability-floor.md`.** It is a true counter-example-grade
> no-go, but a *downstream theorem of the (PI) commitment*, structurally a
> member of the **`#disc-additive-coordinate-forcing`** family (its 4th
> primary instance's client), exactly as its own source segment
> `#deriv-observation-ambiguity-bias-bound` §4 already states in canon. The
> defect is localized in `disc-identifiability-floor.md` (the instance
> list + the "four results" Findings count + the Related-Work table),
> which contradicts (i) its cited source segment and (ii) its *own*
> Sylvester-mechanism Discussion that silently covers only three floors. A
> §4.1-class canon-honesty defect. *Tier: exact* (§2 constitutive + §3
> internal-consistency, two independent routes).
>
> **Object B** (architecturally-distinct, behaviorally-identical agents
> unidentifiable from on-policy summary data; neutral-drift §8 / triage
> Candidate 2 / rho-recheck Regime-C confound): **a GENUINE floor
> instance.** Conditions and tier:
> - **Exact** in the linear-Gaussian / Kalman sub-scope ($\alpha_1$): the
>   no-go is the Kalman-Ho similarity-orbit construction (§4), crisp and
>   closed-form — the construction the neutral-drift spike said it "does
>   NOT yet have" and the triage deferred; supplied here.
> - **Robust qualitative** in the general sub-scope, dual-anchored by CHT-
>   at-agent-as-SCM (general) and Kalman-Ho (sharp sub-scope) — the same
>   exact/robust split Instance 1 carries.
> - **Mechanism: exact** — it is the **Instance-2 mechanism** (Fisher-
>   information null space along a structurally-forced indeterminacy
>   manifold), the manifold being the $GL(n)$ similarity fiber; **NOT
>   Sylvester-for-free** (the mandate's caution is exactly right — the
>   *generating* group action is similarity, not congruence) but its
>   *escape-irreducibility* IS Sylvester at one remove, identically to
>   Instance 2. It is therefore a member of the **rank-collapse subclass**
>   {I1, I2, B}, not a new mechanism and not Instance 3's
>   projection/closure obstruction. This *repairs* canon's Sylvester-
>   boundary Discussion (which currently covers only three floors and
>   places none in a "fourth" mechanism slot).
> - **Escapes (E4): three** genuinely distinct (interventional /
>   higher-moment-in-nonlinear-scope / white-box), with the neutral-drift
>   spike's proposed fourth ("horizon extension") provably collapsing into
>   the interventional one (the triage's worry, here resolved). Fano (the
>   neutral-drift spike's proposed anchor) is the *finite-sample*
>   refinement, **not** the exact-population anchor — it degenerates to a
>   vacuous bound at $I=0$; the exact anchor is Kalman-Ho. A demonstrated
>   no-go on the proposed Fano anchoring, not an assertion.
>
> **The Regime-C confound (CL-2's heavy reserved refinement) IS Object B**
> projected onto the disturbance-statistic coordinate — *proved* (§7), not
> asserted; same no-go, same escapes (Regime A = interventional, Regime B =
> functional-form orbit-quotient), same mechanism. *Tier: exact.* This
> settles, at the mathematical level, the mandate's "same object" linkage
> and the navigator's CL-2 scoping.

**Three legitimate outcomes, which obtained:** the mandate named (a)
genuine→sharpen, (b) triage-right→category-error-to-mark, (c)
deeper/different no-go or precise boundary. **All three obtained, on the
correctly-split objects:** (b) for Object A (category error, exact); (a) for
Object B in the Kalman sub-scope (sharpened to exact-with-conditions, with
the closed-form construction supplied); (c) the precise boundary throughout
— the §4 strengthening attempt closing into the Kalman-Ho no-go, the §5
mechanism being Instance-2-not-Sylvester-not-new, the §6 (b)/(d) escape
collapses, the Fano-is-finite-sample-not-the-floor no-go. The resolution is
that the disagreement among the four documents was an *artifact of
conflating two objects under one ordinal*; once split, canon is wrong on
Object A and incomplete (a sketch) on Object B, the triage is right in
direction but was a judgment not a derivation, the source segment §4 is
right on Object A, and the rho-recheck is right and is the same object as
Object B.

**Confidence (epistemic ladder, AAT tiers):**
- Object A is a category error / not a floor: **exact** (two independent
  routes — constitutive (PI)-status §2; canon-internal Sylvester-taxonomy
  §3 — agreeing, and agreeing with the source segment's own canon text).
- Object B is a genuine floor; exact in Kalman sub-scope, robust-
  qualitative general: **exact in sub-scope** (Kalman-Ho is classical and
  the AAT mapping is forced) / **robust-qualitative general** (CHT-anchored,
  same standing as Instance 1).
- Object B's mechanism is Instance-2 rank-deficiency, not Sylvester-free,
  not new: **exact** (the reduction chain is elementary and forced).
- Regime-C confound ≡ Object B: **exact** (§7 derivation from C3, a
  `status:exact` segment).
- The escape count (3 not 4; Fano is finite-sample not the floor):
  **exact** for the collapses; **robust-qualitative** for machinery
  mappings.

**Honest residual uncertainty.** (1) The general (non-linear-Gaussian)
sub-scope no-go rests on CHT-at-agent-as-SCM, which is robust-qualitative
(an agent is an SCM over its state space — a modeling reading, exactly as
Instance 1's general case is). I did not, and cannot at population identity,
upgrade it to exact in general; the *exact* claim is sub-scope-bounded and
labeled so. (2) The §6 escape-machinery mappings inherit the robust-
qualitative tier of `#der-loop-interventional-access` / `#der-agent-
opacity`; I did not re-derive those segments. (3) The finite-sample Fano
refinement (architectures close-but-not-equal) is real research I did
*not* close — flagged honestly as the refinement, not over-claimed as the
floor, exactly the integration-is-replacement / no-go-is-present-truth
discipline.

---

## 9. Notes for the parent (routing / canon disposition is the parent's call — NOT done here)

Truth-establishing only, per mandate. Recording what the math implies for
the parent's disposition, not executing it:

1. **Object A / canon's Instance 4 is a §4.1-class category error**
   (`disc-identifiability-floor.md`: the Instance-4 block, the "four
   independently-derived results" Findings count line 166, the
   Related-Work table row line 179, the Working-Notes "seven ladders"
   inconsistency). The honest present-truth is: Object A is a downstream
   theorem of `#disc-additive-coordinate-forcing`'s 4th instance —
   *which the source segment `#deriv-observation-ambiguity-bias-bound` §4
   already states correctly.* So the canon defect is **one-sided**: fix
   `disc-identifiability-floor.md` to match its source, do not touch the
   (already-correct) bias-bound segment. This is a present-truth mark
   (audit-routing §4.1/§6: the no-go/correct-classification is canon, not a
   ghost) — but **executing it is the parent's canon-disposition call**,
   and it touches a Joseph-reserved meta-segment-structure decision
   (whether the meta-segment renumbers, whether Object B *replaces* Object
   A as Instance 4, or whether the slot is vacated). Flagged, not edited.

2. **Object B is the genuine fourth floor and is now derived to exact-in-
   sub-scope** (the Kalman-Ho construction §4 + the Instance-2-mechanism
   recognition §5), no longer a sketch (neutral-drift §8) or a judgment
   (triage). If the parent/Joseph elects to land it, the *exact spine is
   short*: §4's similarity-orbit no-go + §5's "it is Instance-2's
   mechanism with a Lie-fiber null space" + §6's three-escape structure.
   The heavy/Joseph-reserved part is only the meta-segment renumbering and
   the Sylvester-boundary Discussion repair (three rank-collapse members,
   not two; Object A removed from the count).

3. **CL-2 linkage is now mathematically settled, not just asserted** (§7).
   The "heavy Joseph-reserved refinement" of CL-2 (the conditional
   M/π/cross split, Regime-C confound) *is* Object B. This means CL-2's
   heavy part and the `spike-neutral-drift-…` Joseph-reserved batch and the
   contested-Instance-4 question are **one decision, not three** — landing
   Object B as the genuine floor instance simultaneously supplies the
   Regime-C confound its honest home (the rho-recheck §3 already says
   "its honest home is the `#disc-identifiability-floor` pattern" — §7
   proves it). The parent may wish to collapse the three navigator items
   (TODO:147 CL-2 heavy, TODO:452 neutral-drift, the Instance-4
   contradiction) into one Joseph-reserved present-truth decision. Flagged.

4. **The triage spike (`spike-identifiability-floor-instance-triage-
   2026-04-24.md`) is *correct in direction* on Objects A and B but its
   verdicts are explicitly *robust-qualitative judgments*, not
   derivations** (it self-labels "five-element test ... Discussion-grade").
   This spike supplies the missing derivation underneath its Candidate-2
   (=Object B) and Candidate-3 (=Object A) verdicts. Recommended parent
   disposition signal (parent decides): the triage is **not a regression
   risk** — it argued *against* canon's Object-A-as-Instance-4 and was
   right; canon drifted away from the triage, not toward it. (The §2a
   regression axis: canon's Instance-4 block is the *less-true, cleaner-
   looking* claim; the triage + source-segment §4 + this spike are the
   *uglier, truer* position — "it looks better" body-signal points the
   right way here: the floor-instance framing of Object A "looks better"
   precisely because it is the over-clean wrong thing.)

5. **The neutral-drift spike's proposed Fano anchor is a no-go**
   (degenerates at $I=0$; §4). If Object B lands, anchor it Kalman-Ho
   (exact sub-scope) + CHT (general), *not* Fano; note Fano as the
   finite-sample refinement, an honest open. This corrects the neutral-
   drift spike's §8/§10.1 proposed anchoring — surfaced, not edited.

---

## 10. Reasoning-trail appendix (what I checked, in order; the dead ends)

1. Read `doc/spike-routing.md` §0 + `audit-routing-instructions.md` in full
   — internalized: truth re-derived on constitutive structure is the sole
   arbiter; canon/triage/segment/navigator/convergence are drifting
   proxies; strengthen-before-soften; a no-go is present truth, demonstrate
   not assert; "it looks better" is the regression body-signal.
2. Read canon `disc-identifiability-floor.md` in full. **Key recognition:**
   the §"Findings" says "four ... results" and the Related-Work table lists
   Object A as the fourth, but the load-bearing Sylvester-mechanism
   Discussion (line 150) + boundary-facet Finding (line 191) only taxonomize
   *three* (rank-collapse I1/I2; projection I3) and explicitly say "the
   floors do not share one mechanism." Internal incoherence located.
3. Read `deriv-observation-ambiguity-bias-bound.md` in full (the cited
   source for Object A). **Decisive:** its own canon §4 line 127 says
   Object A "does not match the five-element test ... downstream theorem
   of the (PI) commitment, not a new floor instance." Segment vs. its
   source contradict — §4.1 shape.
4. Read both disagreeing spikes (triage; neutral-drift) + the rho-recheck
   in full. Logged claims as proxies.
5. Verified constitutive bedrock first-hand (NOT via the spikes' readings,
   per §0): `#def-mismatch-signal` (`axiomatic`; $\delta=o-\hat o$, $\hat
   o$ a functional of $M$); `#result-mismatch-decomposition` (`exact`;
   GA-1 already canon; cross-term zero by orthogonality derived in canon
   step 3); `#deriv-sector-condition` (`exact`; lines 212–228 Itô-Lyapunov,
   disturbance enters only via $\tfrac12\sigma_w^2 n\,dt$, drift cross-term
   zero-mean martingale — the rho-recheck §4 reading is accurate);
   `#scope-agent-identity` (`robust-qualitative`; (PI) is "a genuine
   axiomatic choice ... not directly forced" — the load-bearing fact for §2);
   `#disc-additive-coordinate-forcing` (line 44: (PI)+Čencov→Fisher is its
   4th primary instance; line 56: the bias bound is its *client*).
6. Derived §2 (Object A not a floor, constitutive, exact): three forced
   steps — external-theorem role inverted (Čencov forces the escape, does
   not forbid); single forced escape by Čencov *uniqueness* (logically
   incompatible with ≥2-escape floor structure); consequence is re-use of
   already-load-bearing (PI), not elevation.
7. Cross-checked §3 (canon's own Sylvester taxonomy silently excludes
   Object A — independent confirmation, exact). Two routes agree.
8. Strengthen-first on Object B (§4): tried to make "identifiable" true in
   the strongest (Kalman) sub-scope; it closed into the classical Kalman-Ho
   similarity-orbit no-go — supplied the crisp construction the prior
   documents lacked. Tested Fano: degenerates at $I=0$ (exact construction
   has $I=0$, not small); Fano is finite-sample refinement, not the floor.
   No-go on the proposed Fano anchor, demonstrated.
9. Derived §5 (mechanism): NOT Sylvester-free (generating action is
   similarity $TFT^{-1}$, not congruence $S^\top(\cdot)S$ — mandate's
   caution exactly right); NOT new (reduces to Instance-2 Fisher-null-on-
   indeterminacy-manifold, manifold = $GL(n)$ fiber; escape-irreducibility
   = Sylvester at one remove, same as I2). Repairs canon's three-floor
   taxonomy → {I1,I2,B} rank-collapse, {I3} projection, A not in it.
10. §6 escape audit: resolved the triage's "(b) vs (a) distinct?" worry —
    (d) horizon-extension collapses into (a); (b) higher-moment void in
    linear-Gaussian, distinct only in nonlinear scope; honest count 3 not
    4; E4 passes.
11. §7: derived Regime-C confound ≡ Object B from C3 (`exact` segment),
    not asserted — settles CL-2 linkage mathematically.
12. Dead ends / not chased: (i) finite-sample Fano refinement (real
    research, correctly left open, not over-claimed); (ii) the general
    non-Gaussian no-go beyond CHT-at-agent-as-SCM (cannot upgrade past
    robust-qualitative at population identity — labeled, not forced);
    (iii) whether the meta-segment should renumber / vacate / replace the
    Instance-4 slot — that is a Joseph-reserved canon-structure decision,
    explicitly out of scope for a truth-establishing spike (§9).

---

*End of spike. Verdict: the contested "Instance 4" was two objects under
one ordinal. Object A (universal-$C$ under non-(PI) norms) is NOT a floor
instance — it is a downstream theorem of the (PI) / additive-coordinate-
forcing 4th instance, and canon's `disc-identifiability-floor.md` listing
it as Instance 4 is a §4.1-class category error (exact; agrees with its own
source segment §4 and with its own Sylvester-mechanism Discussion). Object B
(architecturally-distinct behaviorally-identical agents unidentifiable from
on-policy summary data; = the neutral-drift gap = the rho-recheck Regime-C
confound) IS the genuine fourth floor: exact in the linear-Gaussian/Kalman
sub-scope via a now-supplied closed-form Kalman-Ho similarity-orbit no-go,
robust-qualitative in general via CHT-at-agent-as-SCM; its mechanism is the
Instance-2 Fisher-null-on-indeterminacy-manifold (NOT Sylvester-for-free —
the mandate's caution is correct — but its escape-irreducibility IS
Sylvester at one remove, making it the rank-collapse subclass's third
member, not a new mechanism); three genuine escapes not four (Fano is the
finite-sample refinement, not the floor — a demonstrated no-go on the
proposed anchor). The Regime-C confound is proved to be the same object
(§7), settling the CL-2 heavy-reserved linkage at the mathematical level.
All three of the mandate's legitimate outcomes obtained, on the correctly-
split objects. Routing / canon disposition / meta-segment renumbering is
the parent's and Joseph's reserved call; not done here.*
