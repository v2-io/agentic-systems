# Manifest — 2026-05-19 integration-cycle archive batch

22 spike files/dirs moved from `spikes/` (and one from `msc/`) to `spikes/.integrated/` in commit **`9a3db8e`** ("Archive integrated spikes from the last day or so. INDEX WASN'T UPDATED"). This manifest is the durable per-cycle move record the commit itself did not carry; it is written as part of the same reconciliation that brought `spikes/INDEX.md` and [`spikes/ROUTING.md`](../ROUTING.md) back to current truth (the "INDEX WASN'T UPDATED" follow-through). Cycle narratives: [`../../CHANGELOG.md`](../../CHANGELOG.md) 2026-05-18 / 2026-05-19 (F-ADJ-1, accumulation-type confound, continuity-persistence, Φ(fight)).

## Provenance — distinct from the 2026-05-17 spike-routing batch

This is **not** a spike-routing-cycle batch. These spikes were the reasoning trails and decision/verifier artifacts of the *integration cycles* Joseph and a sequence of agents ran across 2026-05-18 and the 2026-05-19 morning (the F-ADJ-1 two-operator split; the accumulation-type confound; continuity-persistence; Φ(fight)). The math landed in canon through those cycles' own commits — not through a routing `git mv`:

- **`da3c6b4`** — F-ADJ-1: `der-turnover-information-recursion` (Vol III §03.II, the $\mathcal A_D$ predictive-sufficiency segment) + `der-identity-continuity-threshold` (Vol IV §04.1, the $\mathcal A_{\mathrm{refl}}$ identity segment) created; `disc-m-preservation` omission-fixed and its wrong additive inequality deleted-and-replaced (integration-is-replacement, forward).
- **`23ae523`** — F-1 corrective: the truthful continuity history layer committed (RESULT.md §5 over-unification *withdrawn* in body with the history in a callout; RECONCILIATION.md tracked) after the closing verifier found that as the one durability defect.
- **`d098997`** — Φ(fight) C1: `form-resource-budget` + `der-resource-bounded-destabilization` landed as an *exploratory off-spine branch* (OUTLINE App A Stage `exploratory`; NOTATION updated; no spine segment depends on either).
- **`aabcf8e` / `639e7e2` / `5158ed5`** — the accumulation-typing canon frame: A1 (`result-sector-persistence-template` as the two-model typed bridge), A3 (the D3 register answer in `disc-stability-certificate` — accumulation-typing is the Interior facet's temporal dual, no fifth meta-segment), D1 (NOTATION accumulation-typing convention).

The 22 renames in `9a3db8e` are **pure `git mv`** (0 insertions, 0 deletions; Joseph-authored), reversible, content unchanged. To find the move in `git log`, look at `9a3db8e`; this manifest + `ROUTING.md` + the CHANGELOG are the durable record of *what* moved and *why*.

## The verification guarantee this batch carries

Per-cycle and content-verified — the forward `.integrated/` guarantee of [`../README.md`](../README.md), satisfied here by the integration cycles' own independent gates **plus** a first-hand decisive-test spot-check at this reconciliation (the named `src/` loci re-opened, frontmatter + Epistemic Status + the integration-is-replacement direction confirmed; the §2a regression axis run via the exclusion-pickaxe logic — the refuted forms are absent from canon, the wrong additive inequality was replaced *forward*, the §5 subsumption lives only in RESULT.md's history callout):

- **continuity-persistence** — independently *adjudicated* (`adjudicate-disc-m-preservation-operator.md` §0, verdict (ii)-with-(iii): the two-operator split is structurally true, re-derived from `#obs-context-turnover`'s structure and the singularity argument, independent of artifact chronology) **and** independently *coherence-verified* (`verify-cycle-coherence.md` §0: the split is structurally true independent of every artifact in the chain; one durability defect F-1 found → fixed in `23ae523`), **and** the embedded RESULT.md independent review (Opus 4.7, 1M, did not write the spike): CONCUR on the no-go and non-transfer; Fixes 1 (III→IV `depends:` inversion) and 2 (eq-(2) chain-rule-not-sub-additivity) applied before landing; the recommended pointwise-vs-uniform scope sentence applied.
- **Φ(fight)** — exploratory; author-gated; Joseph elected to open the resource-structure axis. The C1 guess (environment-mediated coupling as a new directed-separation locus) was *refuted* and recorded as a documented dead-end / guardrail (`fight/99-verdict.md` §4); C2 is a verified bridge that stays trail-only (downstream of the still-unlanded `spike-leakage-locus-2026-05-18.md`).

The guarantee is forward/per-cycle and not retroactive (`README.md`); it makes no claim about pre-policy `.integrated/` residents.

## Deliberately NOT moved (this cycle's trail, but left in `spikes/`)

`9a3db8e` archived the *spent* trail/decision/verifier artifacts. Two spikes named in `verify-cycle-coherence.md` §2(7) as committed reasoning-trail members of this arc were deliberately left in `spikes/` and are **not** in this batch — their disposition is separate and is surfaced for Joseph, not folded here:

- **`spike-composition-scaling-N.md`** — practica's ready composition spike (own disposition; not a spent trail artifact of these cycles).
- **`spike-strategy-dag-composition.md`** — recently active (mtime 2026-05-18); separate disposition.

Filing either into `.integrated/` on the strength of trail-membership alone would be the directory-label collapse `doc/spike-routing.md` §5 exists to prevent. They stay until adjudicated on their own terms.

## Archived spikes / dirs — per-file justification

| Spike / dir | Landing locus (first-hand verified) | Completion | Verifier(s) |
|---|---|---|---|
| `continuity-persistence/` (00–04, 98, 99, FRAMING, INTEGRATION, RECONCILIATION, RESULT) | `der-turnover-information-recursion.md` (03-llm §03.II, `exact`-given-argued-commitment — R1 geometric no-go, non-transfer of `#result-sector-persistence-template`, R2 conditional-positive, R3 replacement); `der-identity-continuity-threshold.md` (04-eli §04.1, `conditional`-with-exact-core — the $\mathcal A_{\mathrm{refl}}$ reflected-Lindley walk); `disc-m-preservation.md` (omission-fixed; wrong additive inequality deleted-and-replaced forward) | (B) no-go on critical path + conditional-positive corollary + model-refinement replacement (integration-is-replacement) | adjudicate-disc-m-preservation-operator → verify-cycle-coherence + RESULT.md embedded review |
| `adjudicate-disc-m-preservation-operator.md` | Decision artifact: its verdict (ii)-with-(iii) *is* the F-ADJ-1 two-operator split realized in the segments above | adjudication realized in canon | self (independent adjudicator, ≠ continuity steward) |
| `spike-accumulation-type-confound.md` | The V1 $\{\mathcal A_D,\mathcal A_S,\mathcal A_{\mathrm{refl}}\}$ split; realized as canon via the F-ADJ-1 split + NOTATION accumulation-typing (D1, `5158ed5`) + `result-sector-persistence-template` two-model framing (A1, `aabcf8e`) + `disc-stability-certificate` D3 register answer (A3, `639e7e2`) | landed across A1/A3/D1 + F-ADJ-1 | verify-A-restatement-model-DS, verify-cdmp-corrected-statement, verify-cycle-coherence |
| `for-joseph-acc-confound.md` (was `msc/`) | D1–D4 decision memo (the $\eta\to\varrho_{\text{rg}}$ hard precondition; conditional-with-exact-core tier requirement). D1 decided; D3 register answer landed (A3); D4 was Joseph's. Promoted from delete-at-any-time `msc/` to the durable trail home | decision record, resolved/gated-as-recorded | verify-cycle-coherence §2(7) |
| `verify-A-restatement-model-DS.md` | Independent-verifier artifact ($\mathcal A$-restatement splits D/S) for the accumulation-confound arc | trail (independent verify) | self |
| `verify-cdmp-corrected-statement.md` | Independent-verifier artifact (C-DMP corrected statement) for the accumulation-confound arc | trail (independent verify) | self |
| `verify-cycle-coherence.md` | Closing independent coherence verifier of the accumulation-type-confound canon-integration cycle (§0 verdict; found F-1 → fixed `23ae523`) | trail (closing verify) | self |
| `fight/` (00–03, 99-verdict) | `form-resource-budget.md` + `der-resource-bounded-destabilization.md` (01-aat App A, `conditional`, exploratory off-spine; NOTATION updated; no spine `depends:`) | C1 (1) landed via strengthen-before-soften succeeded; C1 guess refuted = documented dead-end/guardrail; C2 verified bridge, trail-only | fight/99-verdict (author-gated; Joseph elected the axis) |

INDEX.md row reconciliation for the 2026-05-19 sections is done in the same reconciliation pass that wrote this manifest (the "INDEX WASN'T UPDATED" follow-through), tracked in `ROUTING.md`. `git mv` is reversible.
