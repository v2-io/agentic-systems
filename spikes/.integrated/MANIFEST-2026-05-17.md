# Manifest — 2026-05-17 spike-routing cycle (verified batch)

10 spike files moved from `spikes/` to `spikes/.integrated/` on
2026-05-17 as the durable batch of the spike-routing cycle
([`msc/spike-routing-2026-05-17.md`](../../msc/spike-routing-2026-05-17.md);
process: [`doc/spike-routing.md`](../../doc/spike-routing.md) +
[`doc/audit-routing-instructions.md`](../../doc/audit-routing-instructions.md)).

**This batch carries a stronger guarantee than the 2026-05-12 bulk
move.** Each spike here was (1) adjudicated `integrated-misfiled` by a
fan-out agent that opened the `src/` loci first-hand, then (2)
**independently re-verified by a different agent** (the audit-routing §8
adjudicator-≠-confirmer gate) who re-opened the actual loci — not the
INDEX label, not the adjudication summary. This is the *forward,
per-cycle* `.integrated/` guarantee described in
[`../README.md`](../README.md): content verified present in canon. (The
guarantee is not retroactive; pre-policy `.integrated/` residents,
including the 2026-05-12 bulk, were not re-audited — by design.)

`git mv` is reversible; the per-file audit trail is below.

> **Provenance note (honest archaeology).** The 10 `git mv` renames were
> staged during this cycle but were swept into the concurrent
> pipeline-authors' commit **`0834649`** ("Pipeline: retain Introduction
> label …") rather than a dedicated spike-routing commit — a
> co-mingling Joseph pre-authorized at cycle start ("if something from
> the other spike work gets committed accidentally, that's fine"). The
> renames are 100%-similarity (no content change), reversibility is
> intact, and nothing was lost. History was deliberately **not**
> rewritten: reverting a commit the pipeline authors built on would be
> the genuinely destructive, non-independent move. To find the move in
> `git log`, look at `0834649`, not a spike-routing commit. This MANIFEST
> + the routing tracker are the durable spike-routing record of *what*
> moved and *why*; `0834649` is merely *where* the rename bytes landed.

**This cycle is the disciplined follow-through on the 2026-05-12
manifest's deliberate exclusions** — that bulk move explicitly held back
the operator-sector / dissipativity family ("pending architectural
decision") and the attention-spikes ("NEEDS-JOSEPH-JUDGMENT"). Those are
now resolved or correctly routed (below).

## Excluded from this batch (verified integrated, but NOT moved — reason)

- **`spike-bridge-lemma-nonlinear-strengthening-2026-04-24`** — §7.1 is
  in canon, but the file also carries the §7.2 passivity/port-structure
  **orphan strand** (coupled cluster CL-1, `#dissipativity-template`).
  Moving it would make `.integrated/` lie about §7.2. Stays until CL-1
  lands.
- **`spike-attention-causal-graphs`** — core verified canon, but its
  `git mv` is **coupled** to the Joseph-reserved `spike-attention-governance`
  decision (same `446c7a1` overnight residual cluster). → Joseph batch.
- **`spike-language-as-causal-substrate/`**, **`spike-c2-star-to-integrate`**
  — real orphaned theory whose landing is a reserved promotion call
  (cross-repo source-of-truth in review). → Joseph batch.
- **`class-coercion-wrapping/`**, **`track-a-intent-dag/`**,
  **`track-b-nonlinear-sims/`** — directory spikes; content verified in
  canon and recommendation is `integrated-misfiled` with no reserved
  theory decision, but per `spike-routing.md` §6 the dir-spike filing
  decision is Joseph-adjudicated (lighter gold gate), not parent
  auto-file. → Joseph dir-batch (clean/fast yes expected).
- **Orphaned / heavy / reserved** (stay in `spikes/`):
  `spike-rho-factorization` + `spike-rho-additive-variance-strengthening-2026-04-24`
  (CL-2), `spike-update-operator-sector` (tractable orphan landing, this
  cycle), `spike-alignment-impossibility` (§7 strengthening orphan),
  `spike-identifiability-floor-instance-triage-2026-04-24` /
  `spike-neutral-drift…` (Joseph-reserved triage-contradicted),
  `spike-passivity-composition` / `spike-pid-a2prime` (CL-1).
- **`live-or-open`** (stay): `spike-transient-dependency-amplification`,
  `spike-kl-to-state-distance-template-extraction-2026-04-24`,
  `spike-aporia-sub-agent-adversarial`, `spike-strategic-self-coupling`,
  `spike-local-embedding-benchmark/`, `neurips-back-integration-2026-05-08`.

## Archived spikes — per-file justification

| Spike | Landing locus (first-hand verified) | Completion | Adjudicator → Verifier |
|---|---|---|---|
| spike-operator-sector-unification | `result-certificate-existence.md` (`exact`), `disc-stability-certificate.md` (plural no-go), `deriv-sector-condition.md:84-94` | (B) strengthened-past | pilot 023198 → S2 471639 |
| spike-fenchel-bregman-reframe-additive-coordinate-forcing-2026-04-24 | `deriv-strategy-cost-regret-bound.md:182-196`, `disc-additive-coordinate-forcing.md:14-88` (full meta-reframe incl. axiom-independence guard) | both halves landed | S1 417303 → V1 624813 |
| spike-composition-gaps | `hyp-directed-separation-under-composition.md` (Gap 1, *sharper* — Case-3 excised); Gap 2 16-cell arg-max; residue SP-17/SP-20 | landed + tracked | S3 417739 → V2 504612 |
| spike-strategy-dynamics-gaps | `scope-edge-update-causal-validity` (ι_ij), `form-strategy-complexity-cost`, `def-strategic-tempo`, `disc-exploit-explore-deliberate` | 4/4 landed (2 strengthened-past) | S3 417739 → V2 504612 |
| spike-stochastic-non-exit-strengthening-2026-05-16 | `deriv-stochastic-non-exit.md` (`exact` appendix), `deriv-sector-condition.md` (Cor A.1S.1, (iii′)/(iv)); cascade closed `result-sector-persistence-template.md:90` | (3) no-go, textbook 5A | S4 111710 → V2 504612 |
| spike-active-inference-vs-aad | `form-strategy-complexity-cost.md` (G-BP2; KL direction now *derived*) | strengthened-past | S4 111710 → V2 504612 |
| spike-l1-evidence-axiom | `deriv-edge-update-natural-parameter.md:135` (generalization-in-scope; dual-obstruction → Instance 2) | landed | S4 111710 → V2 504612 |
| spike-jacobian-b1-strengthening | Angle-2 transparency note + Angle-3 PI/Čencov additive-coordinate layer (Legendre–Fenchel unification); strong/heredity correctly left open | strengthened-past (partial, honest) | S4 111710 → V2 504612 |
| spike-fep-suboptimal-approximation | `disc-ciy-unified-objective.md:58/64/66` (dark-room bypass, EFE-isomorphism, no-dominance — spike's §5 scope honored) | landed (scope-honored) | S5 029307 → V3 738041 |
| spike-message-passing-credit-assignment | `disc-credit-assignment-boundary.md:130/95/87` (EP/loopy-BP/Max-Sum + L1 floor); refuted mean-field-VMP core excluded-not-ghosted (grep-confirmed) | integration-as-replacement | S5 029307 → V3 738041 |

INDEX.md reconciliation for these rows is deferred to cycle-close (it
carries concurrent active-author edits; editing now would co-mingle —
independence constraint). Tracked precisely in the routing tracker.
