# Manifest — 2026-05-31 deeply-mathematical de-novo audit (AUDIT-WORKING-526815) strengthen-first batch

Four strengthen-first spikes moved from `spikes/` to `spikes/.integrated/` in this batch-archive commit, after their content landed in canon and was independently residue-verified as FULLY-INTEGRATED. Each began as a challenge from the deeply-mathematical de-novo auditor (AUDIT-WORKING-526815) against an `exact`/`axiomatic`-status move in Part II / the value-object / objective-functional / continuity chapters; each was worked strengthen-first to a (B)-scoped-sharpening (two with an attendant no-go), and landed per *integration-is-replacement* (refuted form deleted, present truth in segment bodies, history in CHANGELOG + Working Notes). This manifest is the durable per-cycle archival record naming what each spike contributed to canon with commit hashes, per the forward `.integrated/` verification guarantee of [`README.md`](README.md).

Cycle narrative: CHANGELOG 2026-05-30 (Causal Access; Value-Object Convention-Monotonicity; Objective-Functional Representation; Continuity Orthogonality entries). The integration commits land per-spike; this archive commit follows the move + this manifest + the INDEX/PROPOSALS navigator reconciliations.

## Verification per spike

- **`spike-causal-access-transportability-2026-05-30.md`** — **commit `52d85b2`** (Option A). Verdict **(B)+no-go**. Challenge: the singular-trajectory$\to$Pearl-$do$ grounding and the forkability$\to$Level-1 step in Part II's Causal Access chapter. Read directly against the primary sources (`ref/bareinboim-2022-pearl-hierarchy.pdf` Defs 4–10 / Thm 1; `ref/r443.pdf` transportability Defs 1–2 / Thm 1) the challenge holds: forkability is orthogonal to the $\mathcal L_1 / \mathcal L_2$ distinction (CHT footnote 19 names forkable RCTs / A-B tests as the canonical Level-2 *source*); the $do$-operator is population-level by construction; the genuine sandbox ceiling is **transportability / external validity** (selection diagrams, R-443) — *sharper, not weaker*. Canon landings: `#der-loop-interventional-access` (exact loop-as-Level-2 claim gated on (C1)–(C3) in Formal Expression, trajectory-grounding corrected); `#disc-sandbox-evaluation-ceiling` (false forkability$\to$Level-1 step deleted, replaced with a transportability / external-validity no-go via Bareinboim–Pearl selection diagrams); `#impl-causal-access` (same reframe); `#scope-agent-identity` (consequence-3 corrected in place); `#def-pearl-causal-hierarchy` (Kalman+LQR row); `#scope-ciy-observational-proxy` (Regime-A wording). The (C1)–(C3) gate is shared verbatim with the value-object spike's Front-2 landing (below).

- **`spike-value-object-convention-monotonicity-2026-05-30/`** (directory: `99-verdict.md` + `sim-rh-counterexample.py` + `sim-rh-strengthening.py`) — **commit `3da7e32`** (Option A). Verdict **Front-1 (C)-no-go-in-(B); Front-2 (B)-sharpening**. Challenge: two `exact`-status moves in `#def-value-object`. **Front 1** (the C2 receding-horizon monotonicity rung $A_O^{(1)} \le A_O^{\text{RH}}$): the **left rung is false in general** for a replanning window $N_r \lt N_h$ — a two-state deterministic myopic-trap counterexample (numerically confirmed in `sim-rh-counterexample.py`) shows the unguarded short-horizon replanner underperforms the frozen current policy; it holds exactly under any one of three named, structurally-checkable order-consistency conditions (RH-1) $N_r \ge N_h$ / (RH-2) value-compared rollout guard / (RH-3) control-Lyapunov terminal cost. The **right rung $A_O^{\text{RH}} \le A_O^{\text{B}}$ stays exact, unconditionally.** **Front 2** ($Q_O$ causal validity): identifying $P(o \mid do(a), M_t)$ additionally needs (C1) positivity / (C2) no-unmodeled-confounder / (C3) identified action-transition mechanism — the same (C1)–(C3) as the sibling causal-access spike. Canon landings: `#def-value-object` (Front-1 right rung exact-unconditional, left rung a (C) no-go gated on (RH-1/2/3); Front-2 $Q_O$ causal-validity gated on (C1)–(C3)); `#def-satisfaction-gap`; `#def-control-regret`; `#der-orient-cascade`; new appendix `#deriv-convention-monotonicity` (the myopic-trap counterexample as demonstration, per *math-lives-in-segments*). Front-2 (C1)–(C3) reconciled verbatim with `#der-loop-interventional-access`.

- **`spike-objective-functional-representation-2026-05-30.md`** — **commit `9fb7c12`**. Verdict **(B)** (scoped representation result). Challenge: the revealed-preference defense in `#form-objective-functional` grounds the real-valued codomain $V_{O_t}: \text{trajectories} \to \mathbb{R}$ as "follows from the comparability requirement," but a local choice relation is not a total order over all trajectories, and recovering a representable real-valued functional needs named axioms. Worked against the decision-theory machinery the challenge holds: acting yields a choice function, scalar-representable iff coherent (GARP / Afriat 1967, existence not uniqueness); a complete preorder is real-valued-representable only under continuity (Debreu 1954 — lexicographic preferences are the canonical no-representation counterexample, and *are* the segment's own "incommensurable + no priority structure" exclusion); the expectation form $\mathbb{E}[V_{O_t}]$ needs independence (von Neumann–Morgenstern 1944). Canon landings: `#form-objective-functional` — the revealed-preference defense re-grounded on named axioms (A1)–(A4) (Debreu 1954 continuity / vNM 1944 independence / Afriat 1967 GARP existence-not-uniqueness), the three original grounds kept as motivation rather than entailment; segment stays `axiomatic`. Four classical-source rows added to `ref/INDEX.md`.

- **`spike-continuity-orthogonality-2026-05-30.md`** — **commit `0d57da2`**. Verdict **(B)** (three-layer split). Challenge: the "formally independent / orthogonal" framing in `#disc-continuity-stance` (inherited by `#deriv-self-actuation-grounding` Corollary 2(iii)): $O_t$ *does* reach the realized persistence margin through the policy ($O_t \to \pi \to a_t \to$ events / sensors / redundancy $\to \rho, R, \alpha$). Traced against `#der-directed-separation`: directed separation closes only the goal-to-belief-update channel and leaves the goal-to-action-to-event (selection) channel open — exactly the channel $\rho, R, \alpha$ ride on. So substrate-orthogonality as written is false for every actuated agent, and the segment was internally inconsistent (Corollary 2's self-defeating-$O_t'$ clause already presupposed the coupling (iii) denied). Strengthen-first lands a three-layer split: L1 *valuation* (architecturally decoupled, derived) / L2 *survival-invariant identity* ($O_t$-independent, $\mathfrak{A}$-unreachable) / L3 *realized dynamics* ($O_t$-coupled via policy). Canon landings: `#disc-continuity-stance` (the L1/L2/L3 split in Discussion + ES); `#deriv-self-actuation-grounding` Corollary 2(iii) ("formally independent of $O_t$" replaced by the $\mathfrak{A}$-unreachability statement; self-defeating-$O_t'$ clause upgraded from internal inconsistency to theorem — $\mathfrak{A}$ can move the operating point across the boundary but cannot move the boundary). The two coupled edits landed in one commit. Unresolved candidate strengthening (not attempted): a structural bound on the L3 action-channel margin-coupling, analogous to $\kappa$-leakage on the belief side — recorded in the segments' Working Notes.

## Residue-verification disposition

All four are **FULLY-INTEGRATED, independent-verified.** The residue check confirmed first-hand that each spike's load-bearing content is present in the named canon segments and that no refuted form survives as a softened ghost (integration-is-replacement: the false forkability$\to$Level-1 step, the unguarded left-rung monotonicity claim, the unqualified comparability$\to$real-valued entailment, and the "formally independent of $O_t$" framing are deleted from the bodies, with history confined to CHANGELOG + Working Notes). The two shared (C1)–(C3) gates (causal-access Front + value-object Front-2) were verified reconciled verbatim across `#der-loop-interventional-access` and `#def-value-object`. The two new appendix/demonstration segments (`#deriv-convention-monotonicity`; the transportability no-go in `#disc-sandbox-evaluation-ceiling`) carry the worked argument in canon, not only in the spike.

## Navigator reconciliations done (same batch)

- **`spikes/INDEX.md`** — the four cycle-section rows (the 2026-05-31 value-object, 2026-05-30 objective-functional, 2026-05-30 continuity-stance, and 2026-05-30 causal-access entries) reconciled from "COMPLETE — awaiting external-eye review / Proposes no canon edits" to terminal **INTEGRATED + FILED 2026-05-31** status: Location columns point at the `.integrated/<spike>` paths, Status columns name the per-spike canon landings + commit hashes and point here. The now-false "No canon edits; per-segment integration drafted ... external-eye review gate" tails in the four section-narrative paragraphs were updated to record the 2026-05-31 integration.
- **`PROPOSALS.md` SP-25** — a dated **Re-grounding note (2026-05-31)** appended to the Status. The historical Thesis (forkability$\Rightarrow$Pearl-Level-1$\Rightarrow$causal-hierarchy-theorem-forbids-sandbox-inference) is **retained as the record of the original proposal**, not rewritten; the note makes clear the promoted result was subsequently found false and replaced — forkability is not a Pearl-level demotion; the real ceiling is transportability / external validity (Bareinboim–Pearl selection diagrams), landed 2026-05-31 in the causal-access integration (commit `52d85b2`, CHANGELOG 2026-05-30 Causal Access). The landed canon is the transportability form.

## Notes

`git mv` is reversible; spike content is unchanged. The two `sim-rh-*.py` simulation files travel with the value-object spike directory into `.integrated/` (they are the reproducibility artifacts for the myopic-trap counterexample + the RH-strengthening check).

---

# Addendum — 2026-05-31 directed-separation foundation-pass batch (four foundation spikes)

A second batch filed the same day: the four spikes constituting the **directed-separation foundation pass** — the W₁ leakage correction (Stage 1) plus the object-and-boundary reframing (Stage 2) and its two grounding boundary-intuition spikes. Each was first-hand residue-verified against `*/src/` before filing (verify-before-archive). The four `.md` files move to `.integrated/`; **the simulation files and figures stay in place in `spikes/`** (settled rule: sims / figures / PNGs / data never go to `.integrated/` or `.archived/` — they remain accessible, referenced by forward pointers blocking on the G3 sim-integration process in `INTEGRATION-CLEANUP-TODO.md`).

Cycle narrative: CHANGELOG 2026-05-31 (the W₁ Stage-1 entry; the object-and-boundary Stage-2 entry). The deferred follow-on spikes from this cluster — `spike-c2prime-realization-spectrum-2026-05-31.md` (realization-spectrum, open finding bearing on a `#def-auxilia-hierarchy` over-claim) and `spike-goal-flow-duality-2026-05-31.md` (discussion-grade duality landing, deferred) — **stay at top level** as open findings (tracked in `TODO.md` §"Strengthen-first candidates"); so does `spike-strategic-persistence-hard-ceiling-convention-2026-05-31.md` (drafted, pending gate). Those three are *not* part of this filing batch.

## Verification per spike (foundation batch)

- **`spike-w1-leakage-vacuity-2026-05-31.md`** — **Stage 1, commit `fbcb36a`.** Verdict **(B)-sharpening + embedded (C)-no-go.** Challenge (AUDIT-WORKING-526815 F127/F128): the W₁ structural leakage bound $\kappa_{W_1} \le I(A(q_M); G_W \mid q_M)$ is circular/vacuous (bounds $\kappa$ by an average of $\kappa$; identically zero for the stateless oracle). Strengthen-first relocated the goal variable to the latent operator goal $G^{\text{op}}$ and dropped the conditioning. **Residue-verified in canon first-hand:** the corrected selection-channel bound $\kappa_{W_1}^{\text{sel}} = I(A(q_M); G^{\text{op}}) \le I(q_M; G^{\text{op}})$ + the new condition (C2′) are present in `#der-class-coercion-via-wrapping` (Conditions + Theorem region + Related-Work positioning); the embedded no-go landed as its own appendix segment `#disc-w1-structural-bound-boundary` (`robust-qualitative`) — the worked argument is in the segment body, not only the spike. The circular form is deleted from the body; history confined to the regression-guard Working Note + CHANGELOG.

- **`spike-directed-separation-foundation-proposal-2026-05-31.md`** — **Stage 2 framing core, commit `ed11222`; re-scope sweep `7d062f6`; lower-boundary scope-check `be1b2c4`; stale-duplicate purge `bd8a0d6`.** Verdict **EXECUTED** (the proposal was read-only; this records its landing). **Residue-verified in canon first-hand:** the object-and-boundary framing (the three classes as positions of one coupling distinguished by certifiability; Boundary 1 certifiability vs Boundary 2 behavioral) is present in `#der-directed-separation` (the classification subsection + the "not a smooth parameter / two boundaries are different kinds" paragraph); the result-scoping noun — **superseded by Joseph from the proposal's `belief-blindness` to `causal discipline` / `causally-disciplined agent`** — is defined in-body there with the scoping rule and the $\varepsilon(\kappa)$ degradation norm; the cleanest downstream re-scope witness `#def-value-object` carries the Class-1 → causally-disciplined re-scope on the $Q_O$ interventional-interpretation leg. Remaining Stage-2 tail (LEXICON/NOTATION vocabulary, pedagogy pass, Stage-1 propagation to `#der-logogenic-as-wrapping` / `#def-auxilia-hierarchy`) tracked in `TODO.md`; full design preserved in `_obs/directed-separation-foundation-execution-plan-superseded-2026-05-31.md`.

- **`spike-w1-w2-boundary-intuition-2026-05-31.md`** — **intuition spike (no own commit; its framing landed via Stages 1+2).** Verdict **INTUITION SPIKE.** **Residue-verified in canon first-hand:** the certifiability-discontinuity framing it sharpened ("behaviour continuous, certificate-validity a step at $\varepsilon = 0$"; the $\Theta(\varepsilon^2)$ flatness) is present in `#disc-w1-structural-bound-boundary` (the "discontinuity is in the certificate, not the behavior" subsection + Findings Brief) and the Boundary-1 framing in `#der-directed-separation`. Sim `sim-w1-w2-boundary.py` + figure `w1-w2-boundary.png` **stay in place** in `spikes/` (forward-pointer in `#disc-w1-structural-bound-boundary` Working Notes, blocking on G3).

- **`spike-guc-class-boundaries-intuition-2026-05-31.md`** — **intuition spike (independently derived; its framing landed via Stage 2).** Verdict **INTUITION SPIKE.** **Residue-verified in canon first-hand:** the two-boundaries-different-kinds characterization (Class 1↔2 certifiability, Class 2↔3 behavioral / adversarial-slope; the $\kappa$-as-scalar error as two distinct errors) is present in `#der-directed-separation`'s "not a smooth parameter" paragraph; the orthogonality-of-adversarial-sensitivity-and-wrappability incidental corroborates the form-determines-wrappability cut in `#disc-partial-coupling-pathways`. Reusable sims `sim_guc_classes.py` / `sim_guc_boundaries.py` + figure `guc_boundaries.png` **stay in place** in `spikes/` (explicit reusable-infra deliverable; the §6 two-state $(M,\Sigma)$ attractor probe extends them).

## Residue-verification disposition (foundation batch)

All four are **FULLY-INTEGRATED, first-hand-verified.** Each spike's load-bearing content / no-go is present in the named `*/src/` segment body (not merely in an INDEX label or a Working-Notes pointer): the corrected W₁ bound + (C2′) in `#der-class-coercion-via-wrapping`; the certifiability-discontinuity no-go in `#disc-w1-structural-bound-boundary`; the object-and-boundary framing + the `causal discipline` noun + scoping rule + $\varepsilon(\kappa)$ norm in `#der-directed-separation`; the re-scope witness in `#def-value-object`. No refuted form survives as a softened ghost (the circular W₁ bound and the over-narrow "Class 1 only" scope labels are deleted from bodies; history confined to regression-guard Working Notes + CHANGELOG). The `belief-blindness` noun the proposal recommended was *not* landed — Joseph superseded it to `causal discipline` — so verification was against the landed noun, not the proposed one.

## INDEX reconciliation done (same batch)

`spikes/INDEX.md` — the four foundation-spike Location columns repointed to the `.integrated/<spike>.md` paths; Status cells annotated **+ FILED 2026-05-31**. Sim/figure parentheticals on the two intuition-spike rows re-worded to "(sims/figure stay in place: …)" so the file-disposition asymmetry (`.md` filed, sims retained) is explicit. Pre-existing bare-Greek lint debt in INDEX left untouched (out of scope); no new violations added.
