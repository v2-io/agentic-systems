# Observation-Ambiguity Bias Bound (appendix derivation — conditional theorem under named sub-scopes)

The derivation of the *Class-3-Coupled bias bound constant* — converting what was previously order-of-magnitude guidance into a *conditional theorem under named sub-scopes*. The bound has the form *bias magnitude bounded by C times architectural-coupling times observation-ambiguity*; the derivation shows that *the constant C cannot be universal without the parameterization-invariance commitment*, and provides two tracks for deriving C under named sub-scopes.

**Track 1 — Transport inequality, linear in information**: under log-Sobolev inequality plus Lipschitz-posterior conditions, the constant equals *twice the squared posterior-Lipschitz constant divided by the log-Sobolev constant*, scaling linearly with the observation-ambiguity information content. **Track 2 — Fisher-Rao, square-root scaling**: under parameterization-invariance plus Čencov's uniqueness theorem plus the small-information regime, the constant equals the universal dimension-free value $\sqrt{2}$, with the bias scaling as the square root of the observation-ambiguity. The two tracks give *the same structural result with different scope conditions and different scaling*.

The framework derives a **no-go counterexample** showing that *no universal constant exists* under Euclidean-parameter norm: a heteroscedastic-Gaussian example produces bias that scales differently from either Track 1 or Track 2 because the Euclidean norm fails to capture the intrinsic geometry of the parameter space. This *forces the (PI) parameterization-invariance commitment from load-bearing for theorem-level status* — without (PI), the universal constant in Track 2 doesn't exist. With (PI), Čencov's uniqueness theorem provides the metric, and the constant takes the dimension-free value.

This is the framework's **fourth formal instance of the identifiability-floor pattern**: the no-go on universal constant under Euclidean-parameter norm is the impossibility; the (PI) commitment is the structural escape; the resulting constant under the escape is the operational content. The pattern: *naming the floor and naming the unique structural commitment that supplies the escape*, in the framework's signature posture.

The framework also documents *two failed derivation routes* — coupling estimates that produced non-universal constants and were abandoned. The framework's discipline of recording *failed strengthening attempts* (not just successful ones) is in evidence here: future agents will not re-attempt the routes that have been shown not to work.

The downstream consequence: **the Class-3 bias bound is now theorem-grade rather than heuristic**, and the framework's reach into LLM-class substrates has a *quantitative* bias-bound result rather than an order-of-magnitude statement. The bias decomposes as *architectural coupling (fixed for the substrate) times observation ambiguity (variable, prompt-engineerable) times the conditional-theorem constant under either track*. The framework's prescription that *prompt engineering is ambiguity-reduction* acquires its quantitative content here.
