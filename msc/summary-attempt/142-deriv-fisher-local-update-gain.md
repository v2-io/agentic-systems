# Fisher-Local Update Gain (appendix derivation — conditional)

The derivation of the **gain principle from Part I** under stated mathematical conditions. The framework names the *Fisher-local invariance regime* — smooth log-likelihood admitting non-degenerate local quadratic expansion at the current model parameter, with single-step update at first order in the step size — and derives the gain principle: the natural-gradient Bayesian posterior mean shift has the form *gain operator applied to the natural gradient*, with the **scalar collapse** to *model-uncertainty over total-uncertainty* along the natural-gradient direction (always in 1-D; under parameterization-invariance and Čencov's theorem in the natural-gradient direction in higher dimensions).

The result *promotes the gain principle* from empirical-claim status to *derived-conditional status* under the Fisher-local regime. The linear-Gaussian (Kalman) and conjugate-Bayesian instances are *cases where the local quadratic expansion is globally exact* — and in these cases the gain principle holds without truncation. For general smooth models, the natural-gradient invariance theorem of Amari 1998 guarantees the form is *exact at the local-tangent-plane Pythagorean projection level*.

The derivation also provides the **per-coordinate primitive** for the tensor adaptive tempo construct in the corresponding Part I segment. Under Fisher-local conditions the gain becomes a *matrix operator* on each channel; the scalar tempo construct is recovered in the shared-eigenbasis collapse. In anisotropic regimes where the prior and likelihoods do not share an eigenbasis (or where different channels pin down different directions), the *tensor form preserves the per-direction information that the scalar form averages away*.

The framework's posture: this is the *companion* to the Fisher-whitened-update-rule derivation. That segment derives update *direction* under correlated evidence; this segment derives update *magnitude* under the Fisher-local regime. Both share the parameterization-invariance commitment and Čencov's uniqueness theorem as AAT-internal forcing for the Fisher metric. Together they convert the gain principle and the credit-assignment scheme from the chapter prose into *AAT-internally derived* objects under stated conditions.

The result is also where the framework's commitment to *coordinate-forcing as load-bearing apparatus* becomes operational at the agent-update level. The gain operator's specific form (Fisher-information matrix inverse times observation Fisher precision) is *forced* by the parameterization-invariance axiom through Čencov; no other operator preserves the invariance. The framework's prescription — *coordinates are forced, not chosen* — extends from divergence-layer (reverse-KL) and update-layer (log-odds) to the *update-gain layer* (Fisher-whitened, Fisher-local), with all four layers living on the same exponential-family Legendre-Fenchel geometry.
