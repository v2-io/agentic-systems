# Matrix-Loewner Persistence Condition (appendix derivation — conditional)

The framework's **canonical persistence condition under cross-dimensional correction**. The operational persistence condition lifts from scalar form, through per-coordinate form (the prior result), to a **matrix-Loewner condition**. The persistence requirement: the stationary covariance — solving a continuous Lyapunov equation involving the tempo tensor and the disturbance covariance — must be *strictly Loewner-dominated by the diagonal matrix of squared per-direction critical thresholds*.

The matrix-Loewner condition *recovers* the scalar Model S form as the *isotropic special case* and the per-coordinate form as the *diagonal-axis-aligned special case*. The matrix form is *strictly sharper* than per-coordinate under cross-dimensional correction.

The framework's striking conclusion: **per-coordinate persistence is unsafe under cross-dimensional correction**. When the tempo tensor has off-diagonal entries that misalign with the coordinate basis — the *generic situation* under cross-dimensional correction — the per-coordinate condition *can declare persistence on systems whose stationary mismatch will exceed task-adequacy along a diagonal direction*. A constructive 2D counterexample makes this concrete: a mildly cross-correcting tempo tensor with isotropic noise and threshold produces a per-coordinate PASS while the matrix-Loewner condition correctly returns FAIL, with the failing direction running 45° off-axis. *Per-coordinate evaluates the wrong directions; matrix-Loewner evaluates the right ones*.

The framework's posture: **scalar and per-coordinate forms are special cases of this matrix object; the matrix form is the load-bearing thing the others project from**. The framework's prescription is concrete: under cross-dimensional correction, *use the matrix-Loewner condition*; per-coordinate is *unsafe* and gives false-pass diagnoses precisely where the eigenbasis of the tempo tensor misaligns with the coordinate axes the agent thinks in.

The result also composes with the per-direction information-rate floor: no Shannon-type aggregation across dimensions can compensate for one *direction* being capacity-starved — *total bandwidth is not the binding quantity; bandwidth-per-direction is*. The matrix-Loewner condition is what makes per-direction bandwidth the formally enforced quantity. The framework's reading: **multi-modal AI architectures with asymmetric per-modality channel budgets** (vision-language pipelines where one modality is structurally undersupplied relative to the other) are the cleanest current application where aggregate-rate metrics systematically miss directional failures the matrix-Loewner reading catches.

The matrix-Loewner condition is the framework's *canonical anisotropic persistence condition* — the sharp form that subsumes all the prior scalar and per-coordinate persistence statements, and the form the framework prescribes for any practitioner doing serious persistence analysis under non-trivial cross-dimensional structure.
