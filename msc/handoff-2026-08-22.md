# Handoff — cleanup / truthification pass, 2026-08-22

*Written by the orchestrating instance as its context neared its end. State as of writing; verify against `git status` / `git log` before trusting any "uncommitted" claim below. `msc/` is scratch — delete this file once its contents are drained (CHANGELOG for what landed; trackers for what's open).*

## Committed this session (main)

- `597a4e5` — records un-staled after Joseph's `490bf42` build change (release notes, agents.sop File-Org, INTRODUCTION WN, valve prune, JOSEPH-TODO).
- `6e75c7b` — fifth sandbox-ceiling lag site aligned (`impl-closed-loop-interiority` forking section); "Three Deaths" retired as current taxonomy at four body sites. **Flag for Joseph:** this commit also *added* a claim in `der-bounded-objective-as-sanity-criterion` (the D3 input-leg reading) on an agent's read; routing.sop §8 wanted an external eye first. Revert that hunk or confirm it.

## In flight, uncommitted, in the main tree (do not lose)

Impl-series status-tagging pass (audit 731548 §B-4; TODO.md ~line 533), pilot batch of three, each edited by a Claude agent, verified by a `grok-dev` agent (reports in the session scratchpad `…/scratchpad/verify/report-*.md` — may be gone; the verdicts were "approve with small fixes", and the fixes were sent back to the editors):

- `01-aat-core/src/impl-orient-cascade.md` — verifier fixes landed (lead-paragraph overclaim; Track-1/2 scaling → both √I; (C2′); `depends:` extended; WN collapsed to regression guards). Lint clean.
- `01-aat-core/src/impl-unity-communication.md` — verifier fixes sent (residual F174 sentence; $\varepsilon_a$ hypotheses; "five sources" vs `#hyp-solicitable-escape`; two attribution nits; WN collapse). Check whether landed.
- `01-aat-core/src/impl-cooperative-adversarial.md` — verifier fixes sent (OODA ratio-offset units; Model S → 1/2 asymptote incl. the Ch.5 list; equality-vs-escape clause; WN collapse). Check whether landed.

Regression pickaxe on the batch was clean (no add-then-delete history for the new forms; deleted forms originate in the gold-lift/impl first drafts).

**To close the batch (the incision must be sewn):** one commit per segment or one per batch (state the plan first); a CHANGELOG entry carrying the per-item dispositions from the three editor reports (the WN no longer carry them — by Joseph's directive narrative lives in CHANGELOG only); shrink `TODO.md` ~533 to "6 of 9 remain: impl-persistence-and-limits, impl-causal-access, impl-strategy-structure, impl-strategy-dynamics, impl-composition-machinery, impl-strategic-composition (+ TST ×4, LLM ×4 later)"; update the `audits/STATUS.md` 731548 row accordingly.

## Other agents launched (grok-dev, headless, reports to scratchpad `…/scratchpad/drain/`)

- **Spikes drain** — top-level `spikes/` `integrated-misfiled` candidates: first-hand content check, regression pickaxe, `git mv` to `.integrated/` + MANIFEST, INDEX/ROUTING/PROPOSED reconciliation; CHANGELOG draft at `drain/changelog-spikes.md`; Joseph-reserved items listed in its report. Review its diff (`git status`), fold the CHANGELOG draft, commit.
- **Trackers drain** — verify-first prune of TODO/PROPOSALS/INTEGRATION-CLEANUP(+others); CHANGELOG draft at `drain/changelog-trackers.md`. Same.
- **Pipeline** — branch `worktree-agent-a3691f7b26309082d` (commit `8f47ec4`, worktree under `.claude/worktrees/`): outline prose no longer dropped, eq-tag/bold/link `#slug`s resolve, empty gaps read "Open question". **Held for Joseph** — two PDF-visible effects: Part I/II scope paragraphs now italic as authored; the `*[Layer 0 — …]*` outline label will typeset as a margin eq-tag unless the outline drops the brackets. Part I intro title lands in `.md` but not the PDF (`typeset_scrbook.rb` `handle_h3` discards Preface/Introduction titles — Stage 3 decision).

## Joseph's three open calls (from the orientation report)

1. The five `missing` Vol-3 segments (`obs-backward-inference-empathy`, `form-structured-rich-context`, `der-active-salience-management`, `der-self-referential-closure`, `def-cognitive-fusion`): write at honest tier, or recast the three `03-llm-core/src/impl-*` passages that narrate them in the present tense ("carries", "defines", "addresses") as anticipated. (Forward refs per se are sanctioned by FORMAT; the present-tense content claims are the defect.)
2. ~~Move `old-tst-*` out of `02-tst-core/src/`~~ — withdrawn: FORMAT sanctions the `old-*` staging mechanism; converting/archiving them is content work.
3. The nine scope-first OUTLINE orderings in 03/04 — whitelist (`OUTLINE-accepted.md`, mechanism proven in 01) if deliberate, else reorder.

Also his: the GitHub v0.4.0 release body still says the root CURRENT-VOL1 files are byte-identical to the assets (`gh release edit v0.4.0 --notes-file releases/v0.4.0.md`); `core.hooksPath` still points at the dead pre-rename path.

## Source-segment defects reported by the pilots (not edited; need whole-read + regression check + drain)

1. `#hyp-auftragstaktik-principle` — total-allocation inequality $B_O \gt B_\Sigma \gt B_M$ stated where the argument is marginal (F162); Conway's Law "is a consequence" unconditional (F165); its WN's AI-team inversion could be Discussion.
2. `#hyp-communication-gain` — known-adversary case (certainty of misalignment ⇒ low $U_{\text{align}}$ ⇒ *higher* gain in the additive form) is a representational gap distinct from strategic deception; trust-transitivity WN still carries the linear distributed-tempo sum (tempo-additivity lag, 07-15/16).
3. `#result-unity-closure-mapping` WN "three disjunctive routes" vs body four incl. C-iv (F151; TODO's C-iv count item).
4. `#obs-gated-tempo-advantage` lead + `#result-adversarial-exponent-regimes` lead — defender-side "noise injection helps" in their own voice, from a simulation that added noise to both channels (origin of F226); the "cooperative-like dynamics" phrase (F227).
5. `#result-contraction-template` §Honest failure modes item 2 — "strategic half outside contraction-metric scope" should be scoped to the shared-truth state variable (sub-scope $\alpha'$ of `#deriv-strategic-composition` is a sector-template transfer); its (CC-feedback) is written *iff* where classical small-gain is sufficient.
6. `#deriv-causal-ib-exploration` Discussion — "physically cannot stop exploring" → "cannot stop exploring and remain within the persistence condition."
7. `#deriv-causal-ib-lmi` intro — directional-floor composition stated without the FIM-vs-Shannon-rate caveat its ES carries; strengthening available: Mitter-Newton rate $\tfrac12\operatorname{Tr}(\mathcal I_o P_{ss})$ against the same FIM the LMI constrains (continuous vs discrete-time seam to name) — candidate spike.
8. `#result-coupled-diagnostic-framework` (03) — "derived for Class 1 (Separated)" should be causally-disciplined (05-31 re-scope didn't reach it); no `## Findings`.
9. `#der-bounded-objective-as-sanity-criterion` — factor-(iii) seam should cite `#der-captured-objective-dynamics`.
10. `impl-closed-loop-interiority` (03) `depends:` on a Vol-4 segment — cite vs depend.
11. `#der-tempo-composition` — internally inconsistent on $C_{\text{coord}}$ (lead/tagged form carries $/\lVert\delta_{\text{critical}}\rVert$; Formal Expression intro omits it).
12. `#deriv-persistence-cost` boxed $C \geq \mathcal T/2$ reads "per dimension" under the $\alpha=\mathcal T$ identification; total-$\mathcal T$ readers are off by $n$ (pre-existing; the impls now say total $n\alpha/2$, $\alpha/2$ per dimension).

## Two standing directions from Joseph, late in the session

- **First passes on implication and introduction segments commonly go wrong** — wrong register, wrong tone, superlatives, false claims when summarizing, incongruent forward-references or terminology usage. Make those first-class check items in every impl/intro pass, not only status alignment. **The `AUDIT-WORKING-*` §14 Wandering Thoughts are the best source for implications gloss and pedagogy** — mine them (the lifted gold already sits in each segment's WN; the dirs themselves are under the consult-Joseph gate).
- **Linting: asf should be using the newer `md-press` (`md-press --math` in particular) for markdown files**, not only `bin/lint-md`. Not done this session; `bin/lint-md` is still what the SOPs and `git-hygiene.sop` gate on. Someone needs to work out the hand-off (md-press edits in place; `--math` uses a local ollama model; `.md-pressignore` for verbatim material) and update `git-hygiene.sop.md` / `agents.sop.md` accordingly.

## Brief shape that worked for the impl pass (for the remaining six + TST/LLM)

Peer brief; the segment's own off-ramp list + the audit FINAL §B-4 + F-rows as the task's input (not my checklist); "align to the weakest source status" spelled out as strengthen-first, not hedging; sources read whole; no source edits (report instead); no commit; *and now*: WN keeps only forward pointers / regression guards / dead-end warnings — dispositions go to CHANGELOG; drain TODO/STATUS in the same batch; regression pickaxe on restated forms. Then a `grok-dev` verifier (reads diff + sources whole, reports, no edits) before commit. `grok-dev` runs headless as `grok -p --prompt-file …` (model grok-4.6, always-approve, carries the full arch-expert doctrine prompt; a shell function in the user's profile wrapping `grok-dev-direct`).
