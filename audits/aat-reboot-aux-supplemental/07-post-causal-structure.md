# Supplemental 07 — `post-causal-structure`

*Finder, 2026-09-25, working from `875e1ac3`. Paths are relative to `~/src/arch/asf/`. Each note is tagged with the slugs it bears on.*

## Checks that passed (please add to the Working Notes of `post-causal-structure`)

- [post-causal-structure, deriv-recursive-update] Checked `#deriv-recursive-update`'s C1 ("$M_{\tau^+}$ cannot depend on future events", :42–46) against the postulate (:23). C1 is the functional-dependence reading of "only a precedent can be a cause". Consistent; holds.
- [post-causal-structure, deriv-graph-structure-uniqueness, def-strategy-dag] Checked P1 and the acyclicity derivation (`deriv-graph-structure-uniqueness.md:27–31, :125–133`; `def-strategy-dag.md:135–143`) against the postulate. They need *strict* precedence on time-indexed event tokens, and the postulate states strict precedence ("temporally precedes"). Holds. (The token-indexing condition is supplied by `#def-strategy-dag`, not by the postulate.)
- [post-causal-structure, emp-update-gain] Checked whether the update gain carries a causal-provenance term, as :49 implies. It doesn't: $\eta^\ast = U_M/(U_M+U_o)$ (`emp-update-gain.md:27`). This *confirms* the 731548/374162 resolution. The segment's :49 therefore *fails*; that's in the aux.
- [post-causal-structure] Checked `depends:` against the Formal Expression. The Formal Expression uses only `#def-chronica`'s ordered sequence, so it is depends-clean (as 471203 noted, `AUDIT-WORKING-471203/.integrated/08-post-causal-structure.md:9`). The Discussion's scope claims (:38) lean on `#scope-agency` / `#scope-adaptive-system`, which precede it in the OUTLINE but aren't in `depends:`. Those are Discussion forward/side references, permissible per FORMAT.
- [post-causal-structure, def-pearl-causal-hierarchy] Checked :13's "the Pearl-style causal hierarchy … builds on this foundation but is not identical to it" against Bareinboim et al. 2022 :408 (the CHT presupposes only a temporal ordering of mechanisms). Holds for "builds on". Only :31's "derive from" overreaches; that's in the aux as FINDER-OBSERVED (c).

## History

- [post-causal-structure] 2026-03-11, `be0e13d6`: created as `src/causal-structure.md` (`type: axiom`, `status: first-principled`) from TFT's TF-02 (`.archive/old-tf-02-causal-structure.md`, 154 lines). The migration kept the header claim, the precedence definition, the coupling tiers, the consequences list and the update-weighting paragraph. It split out the three causality notions and the Pearl levels (to `#def-pearl-causal-hierarchy`), the recursive-update derivation (to `#deriv-recursive-update`) and identity (to `#scope-agent-identity`). The consequences list lost its "combined with the recursive update" clause in the process.
- [post-causal-structure] 2026-03-12, `91573ec8`: axiom → postulate, first-principled → axiomatic.
- [post-causal-structure, scope-agency] 2026-04-01, `4b4ded10`, "Explicitly scope causal effect into agency". Nominal coupling gained its "choice of what to observe produces distinguishable observation distributions … still within scope" clause, and zero coupling became "Outside ACT's scope". 2026-04-02, `5a330a07`: zero coupling was split into outside-agency / inside-adaptive (the current form). In TFT, zero coupling had been in scope when $\lvert\mathcal A\rvert \geq 2$.
- [post-causal-structure] 2026-04-22, `3a51bca9`: `def-chronica` was added to `depends:`. 2026-04-24, `e6adf9eb`: renamed `post-causal-structure`.
- [post-causal-structure] 2026-04-29, `df416564` / `df72dcec`: the "(Descended from TF-02.)" line was moved to the Working Notes, then deleted. So the 2026-05-30 gold-lift "follow-up item" (:70) was already resolved when it was written.
- [post-causal-structure] 2026-05-21, `b1f599d4`: the two-paragraph opening (the summary-attempt sweep). 2026-05-28, `34a16b56`: Sections → Parts. 2026-05-30, `598631e1`: gold lift.
- [post-causal-structure, disc-composition-consistency] 2026-05-28: Ch.1's other postulate (`post-composition-consistency`) was reframed as the meta-segment `#disc-composition-consistency`, leaving this as Ch.1's only postulate (`CHANGELOG.md:425`, which contrasts "methodological" with "physical").

## Notes for later segments and out-of-scope observations

- [der-loop-interventional-access, def-strategy-dag] Stale slugs in eq-tags: `der-loop-interventional-access.md:23`, "*[Derived (loop-interventional-access, from causal-structure + recursive-update)]*", and `def-strategy-dag.md:135`, "*[Derived (from causal-structure + finite planning horizon)]*". Both use the pre-2026-04-24 slug `causal-structure`. The assembler also leaves raw `#post-causal-structure` hashes in eq-tag lines (`msc/aat-v0.4.0-assembled-md-review.md:77`).
- [form-event-driven-dynamics] :51 says "extends #post-causal-structure's recursive update". The recursive update belongs to `#der-recursive-update`. This is flagged at `audits/AUDIT-WORKING-731548/15-form-event-driven-dynamics.md:7`, and it's a fossil of TF-02, which did carry the recursive update.
- [form-event-driven-dynamics, post-causal-structure, def-chronica] Which time does the postulate order: occurrence or arrival? `form-event-driven-dynamics.md:103` (Working Notes) leaves "ordered by arrival-at-sensor rather than physical-occurrence time" unaddressed. The postulate's "event" is undefined before `#form-event-driven-dynamics`. A rewrite that states the postulate on event tokens should pick one. My guess is occurrence for causation and arrival for the chronica, but that's only a guess.
- [post-causal-structure] Precision on "the light-cone structure of relativity … enforce[s] it" (:15, :27). Relativistic causal precedence is a *partial* order: spacelike-separated events are unordered. The postulate's "temporally precedes" reads as a total order. Nothing in Part I needs totality, since the agent's own events lie on one worldline, and `#deriv-graph-structure-uniqueness` already works with a strict partial order. But composition (`spikes/.integrated/spike-strategy-dag-composition.md:107`, Obstruction B: composing agents' plans needs a common order extending each agent's order) is where totality vs partiality could matter.
- [post-causal-structure] Positioning note, from my own knowledge (not a verified cite): the postulate is Hume's "priority" condition taken alone, without contiguity or constant conjunction. 384279's reflection lists Hume among the "stronger notions" (`AUDIT-WORKING-384279/.integrated/08-post-causal-structure.md:34`), which is accurate only for Hume's full account.
- [der-loop-interventional-access, scope-agency] `der-loop-interventional-access.md:88` has "(passive observers, nominal agents with $\lvert\mathcal{A}\rvert \lt 2$ or no causal effect)". It can be read respectively (observers: $\lvert\mathcal A\rvert \lt 2$; nominal agents: no effect), but on a first read it glosses "nominal" as covering both of `#scope-agency`'s excluded classes. That's a small ambiguity on the same word the rename touches.
- [example-kalman] The example's "Causal Structure (#post-causal-structure)" subsection (:45–49) asserts "Action precedes observation and changes $P(y_t \mid do(a_t))$", with $y_t = x_t + n_t^{(a_t)}$ and $a_t$ chosen from $M_t$. That's the off-by-one already in aux 03. Noting it here because it is *this postulate's* worked instance, and on canon indexing it gets the precedence backwards.
- [hyp-causal-discovery-from-git] P-V3 (`audits/pending-findings-2026-04-25.md:206–218`, "causal direction for free" overstating this postulate) appears to have landed: the segment now reads "identifies the *structure of possible in[fluence]*" (:29). I didn't verify it further.
- [04-eli-core] 731548's conditional form of the "Law of Conservation of Consequence" (`AUDIT-WORKING-731548/08-post-causal-structure.md:40`) is sturdier than the unconditional gold at this segment's Working Notes :86. It's worth carrying to whichever Volume-4 segment takes up checkpoint reversion.
- [post-causal-structure] 731548 :24 ("curious"): non-forkability and Level-3 counterfactuals "meet exactly at 'you cannot rerun the trajectory'". `def-pearl-causal-hierarchy.md:19, :25` answers part of this: counterfactuals are simulated, and `git checkout` is literal Level 3 only where the history is content-addressed and replayable.
- [post-causal-structure] `msc/summary-attempt/009-post-causal-structure.md` (9 lines) restates the segment and adds nothing.

## What I included, left out, and why

- I left the Working Notes gold itself out of the aux: he reads it with the segment. I gave only its *current status*: two watch-items resolved, one follow-up stale, and one reader question answered in canon.
- I didn't re-surface aux 04's chronica items (the trichotomy, non-forkability, monotone growth). I only pointed at (d) where this segment repeats "never removed".
- I didn't expand SP-28 / condition 4 itself; the `scope-agency` sibling finder will carry it. I kept only what bears on this segment's taxonomy.
- The "what downstream draws from it" list is my own synthesis from reading the consumers. I put it in the aux because of your own draft note about merging this postulate into the chronica (`aat-reboot/scratch/04-def-chronica.md:60`).
- memorata `--joseph` returned nothing for several phrasings. The one Joseph quote came from a class-filtered search (`-c human`), and I read it at its `history.jsonl` line. Searches phrased around the postulate itself found only renaming chatter.

## Feedback on the brief and process

- The brief worked well. The pass/fail split for my own checks was especially useful: it made me put the positive confirmation of the gain formula here and the failed claim there, rather than blending them.
- One suggestion: the coordinator's own `scratch/NN-*.md` notes are a cheap and very relevant read for a finder. Here they told me he's weighing a merge, which changed what I put first. The brief could mention they exist. (I read only a grep of them, not whole files.)
- `memorata-search --joseph "<query>"` returned "0 of 0 candidates" for every query I tried, while `-c human` found real results. That may be a tool issue worth a look.

## Late addition: overlap with aux 06

- [post-causal-structure, scope-agency] After drafting, I grepped the supplemental directory and read aux 06 (`scope-agency`, written in parallel). It covers the "nominal" rename, the :37/:40 query-only vs `scope-agency:49` contradiction, Joseph's 2026-04-01 Option-A decision (session `615ef48a`; the query-only inclusion was the implementing agent's, `4b4ded10`), and the LEXICON `agentic-system` tension. I cut my versions to pointers and kept only what aux 06 lacks: 731548's independent (xvii)/(xviii) and the verifier's refutation of (xviii); the direction where the $\Omega$-effect is never observed of the axis mismatch; and the fact that *both* LEXICON entries naming this segment describe content it doesn't hold. Aux 06's supplemental (:58) makes the same process point I would have made: a "your neighbour is X" line in each brief would cut this duplication before it's written.
- [post-causal-structure] What the removed nominal-rename text added beyond aux 06: `der-loop-interventional-access.md:88` and `scope-adaptive-system` also use the excluded-class "nominal", besides `der-severed-actuation-dynamics`. So it's this segment's word that should change.
