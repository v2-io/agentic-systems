# aat-reboot aux supplemental — 10 `form-information-bottleneck`

*Written 2026-09-25 by the finder for `01-aat-core/src/form-information-bottleneck.md` (Claude Opus 5.5), for the aat-reboot walkthrough. The coordinator's aux is `~/src/aat-reboot/scratch/aux/10-form-information-bottleneck.md`. Paths are relative to `~/src/arch/asf/`; line numbers are as of `875e1ac3`. Each note carries slug tags so later finders can grep for their own segment.*

## Fact-checks with positive results — please add to the Working Notes of `form-information-bottleneck`

- Checked the displayed objective (:23) against Tishby et al. eq. (15) (relata markdown :155, $\mathcal L[p(\tilde x \mid x)] = I(\tilde X; X) - \beta I(\tilde X; Y)$). The information terms and sign convention match. The differences are the encoder class (a map versus a kernel, see the aux) and the conditioning on $a_{t:\infty}$, which Tishby's setup doesn't have. Holds modulo those two.
- Checked "the Markov chain $Y - X - T$ holds by construction" for a *deterministic* $\phi$ and a fixed endowment. $\sigma(M_t) \subseteq \sigma(\mathcal C_t)$ gives $I(M_t; Y \mid \mathcal C_t, A) = 0$ for any $Y$ and $A$. Tishby's footnote 3 (:182–184) confirms the chain is "not a modeling assumption": it just says $\tilde X$ is computed from $X$ alone. Holds under those two conditions (see the aux for the stochastic and random-$M_0$ failures).
- Checked the double-counting argument (:30) with $\beta$ read as a fixed *price*. At fixed $\beta$ the optimum is determined by the joint, and components whose conditional predictive information falls get dropped at a threshold set by $\beta$. Holds. This agrees with 472913 :33–48, 731548 :10 and 384279 :14. The capacity reading fails; see the aux.
- Checked that 471203 F7 (Tishby–Zaslavsky miscitation) is resolved in the body: :56 now credits Alemi et al. 2017 for the variational bound and Tishby–Zaslavsky 2015 only for the deep-learning instantiation (`e05149c0`). Holds.
- Checked `def-action-transition` in `depends:` (added by AF-6, `4f0315e0`) against the Formal Expression's use of $a_{t:\infty}$. It's a genuine dependency. Holds.
- Checked `#deriv-causal-ib-exploration` :73 against this segment's :30–32. The downstream restatement matches the current body ("The environment dictates the representation natively; survival dictates the policy"). Holds.
- Checked the Formal Expression's pointer `#deriv-causal-ib-exploration` (:32) and the Discussion's `#def-model-sufficiency` (:48), `#def-value-object` (:50), `#def-shared-intent` (:52), `#form-strategy-complexity-cost` and `#deriv-strategy-cost-regret-bound` (:54), `#disc-compression-operations` (:42, :54, :56). All resolve to existing segments.

## History

- **2026-03-11 → 2026-04-02.** Creation, formatting and linting passes. The segment was `discussion-grade`, with a bulleted "Dependence on volatility" paragraph: volatile → low $\beta$, stable → high $\beta$.
- **2026-04-22, `a14682e8`.** Finding 10 moved the status to `exact` ("applied external theorem"; spike `spikes/.integrated/spike-finding-10-ib-status.md`, ~15 minutes). The three-paragraph Epistemic Status, including "$\beta$'s dependence on … $\rho$ and policy $\pi$ stated in the Formal Expression … *robust-qualitative*", was written against the *old* bulleted claim. The VFE paragraph was added in the same commit (AI integration, Phase D).
- **2026-04-22, `b76ee67a`.** The IB versus IT-MDP lineage paragraph, pre-empting "why isn't strategy cost a mutual information?". PROPOSALS O-BP2 row (b) (`PROPOSALS.md:262`).
- **2026-04-25, `e39c17b5`.** Gemini's causal-IB session. 738192 SN-2 (Finding 1, `audits/AUDIT-WORKING-738192/02-reflections-IB-volatility.md:10–27`) was resolved by rewriting the Formal Expression paragraph into the double-counting argument. The Epistemic Status (:40, :42) and Discussion (:50) weren't touched. That remnant is the aux's first finder item. Memorata: Joseph relayed Gemini's report into the Claude session `cccdbf4c…jsonl` (line 437, 2026-04-25 19:27). Claude then corrected Gemini's status overclaim on `disc-ciy-unified-objective`.
- **2026-04-25, `4f0315e0`.** AF-6 added `def-action-transition` to `depends:`.
- **2026-05-15, `e05149c0`.** 471203 F7: the Alemi citation was added.
- **2026-05-29, `1807ed50`.** SP-26: the anti-collapse plant (:34) was added and `#disc-anti-collapse` was created.
- **2026-05-30, `c931bccb`.** Gold lift (Working Notes :58–93).
- **2026-07-02.** 731548 reads the segment (ledger xxiv–xxvi). Its verifier (`verify/03`) names this segment as one of two that the random-$M_0$ variant breaks. Only the $M_0$ part reached `CHANGELOG.md:227`.

## What I left out of the aux, and why

- **The 193847, 471203, 584721, 742613, 773921, 829314, 849201, 527914, 963715 reflections.** Their content is in the Working Notes gold lift already. 384279 (`AUDIT-WORKING-384279/.integrated/11-…md`) isn't in the lift's coverage list, but it adds only praise, plus a suggestion for a small two-lineage table (:27).
- **613842's tier-split recommendation.** Already in WN §3. The aux carries the strengthen-first alternative (GEM B2) instead, because it never reached a tracker.
- **The pending-findings-2026-04-22 Finding 10 text.** Superseded by the landed spike.
- **The IB-unification plan** (`spikes/.integrated/spike-ib-unification-plan.md`, 446 lines) and the causal-IB spikes (`spike-causal-information-bottleneck.md`, `spike-causal-ib-lmi-repair.md`). They belong to `#disc-compression-operations` and the causal-IB appendices. One detail worth knowing: the causal-IB spike (:17) already writes the IB as a minimization over $P(M_t \mid \mathcal C_t)$, the stochastic kernel, and it frames exploration as needing the *interventional* relevance variable $P(o \mid do(a))$ (Wieczorek & Roth 2017). That's the causal-IB extension still open at `TODO.md:160`.
- **The summary-attempt paraphrase** (`msc/summary-attempt/012-form-information-bottleneck.md`, 9 lines). It restates the segment faithfully and adds nothing.
- **Joseph's intent.** I searched with `memorata-search --joseph` for IB, $\beta$ and volatility. The hits are pasted agent reports (Gemini's causal-IB summary; a citation-verification table from 2026-04-22 that flags the Allerton 1999 vs arXiv 2000 date). I found nothing he wrote about this segment's purpose.
- **Bialek, Nemenman & Tishby.** I cited them from memory in the aux, flagged as unchecked. `relata show-markdown` found no open-access PDF.

## Notes for other segments (slug-tagged)

- `[der-directed-separation, disc-modularity-state-dynamics]` **The Class-1 compression price** (731548 :19; FINAL :106, :133). IB-optimal $\phi$ is policy-relative, so a goal-blind $\phi$ pays an information cost that nothing prices. The candidate theorem is a trade-off between that cost and Class 3's $C\kappa I$ bias, possibly with an interior optimum $\kappa^\ast \gt 0$. Still 2009 eq. (20) (the causal-state partition depends on the policy) is the cleanest published statement of the premise. Untracked.
- `[def-model-sufficiency]` **Graceful vacuity** (731548 :41): "$S$'s denominator is itself the world's learnability budget." Also 613842 GEM B3: this segment's `depends:` edge is motivational. And 963715 GEM F3: sufficiency is `deps-verified` on top of this `draft`.
- `[deriv-causal-ib-exploration, deriv-causal-ib-lmi, disc-ciy-unified-objective]` **Uncited prior art: Still 2009** (relata `still-2007-information-theoretic`; PDF registered today; markdown queued). It derives exploration as an emergent term (eq. 6: the $-D[P(z \mid h,a) \Vert P(z)]$ "exploration" term against a "control" term) from a predictive-information objective with feedback. It is "present in the absence of policy randomness", without rewards or preferences. `#deriv-causal-ib-exploration`'s positioning (:87, "a derivation of *why exploration is required* that does not route through epistemic-value-of-information arguments") and its prior-art search (:97–102) don't mention it. Also relevant: `still-2012-information-theoretic` (curiosity-driven RL).
- `[disc-compression-operations]` 731548 :43: "the compression form tracks the substate: models compress toward the world, strategies compress toward action." Also the relocation target for this segment's IT-MDP and VFE paragraphs (WN §3; 731548 :16). And the `ref/INDEX.md:34` Cover & Thomas locator "§I.12–13" is shared with this segment. In the 2nd edition, rate-distortion is Chapter 10 (`deriv-persistence-cost.md:42` cites Theorem 10.2.1).
- `[def-shared-intent]` 526815 F157: the same deterministic-argmin-versus-kernel pattern (`audits/AUDIT-WORKING-526815/.integrated/83-def-shared-intent.md:17`).
- `[form-consolidation-dynamics]` It uses this segment's Lagrangian as its objective (:34), so it inherits the "whose joint?" question from the aux. An agent consolidating against its *own* estimate of $p(\mathcal C_t, Y)$ is optimizing a different functional from the modeler's benchmark.
- `[03-llm-core/der-turnover-information-recursion]` :36 labels its SDPI step "from form-information-bottleneck + Ahlswede–Gács 1976". The chain it needs is plain data processing through $f_{\text{init}}$. It survives under the absorb-into-$\phi$ endowment (frozen weights). If weights were treated as a random informative $M_0$, the chain would need re-checking, per the 731548 verifier.
- `[hyp-mismatch-dynamics, result-persistence-condition]` $\rho$ is defined there as a mismatch-injection rate (surprise per time; `NOTATION.md:134`). The IB segment uses "$\rho$" for the environment's decorrelation rate, a property of the joint. Whoever writes $\rho$'s definition may want to say how the two relate.
- `[disc-anti-collapse]` Its retelling of the $\beta$-vs-$\rho$ instance (:14) adds the claim "$\beta$ and the action channel call for opposite responses", which rests on the conditional/heuristic `#deriv-causal-ib-exploration`. And the instance as stated merges a price with a budget (aux, FINDER-OBSERVED). If the discipline is to be taught through this instance, the instance should itself be clean.
- `[form-agent-model]` The aux's Still 2009 item settles 526815 F3. The deterministic model is the $\lambda \to 0$ (i.e. $\beta \to \infty$) limit of the stochastic IB-with-actions optimum (eqs. 18–19). A deterministic $\phi$ at finite $\beta$ is the separate deterministic-IB problem (Strouse & Schwab 2017, *Neural Computation*; not in relata).

## Process feedback

- **The stale-remnant pattern deserves a generic check.** A fix landed in one section (the Formal Expression) while the Epistemic Status, written three days earlier against the old claim, kept asserting it. Four later readers (472913, 731548 and two gem adjudicators) read the stale Epistemic Status as a *deliberate tier split* and praised it. The Epistemic Status's own phrase "stated above" pointed at text that no longer existed, and that phrase was the tell. A pickaxe on the phrase a fix removes (`git log -S`), run against the rest of the file, would have caught it. This fits the drain-across-surfaces discipline (MEMORY: `feedback_drain_across_surfaces_and_regression_check.md`), applied *within* a single file.
- **Relata.** `show-markdown` successfully fetched the Still 2009 PDF (unpaywall), but its conversion queued behind a 696-page job. `pdftotext` on the registered PDF was a clean stopgap for reading. When citing a still-queued work, equation and page numbers are the stable locators.
- **The brief worked well for a math-bearing segment.** The FINDER-OBSERVED heading was the right home for most of what mattered here. The largest items weren't in any audit, and they came from reading the primary (Tishby's eq. 35 and footnote 2) against the segment's prose. For formulation segments that import a theorem, "read the theorem's own statement, not just its objective" might be worth one line in future briefs.

## Stay-on-the-line

I'm available for follow-ups. In particular, I can finish the Still 2009 line ranges once the relata markdown lands, or attempt the 613842 B2 strengthening (volatility decay of $I(\mathcal C_{t-k}; Y)$ under mixing) if that would help the rewrite.
