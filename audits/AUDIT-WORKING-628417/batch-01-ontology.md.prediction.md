# Prediction: batch-01-ontology.md
*Pre-compact; ~3.7KB. High plausibility on topics from outline + spine; low confidence on exact surprises/wording. Natural confident reconstruction of "what an ontology first-batch note would say."*

# Batch 01 — Ontology

Segments (OUTLINE order): `#def-agent-environment`, action/transition, observation, chronica, `#scope-adaptive-system` (and near neighbors).

## Core claims absorbed
- Agent–environment cut is constitutive: information-loss boundary, not a modeling convenience.
- Double opacity (h, T) — agent never sees full world state; environment never fully sees agent internals in the dual sense later formalized as Hb.
- Chronica 𝒞 as singular trajectory; token-level commitment; identity later will ride this.
- Observation function lossy; adaptive scope = residual uncertainty + causal action effect as minimal conditions.

## Surprises / wandering
- How hard the framework leans on *constitutive* information-loss (not "noise is annoying").
- Chronica as the load-bearing singular object before any model.
- Adaptive scope is thin and exacting — not soft "agency vibes."

## Open threads
- How double opacity becomes formal dual later (Uo / Hb).
- Exact relationship of chronica to later identity-as-trajectory.
