# Batch 7 — Part II Lift: Spectrum, State, Separation, Strategy, Causal Access, Diagnostics
Segments: `#def-mood` (Part I coda), `#der-temporal-nesting` (Part I coda), `#disc-stability-certificate` (meta open), `#def-agent-spectrum`, `#form-complete-agent-state`, `#der-directed-separation` (full re-read), `#form-objective-functional`, `#def-strategy-dimension`, `#def-satisfaction-gap` (prior), `#def-control-regret`, `#def-strategy-dag`, `#der-causal-hierarchy-requirement`, `#der-loop-interventional-access`

## What I hadn't anticipated

**Mood as pre-goal second-order gain.** Leaky integral of tracking surprise modulating $K$ and $\mathcal{T}$ *before* objectives exist. Outer loop on the bathtub drain. MG-1–4 band keeps it inside persistence. Bounded separation-preserving coupling — Class-2-adjacent without Class-3 pathology. I had assumed mood was ELI/affect fluff; it is Part I formal.

**Spectrum grounding×intent reframe (Working Notes gold).** $M_t$ and $O_t$ as nearly same kind of object in same space — wishful thinking is default collapse of two near-identical representations; directed separation protects the 1% where agency lives. Leakage concentrates in *unobserved present* inference, not future projection. Pedagogy that makes DS feel inevitable rather than modular taste.

**$G_t=(O_t,\Sigma_t)$ split as type repair.** Old "goal mismatch = G−M" was a type error once Σ is a graph. Sat-gap and control-regret are properly typed replacements.

**Learning-agent scope carve.** PID/LQR in agency but *outside* learning-agent scope (designer supplied Level 2). Most Part II dynamics need learning-agent. I had not predicted this middle tier.

**Loop = Level-2 data channel, not free identification.** Exact claim is *availability* of interventional-character data; identification gated on positivity / sequential ignorability / known mechanism. On-policy often yields E[o|A=a] not E[o|do(a)]. Singular trajectory grounds *whose* effect, not do-semantics. Consequence-3 re-grounding is exquisite carefulness.

**Strategy DAG: structure partly derived, parameterization chosen.** Acyclicity from temporal order; AND/OR+edges is formulation. L0 exact formal contract; L1'/unobservable common cause is Fisher-rank floor (identifiability pattern recurring).

**Optimally failing.** Regret≈0 with sat>0 — good strategy, impossible goal. Without 2×2 you thrash strategy forever. Active Inference EFE does not make this split.

**Certificate spine meta.** Sector interior / separability scope / coordinate forcing / identifiability boundary / composition projection — one object, plural distinct failure modes (Helmholtz vs Sylvester vs Mori–Zwanzig). Framework starting to name its own architecture.

## Wandering thoughts

- **Hume is/ought as state vector** — M is, G ought; action the only legitimate bridge; motivated reasoning = short-circuit.
- **Chess: simple O, complex Σ** — axes independent; design modularity real.
- **Richens 2025 / world models forced** by multi-step goal competence — +model side not mere posture under bounded GC agents.
- **Temporal nesting → rational conservatism** about structural change is *derived* from pause-cost, not personality.
- **Pedagogy:** Part II should open with spectrum then complete state then DS — the dependency is the story of "where can purpose corrupt knowledge?"

## Questions into strategy dynamics / Part III

1. Edge-update via gain — full Part I machinery transfer to Σ?
2. Observability dominance / frozen edges — composition of deaths?
3. Bias bound Class 3 — continuous with gain collapse?
4. Composition closure ε* — how much Part I survives projection?
