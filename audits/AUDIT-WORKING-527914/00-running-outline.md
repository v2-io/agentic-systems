# Running Outline

## Segment Block 1: AAD Opening Primitives (01-10)

Segments read:

1. `#def-agent-environment`
2. `#def-action-transition`
3. `#def-observation-function`
4. `#def-chronica`
5. `#scope-adaptive-system`
6. `#scope-agency`
7. `#post-composition-consistency`
8. `#post-causal-structure`
9. `#def-pearl-causal-hierarchy`
10. `#form-agent-model`

## Model Update

The opening primitives are cleaner and more scope-disciplined than a generic "agent theory" opening. The key structure is a ladder:

- information-loss boundary
- opaque action and observation functions
- chronica as complete irreversible causal record
- adaptive system as observation plus residual uncertainty
- agency as choice plus Level-2 interventional contrast
- causal structure as temporal possible-influence
- Pearl hierarchy as adopted prior-art access levels
- $M_t$ as compressed reality model / model state

The first ten segments strengthen my initial prediction that several plain names should be defended rather than replaced. The best names so far are not ornate: `adaptive system`, `agency`, `causal structure`, `observation function`, `transition opacity`. The standout invented term remains `chronica`.

## Naming Themes Surfaced

Layer separation matters:

- Formal symbol: `$M_t$`, `$\mathcal C_t$`, `$h$`, `$T$`.
- Formal object name: `observation function`, `reality model`.
- Prose gloss: `complete interaction history`, `model state`, `intervening`, `predicting`.
- Scope/postulate name: `adaptive system`, `agency`, `composition consistency`.

Several candidates in the card blur these layers. My votes should keep distinguishing "replacement canonical name" from "brief-grade gloss" and "prose alias."

## Process Notes

The segment-walk method is working: several votes changed from what I would have guessed from the card alone. Examples:

- `boundary condition` became a strong write-in for `information-loss boundary`.
- `agent model` became a strong preference for `Reality model`.
- Pearl L2's proposed gloss `Exploring` became a rejection because the segment made Level 2 broader than exploration.

I should keep resisting the card's random-order pull. The post-composition segment exposed many downstream terms, but voting on closure/defect/bridge targets now would be premature.

## Watchlist

- Possible dependency-order strain: `scope-agency` uses Pearl/do notation before `#def-pearl-causal-hierarchy`; resolved conceptually by the next segment, but fresh-reader ordering may be rough.
- `opacity` overload: transition opacity, epistemic opacity, later agent opacity.
- `hierarchy` overload: Pearl hierarchy is prior art; AAD will also have correlation/convention/approximation hierarchies.
- `agent model` ambiguity: avoid where "model of the agent" is possible.

## Current Vote Count Estimate

Card votes cast through this block: 24 targets marked in tracker sequence. This is a healthy density for early definitions; continue only while notes remain segment-grounded.
