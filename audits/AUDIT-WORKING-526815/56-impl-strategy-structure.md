# 56 - impl-strategy-structure

Source: `01-aat-core/src/impl-strategy-structure.md`

## First-pass understanding

This chapter-end discussion reads the strategy-structure chapter as four linked products: theorem-backed DAG structure, a convention-indexed two-gap diagnostic, the Correlation Hierarchy with identifiability floors, and chain-confidence as the first instance of a broader coordinate-forcing pattern. The strongest local clarification is the distinction between structure and parameterization: acyclicity and Markov factorization are treated as theorem-backed under stated conditions, while AND/OR nodes and single-parameter edges are framed as convergent representational choices rather than derivations.

The segment is intentionally synthetic, but its synthesis reaches beyond the AAT material already read. It treats `deriv-graph-structure-uniqueness`, `der-causal-insufficiency-detection`, `disc-identifiability-floor`, and `disc-additive-coordinate-forcing` as load-bearing homes. Under the current audit protocol I cannot verify those homes yet, so the right local posture is "useful map, but proof credit deferred."

## Diagram attempt

The useful illustration is a chapter-end bridge: four chapter outputs sit on the left, and the downstream homes they claim to support sit on the right. I highlighted which claims are already anchored in earlier-read segments and which are promissory or future-home dependencies.

## Findings and watches

- F46 candidate: this discussion depends on future segments (`deriv-graph-structure-uniqueness`, `der-causal-insufficiency-detection`) and meta-segments (`disc-identifiability-floor`, `disc-additive-coordinate-forcing`) for claims it presents as chapter-end deliverables. That may be acceptable for a synthesis segment, but the burden of proof is not local and should be clearly labelled as deferred.
- Watch: this segment partially resolves earlier F43 by separating CMC-backed Markov structure from AND/OR parameterization. The earlier local overstatement in `def-strategy-dag` remains, but the chapter-end synthesis has the better formulation.
- Watch: the convention-indexed two-gap diagnostic repeats the C1/C2/C3 monotonicity hierarchy; my earlier concern about C2 receding-horizon monotonicity still applies.
- Watch: "unique broadly-available escape" for identifiability floors is strong language. It may hold under AAT's intended constraints, but alternatives such as external instrumentation, randomized audits, richer sensors, or oracle labels need to be excluded or classified.
- Watch: the coordinate-forcing catalog is methodologically interesting, but in this segment it is a cross-framework promise rather than a locally auditable derivation.

## Local verdict

As a discussion map, the segment is coherent and helpful. As evidence, it should be read as a dependency index: several of its strongest claims wait on later proof homes that remain unread in this modified AAT-first pass.
