# 80 - impl-composition-machinery

Source: `01-aat-core/src/impl-composition-machinery.md`

## First-pass understanding

This segment closes the composition-machinery chapter by connecting closure defect, bridge lemma, class coercion, tempo composition, critical mass, and identifiability-floor claims. As a map, it is useful: the chapter's intended payoff is that composite agency becomes an inequality-bounded coarse-graining problem, not a metaphorical boundary choice.

The segment is also where summary prose runs ahead of the local proof state. Several strongest claims depend on appendix or future homes I have not read in this AAT-order pass, and several inherit unresolved concerns from the immediately preceding segments: route typing for strategic composites, full closure versus A1-A4-only wrapper verification, and tempo double-counting.

## Diagram attempt

I drew the chapter as a synthesis hub. Already-read local machinery flows into the chapter payoff, while deferred proof homes and active caveats sit as side constraints. The visual intent is to avoid mistaking a chapter-end map for a proof home.

## Findings and watches

- F139 candidate: `impl-composition-machinery` is discussion-grade but presents the critical-mass closed form, four signed special cases, identifiability-floor Instance 3, and bandwidth inflection claims as settled. These may be supported in `deriv-critical-mass-composition`, `result-sector-persistence-template`, and `disc-identifiability-floor`, but proof credit is deferred until those homes are read.
- F140 candidate: the segment says `der-class-coercion-in-composition` derives that the wrapped system is a valid AAT composite agent satisfying (A1)-(A4). This inherits F132/F133: A1-A4 macro-dynamics admissibility is not the full `form-composition-closure` criterion, and wrapper composite scope/projection admissibility still need to be shown.
- F141 candidate: the claim that LLM substrates are constructively in AAT scope and that scaffolded loops are structurally non-optional for Class 3 substrates is too broad. The local construction supports a conditional wrapper route under component admissibility, leakage bounds, scope/projection gates, and tempo costs; it does not prove necessity for every useful Class 3 deployment.
- F142 candidate: the tempo section states `C_coord >= epsilon* nu_c` without the critical-distance normalization introduced in `der-tempo-composition`. As written, the units are wrong: `epsilon* nu_c` is a disturbance rate, not a tempo rate, unless `epsilon*` has already been normalized.
- F143 candidate: the Brooks's-Law and bandwidth discussion inherits the double-accounting issues in `der-tempo-composition`. The segment treats closure defect as effective disturbance, tempo overhead, and bandwidth overhead; those need a single ledger before the derived inflection point is reliable.
- F144 candidate: the composite-Level-2 escape for coupling-sign identification assumes broad availability of interventions on sub-agents and identifiable responses. Physical intervention access is not enough; the same positivity, adjustment/sequential ignorability, measurement, and transport caveats from earlier causal-access findings apply.
- F145 soft candidate: examples such as Generative Agents as canonical W1, human dual-process systems as W1-style internal scaffolding, and organizations as W1/W2 are useful analogies but need regime diagnostics. A fixed memory step or deliberative layer is not automatically strict W1 unless the belief-update path is structurally goal-blind.
- Watch: the representability-versus-optimality distinction is one of the chapter's strongest contributions. Preserve it in final synthesis.

## Local verdict

This is a good implications map, not a proof segment. Its final form should explicitly label which payoffs are established by already-read chapter machinery, which are appendix-derived, and which remain conditional on unresolved closure, wrapper, tempo, and identifiability assumptions.
