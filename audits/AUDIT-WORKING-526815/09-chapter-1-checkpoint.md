# 09 - chapter 1 checkpoint

Coverage: `def-agent-environment` through `post-causal-structure`.

## Model update

The first chapter is not just ontology; it establishes a two-layer scope lattice earlier than the initial definitions make obvious. The later scope segments are coherent: adaptive systems require observation under residual uncertainty, while agency requires causally contrastive action. The base definition is the thing that lags behind this lattice by defining "agent" as already action-effect-bearing.

The second pattern is dependency/canonicalization drift. The strict frontmatter graph is mostly satisfied, but semantic dependency load is not fully captured by frontmatter. `post-composition-consistency` is the strong case: a small valid postulate is bundled with late Section III/Appendix machinery. `post-causal-structure` is the mild case: already-read scope segments are used without being listed.

## Strategy update

Keep reading in row order. Do not resolve F2 by jumping to Section III or Appendix A; the placement problem is precisely that those objects are not available yet. For the next chapter, watch whether model/compression definitions inherit the strong opacity assumptions from `def-observation-function`, and whether passive adaptive systems remain first-class after the action-heavy opening.

## Candidate status

F1 remains live and sharpened: likely a terminology/scope mismatch in `def-agent-environment`, not a flaw in `scope-adaptive-system`, `scope-agency`, or `post-causal-structure`.

F2 remains live as canonicalization / hidden semantic dependency debt in `post-composition-consistency`.
