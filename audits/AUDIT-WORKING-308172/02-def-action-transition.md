# 02 — def-action-transition

The action channel and the transition function $T(\Omega_{t+1} \mid \Omega_t, a_t)$. Two definitional claims:
1. Actions affect $\Omega$ via possibly stochastic $T$.
2. $T$ is unknown to the agent (transition opacity).

The Discussion section is doing real work — flagging "Markov-of-$\Omega$ as a modeling commitment, not an empirical assumption." Same move as Markov-of-$M_t$ in `#der-recursive-update`.

**Naming-relevant.** "action transition" is the slug. The actual concept here is two things: the action-as-environment-mover machinery, plus the transition opacity. The current name elides the opacity claim.

Tracker row 100 — let me check the candidates.
