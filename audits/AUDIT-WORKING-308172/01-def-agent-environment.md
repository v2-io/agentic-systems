# 01 — def-agent-environment

The opening definition: agent + environment + lossy boundary. Three conditions for agent-hood (perception, memory, action), one boundary condition (information-loss is constitutive).

**What it commits to.** The information-loss boundary is the *non-trivial-ifier*: agents with perfect $\Omega_t$ access have no use for the rest of the framework. So the entire structure to come is conditioned on this scope.

**Naming-relevant terms surfaced.** "agent" / "environment" / "observation" / "action" / "perception channel" / "action channel" / "internal state" — all standard. None of these are likely R2 vote targets — they're too foundational and the names are too well-established in the literature.

**Reflection on the term "agent" itself.** This is interesting — "agent" carries enormous baggage in AI / control / philosophy. But AAD is explicitly using the broad reading: thermostats are agents. The README's Lexicon doubles down with the agent-class hierarchy (adaptive system → agentic system → actuated agent → self-actuated → logogenic → logozoetic). Naming-cycle implication: if "agent" is over-loaded in the AI register, the framework's stratification by class may be the protective move. Doesn't change R2 votes.

**What I'd expect to surface in the tracker.** Probably no R2 targets directly. Maybe some downstream — "perception channel" / "action channel" might appear later. Let me check.
