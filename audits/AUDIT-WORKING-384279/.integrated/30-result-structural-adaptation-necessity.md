# 30 — result-structural-adaptation-necessity

*Type: result. Status: conditional. Stage: claims-verified. Depends: [def-model-sufficiency, def-model-class-fitness, result-mismatch-decomposition, emp-update-gain].*

## Predictions vs evidence
Predicted: when $\mathcal{F}(\mathcal{M}) < 1-\varepsilon$, parametric updates can't close the floor — must change model class. Found: that, plus bidirectional framing (compression as well as expansion), four-mechanism taxonomy, Miller-2022 neutral-variation mechanism.

## Math verification
Derivation 1-6 (line 39-44) is sound conditional on the alignment assumption (lost info affects one-step conditional mean). Status `conditional` correctly tagged.

## Prose-coherence
- Status `conditional` consistent across frontmatter + body. ✓
- Bidirectional framing (compression / expansion) elegant — IB diagnostic works in both directions.
- Four mechanisms (decomposition / expansion / compression / grafting) is a clean taxonomy.

## Cross-segment consistency
Forward-refs `#form-information-bottleneck`, `#der-deliberation-cost`, `#def-causal-information-yield`, `#der-temporal-nesting`. Coherent.

## Watch list
- The alignment-assumption framing (line 50) is honest — the qualitative conclusion holds even without alignment, in proper-scoring-regret form. Good methodological care.
- Miller 2022 "extreme transition motif" — well-cited prior art, properly attributed.

## Next-segment predictions
`#der-temporal-nesting`. Will derive timescale stratification — fast / slow processes with convergence constraints.

## Brief wandering
The bidirectional structural-adaptation framing is methodologically important — many practitioners think of "structural change" only as expanding capacity. Naming compression as equally a structural change matches IB optimality. The Miller 2022 neutral-variation mechanism is intellectually rich and bridges "incremental change" and "radical restructuring." Good integration of prior art.
