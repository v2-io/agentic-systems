# 13 — def-model-class-fitness  *(no finding; THREAD-F refined; §E; Ch.2 complete)*

`type: definition · status: axiomatic · stage: deps-verified · depends: [def-model-sufficiency]`
Dep upstream (12). DETECTOR clean (`*[Definition]*` only). B7 alive (13/13).

## THREAD-F refined — NOT a finding here; load-bearing test elevated

seg-12 forward-named the predictively-non-vacuous scope as inherited by
`#def-model-class-fitness` and `#result-structural-adaptation-necessity`.
seg-13 does **not restate** the scope clause — but this is *correct
lightweight inheritance*, not an F1/F2 lapse: $\mathcal F(\mathcal M)=
\sup_{M\in\mathcal M}S(M)$ is *mathematically* only defined where $S$ is
(sup of an undefined-in-vacuous-regime quantity is undefined), the
`depends:` edge makes it traceable, and **seg-12 already named the
propagation**. Restating it here would be redundant. So: scope-propagation
discipline is *intact* across 12→13 (named once at source, automatic
mathematically, dependency-linked) — a §E positive, the legitimate direction
of the relocation discipline F1/F2 lapsed on. **The real test is elevated**
to `#result-structural-adaptation-necessity` (Ch.4, FORMAT.md
inevitability-core): it makes the *substantive, universal-sounding* claim
("parametric updates cannot close the mismatch floor; structural change
necessary"); whether *it* carries the predictively-non-vacuous scope (or
asserts the trigger universally) is where a real scope-honesty finding would
bite. THREAD-F → "watch `result-structural-adaptation-necessity`
specifically" (high-priority inevitability-core check).

## §E + Gate-2 (hard-check #3, holds)

- Honest about unobservability + names the proxy: "The agent cannot directly
  compute $\mathcal F(\mathcal M)$ … persistent mismatch despite adequate
  learning … is the observable signature." Doesn't pretend $\mathcal F$ is
  measurable. Good hygiene.
- Bias/variance analogy hard-checked (Gate-2): "$\mathcal F$ is bias;
  $S(M_t)$ reflects bias+estimation" — correct ($\mathcal F=\sup$ over class
  = best-case = bias floor; a learned $S(M_t)$ adds estimation error). Holds.
  Phase-3 disconfirmation attempt #3 → no content defect (13 segs, still
  zero; every hard-checked claim holds).
- **define-here / derive-there done correctly (anti-F2):** "The substantive
  claim about what happens when $\mathcal F$ is low … is developed in
  `#result-structural-adaptation-necessity`." Trigger defined here as a
  *definition*; consequence explicitly deferred, named, NOT forward-derived.
  The seg-09 chapter promissory note ("low ceiling ⇒ change class not
  parameters; trigger here, consequence Ch.4") is being paid with correct
  discipline; the **inevitability-grade payment is owed by
  `#result-structural-adaptation-necessity`** — elevated to a key
  inevitability-core verification (FORMAT.md lists it in the ~15 inevitability
  core; bar = mathematical inevitability, no alternative formulation escapes).

## Prediction / next
Predicted seg-13 carries the ceiling def + the structural-inadequacy
condition as a *definition* deferring the result — confirmed exactly.
**Chapter 2 (The Reality Model) complete** (09 intro, 10 form-agent-model,
11 form-information-bottleneck, 12 def-model-sufficiency, 13
def-model-class-fitness). Next OUTLINE: Chapter 3 "The Cycle in Motion:
Mismatch, Gain, & Tempo" → `#the-cycle-in-motion-intro` (`type: discussion`,
draft). Predict: chapter-bridge (static representation → dynamic cycle),
mental-model-first, DETECTOR-clean (intros are framing), thin deps; treat as
the chapter's promissory note to grade Ch.3 against (per seg-09 method note).

## Diagram — deliberately skipped (and no Ch.2 capstone)
Chapter 2 already carries THREE strong diagrams (09 slider/ceiling, 10
suitcase + THREAD-B skeleton, 11 IB-curve) from the pre-cadence-change
regime — it is *over*-diagrammed relative to the new ~1–2-per-chapter budget.
A seg-13 or capstone diagram would restate seg-09/10's $S$-vs-$\mathcal F$
ceiling picture (already isomorphic and clear). Adding more is exactly the
over-production Joseph's cadence change targets. **No diagram.** Chapter 3
starts the ~1–2-per-chapter regime from a clean slate; I'll spend the
chapter's diagram budget on whichever Ch.3 segment is most load-bearing
(likely the mismatch/gain/tempo dynamic or the persistence setup).

## Wandering (≤1 ¶)
Chapter 2's audit verdict: clean. Its job was to turn "the agent has a
compressed model" into three measurable objects ($\phi$, $S$, $\mathcal F$)
and a deferred trigger, and it did so with exemplary
relocation-naming/scope-propagation discipline throughout — it is the
strongest evidence yet for the Phase-3 *bimodal* picture (formulation/
definition layer is excellent; defects live only at forward-pressured
foundational hinges F1/F2 and a vocabulary seam F3). The chapter also
quietly set up the single biggest upcoming test: the whole $S/\mathcal F$
edifice is *only* load-bearing if `#result-structural-adaptation-necessity`
pays the inevitability-grade promissory note that seg-09 wrote and seg-13
deferred. That segment (Ch.4, Section I) is now the highest-value target in
Part I for the audit — it is where "one of the framework's central results"
either is mathematically inevitable or is robust-qualitative wearing a
central-result frame. Flagged at top priority; everything in Ch.2 is
upstream scaffolding for it.
