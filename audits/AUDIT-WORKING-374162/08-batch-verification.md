# Batch 8 Verification — Part I Appendix A backfill

Verified against full reads of all eleven ground-truth segments (not line-capped — I read complete files, including Discussion/Working Notes tails the answer-writer disclosed as unread). All 23 questions/answers checked; overall the batch is strong — no fabricated formulas, no out-of-batch sourcing, no untagged-WN-as-body errors. One real finding, plus two minor observations.

## Finding: A b08-3.7 reproduces an internal inconsistency in `deriv-variational-sector-condition` rather than catching it

The answer states mean-field VI is "structurally sub-optimal ... by the factor $\sqrt{1-r^2}$ in the sector constant." That sentence is lifted verbatim from the segment's own opening summary paragraph (line 18): *"mean-field VI is structurally sub-optimal as an adaptive system by a factor $\sqrt{1-r^2}$ in the sector constant, regardless of compute or training data."*

But nowhere else in the segment — not in Formal Expression, not in "Per-case verdicts," not in Working Notes — is $r$ ever defined or is a $\sqrt{1-r^2}$ factor derived. The actual derived content for mean-field VI (§"Per-case verdicts") is only: mean-field achieves "small $\varepsilon$" and is "the workhorse $\alpha'$ case," with the ultimate bound degrading by the additive $O(\sqrt\varepsilon)$ term common to all of Regime B — not a multiplicative $\sqrt{1-r^2}$ penalty on the sector constant itself, and no correlation parameter $r$ (presumably meant to capture how well the mean-field factorization matches the true posterior's correlation structure) appears in the Formal Expression at all.

So the segment's own summary paragraph asserts a specific quantitative claim that its body does not derive — a real Gate-2-class gap in the source, not a verification artifact. The quiz answer inherits this by quoting the summary without checking that the body supports it (the same class of error the "untagged WN gloss" pattern names, just one layer removed: here it's an untagged/undischarged **intro-paragraph** claim rather than a Working Note). Recommend either (a) deriving the $\sqrt{1-r^2}$ form in a follow-up pass on `deriv-variational-sector-condition` (it's plausible — likely meant to fall out of comparing the mean-field-factorized covariance to the true joint via the Fisher metric, with $r$ some correlation/coupling measure — but as written it's asserted, not shown), or (b) softening the summary to match what's actually derived (additive $O(\sqrt\varepsilon)$ degradation, not a named multiplicative factor). Strengthen-before-soften applies: worth a real attempt at (a) first.

## Everything else checked clean

- **b08-1.1–1.7** (mental model): all verified against `sketch-structural-adaptation-genericity`, `deriv-persistence-cost`, `deriv-stochastic-non-exit`, `deriv-discrete-sector-condition`, `deriv-tempo-additivity` full text. The Cor A.1S.1 gap analysis (1.1), the persistence-cost scope premises and Kalman-Bucy saturation (1.2), the reusable no-go signature (1.3), the DA2'a/DA2'b split with the transverse-component pathology (1.4), the signed-Δ / no-sign-blind-correction / echo-chamber two-error structure (1.5), the harmonic-mean hypersurface (1.6), and the asynchrony refinement (1.7) all match the source precisely, including the specific numeric/algebraic details (e.g., $c_{\min}\approx c_{\max}$ recovering $\eta^\ast\lt 2/c_{\max}$; $R_s = \sum_k f(q_k) - f(q)$).
- **b08-2.1–2.9** (math): hand-checked the two-channel Δ formula (2.5: $\Delta=0$ iff $c=0$ or $c=2U_1U_2/(U_1+U_2)$ — confirmed algebraically consistent with the source's stated closed form), the Sherman-Morrison echo-chamber $f(q)=q/(1+\sigma_s^2q)$ and saturation to $1/\sigma_s^2$ (2.6), $\lambda_{\text{eff}}^2$ derivation and the DA.1/DA.1S fluid-limit gaps (2.3), the Fisher-local (R1)–(R3) + $K=(H_M+H_L)^{-1}H_L$ + scalar-collapse conditions (2.4), the OU RDF substitution giving $n\alpha/2$ and Kalman-Bucy $\dot I=\alpha/2$ saturation (2.2), the compensated-supermartingale $S(t)$ sign-indefiniteness argument and unbounded-scale-function obstruction (2.1), the matrix-Loewner MP-1/MP-2 + three equivalent restatements + per-coordinate-unsafe counterexample (2.7), the four MG conditions and the augmented-Lyapunov composition (2.8), and the Regime A/B degradation with $c_\varepsilon(\Vert\delta\Vert)$ and $R_\varepsilon^\ast$ (2.9). All match the source verbatim or by direct algebraic check; no formula-invention or sign errors found.
- **b08-3.1–3.6** (implications): the A2' sub-scope ladder placements (adaptive Kalman/Mehra, Adam/AMSGrad, MAML, mean-field/natural-gradient VI) match `deriv-adaptive-gain-dynamics`'s Cases A–D and the sub-scope table exactly, including the "AMSGrad as structural MG-1 repair" framing (3.1, 3.2). The bandwidth-per-direction rebuttal chain (3.3) correctly composes the persistence-cost floor with matrix-Loewner unsafety. The epistemic-tier ranking (3.4) matches each segment's own stated `status:` and Epistemic Status prose, including correctly placing (d) — the genericity hand-off — as flagged-overclaimed/open rather than conditional, and correctly noting (e)'s general form is refuted (the signed-Δ result) while the common-source-scope form is exact. The Mehra non-identifiability / MG-2 / identifiability-floor framing (3.5) and the three bridge-derivation candidates + no-go-protocol fallback (3.6) both match `sketch-structural-adaptation-genericity` and `deriv-adaptive-gain-dynamics` precisely.

## Minor observations (not errors)

- A b08-3.1's "$\eta^\ast \lt 2/c_{\max}$" recovery condition and A b08-1.4's DA2'b framing both correctly reflect that DA2'b is *stronger than* an inner-product bound, not merely different — this distinction is easy to flatten and both answers preserve it correctly.
- The Model-S no-go's Working Notes (`deriv-stochastic-non-exit`) record that this obstruction shape is expected to recur at `deriv-discrete-sector-condition`, `deriv-matrix-persistence-condition`, `deriv-adaptive-gain-dynamics`, and `result-per-dimension-persistence` — worth flagging for a future batch that none of those four segments in *this* batch actually carry a Corollary-A.1S.1-shaped dichotomy of their own; the containment-dichotomy content lives entirely in `deriv-stochastic-non-exit` + `deriv-sector-condition`. Not a defect in this batch's Q&A, just a pointer for whoever eventually checks that WN prediction.

## Verdict

Batch 8 is high-fidelity. One finding (the $\sqrt{1-r^2}$ intro-vs-body gap in `deriv-variational-sector-condition`, inherited faithfully but uncritically by A b08-3.7) is worth routing back to the segment itself rather than the quiz — it's a corpus gap the appendix-backfill audit surfaced, not a summarization error. Recommend a follow-up spike on `deriv-variational-sector-condition` attempting the $\sqrt{1-r^2}$ derivation before touching the prose (per strengthen-before-soften).

Willing to stay on the line for follow-up questions on this batch.
