# Batch 9 Verification — Part II Ch.1: Meta-Architecture I

Verified against full reads of all eight ground-truth segments (`disc-stability-certificate`, `disc-identifiability-floor`, `disc-separability-pattern`, `disc-additive-coordinate-forcing`, `disc-constructive-impossibility-posture`, `disc-anti-collapse`, `disc-value-functional-grounding-floor`, `disc-implementation-impossibility`) — bodies, Findings blocks, and Working Notes (including the gold-lift tails) all read in full, not capped. All 24 questions (18 main + 6 supplement) checked against source. This is the cleanest batch of the relay so far: no fabricated formulas, no untagged-WN-as-body conflation, no out-of-batch sourcing, no unstated extrapolation. I did not find a finding worth routing back to the corpus or the quiz.

## Discrimination note (per the ask)

I answered several questions cold (before re-reading the key) after reading the segments, specifically b09-2.3 (Sylvester/congruence), b09-3.4 (ladder placements), and b09-3.5 (mutual-invariance cross-checks). All three cold answers matched the key on substance. The place I'd flag as genuinely discriminating — where a summary-fed agent is most likely to drift plausible-but-wrong — is **b09-3.5's specific cross-check triples**. The source sentence (disc-stability-certificate, "Each obstruction is untouched by the others' freedoms: a metric change does not fix non-invariance; projection does not fix non-symmetry; rank-augmentation does not fix a memory kernel") is elliptical — "non-invariance" and "non-symmetry" aren't glossed in that sentence itself; you have to carry forward which invariant belongs to which obstruction from two paragraphs earlier (symmetry of $J$ ↔ forced-identity; inertia-under-congruence ↔ existence; $J$-invariance-of-subspace ↔ projection) to correctly pair "metric change" with "projection's problem" rather than, e.g., misreading it as "metric change doesn't fix its own obstruction" (a tautology, not the cross-check the text intends). A b09-3.5 in the answer key resolves this correctly and explicitly (unpacking each pairing with the specific mechanism), which is exactly the kind of place a plausibility-only pass would either garble the pairing or reproduce the elliptical sentence verbatim without demonstrating the unpacking. Second-most discriminating: b09-3.4(b) (placing "purely observational credit-assignment" on the Identification-regime ladder's Regime C rather than, say, misreading it onto the unrelated "row 17 credit assignment" aside buried in `disc-stability-certificate`'s Working Notes, which is a different taxonomy — a doc-collision trap a shallow reader could fall into). The answer key avoids it correctly.

## Everything checked clean

- **b09-1.1–1.6** (mental model): the certificate object, condition (C), four specializations, and anchor equivalence (1.1); the "facets not peers" correction plus the two non-facet style disciplines (1.2); the three obstructions with their classical anchors, invariants, and the open-exhaustiveness caveat (1.3); forced/matched/adopted with the Lyapunov-quadratic-as-matched example (1.4); the anti-collapse diagnostic and its inverse (spurious-split) case with the observability/identifiability example (1.5); the three boundary-face clusters' actor/theorem/remedy triples and the `-floor`/`-impossibility` suffix convention (1.6) — all verified verbatim against the source segments.
- **b09-2.1–2.6** (math): certificate condition (C) and the R0-loss⟸R0-strict⟸R1⟸R2 ladder (2.1); the five-element shape instantiated for M1 Instance 2, including the exact rank-1 Fisher factorization $\mathcal F = uu^T/(\mu_j(1-\mu_j))$ and null-space description (2.2); the Sylvester congruence-orbit argument, rank-augmentation-only escape, and the {1,2,4}-vs-{3} subclass split (2.3); the four forced-coordinate layers, the single Legendre-Fenchel object, and the scope-dependent Čencov-Fenchel caveat (2.4); Instance D's guarantee-substitution (not escape) and why (2.5); the value-functional grounding floor's two-routes-exhaust claim (2.6) — all hand-checked against the segment text and internally consistent.
- **b09-3.1–3.6** (implications): the two-tier discussion-grade reply and what would falsify the spine without falsifying a constituent (3.1); Instance F's task-failure/elevation/recognition-not-construction framing (3.2); the implementation-impossibility cluster's boundary-map (not elevation) strengthened consequence (3.3); the eight-ladder placements for an LLM agent (Architecture/Class 3), a purely-observational regime (Identification/Regime C), and a mean-field population (Dynamic regime/R3) (3.4); the mutual-invariance rejection of the "one integrability failure" collapse, with correctly-paired cross-checks (3.5); the introduced-before-used ordering tradeoffs and the plant/develop/recall device (3.6) — all verified.
- **Supplement (s.1–s.4):** the composition-layer non-extension of additive-coordinate-forcing and the "fourth anchor, not fourth theorem" framing (s.1); the `[^bg2-2026-05-21]` verification-deferred citation discipline and its load-bearing site at Instance 3 escape (e) (s.2); the two-routes-exhausts adversarial spike's three attempted-and-collapsed third routes (observer-side / inter-agent / substrate-level Goodhart) (s.3); the convergent (not generative) provenance of the M1 methodology and Instance 4 as the first methodology-aware instance (s.4) — all verified against the Working Notes they cite.

No untagged-WN-as-body slips: every answer that draws on Working-Notes-only content (s.2, s.3, and the reflections' framing of the capped-then-repaired read) explicitly marks it as WN-sourced ("verification-deferred," "queued spike," "candidate") rather than presenting it with body-tier confidence. No out-of-batch sourcing: cross-references to segments outside the batch (`#form-composition-closure`, `#der-directed-separation`, `#form-objective-functional`, etc.) are used only for facts already stated inline in the eight ground-truth segments, not for content pulled from those external segments' own bodies. No detected plausible-but-unstated extrapolation — I checked the more inference-shaped answers (1.3's exhaustiveness caveat, 3.1's falsification condition, 3.5's cross-check unpacking) against the segments' own explicit statements and found each traceable to a specific sentence rather than an inference from shape.

## Verdict

Batch 9 is high-fidelity with no findings. This is a genuine surprise given the chapter's stated trap-density ("summaries of it sound identical to comprehension of it") — the answer key's density of exact quotation and correct cross-pairing (especially b09-3.5) suggests the answers were composed with the full texts open, not reconstructed from a summary. Willing to stay on the line for follow-up.

## Supplementary questions (verifier-contributed)

Material in the eight segments that the main quiz + supplement don't reach, but that a holistic read surfaces as quiz-worthy — the constructive-impossibility-posture's full instance catalog and three-cluster taxonomy (untested by the main set, which only reaches it via b09-3.3's implementation-impossibility corner), the anti-collapse discipline's two sub-shapes and its self-distinction from its two sibling style-disciplines, and the Interior facet's Helmholtz spectral refinement (dense material in `disc-stability-certificate`'s Discussion that neither the main quiz nor supplement touches).

### Q b09-v.1 [mental-model]

`disc-constructive-impossibility-posture` catalogs seven instances (A–G) and groups them into three clusters. Name the three clusters and which instances belong to each. Then explain the segment's own answer to "is this a fifth cross-sectional meta-pattern?" — what does it call itself instead, and why does that self-classification matter for how a reader should place it relative to the certificate spine's four facets.

### A b09-v.1 [mental-model]

Three clusters: **agent-side data-inference impossibility** (Instances A–C, the `#disc-identifiability-floor` charter instances, plus D and E as operator-temporal/strategic-layer analogs at the same boundary face — D is the Disturbance-Model Containment Dichotomy, E is the Strategic-Persistence Hard Ceiling); **agent-side value-functional-grounding impossibility** (Instances F and G, the `#disc-value-functional-grounding-floor` charter instances — F is within-model self-actuation grounding via Result G′, G is across-model reward-channel learning via the Cohen-2022 strengthening); **designer-side mechanism-design impossibility** (the GS/MS/Arrow charter of `#disc-implementation-impossibility`, referenced but not lettered A–G in this segment's own table). Self-classification: the segment explicitly says it is **not** a fifth meta-pattern alongside the certificate's interior/scope-of-existence/forced-identity/boundary/projection facets — it names itself "M1.5, not M5": a *style* claim about how the boundary-facet results are *stated* (name the floor, name the escape, treat the no-go as apparatus), sitting *atop* the boundary facet rather than constituting a sixth or additional facet of its own. This matters because a reader who has just metabolized the four-facet certificate structure is primed to expect a growing facet count; the segment pre-empts that expectation directly in its own Working Notes and Discussion.

### Q b09-v.2 [implications]

`disc-anti-collapse` names two sub-shapes under its "individuation at the repair-relevant grain" principle. What are they, and which is the dominant/name-giving form? Then distinguish the anti-collapse discipline from its two sibling style-disciplines (`disc-constructive-impossibility-posture` and `disc-additive-coordinate-forcing`) using the segment's own three-way contrast — what does each of the three say a plausible-but-wrong move gets wrong?

### A b09-v.2 [implications]

Two sub-shapes: **one-cause-wrong-knob** (a single cause is tempting to route to parameter/quantity X, but actually routes to Y, with X held fixed — e.g., $\beta$ vs $\rho$ in the information bottleneck, emitter-scalar vs recipient-regime, $\kappa$ vs $\mathcal A$, the two exploration drives) and **two-distinct-quantities-a-naive-reading-merges** (a single quantity a naive reading treats as one is really two orthogonal quantities — e.g., satisfaction-gap vs control-regret, structural bias-floor vs estimation error, target-alignment vs execution-path-alignment). One-cause-wrong-knob is the dominant form and gives the discipline its name ("anti-collapse" = refusing the collapse of cause onto the wrong knob); two-distinct-quantities is a variant the "which-knob" framing originally left outside, absorbed by the sweep's individuation-at-the-repair-relevant-grain reframe. Three-way contrast (all sit atop the certificate's facets as style claims, none carrying its own theorem): `disc-constructive-impossibility-posture` says a plausible-but-wrong move *forbids an inference* that a floor-and-escape structure actually permits under named machinery (it's about what an agent/designer *cannot do* under a regime); `disc-additive-coordinate-forcing` says a plausible-but-wrong move treats a coordinate as a free choice when a uniqueness theorem on an AAT-internal axiom actually *forces* it (it's about what the framework *commits to*); `disc-anti-collapse` says a plausible-but-wrong move is a **modeling** error — routing a cause to the wrong knob, or merging two repair-distinct quantities into one — that isn't about inference-forbidding or coordinate-forcing at all, but about getting the individuation of causes and effects wrong relative to what the remedy needs.

### Q b09-s.5 [math]

`disc-stability-certificate`'s Discussion introduces a refinement of the Interior facet via the Helmholtz decomposition $S_\mathcal{M} := \tfrac12\mathcal{M}^{-1}(\mathcal{M}J + J^\top\mathcal{M})$. What are the two (plus one mixed) sub-regions this splits the Interior facet into, which theorem anchors each, and — applying the segment's own "new facet or new object?" test — what does the segment conclude about whether this refinement adds a fifth facet or a fifth meta-segment?

### A b09-s.5 [math]

Two clean sub-regions plus a mixed case: **$S_\mathcal{M} \succ 0$** gives R0-strict contraction (the prior Interior content, $\dot V \leq -2\kappa V$, $\kappa \gt 0$) — anchored by Letcher-Balduzzi et al. 2019 Lemma 1 + Definition 2 (pure-case sufficient construction, Hamiltonian-game vocabulary). **$S_\mathcal{M} = 0$ at $\mathcal{M} = I$** gives the antisymmetric-subclass R0-loss case (certificate exists at $\sup\kappa = 0$, $V$ exactly conserved). The **mixed case** ($S_\mathcal{M} \succeq 0$ with nontrivial kernel) is Conley 1978's Fundamental Theorem of Dynamical Systems localized at the linearization — chain-recurrent subspace plus strongly-gradient-like complement — anchored by Conley 1978 §8.1 (mixed-case universality). Applying the segment's own test: the answer is **neither** a new facet nor a new object — it is new *internal structure on the existing Interior facet* (the spectral/representation dual of contraction-strength), so the four-facet structure stays four and the framework gains no fifth meta-segment.
