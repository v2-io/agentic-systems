# Batch 4 — der-action-selection, def-mismatch-signal, result-mismatch-decomposition, emp-update-gain, def-causal-information-yield

No appendix `depends:` fired (emp-update-gain leans on deriv-fisher-local-update-gain and deriv-adaptive-gain-dynamics in Epistemic Status/Discussion but does *not* list them in depends — see observation below).

## Predictions vs evidence

- der-action-selection: predicted the core ($a_t = \pi(M_t)$ by completeness, Part-II lift previewed). Unpredicted: the whole **action fluency** apparatus — implicit vs explicit action selection, fluency formalized as $\Delta\eta^\ast(\Delta\tau) \approx 0$, the System-1/System-2 and Boyd IG&C mappings, and the *structural pressure toward implicit action* (persistence penalizes slow tempo ⇒ selective pressure internalizes recurring patterns). Fluency ≠ sufficiency (chess engine: high $S$, low fluency) is a crisp discriminator pair.
- result-mismatch-decomposition: my batch-3 prediction was right that the three-term form is richer than the intro's two-way preview — and the middle term is the money. It is a **Pythagorean refinement of bias-variance**: estimation error (vs the *Bayes predictor*, not vs truth) + **state-uncertainty floor** (binds even the Bayes-optimal predictor; movable only by *acting*) + channel noise (irreducible outright). "Two kinds of irreducible" — by modeling vs outright — is the segment's real teaching, and the Kalman anchor ($HP^-H^\top + R > R$: excess = state floor at the in-class optimum) makes it concrete. Exact under GA-1 (fresh noise), with the honest **alignment qualifier**: $H(\Omega_t|\mathcal C_t) > 0$ is necessary but *not sufficient* for a positive state floor (uncertainty must move the one-step conditional mean). This answers my batch-2 watch-item: it is a genuine identity (orthogonality — uncorrelated, not independence), not a restated definitional split.
- emp-update-gain: the tiering is more sophisticated than predicted. Not just "Kalman exact, else robust-qualitative": there's a **Fisher-local regime** (any smooth log-likelihood with non-degenerate local quadratic expansion; Amari natural-gradient invariance) where the form is *exact* at first order, with Kalman/conjugate as globally-exact instances. `type: empirical` + `status: robust-qualitative` while the Epistemic Status opens "*Derived* under the Fisher-local regime" — the two-tier structure again (like IB's formulation/exact pairing). **Epistemic-opacity paradox resolved**: agent estimates $U_o$, $U_M$ from its own innovation statistics; gain is endogenous meta-adaptive state (proof deferred to deriv-adaptive-gain-dynamics). **Gain collapse** is the named failure mode ($\eta^\ast\to 0$ via spurious confidence or spurious sensor-distrust; Boyd's incestuous amplification), with the reset-after-structural-change requirement.
- CIY: my prediction (KL over do-contrasts) close; actual form $\mathrm{CIY}(a;M) = \mathbb E_{a'\sim q}[D_{KL}(P(o|do(a),M)\,\Vert\,P(o|do(a'),M))]$. The teaching content is **distinguishability ≠ learning value**: high CIY necessary but not sufficient for learning (deterministic beeping button: high CIY, zero EIG after first press). CIY is the tractable surrogate for EIG, gated by heuristic $\lambda(M_t)$. Also answers the batch-2 watch: post-causal-structure's "weight causally-downstream observations more" claim resolves not through the gain formula itself but through CIY/exploration machinery — the gain weights by *uncertainty*, not causal downstreamness; the causal weighting enters action *selection* (which observations to generate), not the update rule. Subtle and quiz-worthy.

## Possible small observation (not pursuing as finding per run scope, but passing along)

`emp-update-gain`'s Epistemic Status is *load-bearingly* built on #deriv-fisher-local-update-gain (the exactness tier claim) and its Discussion's opacity-resolution on #deriv-adaptive-gain-dynamics, yet neither appears in `depends:`. Under FORMAT Gate-1 criterion 2/4 these look like genuine dependencies (the segment "uses the referenced segment's results"), not mere mentions. Contrast: der-recursive-update *does* list its appendix derivation. Possibly deliberate (tier-justification vs content dependency?), but the asymmetry with der-recursive-update reads as drift. Cheap to check/fix.

Also noticed: emp-update-gain's domain table has `$P_{t\Vertt-1}$` — the `\Vert` here is rendering the *conditioning bar* in $P_{t|t-1}$; using `\Vert` (double bar) for a subscript conditioning bar is a lint-workaround artifact that renders wrong (should be $P_{t\mid t-1}$). Trivial FORMAT item.

## Cross-segment notes

- The zero-aporia ambiguity (def-mismatch-signal: silence = peace or deafness; three readings of $\delta\approx 0$) + gain collapse (emp-update-gain) + CIY (active testing) form a tight arc: the ambiguity motivates active testing; gain collapse is the pathology where the resolution machinery itself fails. The arc is the chapter's real spine and is completely invisible in summaries, which present mismatch → gain → tempo as a pipeline of definitions.
- def-mismatch-signal's Mahalanobis note quietly does load-bearing work for dimensional consistency of everything downstream (mismatch in "surprise-equivalent units").
- der-action-selection `status: exact` scoped "within Part I scope" with the exact result arguably being $a_t = \pi(X_t)$ — the WN notes the cleaner restatement. The fluency material is explicitly discussion-grade — honest two-tier segment again.
- Recurring pattern now confirmed across three batches: **two-tier epistemic structure inside single segments** (exact core + weaker interpretive shell, each labeled). This is the single most reliable discriminator between corpus-readers and summary-readers: summaries collapse the tiers into one confidence level, in either direction.

## Wandering

The gain-collapse dichotomy (dogmatism $U_M\to 0$ vs nihilism $U_o\to\infty$, behaviorally identical) is a genuinely good conceptual test: both routes produce $\eta^\ast \to 0$, and asking *which observable statistics distinguish them* (none, from behavior alone — both stop updating; you'd need the agent's internal uncertainty attributions) is an expert-level question. Also noting for Joseph's quiz-consumer use case: the CIY-vs-EIG distinction is precisely the kind of thing a confident summary-fed agent will get wrong *with high fluency* — they'll say "CIY measures how much an action teaches the agent," which is the exact misreading the segment pre-empts. I'm now deliberately mining each segment for its "pre-empted misreading" — the corpus is unusually rich in them, and they make ideal trap questions because the misreading is *more plausible-sounding* than the truth.

## Watch-list updates

- Resolved: mismatch-decomposition identity (genuine, GA-1); interventional-weighting question (routes through action selection, not gain).
- Carry: does def-adaptive-tempo handle the additive cross-channel assumption honestly (deriv-tempo-additivity in appendix says additivity is *conditional* on noise independence — the OUTLINE one-liner says the old general upper bound was refuted); check body-vs-appendix consistency.
- New: hyp-mismatch-dynamics is typed *Hypothesis* — the ODE itself is hypothesis-grade. Summary-readers will call it derived. Check exact wording next batch.
- Carry: $U_M$ symbol collision (model uncertainty here; epistemic unity in Part III) — known in WN.

## Predictions for batch 5 (def-adaptive-tempo, hyp-mismatch-dynamics, persistence-intro, der-deliberation-cost, form-sector-condition)

def-adaptive-tempo: $\mathcal T = \sum_k \nu^{(k)}\eta^{(k)\ast}$, claims-verified, with additivity scoped to noise-independence and a pointer to deriv-tempo-additivity (appendix — will need reading if in depends). hyp-mismatch-dynamics: $d\Vert\delta\Vert/dt = -\mathcal T\Vert\delta\Vert + \rho$ under GA-5 fluid limit, honestly typed hypothesis; steady state $\rho/\mathcal T$. persistence-intro: sector condition as honest generalization; the "$\alpha$ demoted from postulate to property" story. der-deliberation-cost: deliberate iff $\Delta\eta^\ast(\Delta\tau)$ benefit exceeds $\rho_{\text{delib}}\cdot\Delta\tau$ accumulated drift cost — an inequality, not an optimum. form-sector-condition: (A1)/(A2')/(A3) with the $\alpha/\beta$ sub-scope partition; $\delta^T F(\mathcal T,\delta) \geq \alpha\Vert\delta\Vert^2$ for $\Vert\delta\Vert\leq R$ per GA-3.
