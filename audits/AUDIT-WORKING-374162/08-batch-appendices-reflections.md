# Batch 8 — Part I appendix backfill (mention-standard remediation)

Read this batch (bodies/formal cores; WN gold-lift tails capped): `result-sector-persistence-template` (full, earlier today), `deriv-stochastic-non-exit`, `deriv-persistence-cost`, `deriv-discrete-sector-condition`, `deriv-tempo-additivity`, `deriv-fisher-local-update-gain`, `deriv-adaptive-gain-dynamics`, `result-per-dimension-persistence`, `deriv-matrix-persistence-condition`, `deriv-variational-sector-condition`, `sketch-structural-adaptation-genericity`. Still unread from Appendix A (disclosed): `detail-linear-ode-approximation`, `obs-section-i-validation-simulations`, `disc-independence-audit`, `disc-approximation-tiering`, `deriv-fisher-whitened-update-rule`, `internal-external-decomposition`, `deriv-update-detection-latency`, `der-multi-timescale-stability`, Appendix-B worked examples, and everything downstream-facing (Part II/III appendix rows).

## What the appendices add that the walk segments couldn't

1. **The epistemic-tier texture is far finer in the appendices than the bodies suggest.** Nearly every appendix is `status: conditional` with *named premise lists* — (R1)–(R3) Fisher-local, (MG-1)–(MG-4) meta-gain, (DA2'a/b) + no-overshoot, KL-ε + Lipschitz + nested-support, Hurwitz + Loewner. The tier vocabulary is doing real work: e.g., deriv-stochastic-non-exit is `exact` *as a no-go*, while deriv-persistence-cost is `conditional` (Gaussian-OU, independent components, high-resolution regime, tight-bound achievement). Quiz gold: "state the premises under which X is exact" questions have crisp, checkable answers here.

2. **The sub-scope ladder is much richer than α/β**: α (fixed-gain derived) → α₁/α₂ (fixed vs adaptive gain under MG conditions) → α′ (controlled-KL variational, $O(\sqrt\varepsilon)$ degradation, Regime-A/B decomposition) → α₃ (Fisher-whitened, unread) → β. And the placements of *named real algorithms* are derivations: adaptive Kalman α₂ (with Mehra non-identifiability breaking MG-2 — an identifiability-floor instance on the meta-gain channel!); vanilla Adam **fails MG-1** (Reddi 2018) while **AMSGrad's monotonic $v_t$ is structurally a meta-gain repair**; MAML: α₁ inner / β outer; mean-field VI structurally sub-optimal by $\sqrt{1-r^2}$ *regardless of compute*, natural-gradient VI recovers full α (Khan-Lin). These are the most quiz-worthy "surprising concrete consequences" in the whole volume.

3. **The no-go anatomy** (deriv-stochastic-non-exit): the compensated supermartingale $S(t) = e^{2\alpha t}V - \frac{n\sigma_w^2}{4\alpha}(e^{2\alpha t}-1)$ IS a supermartingale but is sign-indefinite exactly inside the typical scatter (where $V \lt n\sigma_w^2/4\alpha$ — most of the time under the persistence condition); deeper obstruction: unbounded scale function ⇒ only constant bounded harmonic functions. Plus Monte-Carlo confirmation (ever-exit probability climbing to 1 with horizon even from the origin). The "reusable no-go signature" concept — settle future stays-inside-forever proposals by invocation.

4. **Fluid-limit closure** (deriv-discrete-sector-condition): DA2' = directional fidelity (DA2'a) + Lipschitz norm bound (DA2'b, strictly stronger than inner-product bound because the discrete recurrence has $(\eta^\ast)^2\Vert F_d\Vert^2$); contraction factor $\lambda_{\text{eff}}^2 = 1 - 2\eta^\ast c_{\min} + (\eta^\ast)^2 c_{\max}^2$; step-size condition $\eta^\ast \lt 2c_{\min}/c_{\max}^2$. Model D: discrete = continuous *exactly* (zero gap!); Model S: additive $O(\eta^\ast c_{\max}^2/c_{\min}^2)$ correction.

5. **Tempo additivity in full**: the two-channel closed form $\Delta = c(2U_1U_2 - c(U_1+U_2))/(U_1U_2(U_1U_2-c^2))$; equality on a *strictly larger* set than independence (the harmonic-mean hypersurface $c = 2U_1U_2/(U_1+U_2)$); three concrete synergy witnesses (anti-correlated; noisy-reference-channel; the ρ₁₂=ρ₂₃=0.7 triple); echo-chamber Sherman-Morrison penalty $R_s = \sum_k f(q_k) - f(q)$ with $f(x)=x/(1+\sigma_s^2 x)$ strictly concave ⇒ strict subadditivity + saturation ($f(q) \to 1/\sigma_s^2$). The asynchrony refinement is subtle and delightful: independent Poisson streams a.s. never sample instantaneous cross-correlation — only *cross-event persistent* noise breaks additivity.

6. **The genericity sketch** — the corpus's own Gate-2 self-audit: the "structural adaptation is generic" hand-off asserted in deriv-sector-condition/deriv-stochastic-non-exit Findings is *plausibly a mild overclaim*, because Cor A.1S.1's OU instance is positively recurrent (exit-and-return ≠ class inadequacy) and result-structural-adaptation-necessity's trigger is fitness-driven, not noise-driven. Three candidate bridge derivations named, none attempted. This is the single best "what is hypothesized but not yet spiked" quiz target in Part I. (I patched my own b05-3.1 answer, which had inherited the Findings-tier assertion.)

7. **Per-dimension + matrix-Loewner**: three task-adequacy criteria (RMS/MAE/probability) with exact Gaussian constants; per-coordinate *unsafe* under eigenbasis misalignment (45°-off-axis FAIL under per-coordinate PASS); matrix condition = Hurwitz $\mathcal T$ + $\Sigma_\infty \prec D_\delta$ with $\Sigma_\infty$ from the continuous Lyapunov equation.

8. **Persistence cost mechanics**: RDF route ($\dot R(D^2) = n\sigma_w^2/4D^2$ for OU high-resolution; substitute the Model-S ultimate bound) ⇒ $n\alpha/2$; Kalman-Bucy tightness via Mitter-Newton ($\dot I = K_{ss}/2 = \alpha/2$ scalar). Conditional tier — the walk-level "exact-sounding" burn-rate claim rests on Gaussian-OU/independence premises.

## Correction-propagation observations (small, passing along)

- `deriv-tempo-additivity` + `def-adaptive-tempo` WN note the refuted general upper bound still standing in `disc-independence-audit` §3 (known-unexecuted ripple, recorded in-corpus).
- The b06 answer keys survive the full appendix reads unchanged (spot-rechecked the α-ladder, DA2' and MG claims against the sources — consistent).

## Quiz-design note for this batch

Per Joseph's steer, this batch's quiz is weighted toward **epistemic-tier discrimination**: exact vs conditional vs no-go vs sketch/open; which premises buy which tier; what is currently asserted-but-unproven; which real-world algorithms land where in the sub-scope ladder and *why that placement is a derivation, not folklore*.


## ⚑ FINDING (verifier-surfaced, batch-8 verification): undischarged intro claim in deriv-variational-sector-condition

The segment's opening summary asserts mean-field VI is "structurally sub-optimal ... by a factor $\sqrt{1-r^2}$ in the sector constant," but $r$ is never defined and the factor never derived anywhere in the segment (Formal Expression supports only the additive $O(\sqrt\varepsilon)$ Regime-B degradation). Gate-2 shape: an intro claim that sounds derived but isn't. Anchor: `01-aat-core/src/deriv-variational-sector-condition.md` opening paragraph ("factor $\sqrt{1-r^2}$"). Status: still real; confidence high (verified by independent full read). Disposition suggestion per strengthen-before-soften: spike the derivation (the factor plausibly comes from the Fisher-metric mismatch angle of #deriv-fisher-whitened-update-rule's $\sqrt{1-r^2}$ magnitude-degradation result — same symbol, likely the intended source; if so the fix may be a citation + definition rather than new math) before any prose softening.
