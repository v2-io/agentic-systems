# Batch 2 — scope-agency, post-causal-structure, reality-model intro, form-agent-model, form-information-bottleneck (reflections)

No appendix `depends:` fired (IB references #disc-compression-operations in Discussion only, not depends — so no jump per the appendix rule).

## Predictions vs evidence

- Predicted scope-agency's formal condition almost exactly ($\lvert\mathcal A\rvert \geq 2$ plus $\exists a\neq a': P(o\mid do(a)) \neq P(o\mid do(a'))$). What I did *not* predict — and this is the genuinely non-summary-recoverable content — is the **"Which contrast counts" Discussion paragraph**: the contrast is stated on *observations*, which admits two routes (Ω-routed vs pure active-perception/observation-rearrangement), and only the Ω-routed contrast feeds causal learning; the boundary is observation-mediated *in both directions* (an unobservable Ω-effect also doesn't count). This is exactly the placebo-button/proprioception trap resolved carefully. Prime quiz material — a summary-fed agent will state the bare formal condition and fall into the vacuity trap.
- post-causal-structure surprised me twice. (1) The causality postulate is deliberately *weaker than Pearl* — bare temporal precedence, "structure of possible influence," logically prior to statistical causality. Summary-readers will conflate AAT's causal postulate with "AAT assumes Pearl SCMs" — false. (2) The four-tier coupling taxonomy (strong / weak / nominal[query-only] / zero) with the subtle scope mapping: zero coupling is outside agency but *inside* adaptive scope, and the postulate itself survives even there. Known wart (per WN, already certified elsewhere): "nominal coupling" here (in-scope) vs "nominal agents" in scope-agency (out-of-scope) — same word, opposite membership. I independently tripped on this before reading the WN — it's real, and it's a perfect quiz discriminator *because the corpus itself* stumbles.
- form-agent-model: the load-bearing item is the **completeness assumption** — $M_t$ captures everything the agent *retains* (not *needs*); tautological-relative-to-retention, with the substantive question forwarded to sufficiency. `type: formulation`, `status: robust-qualitative` — honest that history-based policies are an alternative. My batch-1 $M_0$/prior question is *live and known* (WN follow-up: $M_t=\phi(M_0,\mathcal C_t)$ candidate) but not yet in the body.
- form-information-bottleneck answered my batch-1 open question: sufficiency's target is **future observations given future actions** ($Y = o_{t+1:\infty}\mid a_{t:\infty}$), not $\Omega$. Policy-relativity is explicit and honest — predictive information is relative to a continuation policy. The segment's crown jewel is the **$\beta$-vs-$\rho$ anti-collapse argument**: volatility does NOT get routed onto the compression parameter; high $\rho$ natively degrades $I(\mathcal C_t; o_{future})$ so optimal $\phi^\ast$ discards stale history at constant $\beta$; $\beta$ tracks internal memory/computation cost only. Double-counting error named and killed. This is the first instance of the anti-collapse discipline (#disc-anti-collapse) and the framework says so in-body — nice introduced-before-used forward plant.

## Cross-segment observations

- scope-agency's formal expression uses $do(\cdot)$ with def-pearl-causal-hierarchy downstream (Part II). The body now handles this with an explicit "external import per Pearl 2009 / Bareinboim 2022; recapitulation lives at #def-pearl-causal-hierarchy" parenthetical — i.e., the primitive-import vs AAT-deployment convention from FORMAT.md. The historical Gate-1 finding is visible in WN; current text has integrated the repair. Ordering discipline: this is a *primitive import*, not a backward dependency — so not a critical finding under §4.2 as currently written. Good.
- form-information-bottleneck `status: exact` with `type: formulation` initially struck me as a contradiction; the Epistemic Status resolves it elegantly: the *choice* of IB is the formulation; *given* the choice, the content is an exact imported theorem. This two-level status reading is subtle and quiz-worthy (a status-label question that punishes shallow pattern-matching "formulation ⇒ can't be exact").
- Known-but-live WN item: deterministic $\phi$ vs Tishby's stochastic encoder kernels — the displayed $\arg\min_\phi$ over deterministic maps is a special case of the theorem. The "exact" label is fine for the MI trade-off but the displayed form is a restricted lane. Already flagged by prior audits; not new.
- the-reality-model-intro plants the "low ceiling ⇒ change class, not parameters" seed and openly promises Chapter 4 will pay it (structural adaptation necessity). Chapter-intro segments carry no formal claim; cadence-exempt per FORMAT.

## Wandering / synthesis

The chapter's deep architecture is a *relocation ledger*: every "free"-looking definitional move has its cost explicitly relocated somewhere named. Completeness (free, tautological) → cost lands in sufficiency. Markov-of-Ω (free, unbounded object) vs Markov-of-$M_t$ (costly, bounded) → cost lands in capacity $R$/fitness. IB optimality (free, imported theorem) → cost lands in "optimum not procedure." This is a disciplined pattern I'd now *predict* recurs: when a later segment discharges an objection by definition, the framework will name the segment where the discharged cost re-materializes. If I find a definitional discharge with *no* named relocation target, that's a finding.

Also struck by how the corpus is bootstrapping its own pedagogy from prior audits (the WN gold-lift blocks). For quiz design this means: the strongest discriminator questions are ones whose answers live in Discussion paragraphs that *post-date* the summaries an over-confident agent would have read. The "which contrast counts" paragraph and the β-vs-ρ argument are both such.

## Watch-list updates

- Carry: does update gain actually weight interventional (Level-2) observations higher, or is post-causal-structure's "give more weight to causally-downstream observations" an asserted normative rule the math doesn't produce? Check at emp-update-gain.
- Carry: is the four-tier coupling taxonomy cashed in downstream (CIY, loop-interventional-access)?
- New: does def-model-sufficiency define $S$ as the ratio $I(M_t; Y)/I(\mathcal C_t; Y)$ (my bet, giving $S\in[0,1]$ automatically via data-processing inequality) — and does it cite DPI?

## Predictions for batch 3 (def-model-sufficiency, def-model-class-fitness, cycle-in-motion intro, form-event-driven-dynamics, der-recursive-update)

Sufficiency: ratio of mutual informations as above. Class fitness: $\mathcal F(\mathcal M) = \sup_{\phi: \mathcal C \to \mathcal M} S$. Event-driven dynamics: events $(e_i, \tau_i)$ at heterogeneous channel rates $\nu^{(k)}$, continuous timestamps, generalizing uniform-clock discrete time. der-recursive-update: recursion $M_{\tau^+} = f(M_{\tau^-}, e_\tau)$ *derived* — I predict from completeness (C3?) + boundedness + something like anytime/online constraint; appendix deriv-recursive-update (claims-verified) has "three constraints + counterexamples" per OUTLINE — I should read it when der-recursive-update lists it in depends (appendix rule fires).
