# S6 Spike-Routing Adjudication — directory reasoning-trail clusters + 2 liveness checks

*Working dir `audits/SPIKE-WORKING-418736/`. Slice S6 of the 2026-05-17
spike-routing cycle. Governing: `doc/spike-routing.md` (esp. §3 cross-repo
rule, §6 lighter dir-spike gold gate + reserved-decision axis, §7 evidence
hierarchy incl. git-provenance) + `doc/audit-routing-instructions.md`
(shared core). All recommendations are **read-and-recommend** per the §6
lighter gate — the parent does not auto-file these; they go to the
Joseph batch. Nothing here is deleted; both terminal homes preserve.*

> Decisive test applied throughout: load-bearing content present in a
> `src/` segment/appendix, **verified first-hand** (grep + git pickaxe
> `-S` provenance + segment read), not the INDEX label. Per §7 the
> INDEX label is a hypothesis; it was found wrong/stale here too.

---

## Summary table

| Spike (dir) | Recommended state | One-line basis | Loss under naive "is the math in canon" triage |
|---|---|---|---|
| `class-coercion-wrapping/` | **integrated-misfiled** → `.integrated/` | `#der-class-coercion-via-wrapping` + `-in-composition` + `der-logogenic-as-wrapping` + auxilia-W₁ all landed (git `6aae978` same-day, verified) | Low — deferred sub-spikes E/G are honestly recorded future-work, not orphaned |
| `temporal-nesting-rg/` | **integrated-misfiled (load-bearing) — but Joseph-reserved residue** | Form-preservation pillar + full Friston/IB-RG/CGO citation substrate verified in `der-class-coercion-in-composition.md:75–97`; **Moves B/C/D + temporal-nesting-half of A explicitly parked for Joseph and never resolved** | **Medium-high** — the §3.3 unification-meta-segment decision + 2 gap-closing strengthenings live only in the verdict |
| `track-b-nonlinear-sims/` | **integrated-misfiled** → `.integrated/` | Strengthen-first exemplar fully landed: Model D b=2 + Model S b=3/2 `exact`-conditional in `result-adversarial-tempo-advantage.md`, regime table + finite-ν correction all in canon | Low — sims are reproducibility provenance; findings fully canonical |
| `track-a-intent-dag/` | **integrated-misfiled** → `.integrated/` (archaeology) | Pre-AAT (Mar 2026) first intent-DAG sketch; converged into `#def-strategy-dag` family; consolidated form already `_obs/`-archived 2026-04-28 (INDEX:163) | Low — but see cross-domain note: it is the *genesis* trail of the core novel object |
| `spike-local-embedding-benchmark/` | **live-or-open** (hands-off; stays) | Tooling spike, no theory content; complete architecture-brief is the **cited source-of-truth for an open `[ ]` PRACTICA/TODO Phase-2 tool** not yet built | N/A (no math) — but methodology ideation has orthogonal value |
| `spike-language-as-causal-substrate/` | **live-or-open → Joseph batch** (reserved decision; NOT live-author) | Work **complete** ("yield on all five sub-claims"), explicitly "Pending Joseph's promotion-routing call"; empirical half forked to live sibling `~/src/causal-language/`; **theoretical C1/Theorem-1 result lives only in spike** | **High** — a real derivation-grade strengthened result is orphaned pending a reserved promotion call |
| `spike-strategic-self-coupling.md` | **live-or-open** (open research direction; stays) | Self-described "scope-defined; investigation pending"; deliberately contains *no* results; §7 is a "when taken up" roadmap; no author in it but nothing to lose | N/A (no result yet) |

---

## Liveness checks (gating, done first)

Both S6 liveness-check spikes show INDEX "IN PROGRESS" but git provenance
shows **no author activity** since creation — only the 2026-05-15 AAD→AAT
sweep commit (`4d82df8`) and (for the language dir) the 2026-05-14
creation commit. Neither has a cycle-dated `msc/` artifact referencing it
as ongoing (the only `msc/` hits are this routing tracker + the
2026-05-09 modularity-cycle-plan). Per `spike-routing.md` §1 the
fail-safe is "treat as live when genuinely unsure" — but here the spikes'
**own status lines** settle it without ambiguity:

- **`spike-strategic-self-coupling.md`** — own header: *"Scope-defined;
  investigation pending. This spike does not contain investigation
  results — those land when the spike is taken up."* §7 is literally
  titled "Investigation Roadmap (when this spike is taken up)." This is
  **not live work** (no author in it) and **not orphaned** (deliberately
  zero result-content to lose). It is `live-or-open` in the precise
  five-state sense: *incomplete and still needed* — an open, intact
  research **direction**. Recommendation: **stays in `spikes/`,
  hands-off, no move.** INDEX status "IN PROGRESS / investigation
  pickup awaiting" is already accurate.

- **`spike-language-as-causal-substrate/`** — **different in kind.** Not
  live (no author), not scope-only: the work is **complete** (README:
  *"Yield on all five sub-claims after three follow-on pushes within the
  same cycle"*; `04-no-gos-and-followon.md` carries the resolved C5
  two-pass correction and the C3 precisely-specified closure-gap). It is
  blocked **only** on *"Pending Joseph's promotion-routing call"*
  (README §"Promotion targets", `04` §"Immediate" item 1). This is the
  §3 cross-repo / §6 reserved-decision pattern — detailed below.

---

## 1. `class-coercion-wrapping/` → **integrated-misfiled**

**State.** Completion-state (A→B): the spike's recommended landing path
(`99-verdict.md` §4.1/§7) is **fully executed**. Git provenance is exact:
`6aae978` "Land: class-coercion-via-wrapping; register
modularity-state-dynamics cycle" (2026-05-09, *same day* as the verdict),
later `758cd89` "Refactor: bifurcate der-class-coercion-via-wrapping into
two derivations."

**First-hand verification (decisive test passed):**

- `01-aat-core/src/der-class-coercion-via-wrapping.md` — Theorems 1 & 2
  with proofs, conditions C1–C3, the **W₀/W₂/W₁ leakage hierarchy** with
  the structural bound $\kappa_{W_1} \le I(A(q_M); G_W \mid q_M)$ and the
  data-processing-inequality proof. Every load-bearing claim from
  `01-theorem-statement.md` / `03-leakage.md` present.
- `01-aat-core/src/der-class-coercion-in-composition.md` — the
  composition-closure half (the recommended §4.1 bifurcation).
- `03-llm-core/src/der-logogenic-as-wrapping.md` — the §4.3 logogenic
  specialization (surfaced OUTLINE row L2).
- `04-eli-core/src/def-auxilia-hierarchy.md:80` — auxilia-as-W₁-
  realization (§4.4).
- `#der-directed-separation`, `#hyp-directed-separation-under-composition`,
  `#form-composition-closure`, `#der-tempo-composition` all
  reference/integrate it.
- CHANGELOG:380–382 records the cycle and the segment-voice discipline
  (spike cited only as Working-Notes reasoning-trail provenance —
  verified at `der-class-coercion-via-wrapping.md:157`).

**Loss under naive triage: low.** The only spike content not in canon is
the **deliberately-deferred** sub-spikes E (tempo accounting) and G
(quantitative LLM bounds) — honestly recorded as future work in
`99-verdict.md` §5 with the explicit "these deferrals do not block the
verdict because…" justification. That is correctly-recorded
not-in-canon, not orphaned truth. Prior-art differentiation (`09`)
landed via citation discipline.

**Recommendation.** `integrated-misfiled`; parent `git mv` →
`.integrated/` after independent-verify (confirmer ≠ me). No
Joseph-reserved decision attaches. (`INTEGRATION-PLAN.md` stays with the
dir as the recorded intern map.)

---

## 2. `temporal-nesting-rg/` → **integrated-misfiled on the load-bearing pillar; Joseph-reserved residue**

This is the consequential one in the slice and the one a naive triage
mishandles in *both* directions.

**What landed (verified first-hand — decisive test passed).** The
verdict's surviving pillar 1 (the structural-RG / form-preservation
framing) **is in canon**: `der-class-coercion-in-composition.md:75–97`
carries an explicit **"Form-preservation reading"** section — (A1)–(A4)
as the requirement that AAT form survive coarse-graining, the wrapping
construction as the constructive form-preservation move, $\varepsilon^\ast_{\text{track}}$
as distance from the form-preserved fixed point — *with the full
citation substrate the RG verdict recommended*: Friston 2019 *JTB* +
Friston 2025 RGM, Mehta-Schwab 2014 + Kline-Palmer 2022 (IB-as-RG),
Chen-Goldenfeld-Oono 1996 (singular-perturbation-RG). `INTEGRATION-PLAN.md:70`
records this as a deliberate, segment-voice-disciplined intern ("DO NOT
cite the temporal-nesting-rg spike directly"). CHANGELOG:380–382
confirms the spike is "no longer load-bearing for any segment content."
Pillar 2 (graded directed-separation) landed as W₀/W₂/W₁. The
(O,Σ)-recursion **no-go** (pillar 3, correctly dropped) is the project's
stated cite-don't-claim position — by construction needs no canon
segment. Move F is tracked: PRACTICA:24 + TODO:152.

**So far this is a clean `integrated-misfiled`.** But —

**The Joseph-reserved residue (the part a naive triage drops).**
`INTEGRATION-PLAN.md:221` is an explicit, never-closed action:
*"Surface to Joseph: … whether to also integrate selected RG-spike
results … Present specific candidates from the RG verdict's Moves
A/B/C/D/F."* Verified-unlanded, first-hand:

- **Move D** — the standalone `#disc-structural-rg` meta-segment
  carrying the §3.3 unification table (temporal-nesting +
  composite-formation + persistence-template + directed-separation as
  *one* structure under coarse-graining, alongside
  `#disc-separability-pattern` / `#disc-identifiability-floor`). **Not
  in canon** (no segment; git pickaxe finds only the rename sweep). The
  form-preservation reading landed *locally* in the class-coercion
  composition segment; whether the *broad cross-segment unification*
  deserves its own meta-segment is exactly the kind of
  promotion-level / framework-legibility call Joseph reserves.
- **Move B** — promote `#sketch-multi-timescale-stability` (verified:
  still `sketch`-stage) via template-stacking + Tikhonov +
  Chen-Goldenfeld-Oono. The verdict flags this "closes a known gap";
  unexecuted.
- **Move C** — the two-loop non-Kalman worked example (Kalman inner +
  Beta-Bernoulli outer). Verdict: "closes the no-worked-nested-loop-
  instance gap"; unexecuted.
- **Move A, temporal-nesting half** — `#der-temporal-nesting`
  Discussion still does **not** carry the structural-RG-depth reading
  (verified: no match). Only the composition-closure half of Move A
  landed (via the class-coercion segment).

**Why this is not a simple auto-file, and not orphaned-in-the-bad-sense.**
The *load-bearing* result is in canon — nothing false; the canon does not
lie about itself; the spike is honestly non-load-bearing. So this is
**not** the "no-go/math lives only in the spike" failure. What lives only
in the verdict is a set of **recommended-but-unexecuted strengthenings**
plus a **parked unification-meta-segment decision** that the integration
plan itself routed to Joseph and that nothing resolved. Per
`spike-routing.md` §6 (reserved-decision axis, Joseph-ratified): this
routes to the **Joseph batch** with a specific question, *not*
auto-filed and *not* auto-landed.

**Recommendation.** State = **`integrated-misfiled` on the load-bearing
content** (the dir can ultimately go to `.integrated/` — its substantive
theory is in canon, verified). **But hold the `git mv` for the Joseph
batch**, paired with the decision he was already owed:

> *Moves B, C, D and the temporal-nesting-Discussion half of Move A from
> `temporal-nesting-rg/99-verdict.md` were surfaced for your call in
> `class-coercion-wrapping/INTEGRATION-PLAN.md:221` and never adjudicated.
> The load-bearing form-preservation result is in canon
> (`der-class-coercion-in-composition.md:75–97`). Open: (D) does the
> §3.3 cross-segment unification deserve its own `#disc-structural-rg`
> meta-segment, or is the local landing sufficient? (B/C) execute the two
> gap-closing strengthenings now (queued, heavy), or accept the gaps as
> scoped? Filing the dir to `.integrated/` is correct **once you've
> ruled on whether anything in B/C/D should land first** — otherwise the
> dir is the only place those recommendations live.*

This is the §4-landing-scope "heavy" path: if Joseph wants D (or B/C),
it is a written landing-plan + PRACTICA item, best done by context-holders
— not this cycle's mechanical subset.

---

## 3. `track-b-nonlinear-sims/` → **integrated-misfiled**

**State.** A textbook strengthen-before-soften exemplar, **fully
landed.** Original `sim2` found exponent ~1.05 — surface-reading "the
squared tempo advantage does NOT survive" (an apparent no-go against
Corollary 11.2). Instead of softening the corollary, the variants pushed:
`variants/variant_ab_results.md` shows the ~1.05 was a *coupling-mechanism
artifact* (noise-variance coupling vs. TF-11's deterministic-drift ρ).
Variant A then **confirmed** b → 2.000 in the coupling-dominant limit
*and* surfaced a genuine companion result (Model S stochastic → b = 3/2).

**First-hand verification (decisive test passed).**
`01-aat-core/src/result-adversarial-tempo-advantage.md` (`status:
conditional`) carries Model D (b=2) and Model S (b=3/2) both **`exact`
conditional** on their disturbance models, the regime-dependence table
(coupling-dominant vs. not), the coupling-dominant qualifier as
load-bearing (explicitly "not a technicality"), and even the finite-ν
correction factor explaining the 0.019 simulation gap. Simulation
validation cross-referenced to `#result-adversarial-exponent-regimes`.
INDEX:177: "6 variants, all validated."

**Loss under naive triage: low.** The `sim*.py` / `.npz` / `.png`
artifacts are reproducibility provenance for canonical results; nothing
of the simulations' truth lives only in the spike. Disposition:
`integrated-misfiled` → `.integrated/` (reproducibility artifacts travel
with the spike).

**Recommendation.** `integrated-misfiled`; parent `git mv` after
independent-verify. No reserved decision.

---

## 4. `track-a-intent-dag/` → **integrated-misfiled (archaeology)**

**State.** The **March 2026, pre-AAT** first formal treatment of
intent-as-probabilistic-causal-DAG (`00-intent-dag-formalism.md` header:
*"First formal sketch of the core novel object in AAT"*, depends on
TF-00…TF-11). Per INDEX:163 the consolidated form
(`_obs/04-intent-dag-consolidated.md`) "converged into the strategy-DAG
segments (`#def-strategy-dag` etc.). **Archaeology** — moved to `_obs/`
2026-04-28." The directory is the *reasoning trail* whose result is the
now-canonical strategy-DAG segment family.

`01-conference-paper-review.md` is a completed prior-art screen (AT-Field,
AAAI 2026) whose verdict is "AAT has no reason to cite or build on this" —
a finished negative screen, correctly not in canon (nothing to integrate).
`02-alt-clean-slate.md` / `03-alt-and-or-dag.md` /
`04-multi-agent-intent-priorities.md` are superseded design alternatives
whose chosen path is in canon.

**Loss under naive triage: low** on the math axis (the chosen formalism
is canonical). **But — cross-domain note (below):** this is the *genesis
document* of AAT's central novel object; its value as origin-trail is
real and is preserved by `.integrated/`, not lost.

**Recommendation.** `integrated-misfiled` → `.integrated/` (genesis
archaeology of the strategy-DAG family; consolidated form already
`_obs/`-archived). No reserved decision; mechanical.

---

## 5. `spike-local-embedding-benchmark/` → **live-or-open (stays; hands-off)**

**Not a sibling-repo spike** (the partition hypothesis "likely belongs to
the `causal-language`/embeddings sibling" is **wrong** — that confusion
came from the *benchmark methodology* superficially resembling the
embeddings work; this spike is wholly ASF-internal). It is a **tooling
spike with zero theory content**: 8-model retrieval benchmark to pick a
default embedding model for a *semantic-index-over-ASF-segments* tool
(renamer/researcher aid). Its "result" is an engineering recommendation
(`nomic-embed-text-v2-moe`; lift memorata's data layer + the §5 build
sequence), not a claim or no-go.

**State.** The architecture brief is **complete and fully mirrored** into
the live navigator: PRACTICA:126 `[ ]` "Phase 2 semantic index (queued
2026-05-01)" and TODO:279 "Phase 2 semantic-index spike → live system"
both cite `spike-local-embedding-benchmark/FINDINGS.md` as the
source-of-truth architecture brief. The tool is **not built** (verified:
no `bin/` semantic-index, no pgvector ingest). Per §7 evidence hierarchy
an open `[ ]` PRACTICA/TODO backlink is sufficient for *not-integrated /
still-needed*.

There is no AAT canon this could land in; it is not orphaned (nothing
mathematical to lose; the brief is mirrored to PRACTICA/TODO with the
file cited as spec); not archived (actively needed as the build spec).
It is the spike analog of *reference material that is the cited
source-of-truth for queued live work*.

**Recommendation.** **`live-or-open` — stays in `spikes/`, no move, no
Joseph decision needed for routing.** INDEX:218 "Architecture brief
landed, deployment queued" is already accurate. (Flagged for Joseph
only as: this dir-spike's routing is mechanically unambiguous and does
not actually need the dir-spike human-read for the *routing* decision,
though its tooling-design ideation has orthogonal value — see note.)

---

## 6. `spike-language-as-causal-substrate/` → **live-or-open → Joseph batch (reserved promotion call; NOT live-author, NOT auto-file)**

The slice's highest-loss spike under naive triage, and a clean instance
of the `spike-routing.md` §3 cross-repo + §6 reserved-decision pattern —
with a twist worth foregrounding.

**The deliberate cross-repo split (verified).** The *empirical* arc (the
`08-aies-paper-proposal.md` three-causal-axes program) was forked into
the **live sibling `~/src/causal-language/`** (exists, thriving — at
V2.1.1-panel + paper-drafting, E1–E19, `paper/`, dated 2026-05-14/15;
its CLAUDE.md: *"began (2026-05-13) as the empirical follow-on to a
theoretical result established in
`~/src/agentic-systems/spikes/spike-language-as-causal-substrate/`"*).
Project memory confirms "canonical home stays in agentic-systems" for
the theory. Per §3: the empirical half's source-of-truth is the
sibling's unsettled in-review artifact — correctly **not** landed here.

**The orphaned theoretical half (the failure this cycle exists to
catch).** The *theoretical* result — Theorem 1 / C1 (discourse-act →
Pearl Level 2, derivation-grade under SLC+SC+CS, CHT non-reduction
audit-clean per `06`), C4 (already AAT-internal via
`#deriv-causal-ib-lmi`, just unsurfaced), C5 (succeed-via-asymmetric-
comprehension, the resolved two-pass correction), C3 (a *precisely-
specified closure-gap* — a real partial-no-go) — **lives only in the
spike.** Verified first-hand, decisive test:

- No `#deriv-pearl-level2-language-encoding` / `#deriv-discourse-pearl-
  encoding` segment exists (slug listing + grep + git pickaxe `-S
  'discourse-act encoding'` / `'language-encoded Level 2'` on
  `*/src/*.md`: **nothing**).
- `der-loop-interventional-access.md` does **not** carry the
  inherited-vs-fresh Level-2 split (the recommended secondary landing).
- `msc/llm-causal-access-note.md:7` Response 2 is **verbatim its
  pre-spike plausibility-grade text** ("a plausible empirical claim …
  quantifying how much genuine causal structure survives compression is
  an open empirical question") — the spike's whole point was to lift
  this, and it has not been lifted.
- **Zero** segment Working-Note references the spike (so it is not even
  "referenced-not-integrated" — it is *entirely* uningested in canon).

**Why this is `live-or-open → Joseph batch`, not `orphaned` (auto-land).**
Strengthen-first already happened (the spike *is* the successful
strengthening: plausibility-grade → derivation-grade under named
postulates). The block is purely a **reserved promotion-routing call**:
README §"Promotion targets" and `04` §"Immediate" item 1 both state
*"Joseph's call. Until promoted, the result is spike-resident."* New
appendix-grade segment + promotion grade + cross-repo coordination with
the live sibling and the Synthese paper = squarely Joseph-reserved
(§6 decision-type axis, ratified 2026-05-17). Auto-landing would import
a promotion-level decision the author explicitly reserved.

**Recommendation.** **`live-or-open` (reserved-decision sense — not
live-author; the spike is complete and the dir stays put this cycle),
routed to the Joseph batch** with the canon-gap surfaced, not actioned:

> *`spike-language-as-causal-substrate/` is complete and its empirical
> half is correctly homed in the live sibling `~/src/causal-language/`.
> Its **theoretical** result — Theorem 1 (discourse-act → Pearl Level 2,
> derivation-grade under SLC+SC+CS, CHT-non-reduction audit-clean) plus
> the C5 asymmetric-comprehension resolution and the C3 precisely-
> specified closure-gap — is **derivation-grade and lives only in the
> spike**; `msc/llm-causal-access-note.md` Response 2 is still verbatim
> pre-spike plausibility-grade. This is the orphaned-strengthened-result
> shape — but the landing is your reserved promotion call (new
> `01-aat-core/src` appendix `#deriv-pearl-level2-language-encoding`;
> secondary inherited-vs-fresh split in `#der-loop-interventional-access`;
> Synthese-paper cross-cite). Strengthen-first is already done; this
> needs your routing decision + a heavy landing-plan, not a soften and
> not an auto-file.*

(Structurally identical to the pilot's `spike-c2-star-to-integrate`
disposition and the C2★ canon-gap at `der-loop-interventional-access:76`
— same segment family, same reserved-promotion shape. Worth Joseph
seeing these two together.)

---

## 7. `spike-strategic-self-coupling.md` → **live-or-open (open research direction; stays)**

**State.** Self-described scope-only spike (`status: scope-defined;
investigation pending`). Establishes the research *direction* for an
enabling-polarity sister to `#disc-adversarial-coupling-pressure`:
the (M1)–(M3) structural extension ($\mathcal{A}(\kappa_t)$
coupling-dependent action space; strategy-DAG enabling edges; asymmetric
reversibility cost), a prior-art adoption landscape
(Schelling/Ainslie/Akerlof-Kranton/Frank/Elster/Skyrms), four candidate
segment products (P1–P4), five open questions, and §7 "Investigation
Roadmap (when this spike is taken up)." It deliberately **contains no
investigation results**. No author currently in it (only the AAD→AAT
sweep touched it); INDEX: "investigation pickup awaiting."

**Why `live-or-open`, not anything else.** Nothing of result-truth can be
orphaned because there is, by design, no result. Not archived — it is
explicitly *needed* (the third operation of the M4 modularity-state-
dynamics picture, scoped in `msc/modularity-cycle-plan-2026-05-09.md`,
forward-referenced from canon as `#disc-modularity-state-dynamics`). It
is an intact, open, still-needed research direction.

**Loss under naive triage: N/A** (no math/no-go yet). The cross-domain
value is the *prior-art map itself* — see note.

**Recommendation.** **`live-or-open`; stays in `spikes/`, no move, no
Joseph decision needed.** If anything, flag for Joseph only that it
remains an explicitly-open M4-cycle direction (already in PRACTICA's
"Modularity-as-contested-property cycle" per CLAUDE.md §7).

---

## What a naive "is the math in canon" triage would lose (cross-domain ideation)

The §6 lighter gate exists because dir-spikes carry value orthogonal to
"is the math in canon." Concretely, for this slice:

1. **`temporal-nesting-rg/` — the §3.3 unification table itself.** Even
   though the *form-preservation result* landed, the table that reads
   `#post-composition-consistency` / (A1)–(A4) / persistence-template /
   `#der-temporal-nesting` / closure-defect / bridge-lemma /
   directed-separation-classes / Hafez-meta-machine / Brooks's-Law as
   *one structure under coarse-graining* is a project-legibility asset
   (`respectful-pedagogy` / mental-model-first; CLAUDE.md §Feynman). A
   triage that checks "form-preservation cited? yes → file" loses the
   pedagogical synthesis Joseph was explicitly asked about and never
   answered. **This is the single most important non-obvious finding in
   the slice** and the reason `temporal-nesting-rg/` must hit the Joseph
   batch rather than auto-filing.

2. **`spike-language-as-causal-substrate/` — the methodological
   diagnostic.** `04-no-gos-and-followon.md` §"Two errors as diagnostic"
   distills a reusable epistemic-discipline pattern (the
   external-standard-above-comprehension category error; the
   asymmetric-comprehension test) that is *already* memorialized as a
   global memory (`feedback_external_standard_category_error.md`) — the
   spike is its origin worked-example. Also the cross-paper recognition
   that asymmetric-comprehension does double duty (Synthese AI-welfare +
   language-keeps-up-with-intelligence) is framework-coherence evidence,
   not just a result.

3. **`spike-strategic-self-coupling.md` — the prior-art adoption map.**
   The Schelling/Ainslie/Akerlof-Kranton/Frank/Elster/Skyrms landscape
   and the (M1)–(M3) decomposition is reusable scaffolding for the M4
   cycle regardless of when P1 is authored; it is exactly the
   "adopt-don't-invent" prior-art-integration asset the project values.

4. **`spike-local-embedding-benchmark/` — tooling-design ideation.** The
   model-identity-per-vector schema discipline, source-class tagging,
   and the AUDIT-WORKING-should-be-indexed correction are
   infrastructure-design insight that outlives the specific model pick.

5. **`track-a-intent-dag/` — genesis provenance.** The origin-trail of
   AAT's central novel object (intent-as-probabilistic-causal-DAG). Pure
   "math is canonical → file" triage is *correct* here but would
   under-value the dir as the place a future agent reconstructs *why*
   the strategy-DAG took the AND/OR-single-parameter-edge form (the
   `02`/`03` rejected alternatives are the reasoning, not noise).

---

## State machine status (for the parent / independent-verify)

All seven are `adjudicated` (state + recommended home + reasoning
written, content-in-`src/` claims first-hand-verified with named loci for
a confirmer ≠ me). None moved/edited/committed (report-only per the
delegation design). Independent-verify targets, by priority:

- **High:** `temporal-nesting-rg/` — confirm Move B/C/D + Move-A-temporal-
  half are genuinely unlanded (I verified: `sketch-multi-timescale-
  stability.md` still `sketch`; no `#disc-structural-rg`; `#der-temporal-
  nesting` Discussion lacks RG-depth reading; `INTEGRATION-PLAN.md:221`
  open) **and** confirm the form-preservation pillar IS in
  `der-class-coercion-in-composition.md:75–97`.
- **High:** `spike-language-as-causal-substrate/` — confirm
  `msc/llm-causal-access-note.md:7` Response 2 is unchanged pre-spike
  text and no `#deriv-pearl-level2-*` segment exists.
- **Mechanical:** `class-coercion-wrapping/`, `track-b-nonlinear-sims/`,
  `track-a-intent-dag/` — confirm the named canonical loci before any
  `git mv`.
- **No-verify-needed (stay):** `spike-local-embedding-benchmark/`,
  `spike-strategic-self-coupling.md`.

## Joseph batch (the parent's, surfaced here)

1. **`temporal-nesting-rg/`** — rule on Moves B/C/D + Move-A-temporal-half
   (the `INTEGRATION-PLAN.md:221` decision he was owed) *before* the dir
   files to `.integrated/`. The form-preservation result is safely
   canonical; the open question is the unification-meta-segment + 2
   gap-closing strengthenings.
2. **`spike-language-as-causal-substrate/`** — reserved promotion-routing
   call for the orphaned theoretical result (new AAT appendix grade;
   `#der-loop-interventional-access` secondary; Synthese cross-cite).
   Pair with the pilot's C2★ gap — same segment family, same shape.
3. (Informational) `spike-local-embedding-benchmark/` and
   `spike-strategic-self-coupling.md` need *no* routing decision — they
   correctly stay live; flagged only so the batch is complete.

## Frame feedback

The S6 slice frame held well. Two refinements worth recording:

- **The §3 cross-repo rule and the §6 reserved-decision axis
  *co-fire* on `spike-language-as-causal-substrate/`** in a way the
  pilot's `spike-c2-star` (cross-repo, externally-blocked) did not
  cleanly cover: here the empirical half is cross-repo-homed
  (live sibling) **while** the theoretical half is *orphaned-in-this-repo
  pending a reserved promotion call*. The disposition that fits is
  `live-or-open (reserved-decision) → Joseph batch`, but the loss-bearing
  observation (a derivation-grade result lives only in the spike) must be
  stated as loudly as for an `orphaned` — otherwise the "it's blocked on
  Joseph" framing buries the canon-gap. Suggest `spike-routing.md` §3/§6
  note that a single spike can be **cross-repo on one half and
  reserved-orphaned on the other**, and the orphan-signal is not muted by
  the reserved-decision routing.

- **"live-or-open" is carrying three distinct meanings** in this slice:
  (a) author-still-in-it (true step-zero live — none here);
  (b) scope-only open research direction (`strategic-self-coupling`);
  (c) complete-but-reserved-decision (`language-as-causal-substrate`);
  plus the spec-for-queued-tool case (`local-embedding-benchmark`) that is
  also "stays" but for a fourth reason. They route differently (b/d stay
  silently; c goes to the Joseph batch loudly). The five-state table
  collapses these; the *disposition* is right but a sub-tag
  (`live-author` / `open-direction` / `reserved-decision` /
  `spec-for-queued-work`) would prevent a future agent reading
  "live-or-open" as uniformly "hands-off, ignore" and missing the
  loud-canon-gap case. Recommend to Joseph as a §3/§5 refinement, not
  taken unilaterally.
