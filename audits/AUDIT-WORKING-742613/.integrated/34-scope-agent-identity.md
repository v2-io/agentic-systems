# 34 — scope-agent-identity

Dependencies checked: `def-chronica` and `def-model-sufficiency` already read.

## Reflection

This segment defines identity/tokenhood through singular causal trajectory rather than copyable model state.
`status: robust-qualitative` is appropriate for a scope commitment with philosophical implications.

Core contribution:
Sufficiency is trajectory-indexed, which is already implicit in `def-model-sufficiency` but helpful to state.
This is important for logogenic/context-turnover work and for clone/backup discussions.

Potential precision issue:
The clone example says copying LLM weights gives "same model state, same causal history $\mathcal C_t$."
Copying weights gives the same model state only if $M_t$ is identified with weights; it does not necessarily copy the chronica unless the chronica or its sufficient summary is included.
The text later distinguishes model state from causal trajectory, so the example could be tightened: "if the copied state includes the same retained summary of the same prefix..." or "same $M_t$ induced by a shared prefix."

Dependency/frontloading issue:
The "Natural extension: parameterization-invariance (PI)" block imports Čencov, Fisher metrics, `der-gain-sector-bridge`, `disc-additive-coordinate-forcing`, and coordinate-forcing meta-patterns in the Formal Expression.
Those are downstream and not declared dependencies.
This block may belong in Appendix/meta-pattern material or Discussion rather than in the formal scope expression.
It reinforces the broader canonicalization finding: early/mid Section I segments often carry later meta-architecture content.

Substantive note:
The segment says model merging is lossy "by construction" and no generally optimal merge exists.
That is plausible, but "no generally optimal" depends on defining optimality; under a specified objective / shared sufficient statistic, an optimal merge may exist.
This is okay as robust qualitative scope, not a theorem.

What this enables:
Provides formal vocabulary for continuity persistence and makes the logogenic memory problem more precise.

Section I completion:
This completes the main Section I outline pass, with several appendix jumps.
I am switching to triage and final-report drafting rather than continuing the whole AAD/TST/logogenic corpus, because the audit already has high-confidence core findings and full-corpus continuation would be a separate long pass.

Running report update:
- Add scope-agent-identity as evidence for canonicalization/frontloading, not as a standalone finding.
