# Batch 7 — result-structural-adaptation-necessity, der-temporal-nesting, scope-agent-identity, impl-persistence-and-limits (Part I complete)

Coverage note: I read result-structural-adaptation-necessity and scope-agent-identity through their WN gold-lift heads (bodies complete); der-temporal-nesting body complete through Discussion; impl-persistence-and-limits complete. No new appendix depends fired that I hadn't already read (impl- depends on deriv-persistence-cost, result-per-dimension-persistence, deriv-matrix-persistence-condition, result-sector-persistence-template — none previously read in full; their content reached me through the impl- summary and OUTLINE rows. Honest gap: those four appendix segments are known to me at summary level only.)

## ⚑ FINDING (passing along per Joseph's "if you find issues don't hesitate")

**Cross-segment contradiction: `impl-persistence-and-limits` still carries the pre-correction loop-interventionality framing that `scope-agent-identity` explicitly retracted.**

- `scope-agent-identity` (re-grounded 2026-05-30, transportability spike; WN records the correction): consequence 3 now reads "Singularity grounds *whose* effect a loop datum is — it does **not** by itself manufacture the interventional character... the $do$-operator's semantics is population-level and forkability-agnostic... the interventional character itself comes from the action being the agent's own causally-efficacious move [under (C1)–(C3)], **not** from any architectural property of the agent and not from non-forkability."
- `impl-persistence-and-limits` §"Identity, trajectory, and the bridge into Part II": "The loop generates interventional data **not because of any architectural property of the agent, but because the agent is on a singular trajectory**... **Strip the singular-trajectory commitment and the loop's interventional content vanishes.**"

The impl- paragraph asserts exactly the overstated claim the scope segment's 2026-05-30 landing corrected — same shape as the drift class the audit instructions call "recently-added structural move lands in the introducing segment but doesn't propagate." Anchor: `01-aat-core/src/impl-persistence-and-limits.md` §"Identity, trajectory, and the bridge into Part II" (both the "not because of any architectural property" sentence and the "strip the commitment" sentence); counterevidence checked: scope-agent-identity body + its WN provenance note; status **still real**; confidence **high** (the two texts are directly contradictory on whether singularity supplies interventional character); type: cross-segment contradiction / integration debt (Known-unintegrated — the correct idea exists in scope-agent-identity; the impl- segment is stale); effort: editorial (rewrite one paragraph to the "identity-grounding half" framing, pointing the interventional-character half at der-loop-interventional-access's (C1)–(C3)).

## Predictions vs evidence

- result-structural-adaptation-necessity: essentially as predicted (fitness < 1−ε ⇒ class floor above what parametric update reaches; six-step derivation through the mismatch decomposition's estimation term). Status honestly `conditional` on the **alignment assumption** (lost information must move the one-step conditional mean; else the result holds in proper-scoring regret). The diagnostic corollary (structured-not-white residuals) is stated crisply — batch-3's verifier-corrected question now has its canonical in-corpus answer, *here*, exactly where def-model-class-fitness's WN deferred it. Four mechanisms (decomposition-recombination / expansion / compression / grafting) + structural *overfitting* as the bidirectional case + Miller's neutral-variation five-phase motif. Note: the intro's promise ("central result") is honored at conditional-not-inevitability grade — and the appendix `sketch-structural-adaptation-genericity` (unread; OUTLINE row) keeps the Cor-A.1S.1-genericity hand-off honest by flagging the possible mild overclaim as an open strengthen-direction.
- der-temporal-nesting: as predicted — singular-perturbation (Tikhonov) adopted honestly as imported machinery; five-level illustrative ladder (now including consolidation); rational conservatism *derived* from nesting; the composite N-level stability since made quantitative in der-multi-timescale-stability ($\epsilon_{\max} = \Delta\rho^\ast/(L_h v^{\max})$).
- scope-agent-identity: richer than predicted. The (PI) axiom lives here (predicted ✓), with honest cost/benefit framing (opt-in invariance commitment; Čencov then forces Fisher metric; forced-load-bearing for a NeurIPS paper's no-go). The **corrected consequence 3** is the most epistemically careful passage in Part I — a strengthening-by-narrowing with the retraction documented. Type/token exclusion ("the GPT-4 model" vs this-session-this-trajectory) is the cleanest statement of AAT's applicability boundary for LLM discourse.
- impl-persistence-and-limits: catalog-grade synthesis; the "single template, six instantiations" and "persistence is a min-operation across directions in both correction rate and information-rate cost" are the two genuinely integrative moves. Plus the finding above.

## Part I retrospective (the experiential summary the experiment asks for)

What I could not have gotten from summaries, ranked by surprisal:
1. **Certificate voice** as a register — persistence claims are claims about available guarantees and their witnesses; the corpus systematically distinguishes lost-certificate from certified-escape, class-level from agent-level necessity, distributional from pathwise guarantees. Summaries collapse all of these.
2. **The strengthen-before-soften scar tissue** — Lemma A.1N, Cor A.1S.1, the tempo-additivity refutation, the mood-timescale refutation of naive matching, consequence-3's re-grounding. Each records a false-stronger-claim → attempted strengthening → exact-but-different landing. The corpus's reliability is *legible in its correction trails*.
3. **Two-tier segments** — exact core + honestly-tiered shell, everywhere.
4. **The relocation ledger** — definitional discharges always name where the cost re-materializes.
5. The **α/T/η/c_min ladder** and per-event/per-time normalization.

## Context-budget gate (§4.6) — firing now

By feel I am at roughly 75–80% context. Part I of 01-aat-core is complete: 7 batches, 35 segments read in full + 2 appendices in full + 1 appendix partial (deriv-gain-sector through B.4) + 4 appendix segments at summary level, 21 quiz files (7 batches × 3), 6 verification reports (7th in flight). Per protocol I am pausing here to surface the state to Joseph rather than pushing into Part II on a thinning budget: Part II's meta-architecture chapter (certificate spine, M1–M4) is dense, and starting it with <20% remaining risks exactly the time-pressure corruption the instructions warn about. Continuation state: this file + 00-initial-predictions + the per-batch reflections are the handoff trail; a continuation session should resume at Part II Ch.1 (`disc-stability-certificate`) per §7.11's continuation protocol.
