# 31 — result-persistence-condition (result, exact, claims-verified)

**1. Predictions vs evidence.** Predicted the iff would recur in gentler prose — **it recurs in stronger prose**: the Formal Expression says the machinery "bounds $\delta$ within the model class capacity **iff**" the threshold holds, and the Discussion states the false consequence outright: *"When $\alpha \leq \rho/R$, mismatch is not merely large — it grows without effective bound… The correction machinery is overwhelmed."* My segment-30 counterexample (slack sector floor near the origin, strong correction beyond) refutes this directly: $\alpha \leq \rho/R$ with mismatch held at $\varepsilon \ll R$ forever. **Ledger (lxxiv) now spans three sites** — appendix Prop A.1's closing line, the headline at result-sector-condition-stability, and this segment's summary + "Below structural threshold" paragraph — making it a coherent finding-family with one root cause (certificate/behavior conflation, harmless in the linear case where they coincide, false in the sector generalization) and one repair (state sufficiency; prove necessity for tight/linear correction; the tightness-lemma strengthening from 30-wandering-1).

**2. Everything else here is very good.** The two-condition decomposition (structural vs task adequacy) is exactly the right cut and beautifully honest ("downstream segments using the linear operational forms should be understood as expressing task adequacy, not structural stability" — a sentence that pre-answers a whole genre of referee attack). $\delta_{\text{critical}}$ and $R$ as declared domain parameters ✓ (my lxx watch: handled honestly here). The cost-shadow and per-dimension/matrix-Loewner escalations are properly integrated. The Findings block's Brief is deservedly praised. Model-S structural form $\alpha \gt n\sigma_w^2/(2R^2)$ consistent with the appendix ✓.

**3. Verification.** Dimensional: Model-S operational form $\mathcal T \gt n\sigma_w^2/(2\lVert\delta_c\rVert^2)$ — units: RHS = [surprise²·t⁻¹]/[surprise²] = t⁻¹ ✓ (given $\sigma_w^2$ as variance *rate*; consistent with the SDE reading; the gold's per-coordinate convention item (lxxvi) still applies). The "$\alpha \approx \mathcal T/2$ saturating, $0.76\mathcal T$ tanh" simulation constants — plausible; validated per obs-section-i-validation (not re-run).

**Auditing the gold.** Richest harvest yet (14 substrates), and its quality distribution tells the story of the whole gold system: the *best* items are genuinely excellent — **transient adequacy as a third condition** (471203: $1/\alpha \lt T_{\text{tolerance}}$; two agents with equal thresholds and 1000× different shock-recovery times — endorse as the strongest single follow-up in the corpus's gold, a well-formed subsection candidate that completes the structural/task pair into a triple); **hysteresis absence** (471203 — real, mild, honestly verdicted); **hiring-into-structural-failure accelerates collapse** (829314 — verified against the machinery: added coordination noise raises effective $\rho$ while $\alpha$ is saturated; Brooks's Law derived from the decomposition, sharper than TST's usual statement); the **notation-handle box** ($\eta$/$\mathcal T$/$\alpha$, 526815 — cheap, high-yield, flagged open in-body). And the *weakest* items are safely quarantined (the Prigogine mapping carries its own verify-isomorphism warning ✓; the Findings-block-looks-wrong false alarm is preserved *as texture with the correction attached* — the gold system auditing its own false positive, which is exactly right). *One pushback*: the "two-gate test" gloss (526815) should be a *three*-gate test the moment transient adequacy lands; if the 2×2 figure gets built, build it extensible.

**4–6. Direction / predictions.** `#def-mood` and `#der-mood-timescale` next — the fresh-math pair, my declared least-audited-math priority. Pre-registered from 00: (a) stationarity/timescale-separation assumption doing unacknowledged load-bearing work in $\tau^\ast \propto \sqrt{\tau_{\text{env}}}$; (b) no verification against `#deriv-adaptive-gain-dynamics`'s MG-conditions (mood *is* adaptive gain one level up — if mood modulates $\alpha$/gain, the persistence machinery's constant-$\alpha$ assumption needs the MG-composition, and I predict the mood segments don't cite it); (c) the bias–variance argument will be dimensionally fine but its optimum's *interiority* will rest on unstated regularity.

**7. Change.** The (lxxiv) family repair here (summary sentence + "Below structural threshold" paragraph); adopt the transient-adequacy subsection; the notation box.

**8–9.** Curious: whether "persistence is not a degree, it's a regime" survives the (lxxiv) repair — it does, but only as a statement about the *guarantee* (the certificate is binary), not about behavior (which can degrade gradually through the slack region). The slogan should be re-anchored to the certificate. Enables: the decomposition + transient triple + floor taxonomy now constitute a complete diagnostic architecture; the FINAL's §F will assemble it as one proposal.

**10–11.** Ledger: (lxxiv) extended (three sites, one root cause); (lxxviii) transient-adequacy third condition [endorsed, elevated]; (lxxix) notation-handle box [endorsed]. §E: the linear-forms-express-task-adequacy caveat is Gate-2 discipline at its best.

**12–13. Value-feel / contribution.** Very high, with the one flaw. This segment is the volume's center of gravity and *mostly* deserves it: the decomposition is the load-bearing original contribution, more so than the inequality itself (which is standard Lyapunov dressed for travel). Post-repair, this segment would be the strongest single artifact in Part I's main chain.

**14. Wandering thoughts.** *One:* the (lxxiv) family has a redeeming reading the repair should preserve: for *linear/tight* correction the iff is true, and linear-tightness is exactly the regime where $\alpha = \mathcal T$ — so the *operational* forms (the ones domains actually cite, $\mathcal T \gt \rho/\delta_c$) live in the regime where the iff is *closest to honest* (modulo worst-case-disturbance semantics). The error is thus concentrated in the *general-sector rhetoric*, not the applied practice — which is both why nobody noticed and why the fix costs so little. The referee-risk asymmetry is the reverse: theorists read the general statement, practitioners the linear one.

*Two:* the crèche reading in the gold (persistence condition as "the mathematical blueprint for a safe nursery — artificially lower $\rho$ while $\mathcal T$ develops") plus the transient-adequacy candidate yields something concrete: a developmental schedule is a *curve* $\rho(t)$ chosen so that the growing agent's margin $\Delta\rho^\ast(t)$ never goes negative *and* shocks resolve within $T_{\text{tolerance}}$ — i.e., curriculum design as a control problem on the environment side. That's formalizable *today* with only walked machinery, it's the mathematical content of "crèche," and it would be the first Volume-4 concept derived end-to-end from Part I. Seventh shadow-catalogue entry, and the one I'd nominate first for actual writing.

**Next:** `#def-mood`.
