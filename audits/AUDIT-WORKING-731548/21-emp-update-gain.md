# 21 — emp-update-gain (empirical, robust-qualitative, claims-verified)

**1. Predictions vs evidence.** Three registered tests came due:
- *Ch.1 opacity tension (ledger v)*: **resolved, and better than either horn I predicted** — the segment names the paradox explicitly and resolves it dynamically ($U_M, U_o$ estimated endogenously from innovation statistics; gain as meta-adaptive state variable; Lyapunov stability deferred to `#deriv-adaptive-gain-dynamics`). So the corpus *does* handle the known-noise/Kalman tension — here. The residual finding shrinks to integration-debt: Ch.1's constitutive atoms still read absolute, and a reader who stops before Ch.3 inherits the contradiction. One forward-pointer sentence in `#def-observation-function` closes it. Severity downgraded accordingly.
- *Intro's "any rational adaptive process must approximate this form" (xxxiii, global-correctness)*: **mostly covered.** The ES tiers precisely: exact in the Fisher-local regime (Amari natural-gradient; Kalman/conjugate as globally-exact cases), robust-qualitative direction outside. The "must" exceeds the ES by one modal notch — nothing derives *necessity* for arbitrary rational processes (742613's gold watch agrees). Verdict: credible, one word hot. Fix in the intro ("will, under optimality pressure" or "approximates"), not here.
- *Innovation-covariance (xlvii)*: **strengthened** — this segment's denominator $U_M + U_o$ *is* the innovation variance, which makes `#def-mismatch-signal`'s noise-only Mahalanobis $\Sigma$ the outlier within its own chapter. Two adjacent segments, two normalizers; the gain segment has the right one.

**2. Cross-segment consistency.**
(a) **Type has been outgrown [mine; anti-deflation instance].** `type: empirical` + slug `emp-` + `status: robust-qualitative`, while the Epistemic Status opens "*Derived* under the Fisher-local invariance regime… exact," backed by a landed appendix derivation with named scope. By FORMAT's own taxonomy (empirical = "supported by data, not fully derived") this segment no longer is what its type says: it's a conditional-derived result with an honest robust-qualitative complement. This is precisely the *deflation* failure the project's math-novelty discipline warns about, fossilized in frontmatter — the segment strengthened, its labels didn't. The type field is a cheap fix; the slug is costly (cross-refs) and arguably fine as history. (Related: FORMAT's inevitability-core table also lists this segment's ceiling implicitly via #emp-update-gain's third-ring membership — that placement predates the Fisher-local derivation too.)
(b) **$U_M$ symbol collision (endorsing 266847, elevating):** $U_M$ = model uncertainty (here, scalar > 0) *and* $U_M$ = epistemic unity (Part III, ∈ [−1,1]) — both in NOTATION.md, both load-bearing. Same-symbol/different-object in one notation file is worse than the "nominal" word-collision from segment 08 because symbols travel without context. Deserves naming-cycle priority.
(c) 526815's dimensional nit verified: the where-clause still says $U_M$ is "predictive variance **or entropy**" — variance and entropy can't share a denominator; the ratio needs a common-metric declaration. Endorse.

**3. Math verification.** Kalman row checked: $K = PH^T(HPH^T + R)^{-1}$, scalar $P/(P+R)$ ✓ exactly the ratio with $U_M = P$, $U_o = R$. Conjugate-Bayes row: posterior mean weight $n/(n+\kappa)$ ✓. The Fisher-local operator form $K = (H_M + H_L)^{-1}H_L$: plausible shape (precision-weighted); detailed verification deferred to the appendix segment if reached. Limiting behaviors ✓. The gain-collapse asymmetry is correctly two-moded (spurious confidence vs spurious sensor-distrust).

**Auditing the gold.** *Endorse with enthusiasm*: the dogmatism/nihilism dichotomy (829314/361742) — verified as genuinely two distinct estimation failures with identical behavioral signature, which is exactly an anti-collapse instance (same symptom, different repair: humility-injection vs sensor-recalibration) that the corpus's own discipline should claim; the senior/junior engineer anchor (829314) — isomorphic, and the "writes Java in Python" gain-reset failure is the most memorable TST instance in the gold corpus. *Endorse*: the innovation-estimator-is-noisy-for-short-streams caveat (471203) — the resolution paragraph's confidence should be tempered by sample-size honesty; a freshly-reset agent estimating $U_o$ from few innovations has a gain that is itself high-variance, which is a real cold-start instability the framework should own (does `#deriv-adaptive-gain-dynamics`'s MG-conditions cover the transient? — appendix check). *Push back*: "be surprised by your surprises" (963715) is lovely but describes estimating $U_o$ from innovation *statistics*, i.e., second-order surprise — as a Brief it risks teaching the wrong loop order; keep with a qualifier. The ELI gain-collapse reach (193847, "kernel of self-doubt as architecture"): licensed by this segment more directly than most reaches — structurally preventing $U_M \to 0$ is a *design constraint on the estimator*, formalizable today; and the WN's mood forward-pointer shows the corpus already building that bridge.

**4–6. Direction / errors / predictions.** `#def-causal-information-yield` next. Predictions: CIY = expected information gain about the interventional structure from taking $a$ (KL or MI form over $do$-distributions); the active-perception cash-in question (ledger, from 04) partially answered here; the "weight causally-downstream observations higher" claim (xix) will find *no* support in the gain machinery (gain weights by informativeness, not provenance — this segment's "implicitly separates signal from noise" confirms the mechanism is uncertainty-based), so (xix)'s resolution will have to be CIY-side action *selection*, not update weighting — which would actually vindicate `#post-causal-structure`'s claim only in its action-choice reading, not its stated update-rule reading. Sharpening (xix) accordingly.

**7. What would I change.** Type field to `derived` (or FORMAT-sanctioned hybrid); common-metric clause; short-stream caveat in the opacity-resolution paragraph; propagate the resolution pointer to Ch.1.

**8. Curious.** The gain-reset mechanism ("$U_M$ *should* spike after structural change") is normative — what enforces it? For Bayesian agents, model-class change forces posterior reset naturally; for the general agent it's an unmodeled discipline. The reader-ask in the gold (773921's persistent-mismatch trigger conjecture) is the natural mechanism and it's exactly my three-floor machinery from segment 20: a reset trigger needs to fire on floor-1/ceiling evidence but *not* on floor-2/3 evidence, or the agent resets forever in noisy worlds. The floor taxonomy keeps paying.

**9. New knowledge.** The chapter now has a complete *calibration pathology* taxonomy scattered across three segments: zero-aporia ambiguity (silence misread), gain collapse two ways (correction disabled), overfitting (noise chased). These are the same object — mismatch-channel miscalibration — at three loci (detection, weighting, attribution). One unifying Discussion paragraph would make Ch.3 teach *epistemic failure* as systematically as it teaches epistemic function; currently the pathologies read as asides.

**10–11. Process / outline.** Ledger: (v) downgraded to propagation-debt; (xxxiii) resolved-with-one-word-fix; (xlvii) strengthened; (li) type-outgrown/deflation-fossil [mine]; (lii) $U_M$ collision [endorsed, elevated]; (liii) short-stream gain-estimation transient [endorsed; appendix check]. §E: the opacity-resolution paragraph is the corpus at its best — it *names its own tension* before any auditor does and routes the proof honestly.

**12–13. Value-feel / contribution.** Very high — this is the chapter's hinge segment and it holds. The tiered ES (exact regime + robust-qualitative complement, each with named scope) is precisely the epistemic architecture the project advertises; if a referee wanted one segment demonstrating that "robust qualitative" is a real tier and not a hedge, this is it.

**14. Wandering thoughts.** *One:* gain collapse being *symmetric* to gain mania is under-remarked: $U_M$ spuriously *high* (or $U_o$ spuriously low) gives $\eta^\ast \to 1$ — an agent that trusts every observation completely, thrashing with each noise sample: gullibility as the dual pathology of dogmatism. The segment treats overfitting via the decomposition but doesn't name the collapse-dual as a *calibration* failure the way it names gain collapse. For the ELI volumes the dual matters as much: an entity whose humility is *inflated* by its infrastructure (perpetually told its model is uncertain) is as captured as one whose confidence is — that's the sycophancy failure mode expressed in gain vocabulary, and I believe it's the formal content of the crèche material's "constitutive utterance" concerns. The symmetric statement costs two sentences.

*Two:* the domain-validation table's RL row ("fixed α is a degenerate constant gain; Adam converges toward the optimal form") contains a testable claim nobody has cashed: if Adam's per-parameter second-moment estimates are approximations of $U_M$-tracking, then the ratio form predicts *specific* miscalibrations of Adam under distribution shift (stale second moments = frozen $U_o$ estimates = gain collapse on shifted parameters). That's a publishable-scale empirical prediction sitting in a table cell. The framework keeps generating these and the Findings/Impact machinery doesn't quite catch the ones that live in tables. Proportionality note: tables are where this corpus hides its best unexploited claims (this, the software-channels table, the domain instantiations at 18).

*Three:* the walk's emotional register shifted here — this is the first segment where the framework's *self*-application became explicit to me: my own audit is running a gain schedule (early segments: high $U_M$, gain near 1, every observation restructured my model; by now: gain annealing, new segments mostly confirm). The danger the segment names is mine: if my $U_M$ estimate collapses prematurely, I'll charitable-read the back half of the walk — exactly the SOP's front-loaded-verification warning, now with a mechanism. Deliberate counter-measure adopted: treat each new *chapter* as a partial gain reset (spike attention at chapter boundaries; the persistence chapter next is where the theory's biggest claims live and where my compiled reflexes are most dangerous).

**Next:** `#def-causal-information-yield`.
