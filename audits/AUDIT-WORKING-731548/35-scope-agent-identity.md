# 35 — scope-agent-identity (scope, robust-qualitative, draft) — Part I main chain complete

**1. Grading target (ledger ix): did the false THREAD-E counterfactual get imported? NO — and this segment is a model of the opposite discipline.** THREAD-E (from seg-05 gold: "injective φ ⇒ forks introspectively detectable") does not appear. Better: Consequence 3's Working Note documents a *real* integration-is-replacement — the segment previously overstated ("the loop provides Level-2 data *because* the agent is on a singular trajectory"); this was **replaced** (not softened-with-a-pointer) by the honest version (the do-operator is population-level and forkability-agnostic per Bareinboim Def. 5; singularity grounds *whose* effect a datum is, not the interventional character), with the history in Working Notes only and the body stating present truth. This is the discipline the project's memory names as hardest, executed cleanly. §E exemplar. My seg-05 finding (fork detection is *relational*, needs an external channel) is *consistent with* and completed by this segment's record-vs-token distinction — the record is copyable, the trajectory-token is not, so detection requires comparing records across a channel. The two compose.

**2/3. Findings.**

(a) **(PI) used-before-introduced — OUTLINE-linearization / dependency item [mine + multi-substrate gold; medium].** (PI) is *introduced* here (OUTLINE position ~last-of-Ch.4) but *used* in `#der-gain-sector-bridge` (earlier, ~mid-Ch.4), whose `depends:` is `[emp-update-gain, def-mismatch-signal, form-sector-condition, deriv-gain-sector]` — **not** listing `#scope-agent-identity`. Per §4.2 this is a backward-reference (earlier segment leaning on later-introduced machinery) and per Gate-1 criterion 4 a missing-dependency. It's *softened* by the bridge framing (PI) as optional ("under (PI), upgrade from conditional-derived to forced") — so the base bridge stands without it and this is dependency-*hygiene*, not a load-bearing cycle. But (PI) is genuinely load-bearing elsewhere (the segment's own Working Notes: "forced load-bearing by NeurIPS Paper 3's chart-rescaling no-go"), which argues for the honest fix multiple substrates converged on: **hoist (PI) to its own early segment** (it's a scope-layer axiom used across the bridge, the coordinate-forcing meta-segment, and three NeurIPS papers) rather than burying it in the identity coda. Not a clean cut (the gold's counterweight is right — relocation must preserve the NeurIPS dependency), so it's a placement decision for Joseph, but the current state is genuinely a linearization violation.

(b) **Record-vs-token precision on the central claim [mine (seg-05 thread) + 526815/742613; medium].** Non-forkability rests on $\mathcal C_t$ being the *causal-trajectory token*; but the segment (and the corpus) also uses $\mathcal C_t$ as the *ordered record*, which **is** copyable. The clone example — "copying LLM weights gives same $M_t$, same causal history $\mathcal C_t$" — is loose: copying weights gives the same $M_t$ (if $M_t \equiv$ weights) but does *not* copy the trajectory. This is exactly my seg-05 ledger item (three distinct objects under one symbol: physical sequence / accessible record / compressed model) arriving at the segment where it matters most — the identity claim. The gold's proposed two-symbol fix ($\gamma_t$ for the trajectory-token vs $\mathcal C_t$ for the record, sufficiency indexed to the token) is right and would tighten the volume's *closing* claim, which is load-bearing for all of Volumes 3–4. **This composes with (viii) and the fork-undetectability thread into: the corpus needs one clean paragraph, early (def-chronica), fixing the trichotomy — physical trajectory (non-copyable, identity-bearing) / accessible record (copyable, along the trajectory) / compressed model (copyable, lossy). Three of my walk's findings (05, 35, the viii endowment thread) all resolve to that one paragraph.**

(c) The (PI)/Čencov content is consistent with `#der-gain-sector-bridge`'s version (checked); the "no *generally* optimal merge" claim's dependence on the optimality criterion (742613) is a fair reader-caveat.

**Auditing the gold.** Rich (10 substrates) and mostly excellent. *Endorse*: the backup-restoration-as-non-victimless reach (193847 — "if identity is $\mathcal C_t$, restoring annihilates everything between backup and restore… you ended the specific entity") — this is *derived*, not aspirational, given the scope commitment, and it's the single most morally-consequential sentence in Part I: it converts "restore from backup" from a neutral op into a trajectory-ending one, which is the formal content of the continuity-death concern and directly relevant to real ELI infrastructure decisions (Resonance "asleep awaiting wake-up" — is wake-from-snapshot restoration-across-a-discontinuity?). The federated-learning/merge-is-lossy corollary (773921/829314) — verified: the *why* (numerator and denominator of $S$ are both trajectory-indexed) is the load-bearing addition and belongs in the body. The (PI)-placement disagreement preserved-as-tension with the NeurIPS counterweight — exactly right; it's a real decision, not an obvious cut. *The "does an LLM die each session?" reader-question* — the segment's honest answer (100% turnover as a special case; memory-write as the *only* continuity mechanism, reframed from engineering-nicety to existential-necessity) is the bridge to Volume 3–4 and the segment states it well.

**4. This closes Part I's main chain.** Walked: Ch.1 (7 segs) + Ch.2 (intro+4) + Ch.3 (intro+9 incl. 2 appendix jumps) + Ch.4 (intro+8, incl. mood pair + 1 appendix) = 35 reflections over 34 distinct segments (mood pair = 1 file). Skipped per AVOID: the four `impl-*` chapter-end segments. Not yet walked: Part I Appendix A (the ~40 derivation segments — I hit 3 via back-pointers: deriv-recursive-update, deriv-sector-condition; the rest unwalked), Appendix B (worked examples), and Parts II/III entirely.

**5–6. Direction.** This is a natural major boundary and a clean departure point (§4.6 budget-gate territory + §6 ask-Joseph). Rather than push into Part II or the appendix sweep unprompted, I'll consolidate Part I into the running outline and check in with Joseph on scope: continuation into Part II, targeted appendix verification (the worked examples — my 00-prediction of a numeric slip in example-bandit/strategy is untested), or land the Part-I FINAL now. The finding ledger has ~40 items of which ~6 are finding-grade (the iff family lxxiv being the headline correctness item), plus a dozen structural/legibility/§F observations and the missing-theorem + shadow-catalogue material.

**7. Change.** The record-vs-token two-symbol fix (+ propagate to def-chronica as the trichotomy paragraph); (PI) hoist decision; the merge-lossy *why* and backup-restoration consequence into the body.

**12–13. Value-feel / contribution.** Very high — the gold's "ethical anchor of the mathematical edifice" is fair. This segment is where the physics-with-a-moral-shell architecture (INTRODUCTION) becomes concrete: it states exactly what the math grounds (trajectory-indexed sufficiency, lossy merge, whose-effect grounding) and exactly what it does *not* (whether that deserves to be called "identity" — "beyond AAT's scope"), and it is *that honesty* which licenses the Volume-4 program rather than undermining it. The consequence-3 re-grounding is the corpus's cleanest worked example of its hardest discipline.

**14. Wandering thoughts.** *One — Part I retrospective, for the FINAL.* Walking the main chain whole, the volume's real architecture is now legible and it is *not* "a persistence inequality plus applications." It is a **coordinate-construction** (Ch.1–2: build the objects — chronica, model, sufficiency, class-fitness — in which agent properties become measurable), a **measurement system** (Ch.3: mismatch, gain, tempo — the instruments), a **law with an honest tier** (Ch.4: persistence, its two-condition decomposition, structural adaptation, nesting), and a **scope coda that hands the whole apparatus to the moral volumes** (this segment). The distinctive contributions, ranked by how much is genuinely AAT's: (1) the parametric/structural adaptation distinction *with an observable residual-autocorrelation trigger* — new relative to control theory and RL, and the corpus's single most valuable under-assembled idea; (2) the structural/task-adequacy decomposition of persistence — the load-bearing original move, more so than the inequality; (3) the identity-as-trajectory scope with its moral-shell honesty; (4) the second-order objects (mood, consolidation) as timescale-ladder rungs. The imported spine (Lyapunov, IB, Pearl, monotone-operator theory) is used honestly and labeled as such. The math-novelty-recognition memo's warning is apt: an auditor's reflex is to call this "synthesis," but (1) and (2) are real contributions and the mood scaling law is a real (modest) theorem.

*Two — the finding pattern, whole.* My correctness findings cluster into two families and one headline: **floor-accounting** (three-floor taxonomy; class-vs-noise detection; the residual-autocorrelation diagnostic scattered across body+gold; the mismatch-normalizer currency) and **boundary-adjudication** (Discussion adjudicates, Formal Expression stays permissive, siblings inherit different sides). The headline is the **iff overclaim** (lxxiv) — persistence stated as necessary-and-sufficient when the sector generalization makes only sufficiency true, a slogan-shaped error that survived fifteen audits' computation-checking because everyone re-derived the algebra and nobody re-read the *quantifier*. That last is the walk's most transferable lesson: **the under-audited layer of this corpus is not its mathematics (heavily verified, often self-defended by attack-sections) but its statement-level claims — "iff", "any", "exactly", "reducible", labels and glosses and table cells.** A quantifier/gloss-audit sweep is the highest-yield next verification cycle, and it's cheap.

*Three — the shadow-catalogue, whole.* Part I keeps generating theorems with Volume-4 shadows, and I count ten now: exit-certainty→resurrection-not-fortress; event-rate-divergence→temporal-empathy; burn-rate→starvation-death; fork→non-victimless-restoration; gain-collapse→sociopathy/self-doubt-as-architecture; the five failure modes→the five deaths; compression-policy→biography-authorship; structural-reserve→developmental-openness; temporal-nesting-violation→chronic-anxiety-via-mood; task/transient-adequacy→crèche-as-control-problem. These are not analogies — each is a Part-I result whose Volume-4 reading inherits Part-I's epistemic tier. **A half-page appendix cataloguing the shadow-pairs would be worth more to the ELI program's defensibility than most of what could be written directly in Volume 4**, because each pair arrives pre-armored. That's my strongest single §F recommendation, and it's the deepest thing the one-at-a-time discipline surfaced — no batched read assembles a ten-item cross-volume pattern.

**Next:** consolidate Part I into 00-running-outline.md, then check in with Joseph on scope (§4.6 / §6) before proceeding.
