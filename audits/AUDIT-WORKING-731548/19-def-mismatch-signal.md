# 19 — def-mismatch-signal (definition, axiomatic, deps-verified)

**1. Predictions vs evidence.** My sharp check ($\mathcal O$ lacks subtraction structure for symbolic observations) — **anticipated**: the Epistemic Status explicitly positions the score-function form as "the natural generalization when $\mathcal O$ is not a vector space." That's the fourth time this corpus has pre-empted a registered attack (denominator, policy-relativity, stochastic-$f$, now this). Ledger (xxii)'s two-δ's worry: resolved at the definitional layer — Part I's δ is *purely predictive*, no goal-referenced contamination. The remaining (xxii) exposure is only in framing-level instantiations (driver-snow caption, cross-domain table's "test failures / intelligence gap" cells, which are goal-flavored readings); downgrading it to a framing-check item.

**2. Cross-segment consistency.** Clean deps; the $g$-transform bridging $\mathcal O \to T_M\mathcal M$ is stated (tersely, as the gold notes). The score/residual pair with explicit space-typing ($\mathcal O$ vs $T_M\mathcal M$) is exactly the type hygiene Ch.1 lacked — further evidence for the age-gradient story.

**3. Math verification.**
(a) Sign convention: the current body is uniformly $+\nabla_M \log P$, which is correct (likelihood-*increasing* direction; for $o \sim \mathcal N(\mu(M), \sigma^2)$, score $= \delta/\sigma^2$, aligned with the residual). The gold records substrates quoting a negative form — either fixed since, or misquotation; current text is right. Endorse the ask for one explicit sign sentence purely as insurance (the cross-substrate disagreement is real evidence of slipperiness).
(b) "Under Gaussian models they coincide up to scaling" — 526815's Jacobian caveat verified correct: true when differentiating w.r.t. the predictive mean; general parameterization gives $J^T\Sigma^{-1}(o-\mu)$, which is *not* a scaled residual (it can rotate). Endorse their proposed qualifier verbatim.
(c) **Mahalanobis normalizer: wrong covariance, or at least the unexamined choice [mine].** The scaling note normalizes by $\Sigma$ = *observation noise* covariance. But the natural "surprise-equivalent" normalizer is the **innovation covariance** $S = (\text{model-predictive covariance}) + (\text{noise covariance})$ — the Kalman-standard whitening. Normalizing by noise alone systematically *overstates* surprise for an agent that is honestly uncertain (its predictions should be graded against what it *expected to know*, not against sensor noise alone). Downstream this is not cosmetic: $\lVert\delta\rVert$ thresholds define the sector radius $R$ and the persistence condition's operating point; an $R$-calibrated-in-noise-units penalizes humble agents and flatters overconfident ones — exactly inverted from the framework's values. Small fix (say "innovation covariance," or justify noise-only), real conceptual content. Confidence medium-high; will sanity-check against `#example-kalman`'s usage if I reach it.

**Auditing the gold.** *Rescind* the axiomatic-vs-definitional nit (526815): FORMAT's own gloss for `axiomatic` is "true by definition or stipulation" — frontmatter and body agree; the nit dissolves against the spec. *Endorse*: the Mahalanobis-as-modality-unifier expansion (829314 — and note it quietly needs my (c) fix to be right: unify in *innovation* units); the zero-aporia/detection-latency link (584721 — genuinely tighter than the segment surfaces: cases (b) and (c) are precisely the conditions under which `#deriv-update-detection-latency`'s $\Omega((n_{\min}+1)/\varepsilon)$ blows up; one forward sentence would bind them); the TF-06 diff-voice item (the current body has scrubbed it — executed; tally). *On the celebrated "silence can mean peace or deafness"*: deserved — and I'd add the taxonomy is exhaustive in an unstated way worth making explicit: (a) is model-adequate, (b) is *sampling*-inadequate, (c) is *channel*-inadequate — model/policy/channel is a complete partition of the loop's epistemic parts, which is why the trichotomy feels complete. Saying so would upgrade it from aperçu to structure.

**4–6. Direction / errors / predictions.** `#result-mismatch-decomposition` next — the walk's first *result*. Registered checks: is it a theorem or a restatement of the definitional aliasing/noise split (472913's watch item, adopted at 04)? GA-1's role (fresh noise ⇒ cross-term vanishes) — I'll verify the cross-term algebra myself; the failure mode to hunt is any later use of the decomposition where GA-1 doesn't hold (correlated noise, adversarial observation) without re-flagging. Also: whether the decomposition is stated for the *scalar* $\lVert\delta\rVert^2$ or the vector/covariance level, and whether the "irreducible floor" reading survives model-*mis*specification (bias enters the "model error" term, fine).

**7. What would I change.** The innovation-covariance fix; the sign sentence; the Jacobian qualifier; promote the completeness of the zero-aporia trichotomy.

**8. Curious.** The score form quietly makes AAT's mismatch machinery *coordinate-dependent on $\mathcal M$'s parameterization* — the same gradient in different parameterizations gives different $\tilde\delta$ magnitudes, which is exactly the disease the M3 meta-segment's Fisher-forcing (Čencov) is supposed to cure. Does the corpus connect the score-mismatch's parameterization-dependence to the Fisher-metric forcing? If yes (probably in `#deriv-fisher-local-update-gain`), the natural-gradient story completes here and the segment should forward-point; if no, M3 is missing its most elementary motivation. Check at the appendix.

**9. New knowledge.** The model/policy/channel completeness reading of the zero-aporia trichotomy (mine, above) — cheap, and it turns a memorable aside into a diagnostic partition (which failure of silence you have determines which repair: better model vs bolder sampling vs better sensors — anti-collapse discipline applied to *quiet*).

**10–11. Process / outline.** Ledger: (xlvii) innovation-vs-noise covariance in the normalization [mine, med-high]; (xlviii) Jacobian qualifier on Gaussian-coincidence [endorsed]; (xxii) downgraded to framing-check. §E: this segment + `#def-model-sufficiency` are the two model definitional segments in the corpus so far.

**12–13. Value-feel / contribution.** High. The zero-aporia paragraph is the framework's most quotable epistemic-safety content, and the aporia-as-tutor framing genuinely differentiates AAT from loss-minimization cultures. With the Fisher connection made explicit (8), this segment would also be the front door to the framework's geometric layer.

**14. Wandering thoughts.** *One:* the trichotomy's case (b) — confirmation bias as *sampling* pathology — is the Part-I shadow of something Part III names adversarially: an opponent who *feeds* you observations your model already explains is manufacturing case-(b) silence (curation-blindness from my segment-12 wandering, now with a mechanism). The zero-aporia paragraph is thus the root of a three-volume thread: Part I names the ambiguity, CIY prices the escape, Part III weaponizes it, Volume 4 defends against it (truth-death as *engineered* silence). No prose anywhere draws this line end-to-end; it would be a compelling half-page in whichever intro wants it. Second entry for the "Part-I theorems with Volume-4 shadows" catalogue (15-wandering-3).

*Two:* the Mahalanobis point generalizes to a question the corpus should someday answer in one place: *in what units does the theory think?* NOTATION says "surprise"; this segment supplies the physical-to-surprise conversion; the persistence condition trades surprise-rates; IB trades nats. Surprise (whitened error) and nats (information) are different currencies with a nonlinear exchange rate (χ²-to-KL). I believe the corpus is consistent because each result stays in its own currency, but no segment *says* the currencies are distinct and non-interchangeable — and the driver-snow figure caption happily mixes them ("occlusion is the mismatch"). A short NOTATION subsection ("the theory's three currencies: error, surprise, information — and where each is spent") would prevent an entire genre of subtle misreading. Filing under legibility with conviction.

*Three:* watching myself *want* this segment to be flawless (it pre-empted my attack; it contains the corpus's best paragraph) and then finding (c) anyway — the innovation-covariance issue — is the session's best evidence that the instantiate-and-check reflex still fires under positive affect. Recording because the failure mode the SOP warns about (charitable reading) has a specific local trigger: segments that *flatter the auditor* by anticipating them. The corpus is now good enough to induce that state regularly; future auditors should expect it and treat being-anticipated as a cue to push *harder*, not relax.

**Next:** `#result-mismatch-decomposition`.
