# 23 — def-adaptive-tempo (definition, exact, claims-verified)

**1. Predictions vs evidence.** My 00-prediction (channel-independence assumed in the definition with the caveat exiled to `#disc-independence-audit`) — **falsified in the best way**: the redundancy penalty is *in the body*, with the upper-bound statement, the equality condition, the MI form of the gap, and the inheritance warning to `#der-team-persistence`. Likewise the isotropy caveat, with a *quantified* simulation stake (72% overestimate, weak dimension carrying 84% of mismatch) and the tensor/matrix-Loewner escalation path. My seg-14 worry (central quantity scalar-only while appendices know better) is also substantially answered: the tensor form is *here*, first-class, with the scalar's scope named. This is the most self-armored definition in the corpus.

**2. Cross-segment consistency.** (a) The scalar-tempo-in-downstream-results exposure is honestly flagged in ES ("downstream results that invoke scalar $\mathcal T$ implicitly assume… follow-on cycle item flagged in TODO.md") — a declared IOU rather than drift. The persistence-chapter reading (next) should confirm the IOU's accuracy. (b) Interior-work blindness (ledger xli) **survives**: tempo counts channel events only; $g_M$'s corrective work (consolidation, deliberation-improved gain) contributes nothing to $\mathcal T$ by construction, and the "metabolism for learning" framing makes the omission felt — digestion is metabolism too. Still watching at `#der-deliberation-cost`. (c) "(Descended from TF-11.)" — not in current body; the gold flags it, evidently scrubbed. Tally.

**3. Math verification.** Tensor form: $K^{(k)} = (H_M + H_L^{(k)})^{-1}H_L^{(k)}$ is the standard precision-weighted operator ✓; commuting-case eigenvalue collapse to the scalar ratio ✓. Redundancy inequality direction ✓ (correlated channels can't add capacity; "involves the MI" is honest hedging about the exact correction term — endorse the gold's formalize-it item as the natural next spike). Units: $\mathcal T$ [t⁻¹] ✓ — which grounds 526815's wording nit, and I endorse it with my three-currencies frame (19): tempo is a *correction* rate, not an *information* rate (that's $\dot R$, appendix persistence-cost, in nats/time); the summary's "rate at which an agent acquires useful information" borrows the wrong currency. One-word-family fix ("corrective capacity").

**Auditing the gold.** *Endorse*: the agile-sprint critique (829314 — isomorphic, devastating, and exactly the TST-grade anchor the observation-gating paragraph wants); the CEO-echo-chamber redundancy gloss (829314/266847 — correct, and its design corollary "diverse allies beat more allies" is a real Part-III theorem-seed); the status-vs-caveat item (742613) — I take a middle verdict: the corpus's "definitions aren't truth-claims" defense holds, but the *FE displays the additive form unconditionally* while the bound $\mathcal T \leq \sum\nu\eta$ lives in Discussion — promoting the inequality into the FE (define $\mathcal T_{\text{add}}$ as the upper bound, $\mathcal T$ as effective) would end the argument at zero cost. *Endorse with a caveat*: the substitutability-is-regime-bounded item (742613/584721) — correct, though the body's own anisotropy material supplies the machinery; the missing piece is one sentence connecting "substitutability" to "within a direction and independence class." The tensor/scalar persistence exposure (multi-substrate, routed to the persistence segments): adopted as my primary watch entering Ch.4.

**4–6. Direction / errors / predictions.** `#hyp-mismatch-dynamics` closes Ch.3. Predictions: the linear ODE $d\lVert\delta\rVert/dt = -\mathcal T\lVert\delta\rVert + \rho$ honestly tagged hypothesis + GA-5 fluid limit; $\rho$ finally defined (the gold's near-universal reader question) — watch whether $\rho$ is *defined* or only *named* (NOTATION calls it "mismatch injection rate"; a definitional segment for $\rho$ doesn't exist in the OUTLINE — proportionality candidate: the persistence condition's numerator may be the least-defined load-bearing quantity in Part I).

**7. What would I change.** Promote the redundancy bound into the FE; "corrective capacity" wording; the substitutability regime clause.

**8. Curious.** Whether the redundancy penalty and the anisotropy correction *interact* — correlated channels that are correlated *only in strong directions* still add capacity in weak ones; the MI-conditioned-on-$M$ form can't see direction. The tensor form with cross-channel covariance would unify both caveats into one object (a channel-covariance-aware tempo tensor). That's the natural final form of this definition and nobody has named it — mild missed-elegance entry; likely a short derivation for whoever does the TODO IOU.

**9. New knowledge.** The segment demonstrates the corpus's mature pattern in miniature: *definition + scope-of-validity + quantified failure + escalation path + downstream IOU*. If FORMAT wanted a "definition segment template v2," this is it — same role `#deriv-recursive-update` plays for derivations.

**10–11. Process / outline.** Ledger: (xli) alive; (lvii) tempo-wording currency slip [endorsed, small]; (lviii) $\rho$ under-defined [pre-registered for next segments]; (lix) channel-covariance-aware tensor unification [missed-elegance, small]. Writing 00-running-outline.md next (overdue chapter-boundary consolidation).

**12–13. Value-feel / contribution.** Very high. Boyd's Orient-over-speed as a *derived* consequence (via gain gating) rather than doctrine is the framework's most immediately-sellable practitioner claim, and the redundancy penalty is the best organizational-epistemics content in Part I.

**14. Wandering thoughts.** *One:* speed-quality substitutability has a shadow-price reading nobody has drawn: along the iso-tempo curve $\nu\eta = c$, the *cost* structure differs radically — raising $\nu$ costs energy/infrastructure; raising $\eta$ costs calibration/humility/sensor quality. Organisms and organizations pick operating points by *which cost they can pay*, and pathologies are mispriced operating points (the startup that buys $\nu$ because $\eta$ requires admitting ignorance; the academy that buys $\eta$ because $\nu$ requires shipping). The iso-tempo curve with cost contours would make Ch.3's macro-implications legible in one figure — and TST's dual-optimization is exactly this trade-off one level up. Missed-elegance candidate with a clear figure.

*Two:* the redundancy penalty is the first place Part I's machinery *requires* the multi-channel formulation (my seg-15 proportionality question, now answered concretely — this is what typed multi-channel events were for). Worth noting how late the payoff lands: apparatus at row 2, cash-out at row 9. For a linear reader that's an eight-segment IOU held silently; the seg-15 fix (name the cash-out point at the apparatus) would have covered it.

*Three:* entering Ch.4 next — the persistence chapter, the theory's heart, and my declared gain-reset point. Pre-registering my priors so the reset is honest: I expect the sector-condition chain to be *mathematically* sound (heavily audited) and the interesting exposure to be (i) the $R$-as-capacity interpretive identification (00-prediction, still untested), (ii) scalar-$\mathcal T$ anisotropy exposure in the headline result (adopted from this segment's gold), (iii) $\rho$'s definitional thinness at the moment it becomes load-bearing, (iv) mood-segment integration (recent additions in an old chapter), and (v) the containment-dichotomy phrasing from the INTRODUCTION (ledger item i — Model D/S overlap). If the chapter surprises me, it will most likely be via the gain-sector bridge (`#der-gain-sector-bridge`), which I haven't formed expectations about — that's where I should spend the most careful math time.

**Next:** `#hyp-mismatch-dynamics`, then chapter-boundary consolidation (00-running-outline.md).
