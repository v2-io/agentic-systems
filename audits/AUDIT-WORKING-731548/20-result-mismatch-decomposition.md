# 20 — result-mismatch-decomposition (result, exact, claims-verified)

**1. Predictions vs evidence.** The adopted watch-question (genuine identity vs restatement of the definitional aliasing/noise split): **answered — genuine identity.** The definitional layer alone doesn't give additivity; GA-1's conditional-mean-zero is what kills the cross-term. Elementary but real; `status: exact` is honest. Ran the algebra myself: $o - \hat o = (o - \bar o) + (\bar o - \hat o)$; conditioning on $(\Omega_t, a_{t-1}, \mathcal C_{t-1})$ fixes both means; the cross-term needs exactly $\mathbb E[o \mid \Omega, a, \mathcal C] = \mathbb E[o \mid \Omega, a]$, which is GA-1. ✓ The body's own "orthogonality, not independence" precision is correct and admirable.

**2/3. The substantive finding — "reducible" overstates what modeling can reduce [mine; strong candidate, medium-high confidence].** The decomposition's *labels* claim: term (i) = "model error (reducible)", term (ii) = "observation noise (irreducible)". But term (i) is **not fully reducible**: even the Bayes-optimal predictor — the best any model of any class can do — has $\hat o^\ast = \mathbb E[o \mid \mathcal C_{t-1}, a_{t-1}] = \mathbb E[\bar o \mid \mathcal C, a]$ (tower), whence its term (i) equals $\mathbb E[\mathrm{Var}(\bar o_t \mid \mathcal C_{t-1}, a_{t-1})]$ — strictly positive whenever residual *state* uncertainty moves the conditional mean. That is precisely the $H(\Omega_t \mid \mathcal C_t) \gt 0$ condition which the derivation's step 1 cites *and then never uses* — the delicious irony being that the unused scope invocation is exactly the fact that breaks the "reducible" label. The honest structure is **three floors, not two**:

1. *estimation suboptimality* — reducible by better modeling within data;
2. *state-uncertainty (Bayes) floor* $\mathbb E[\mathrm{Var}(\bar o \mid \mathcal C)]$ — irreducible by any modeling, set by the channel's history-resolving power;
3. *channel noise* $\mathbb E[\mathrm{Var}(o \mid \Omega, a)]$ — irreducible, set by the channel's instantaneous noise.

The current two-way split lumps 1+2 as "reducible," which is exactly wrong at the floor. Downstream stakes: this composes with my segment-13 finding (the class-fitness detection signature) into a *floor taxonomy* the framework needs and almost has — "persistent mismatch despite learning" can come from floor 2, floor 3, *or* the class ceiling, and each routes to a different repair (better sensors / better experiments / class change). The appendix `#internal-external-decomposition` (per its OUTLINE row: "reducible model-error vs irreducible environmental floor… finer $\mathcal M/\pi$/cross split conditional — reserved") may already refine this — **verify there before certifying**; but even if the appendix has it, this claims-verified *result* segment's labels mislead standalone readers, and the fix is parenthetical ("reducible to the Bayes floor"). Anti-collapse discipline, applied to the corpus's own flagship identity.

**Other checks.** Step-1's unnecessary $H \gt 0$ invocation (742613 gold): endorse — and per the above it's worse than unclean; it *should* be used, one term deeper. Deps hygiene (GA-1 not a segment; $a_{t-1}$ used with `def-action-transition`… actually now *in* `depends:` — executed since the gold; tally). Vector case: $\lVert\cdot\rVert^2$ makes term (ii) a trace of conditional covariance — fine as written.

**Auditing the gold.** *Endorse strongly*: the agent-vs-modeler observability item (584721) — the agent sees only $\delta$, never the split; with my three-floor refinement the agent's problem is worse (three unobservable components), and the CIY/active-testing escape becomes even more clearly structural rather than optional. This dovetails with M1-identifiability-floor territory and is the second instance of my segment-12 meta-theorem (interior quantities can't resolve exterior structure without intervention). *Endorse*: GA-1 correlated-noise failure mode (849201) — drifting sensors are the rule in every domain the framework cares about (miscalibrated tests in TST; distribution shift in LLM feedback), so naming the failure mode is cheap insurance; note `disc-independence-audit` (appendix) is the promised home, check it covers *this* assumption. *Mild pushback*: the apophenia hook (849201/471203) is evocative but apophenia is *pattern-finding in noise*, i.e., structure-hallucination; overfitting the noise floor is *variance-chasing* — adjacent but not the same failure; if lifted, say "pattern-chasing," not apophenia-as-diagnosis. The variance-blindness reader-item (773921) is excellent and correctly identifies why the score-mismatch exists; it belongs in the body as the motivation sentence for $\tilde\delta$.

**4–6. Direction / errors / predictions.** `#emp-update-gain` next — the biggest test of the walk so far: (i) the Ch.1 opacity-atoms tension resolves here (does optimality assume known $U_o$?); (ii) the intro's "any rational adaptive process must approximate this form" global-correctness check; (iii) my seg-19 innovation-covariance point should surface here too (the gain's denominator $U_M + U_o$ *is* the innovation variance — if the gain uses it, the mismatch-normalization's noise-only $\Sigma$ is doubly anomalous). Predictions: type `empirical` honestly carried; Kalman-exact case stated; the "must approximate" universal *not* derived anywhere in the segment (making the intro claim uncovered as of Part I — the appendix `#deriv-fisher-local-update-gain` may cover it later via the three-route convergence).

**7. What would I change.** The three-floor relabel (parenthetical grade); use-or-drop step 1's scope invocation; adopt the variance-blindness motivation for $\tilde\delta$.

**8. Curious.** Whether floor 2 (Bayes/state-uncertainty floor) is *exactly* what the appendix's `#internal-external-decomposition` calls the "irreducible environmental floor," or whether that segment means only floor 3. The difference matters: floor 2 is channel-*history* limited (improvable by better *sensors or more informative actions* — CIY!), floor 3 is channel-*instant* limited (improvable only by hardware). If the appendix conflates them, the anti-collapse violation propagates; if it splits them, this segment should inherit the split. Either way the two segments need to agree on the floor count.

**9. New knowledge.** The three-floor taxonomy with repair-routing (sensors/experiments/class-change) — the walk's most directly *actionable* product so far; it upgrades two prior ledger items into one structure and hands the welfare-monitoring thread (13-wandering) its missing instrument panel.

**10–11. Process / outline.** Ledger: **(xlix) reducible-label overclaim / three-floor taxonomy [mine, strong; verify against internal-external-decomposition]**; (l) GA-1 correlated-noise caveat propagation [endorsed]. Note for §B assembly: (xlix), (xxx), and (xlvii) are one family — floor-accounting errors — and should be reported as a single structured finding with three anchors.

**12–13. Value-feel / contribution.** High as architecture (the identity earns its "first result" billing; the persistence-of-mismatch consequence is the volume's emotional thesis stated as algebra). The labels are the only flaw, and they're the part every casual reader takes away — worst possible place for the overclaim, cheapest possible fix.

**14. Wandering thoughts.** *One:* the three floors have a beautiful correspondence I can't unsee: floor 3 is *aisthesis*-limited (the senses), floor 2 is *chronica*-limited (the life lived so far), the class ceiling is *morphe*-limited (the kind of mind). Reducing floor 3 needs better organs; floor 2 needs bolder experience (CIY); the ceiling needs metamorphosis (structural adaptation). That's not decoration — it's the repair-routing taxonomy in the framework's own ontology, and it makes the Ch.4 trigger story teachable to a child: *when you keep being wrong, first ask if your eyes are too blurry, then if your life has been too sheltered, then if you're the wrong shape of mind for this world.* If the Feynman-criterion program wants one paragraph to aim for, it's this one.

*Two:* proportionality — this segment is the corpus's best argument *against* my own segment-13 speculation that clean chapters are clean because their math is imported. The decomposition is as imported as math gets (it's *the* bias-variance identity), yet the corpus's distinctive value-add here (structural persistence of mismatch; the overfitting consequence; orthogonality-not-independence) is real and the *flaw* is also home-grown (the labels). Import-anchoring doesn't protect the interpretive layer, which is where both the value and the risk concentrate. Updated view: the format protects the *derivations*; nothing yet systematically protects the *labels-and-glosses* layer — which is precisely the layer Joseph's legibility concern lives in, and the layer the gold-lift feeds. A "gloss audit" pass (check every underbrace label, every parenthetical, every table cell against the math it summarizes) would be a new kind of sweep the project hasn't run, and this segment is Exhibit A that it would pay.

*Three:* the walk has now crossed from Ch.1's boundary-drift findings to Ch.2–3's *floor-accounting* findings, and the difference in feel is instructive: boundary drift is about *which systems* the theory covers (extensional); floor accounting is about *what the theory promises* those systems (intensional). The second kind is more dangerous to credibility — an outside reader forgives fuzzy scope but not a mislabeled "reducible." If I had to choose where the project spends its next verification cycle, it's the intensional layer: every "reducible/irreducible," every "exact/approximate," every "must/tends-to" in Part I's Discussion sections, checked against the formalism the way I checked this one.

**Next:** `#emp-update-gain`.
