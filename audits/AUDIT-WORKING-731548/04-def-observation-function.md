# 04 — def-observation-function (definition, axiomatic, deps-verified)

**1. Predictions vs evidence.** Predicted the exact form $o_t = h(\Omega_t, a_{t-1}, \varepsilon_t)$ — confirmed. Predicted aliasing-vs-noise would be separated in prose but not formalism — roughly confirmed: the two loss sources (many-to-one $h$; noise $\varepsilon_t$) are present in the formal object but their *distinctness* is not remarked on in the body (the gold noticed it; see below).

**2. Cross-segment consistency — the Kalman tension, now two-for-two.** The epistemic-opacity atom ("the agent knows neither $h$ nor the distribution of $\varepsilon_t$ exactly") is the observation-side twin of segment 03's transition-opacity, and it collides with the Kalman instantiation the same way: a Kalman filter is *handed* $H$ (observation matrix) and $R$ (noise covariance) and still runs the full adaptive machinery, because what it lacks is $\Omega_t$ itself. So the framework's root definitions now declare **two** constitutive-opacity conditions that the flagship exact example violates. And this time the gold block shows *four prior substrates* flagged exactly this (526815, 742613, 829314, 849201), with a proposed resolution (constitutive inaccessibility-of-$\Omega$ vs the stronger unknown-observation-law) — yet the segment body still carries the strong form, unqualified. **This upgrades my ledger item (v): the concern is recorded in the segment's own Working Notes and remains unintegrated into the body across at least one full cycle. Disposition-shaped as Known-unintegrated / scope-status mismatch, pending my Phase-2 check of whether a certified finding already tracks it.** My segment-03 arrival at the $T$-side version was independent (that one the gold does *not* carry for seg-03; the observation-side flags are the cousins).

**3. Math verification.** None to run. Typing: same eq-tag question as seg 03 (epistemic-opacity atom under `*[Definition]*` introduces no object) — consistent ambiguity, one FORMAT-level answer wanted.

**Auditing the gold (contrarian pass, per Joseph's instruction).**
- *"Lossiness forces memory"* (829314, candidate Brief): **overstated as it stands.** Lossiness alone forces nothing — if $\Omega_t$ were temporally independent, no memory would help; memory is forced by lossiness *plus temporal correlation* (predictive information in the past). The bathtub-grade version needs both legs or it teaches the wrong lesson. If this line is ever lifted to a Brief, add the correlation leg.
- *"Definitional bias/variance pre-install"* (472913): sharp, and the note-to-self it carries is exactly right — at `#result-mismatch-decomposition` I must ask whether the decomposition is a theorem or a restatement of this definitional split. Adopted as my own watch item.
- *"Reader wonders why lossiness is key"* (963715): **stale or wrong** — the current body says it twice (summary: "forces the agent to maintain an internal model"; Discussion: same sentence with the cross-ref). Either the body was improved post-harvest or the item was pedantic at birth. Evidence that gold items need re-verification against current bodies before being acted on — which is precisely the project's audit-findings-as-gem-hints discipline; the gold inherits the same staleness property.
- *"Agent's-perspective vs modeler's-perspective"* (963715): good distinction, but its punchline ("AAT can't directly say what an optimal agent does") proves too much — `#emp-update-gain` states an *optimal* gain. The defensible version: AAT's optimality claims are conditional on the agent's uncertainty structure, while its persistence claims are not.
- *$T$/$h$ entanglement* (193847, readers-ask): genuine and under-appreciated — the formalism gives *two encodings* of the same physical act of looking (through $\Omega$ via $T$, or directly via $a_{t-1}$ in $h$). Redundant parameterizations are fine in a `formulation` but this segment is `type: definition, status: axiomatic`. Mild type strain, not a bug.

**4. Theory direction.** The chapter's remaining rows (chronica, two scopes, postulate) should assemble the pieces into the first real scope condition; the load-bearing question is whether `#scope-adaptive-system` resolves the accumulated opacity questions (which opacity is *constitutive*: kernel/law ignorance vs state inaccessibility) or inherits them.

**5. Errors to watch.** Wherever the framework later needs "the agent doesn't know the noise distribution" as a *premise* (e.g., if $\eta^\ast$'s uncertainty ratio requires estimating $U_o$ online), fine; but if `#emp-update-gain` *assumes known* $U_o$, the opacity claims here are directly contradicted two chapters later. High-priority check at Ch.3.

**6. Predictions.** `#def-chronica`: $\mathcal C_t = (o_1, a_1, \ldots)$, non-forkability asserted; probably also the claim that $\mathcal C_t$ is the maximal information object (everything the agent can ever know is a function of it). `#scope-adaptive-system`: $H(\Omega_t \mid \mathcal C_t) \gt 0$ as the residual-uncertainty condition; dynamics-in-$\Omega$ left implicit (my seg-02 question unresolved); I additionally now predict it will use the *state-inaccessibility* form, quietly weaker than the two opacity atoms — which would actually be the *correct* fix, leaving the strong atoms upstream as orphaned overclaims.

**7. What would I change.** One clause in the epistemic-opacity atom: "knows neither $h$ nor the distribution of $\varepsilon_t$ exactly" → "cannot recover $\Omega_t$ from observations — typically because $h$ and the noise law are themselves imperfectly known" (constitutive core = inaccessibility; strong form = typical case). This also fixes Kalman.

**8. Curious.** Whether $a_{t-1}$-in-$h$ gets structurally used (gold watch-item 471203/472913 — adopted). My sharper version: `#def-causal-information-yield` is the natural cash-in point; if CIY is defined over $T$-interventions only and ignores $h$-steering, active perception is decorative in the formalism.

**9. New knowledge.** The read-after-write framing from seg 03 completes here: mismatch is the round-trip residual through two opaque channels. Still absent from framing prose; still my best legibility candidate for Ch.3's opener.

**10. Process.** Gold-contrarian pass added (this file is its first instance). Working well — it caught one stale item and one overstatement in a single segment's gold.

**11. Running outline.** Ledger (v) upgraded (Kalman tension: two constitutive-opacity atoms, prior-flagged, unintegrated). New: (vi) eq-tag taxonomy gap for constitutive-restriction atoms [recurring]; legibility: this segment clean.

**12. Value-feel.** Moderate — competent workhorse segment; the interesting content (two loss sources, active perception) is present but under-narrated in the body relative to what the gold shows readers actually do with it.

**13. Field contribution.** Active perception as a first-class argument of the observation primitive (rather than a bolt-on "options" mechanism) is quietly ahead of standard POMDP pedagogy; the TST cash-out (tests as channel-engineering) is the memorable instance.

**14. Wandering thoughts.** *One:* the constitutive-opacity *triad* (info-loss / transition opacity / epistemic opacity) that 471203 wanted named — I'd go further: the triad is really *one* commitment stated three times at increasing resolution, and the repetition without a naming paragraph makes a linear reader wonder whether they're meeting new content. The chapter would read better with the triad named once, and each segment's atom marked as its channel-specific face. This is a legibility item of the exact kind Joseph asked me to watch for: the *structure* is clean but the reader has to reverse-engineer it.

*Two:* the noise term $\varepsilon_t$ enters as an argument of $h$ rather than additively — maximal generality (state-dependent, multiplicative, adversarial noise all covered). But nothing is ever assumed about $\varepsilon_t$'s relationship to $\varepsilon_{t-1}$ here; GA-1 (fresh noise) lives in NOTATION and gets invoked by the mismatch decomposition. A reader who meets $\varepsilon_t$ here and GA-1 three chapters later has no signpost that the noise's *temporal structure* is where a key global assumption will bite. One forward-pointing clause would pre-empt it.

*Three:* watching four prior substrates converge on the Kalman-opacity issue, have it recorded in the segment's own notes, and *still* find the body unchanged is the most instructive thing so far — it suggests the gold-lift pipeline's last mile (gold → body edit) is the weak link, or a deliberate queue I'll see in Phase 2. Either way it calibrates me: convergent flags in this corpus don't self-execute, and my FINAL should distinguish "newly seen" from "seen, recorded, and stalled" — the second category is arguably the more actionable one for Joseph.

**Depends-check next:** `#def-chronica` — expect `[def-agent-environment, def-action-transition, def-observation-function]`.
