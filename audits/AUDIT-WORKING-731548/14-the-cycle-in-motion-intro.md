# 14 — the-cycle-in-motion-intro (discussion, discussion-grade, draft)

**1. Predictions vs evidence.** As predicted at 09's pattern: the bridge is again the chapter's legibility backbone — the nine-segment flow is narrated once, in order, with the capacity-synthesis point (tempo) named. The chapter-intro discipline is now 2-for-2 in answering Joseph's legibility concern where it exists; Ch.1's lack of one is the anomaly.

**2. Cross-segment consistency.** Three registered items:
(a) *"Any rational adaptive process must approximate this functional form"* — a universal-necessity claim made in a discussion-grade intro on behalf of a segment whose type is **emp**irical. If `#emp-update-gain` supports "must," its type is wrong; if its type is right, the intro overclaims. One of the two must move. This is now my sharpest pending tier-consistency test (two segments ahead).
(b) *"Both are derived, not chosen"* — endorse the gold's conditional fix (derived *given* the completeness formulation, which is itself chosen). The intro's own Working Note says exactly the right thing ("joint consequences of #form-agent-model's completeness clause") — the body sentence just drops the conditional. Cheap fix, real difference: "derived, not chosen" is the framework's highest-status vocabulary and shouldn't be spent loosely.
(c) Dimensional check on the preview math: $\lVert\delta\rVert_{ss} = \rho/\mathcal T$ = [surprise·t⁻¹]/[t⁻¹] = [surprise] ✓; $\eta^\ast$ dimensionless ✓; $\mathcal T = \sum \nu\eta$ [t⁻¹] ✓. Consistent with NOTATION's units table.

**3. Math verification.** Preview-level only (verified above); the real derivations are downstream.

**Auditing the gold.** Sparse and disciplined; nothing to refute. The "linear-ODE-for-reading, sector-condition-for-proving" framing (384279) is the right articulation of the chapter's honest two-layer stance and deserves body placement. The Pearl-convention-upstream item (384279): partially overtaken — `#scope-agency`'s body now carries the import parenthetical inline (seen at 07), so the convention *is* stated at first use; the residual ask (FORMAT-level statement) exists in FORMAT.md's "Imported external machinery" section (seen in orientation). I'd mark this gold item **resolved** on both prongs — a third specimen of executed corrective edits, further weakening my early pipeline-bias hypothesis (updating: the pipeline executes; what stalls is a specific *class* — edits that weaken constitutive-sounding claims, e.g. the opacity atoms).

**4–6. Direction / errors / predictions.** Entering the chapter: predictions already registered at 13 for recursive-update, decomposition, gain, tempo. Adding: `#form-event-driven-dynamics` will formalize typed events at heterogeneous rates with $M_{\tau^\pm}$; the clock question from 05 (ordinal chronica vs metric event stream) comes due — where does wall-clock live, and is the chronica's event-index reconciled with $\tau$-timestamps? Watch also whether "the only way new information enters is through events" survives the consolidation machinery later (offline replay generates *pseudo*-events — `#form-consolidation-dynamics` per the OUTLINE — which either violates events-only or requires the event notion to include internal events; the intro's phrasing is the strict-external reading).

**7. What would I change.** The (a) and (b) one-clause fixes; otherwise leave it alone — it's good.

**8. Curious.** The intro calls tempo "the load-bearing capacity variable for the rest of the framework." Proportionality lens: tempo gets that title *and* a scalar-reduction caveat in NOTATION (the tensor treatment "requires a tempo tensor; scalar reduction valid when approximately isotropic"). If the central capacity variable is scalar-only in canon while the framework's own persistence sharpening (matrix-Loewner, appendix) shows per-direction structure matters, the center of the theory is coarser than its own appendices — the inverse of the usual failure. Checking when I reach `#def-adaptive-tempo`.

**9–11. Enables / process / outline.** Ledger: (xxxiii) intro's universal-necessity claim vs emp-type [test at gain]; (xxxiv) events-only information entry vs consolidation pseudo-events [watch]. §E note: the CIY-placement Working Note documents a *placement anomaly repaired by relocation* (Pearl recap moved to Part II) with the apologetic paragraph then rewritten declaratively — that's the prune-completed-narrative discipline working in miniature.

**12–13. Value-feel / contribution.** High/high — the HFT-vs-slow-deliberation tempo gloss in the gold (773921) is the best two-line teaching instrument in the chapter's orbit and should be in the body.

**14. Wandering thoughts.** *One:* the intro performs a quiet epistemics I want to name for the FINAL's §E: it states, in one page, which of the chapter's five claims is derived (recursion, action-locality), which is an identity (decomposition), which is empirical-with-exact-special-case (gain), which is definitional synthesis (tempo), and which is heuristic preview (ODE). That's five different epistemic tiers correctly signposted *in flowing prose* without a single tier-label — the register the monograph wants everywhere. The corpus's framing prose has learned to carry epistemic status *implicitly and accurately*; the remaining failures (like (a)) are where rhetorical lift ("must approximate") outruns the signposting.

*Two:* "the only way new information enters is through events" is the completeness assumption's dynamic face, and it has a subtle exception class the framework will need: *information that enters by computation* — the agent deriving consequences it already possessed but hadn't computed (proof, planning, consolidation). No event arrives, yet $M_t$'s *usable* content changes. Either the framework treats deliberation as a null-event self-transition (then tempo's event-rate framing undercounts internal work), or usable-content vs information-content is a distinction it needs and doesn't have. The deliberation-cost segment (Ch.4) is nearby territory; if it prices thinking time without an object for what thinking *adds*, this gap will show there. This might be the most consequential missing distinction in Part I — logical vs empirical uncertainty is exactly the axis on which LLM agents differ most from Kalman filters, and Volume 3 will feel its absence hard.

*Three:* meta — four prior audits reflected on this intro vs eleven on the definitional segments; chapter-intros are systematically under-audited because several walk-orders skip them. Yet intros are where tier-inflation risk concentrates (they *summarize with confidence*). If the SOP's row-order rule ("walk every row") isn't being followed uniformly, the priming-free layer (intros) that newcomers actually read first is the least-checked layer. Worth a §G sentence.

**Depends-check next:** `#form-event-driven-dynamics` — expect deps: def-chronica, form-agent-model.
