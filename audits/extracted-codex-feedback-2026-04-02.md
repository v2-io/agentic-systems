# Extracted Codex Feedback — 2026-04-02

**Source model:** OpenAI Codex (multiple rounds same day)
**Date:** 2026-04-02
**Sessions:** `615ef48a` (3 batches: midnight–early AM), `9f89ae5d` (evening), `14c96d33` (late night), `b40b551e` (late night)
**Topic:** Iterated Codex passes over the ACT (now AAD) core covering CIY mis-specification, Q_O conditioning vs intervention semantics, strategy-maintenance loop incompleteness, composition bridge-lemma debt, scope-boundary inconsistencies, and per-dimension-persistence regime mixing.

## Context

A heavy-feedback day. Joseph relayed at least four distinct Codex passes on 2026-04-02, each examining the segment corpus from a slightly different angle. The local Claude session synthesized these into `audits/analysis-2026-04-01-remaining.md`, `audits/analysis-2026-04-02-comprehensive.md`, `audits/analysis-2026-04-02-round2.md`, and `audits/analysis-2026-04-02-synthesis.md` — but those files contain Claude's verifications/responses, not the verbatim Codex text. Several of these findings (Q_O ambiguity, CIY mis-specification, strategy DAG forcing overclaim, composition bridge contraction) became foundational repair items.

## Disposition

These items drove the 2026-04-02 to 2026-04-06 strengthening cycle. By the comprehensive review (`analysis-2026-04-02-comprehensive.md`), nearly all numbered items here had been verified, with most marked Resolved (`Codex #1` through `Codex #6` shown struck through). Several items resurfaced in expanded form on 2026-04-06.

## Verbatim feedback Joseph relayed

### Batch 1 — Five-finding pass (`615ef48a` line 586, ts 2026-04-02T00:12:59Z)

> Codex just looked carefully at all of 01-act* again. Here are his current set of notes:
> • Findings
>
> 1. `structural-adaptation-necessity.md:24` derives a positive mismatch floor from model-class insufficiency by moving from "lost predictive information" to "systematic one-step mismatch." That step is not licensed by the current dependency chain: `mismatch-decomposition.md:40` explicitly says insufficiency implies positive one-step mean error only under an extra alignment assumption. As written, the result is overclaimed as exact; it is exact only with that extra assumption, or after reframing the theorem in terms of proper-scoring regret rather than one-step mismatch.
> 2. `per-dimension-persistence.md:22` gives a stochastic AR(1) stationary absolute-mismatch formula, but `per-dimension-persistence.md:28` then states a linear persistence threshold that does not follow from that formula, and it switches from per-dimension tempo in the summary `per-dimension-persistence.md:12` to raw gain $\eta_k$ in the derivation. The inconsistency is reinforced by `persistence-condition.md:57`, which still treats the extension as empirical and awaiting formal derivation. This section currently mixes deterministic-drift and stochastic-noise regimes and should not be marked exact in its present form.
> 3. `strategy-dag.md:85` correctly says the graph-forcing argument is still conditional and that the P3→Markov step is not proved, but `strategy-dag.md:95` immediately reverts to "the graph structure is forced." `graph-structure-uniqueness.md:163` is even clearer: acyclicity is exact, but DAG-with-Markov-property is still a sketch and prior equivalence claims were overstated. This is the main epistemic overreach in Section II: DAG strategy is strongly motivated, not yet theorem-grade.
> 4. `orient-cascade.md:30` makes the strategic-evaluation stages part of an "exact" cascade `orient-cascade.md:45`, but those later stages depend on `strategic-calibration.md:37`, which is discussion-grade because credit assignment and execution fidelity are unresolved `strategic-calibration.md:49`. The epistemic-first ordering is solid; the full five-step cascade is not exact until the strategic residual is typed and estimable.
> 5. The theory's audit trail is still inconsistent. `OUTLINE.md:64` omits `graph-structure-uniqueness.md` even though `strategy-dag.md:87` treats it as the main support file, while `OUTLINE.md:130` still lists a missing `linear-ode-approximation` segment. Separately, `FORMAT.md:13` says canonical ordering lives in `ACT-FULL.md`, but that file is not present. This does not break the math, but it makes the core materially harder to review and easier to overstate.
>
> Overall
> The project is in better shape than a typical theory repo: the `01-act-core/src` dependency graph is acyclic, declared dependencies resolve, and Section I has a reasonably coherent backbone. The weak points are not random; they cluster where the theory becomes most novel. Section I is mostly "conditional but honest." Section II's original machinery is promising, but the repo still promotes "strongly motivated" into "forced" too early. Section III then inherits those promotions and adds further modeling choices on top.
>
> The highest-value next pass is not new content. It is an epistemic cleanup pass: demote or conditionalize the four files above, propagate those demotions downstream, and separate deterministic-drift results from stochastic-noise results wherever persistence thresholds are stated. After that, the biggest remaining gap is an end-to-end worked example for the purposeful machinery in Section II, because right now the novel part of the theory is much less operationally validated than Section I.

### Batch 2 — Six-finding pass (`615ef48a` line 691, ts 2026-04-02T00:23:14Z)

> Excellent work. Committed. Here is the next batch of things noticed by Codex:
>
> • Findings
>
> - **High**: Section II's core action-value object is mathematically ambiguous between conditioning and intervention. `01-act-core/src/value-object.md:24` defines $Q_O$ as a conditional expectation with $a_t = a$, while `01-act-core/src/causal-hierarchy-requirement.md:21` says that same expression should be read as a Level-2 intervention. Because `01-act-core/src/satisfaction-gap.md:18` and `01-act-core/src/control-regret.md:18` are built on $V_O/Q_O$, the theory's main purposeful diagnostics are confounded unless you standardize on explicit `do(a)` semantics or an equivalent SCM convention.
> - **High**: The information-theoretic core conditions on future actions without fixing whose future policy generates them. `01-act-core/src/information-bottleneck.md:17` and `01-act-core/src/model-sufficiency.md:18` both use predictive information "given future actions," but unlike `01-act-core/src/value-object.md:28`, they never specify a continuation-policy convention. In practice that makes $S(M_t)$ and $F(M)$ policy-relative quantities treated as if they were policy-invariant structural properties.
> - **High**: The strategy-maintenance loop is not identified well enough to support the weight Section II puts on it. `01-act-core/src/strategy-dag.md:59` explicitly says the plan-confidence score systematically overestimates success under correlated failures; `01-act-core/src/strategic-calibration.md:30` requires execution-fidelity monitoring many agents will not have; `01-act-core/src/strategic-calibration.md:49` admits unresolved credit assignment; and `01-act-core/src/edge-update-via-gain.md:21` leaves the crucial $\text{signal}(o_t, i, j)$ undefined. Despite that, `01-act-core/src/orient-cascade.md:30` still treats this layer as the actionable route for revising $\Sigma_t$.
> - **Medium**: The "graph structure uniqueness" claim is stronger than the derivation supports. `01-act-core/src/graph-structure-uniqueness.md:13` says the four postulates "force" a DAG with the Markov property, but the key P3→Markov step is explicitly only a sketch and concedes alternative sparse factorizations may satisfy locality `01-act-core/src/graph-structure-uniqueness.md:67`. That means the DAG is currently a strong representation choice, not a proved unique consequence of the postulates.
> - **Medium**: Section III's macro-agent story depends on an underdefined admissibility layer. `01-act-core/src/composition-closure.md:33` defines closure defect over admissible projection and macro-dynamics classes that are still placeholders in important respects, and the result still depends on unspecified norms `01-act-core/src/composition-closure.md:128`. Downstream results such as `01-act-core/src/tempo-composition.md:18` and even `01-act-core/src/directed-separation-under-composition.md:51` acknowledge this, but they still rely on a macro-agent whose admissible projection space is not yet concretized.
> - **Medium**: The per-dimension persistence extension mixes regimes and then states a stronger theorem than it has earned. `01-act-core/src/per-dimension-persistence.md:22` derives a stochastic AR(1) mismatch formula in raw gain $\eta_k$, but the headline threshold is then given in deterministic persistence form, and the file itself admits that jump is not derived `01-act-core/src/per-dimension-persistence.md:34`. The issue is not honesty, it is placement: readers can easily mistake this for a theorem-sized upgrade to the core persistence condition.
> - **Low**: Dependency and reference hygiene is weaker than the project's "claim DAG" framing implies. `01-act-core/src/agent-model.md:5` omits chronica even though the formal expression uses it at `01-act-core/src/agent-model.md:18`; `01-act-core/src/directed-separation.md:5` omits scope-condition despite citing it in the derivation line and scope conditions `01-act-core/src/directed-separation.md:16` and `01-act-core/src/directed-separation.md:33`; `01-act-core/src/strategy-dag.md:53` relies on $V_{O_t}^{\min}$ before depending on satisfaction-gap; and several docs point at unresolved future slugs or files such as `01-act-core/src/observation-function.md:39`, `01-act-core/src/event-driven-dynamics.md:76`, `01-act-core/src/agent-identity.md:36`, `01-act-core/src/shared-intent.md:44`, `01-act-core/src/adaptive-tempo.md:37`, and `01-act-core/src/adversarial-destabilization.md:76`. That does not break the prose, but it does weaken the theory-as-proof-graph discipline.
>
> Assessment
>
> The strongest part of `01-act-core/` is Section I. The recursive-update, mismatch, gain, sector-condition, and persistence chain is comparatively coherent, and the documents are unusually honest about what is exact, conditional, heuristic, or just motivated.
>
> The theory becomes materially less secure when it crosses into explicit agency and composition. Section II has a good coarse diagnostic vocabulary, but the fine-grained strategy update mechanism is not yet closed. Section III has strong intuitions and some promising structure, but too many core objects remain admissibility- or regime-dependent to treat it as mathematically locked down.

### Batch 3 — Six-finding pass on CIY, scope, strategy, composition, DAG, status inflation (`615ef48a` line 746, ts 2026-04-02T00:36:13Z)

> Here is the latest fresh feedback from codex:
>
> • Findings
>
> 1. **High**: CIY is currently mis-specified for the role the theory gives it. The prose says CIY measures how much an action "reveals about causal structure," but the canonical definition is KL divergence between interventional outcome distributions under the current model, which measures action-conditioned separability, not expected learning value. That means an action can score high even when it teaches the agent nothing new because the agent already knows the distributions. This becomes load-bearing because the unified policy objective optimizes CIY directly. References: `01-act-core/src/causal-information-yield.md:13`, `:21`, `:63`, `01-act-core/src/worked-example-bandit.md:51`.
> 2. **High**: The scope boundary is internally inconsistent about whether ACT requires real interventional leverage or merely multiple nominal actions. The scope condition says binary choice is sufficient for Level 2 access, but the causal-structure segment says zero-coupling systems can still be in scope even though Level 2 vanishes, and the agent-spectrum/scope examples include passive Bayesian learners and Kalman filters that do not obviously satisfy the binary-action requirement. Section II then treats "the loop provides Level 2 data" as a load-bearing fact. Right now the theory has not cleanly chosen between "ACT studies adaptive systems with interventions" and "ACT studies adaptive systems with possible but not necessarily effective actions." References: `01-act-core/src/scope-condition.md:13`, `:24`, `:41`, `01-act-core/src/causal-structure.md:34`, `01-act-core/src/agent-spectrum.md:23`, `01-act-core/src/loop-interventional-access.md:13`.
> 3. **High**: The strategic half of the theory is not operationally closed. The edge-update rule is introduced before the signal function is defined; strategic calibration depends on credit assignment that the document explicitly does not solve; edge-update validity then adds unresolved identifiability and possible evidence double-counting with the prior $M_t$ update. The orient cascade is honest about this, but that honesty means the core strategy-maintenance loop is still an architectural sketch rather than a derived mechanism. Section I is much more mature than Section II. References: `01-act-core/src/edge-update-via-gain.md:21`, `:42`, `01-act-core/src/strategic-calibration.md:20`, `:49`, `01-act-core/src/edge-update-causal-validity.md:98`, `:106`, `01-act-core/src/orient-cascade.md:45`, `:66`.
> 4. **High**: Section III's composition theory depends on a bridge lemma that is not yet mathematically secure. The argument jumps from a continuous-time sector condition on mismatch correction to a discrete per-step contraction factor for the full macro update map, then uses that to derive bounded closure error and tempo-composition overhead. The document itself admits this extra contraction assumption is doing real work. Until that bridge is proved, the composition closure criterion and tempo inequality are promising formulations, not established results. References: `01-act-core/src/composition-closure.md:98`, `:100`, `:130`, `01-act-core/src/tempo-composition.md:37`, `:39`.
> 5. **Medium-High**: The strategy-DAG foundation is rhetorically stronger than the derivation currently supports. The uniqueness derivation says the postulates "force" a DAG with the Markov property, but later concedes that the crucial P3→Markov step remains conditional and still needs tightening against alternative local computation structures. The practical consequence is that the DAG is a strong and probably good representation choice, but not yet something the theory can honestly present as uniquely forced. References: `01-act-core/src/graph-structure-uniqueness.md:13`, `:181`, `:201`, `01-act-core/src/strategy-dag.md:96`.
> 6. **Medium**: There is some epistemic-status inflation in places where a modeling commitment is doing most of the work. The recursive-update result is presented as a unique exact derivation, but the appendix correctly notes that the key step is partly definitional because completeness of $M_t$ absorbs counterexamples. Likewise the per-dimension persistence segment explicitly says it mixes deterministic and stochastic regimes, while upstream text already uses it as the corrective generalization of scalar tempo. These are fixable presentation problems, but they matter because the theory is otherwise careful about epistemic labels. References: `01-act-core/src/recursive-update.md:13`, `01-act-core/src/recursive-update-derivation.md:154`, `01-act-core/src/per-dimension-persistence.md:34`, `01-act-core/src/adaptive-tempo.md:43`.
>
> Open Questions
>
> 1. Decide whether ACT's base scope is "adaptive systems with interventions" or "adaptive systems with action channels, even if ineffective." Until that is fixed, the causal hierarchy, loop-intervention claim, and example set pull in different directions. References: `01-act-core/src/scope-condition.md:41`, `01-act-core/src/causal-structure.md:34`, `01-act-core/src/pearl-causal-hierarchy.md:12`.
> 2. Decide whether CIY is supposed to be posterior information gain, action-distinguishability, or a practical exploration surrogate. The current document uses language from the first, a formula from the second, and policy interpretations closer to the third. References: `01-act-core/src/causal-information-yield.md:13`, `:21`, `01-act-core/src/worked-example-bandit.md:51`.
> 3. Decide whether Section II is intended as a formal theory of strategy revision or as a principled design sketch. Right now the document occasionally presents it as the former, while its own caveats describe the latter. References: `01-act-core/src/orient-cascade.md:66`, `01-act-core/src/strategy-persistence-schema.md:32`.
>
> Overall
> The strongest part of `01-act-core/` is Section I: the adaptive-loop framing, mismatch decomposition, gain intuition, and persistence story form a coherent core. The weakest part is the lift from epistemic adaptation into explicit strategy and then into composition; that layer has good architecture and good intuitions, but it is not yet closed at the level the prose sometimes claims.
>
> If I were hardening this next, I would do it in this order: replace CIY with an actual epistemic-value quantity, lock the scope boundary around intervention, and then either fully derive or explicitly downgrade the Section II/III strategy and composition machinery. That would make the theory materially more coherent without needing to rewrite its overall shape.

### Evening pass (session `9f89ae5d`, line 34, ts 2026-04-02T21:07:50Z)

> Add the following feedback from codex to your analysis, and then look at `msc/analysis-2026-04-01-remaining.md` and create a new synthesis that is your recommended path forward:
>
> • Findings
>
> 1. The flagship persistence theorem currently merges two different notions of persistence: structural stability of the correction dynamics and task-level adequacy of the model. In the main result, $\alpha > \rho / R$ is the Lyapunov-style structural condition, but it is then treated as interchangeable with $\mathcal{T} > \rho / \lVert\delta_{\text{critical}}\rVert$, where $\delta_{\text{critical}}$ is an operational performance threshold, not model-class capacity. The appendix even states that the linear case has $R \to \infty$, so structural stability there does not depend on $\delta_{\text{critical}}$ at all. That means many downstream uses of "persistence" are semantically unstable. See `01-act-core/src/persistence-condition.md:11` and `01-act-core/src/sector-condition-derivation.md:199`.
> 2. Adaptive tempo is currently defined in a way that can overcount redundant information. The theory sums $\nu^{(k)} \cdot \eta^{(k)}$ across channels, and then Section III adds communication tempo in the same additive way, but there is no overlap penalty for correlated sensors, repeated teammate reports, or otherwise redundant evidence. Because tempo is the core capacity variable, this can inflate persistence margins and adversarial advantages whenever channels are not informationally independent. See `01-act-core/src/event-driven-dynamics.md:59`, `01-act-core/src/adaptive-tempo.md:18`, and `01-act-core/src/team-persistence.md:20`.
> 3. The loop-to-Level-2 argument still slides between "the agent executed an intervention" and "the agent has identified an interventional effect." The files correctly note that confounding, delay, and partial observability can block clean identification, but then still use stronger examples like Q-learning converging to $E[R \mid s, do(a)]$ as if agent-generated action data were enough by itself. That weakens one of Section II's load-bearing claims: the loop certainly creates intervention-generated data, but ACT has not yet shown that this yields identified causal quantities in the partially observed cases it most cares about. See `01-act-core/src/loop-interventional-access.md:21` and `01-act-core/src/causal-hierarchy-requirement.md:21`.
> 4. The "exact" diagnostic core around satisfaction gap and control regret is underdetermined until the theory fixes a solution concept. $A_O$ depends on policy class $\Pi$, horizon $N_h$, and especially the continuation convention, and value-object explicitly says ACT does not prescribe one. That means $\delta_{\text{sat}}$ and $\delta_{\text{regret}}$ are mathematically well-defined only pointwise under a chosen convention, not yet canonical diagnostics that can be compared cleanly across agents or even across analyses of the same agent. See `01-act-core/src/value-object.md:28`, `01-act-core/src/satisfaction-gap.md:18`, and `01-act-core/src/control-regret.md:18`.
> 5. The strategy representation is still less closed than the outline suggests. `strategy-dag` uses the satisfaction criterion $V_{O_t}^{\min}$ before that object is formalized, and the outline currently orders `strategy-dag` before `satisfaction-gap` despite the dependency running the other way. More importantly, the uniqueness derivation still admits that the load-bearing P3→Markov step is not actually proved, so the claim that the DAG-with-Markov-property is "forced" remains conditional rather than established. See `01-act-core/src/strategy-dag.md:54`, `01-act-core/OUTLINE.md:78`, and `01-act-core/src/graph-structure-uniqueness.md:67`.
> 6. The composition bridge lemma imports an extra assumption that the current ACT assumptions do not buy you. The lemma moves from a sector condition on macro mismatch correction to contraction of the full macro state update map, and then to bounded trajectory error. The file itself later admits that this implication only holds when correction dominates the state update, which is an added regime assumption rather than a consequence of A4. So the central move from "closure defect is small" to "macro-agent tracks micro-reality" is not yet a derived theorem of the published framework. See `01-act-core/src/composition-closure.md:118`.
> 7. ACT is still internally inconsistent about passive observers. `scope-condition` explicitly excludes passive estimation and even says a Kalman filter estimating a passive signal is out of scope, but `agent-spectrum` still places "passive Bayesian learner" in the adaptive-tracker quadrant and says Section I fully describes that region. You need one crisp answer here: either Section I is only about active adaptive systems, or passive trackers remain in scope for a subset of claims. Right now both positions appear in the core. See `01-act-core/src/scope-condition.md:30` and `01-act-core/src/agent-spectrum.md:23`.
>
> Open Questions
>
> - Is persistence meant to name structural stability, operational viability, or task adequacy? The current text uses all three, and the theory needs one primary sense plus explicit secondary terms.
> - Do you want ACT to exclude passive trackers entirely? If yes, several examples and parts of the agent spectrum should stop talking as if Section I covers them directly.
> - Should Section II adopt a canonical continuation convention, even if only as a default? Without that, the satisfaction-gap/control-regret pair stays formally clean but operationally under-specified.
>
> Assessment
> The strongest part of `01-act-core/` is Section I's disturbance/stability machinery once it is read through the more careful appendix language rather than the headline formulations. The disturbance-model split, sector-condition derivation, and simulation-backed regime separation are the most defensible part of the core. The cleanest conceptual contribution in Section II is the satisfaction-gap/control-regret split, but the actual strategy-maintenance loop is still openly incomplete. Section III has promising structure, especially around closure and coordination cost, but it is still more a research program than a closed theorem stack.

### Late-night Codex (session `9f89ae5d`, line 477, ts 2026-04-02T21:49:00Z) — paired with a fresh Claude review

> Here they are, first codex and then a fresh Claude instance. I recommend updating the analysis as you see fit (or creating a new one), and then we can discuss next actions.
>
> ---
>
> • 1. The foundational scope is internally inconsistent. `01-act-core/src/scope-condition.md:19`, `:38`, `01-act-core/src/agent-spectrum.md:43`. The top-level domain says ACT requires binary action with genuine causal effect, but the scope notes and Section I framing then put passive trackers back into scope for mismatch/gain/tempo/persistence. That leaves it unclear whether those results are ACT results or a broader precursor theory that ACT later specializes. This is not just editorial: downstream statements that claim validity "within ACT's scope" inherit the ambiguity.
> 2. The formal statement of the causal-hierarchy requirement drops the `do(.)` operator exactly where the theorem depends on it. `01-act-core/src/causal-hierarchy-requirement.md:21`, `01-act-core/src/value-object.md:24`. The prose correctly says $Q_O$ is interventional, but the displayed equation in `causal-hierarchy-requirement` uses ordinary conditioning ($a_t = a$). That collapses the distinction the segment is trying to prove. Unless you add an explicit ignorability/sufficiency condition, the formal expression needs to match value-object and use $do(a_t = a)$.
> 3. The manuscript still oscillates between "the strategy DAG is forced" and "the key P3→Markov step is still a sketch." `01-act-core/src/graph-structure-uniqueness.md:161`, `:186`, `:202`, `01-act-core/src/strategy-dag.md:95`. Acyclicity is proved. Parent-conditional Markov factorization is not. Those two need to be separated much more strictly, because Section II currently reads in places as if the full DAG-with-Markov package is theorem-strength when your own appendix says the central step is still open.
> 4. "The loop gives interventional data" is stated too strongly relative to the later admissibility machinery. `01-act-core/src/loop-interventional-access.md:21`, `:33`, `01-act-core/src/ciy-observational-proxy.md:30`, `01-act-core/src/edge-update-causal-validity.md:27`. Acting in the world produces physically interventional episodes, but it does not automatically produce identified estimates of $P(o \mid do(a))$. Your later Regime A/B/C and C1-C3 conditions are the right discipline. The earlier exact claim should be narrowed to "the loop generates action-conditioned outcome data with potential interventional content," otherwise the theory blurs intervention, exploration policy, attribution, and identification.
> 5. Section III still contains a stale blocker that no longer matches the manuscript state. `01-act-core/src/directed-separation-under-composition.md:55`, `01-act-core/src/composition-closure.md:56`, `:90`. `directed-separation-under-composition` says the admissibility classes are still placeholders, but `composition-closure` now gives A1-A4 and P1-P3. That makes the composition argument look less mature than it actually is and muddies what the real remaining gap is: validation and computability, not missing definitions.
>
> Assessment
> The strongest part of the core is Section I. The chronica/model/update chain is coherent, the gain/tempo vocabulary is useful, and the persistence split between structural persistence and task adequacy is the cleanest and most defensible contribution in the folder. The exact Kalman mapping in `01-act-core/src/worked-example-kalman.md:21` is a real strength because it shows the machinery cashes out in a canonical domain instead of staying purely rhetorical.
>
> The weakest part is still the Section II strategy-maintenance loop, and your own text is candid about that. The unresolved pieces are the signal function for edge revision, credit assignment, identifiability at depth, correlated failure, and the missing "tempo/cost of $\Sigma_t$" gaps in `01-act-core/OUTLINE.md:88`. Section III is promising, but it is still more formulation than closure theorem until the composition machinery is exercised on a purposeful multi-agent example rather than mostly estimation-style toy cases.
>
> What I'd tighten next
>
> 1. Split scope cleanly into an adaptive-core scope and a narrower agency/causal-learning scope.
> 2. Standardize every interventional formula on explicit `do(.)` notation and reserve "identified causal estimate" for Regime A type settings.
> 3. Demote all "forced graph structure" language to "acyclicity derived, Markov factorization conditional" until P3 is actually formalized.
> 4. Add one worked example where $\Sigma_t$ genuinely updates over time; right now the exact core is much better grounded than the strategy core.
> 5. Add one purposeful composite worked example to test whether the Section III admissibility machinery survives contact with real $G_t$, routing, and coordination.

### Late-night Codex (session `9f89ae5d`, line 1016, ts 2026-04-02T23:33:53Z)

> Excellent work. Some current feedback on the project as it is (codex and then claude) coming up. I wonder if we have too much sitting in the spikes still instead of in the segments and appendices...:
>
> • Findings
>
> 1. **High**: The nonlinear persistence result is not exact as written. A2' is explicitly local, applying only inside radius $R$, but the proof then concludes $\dot V < 0$ "outside the ball $B_{R^*}$" and promotes $\alpha > \rho/R$ to an exact threshold without stating an initial-condition or invariance requirement. `01-act-core/src/sector-condition-derivation.md:56`, `:106`, `01-act-core/src/sector-condition-stability.md:36`, `01-act-core/src/persistence-condition.md:100`. As written, this is a strong local-stability argument, not yet a globally exact persistence theorem for arbitrary trajectories.
> 2. **High**: The claimed bridge from credence error to $\delta_{\text{strategic}}$ does not actually bridge to the diagnostic ACT defines. `strategic-calibration` defines $\delta_{\text{strategic}}$ as an L2 aggregation of per-edge value-increment residuals, but Proposition B.5 replaces that with $\delta_s = P_\Sigma(p) - P_\Sigma(\theta)$, i.e. plan-confidence error, and then treats it as the operational mismatch. `01-act-core/src/strategic-calibration.md:27`, `:46`, `01-act-core/src/strategic-dynamics-derivation.md:290`, `:297`, `01-act-core/src/strategy-persistence-schema.md:50`. The $J^{-T}$ term in the bridge setup is another sign the algebra is not fully sound for the many-to-one scalar case. `01-act-core/src/strategic-dynamics-derivation.md:311`.
> 3. **High**: Even on its surrogate mismatch state, the strategy-persistence appendix does not produce a persistence theorem over time. The file itself says $\alpha_\Sigma$ shrinks as evidence accumulates, the sector framework assumes constant $\alpha$, and the current result is only an instantaneous check at the current experience level. `01-act-core/src/strategic-dynamics-derivation.md:394`, `:398`, `01-act-core/src/strategy-persistence-schema.md:34`, `:44`. That is materially weaker than the surrounding language suggesting the schema is mostly resolved.
> 4. **Medium**: The strategy representation is not yet forced at the Markov/factorization level. The acyclicity result is good, but the same derivation that states the Bayesian-network conclusion still marks "P3 implies Markov" as the critical open question. `01-act-core/src/graph-structure-uniqueness.md:117`, `:157`, `:203`, `01-act-core/src/strategy-dag.md:96`. The right claim today is "DAG is strongly motivated and acyclicity is derived," not "DAG-with-Markov is mathematically forced."
> 5. **Medium**: Section II is not yet an operational theory of strategy revision, only a dependency ordering over partially specified pieces. The cascade explicitly says steps 3-5 inherit discussion-grade status, and the strategy-maintenance loop still depends on an undefined signal function, unresolved credit assignment, and unformalized execution-fidelity monitoring. `01-act-core/src/orient-cascade.md:46`, `:67`, `01-act-core/src/edge-update-via-gain.md:41`, `:43`, `01-act-core/OUTLINE.md:61`. This makes Section II diagnostically interesting, but not yet a closed update theory.
>
> Open Questions / Residual Risks
>
> - The clean Section I → Section II lift only applies exactly to Class 1 modular agents. The theory itself classifies transformer LLMs as Class 2, where directed separation fails and the sequential cascade becomes only an approximation. `01-act-core/src/directed-separation.md:53`, `:72`, `:85`, `01-act-core/README.md:18`.
> - Validation is mostly internal. The repo is a theory corpus with no executable tests in `01-act-core/`; the worked examples and simulations validate the math inside toy models, and the simulation appendix explicitly says that external domain validity remains open. `01-act-core/src/simulation-results.md:86`.
> - A lot of confidence still lives outside the core folder in `msc/spike-*` documents. That weakens self-containment for a directory presented as the mathematical core.
>
> Overall Read
> Section I is the strongest part of the corpus, but it currently overstates theorem strength around the local-sector persistence result. Section II has several genuinely strong definitions, especially the $O_t / \Sigma_t$ split and the satisfaction-gap / regret diagnostic, but the actual strategy-update machinery is still too incomplete to support the current persistence and "mature derivation chain" framing. Section III reads mostly as promising formulation rather than closed theory.

### Late-night Codex (session `14c96d33`, ts 2026-04-02T01:20:59Z)

> The following is fresh-eyes feedback from a codex agent looking at the same snapshot:
>
> • Findings
>
> - The main structural gap is still Section II's strategy-update loop. The corpus explicitly says the orient cascade's ordering is exact but its strategy-maintenance content is not, because $\delta_{\text{strategic}}$ depends on unresolved credit assignment and execution-fidelity monitoring, the edge-update signal(...) is undefined, and strategy persistence is still only a schema rather than a proved result. `01-act-core/src/orient-cascade.md:45`, `:66`, `01-act-core/src/strategic-calibration.md:39`, `:49`, `01-act-core/src/edge-update-via-gain.md:40`, `01-act-core/src/strategy-persistence-schema.md:32`.
> - Section III is promising, but it is not yet strong enough to fully cash out the early scale-invariance claim. The macro-agent is only defined relative to admissibility choices, load-bearing norms are still unspecified, projection admissibility is not formalized, and one of the core composition notes still says claims about the composite are about a "not-yet-fully-defined object." That means composition consistency cu[...]
