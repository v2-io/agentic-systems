# Reflection: scope-adaptive-system

**Segment:** `#scope-adaptive-system`

## What this does

Defines the broadest scope: residual uncertainty + observation channel. Two clean conditions that are necessary and sufficient for Section I machinery to be non-vacuous.

The mathematical formulation is elegant: $H(\Omega_t | \mathcal{C}_t) > 0$ captures "genuine uncertainty persists after all observations." This is the right condition.

## Naming relevance

Tracker row 191 is "adaptive system" with no alternatives listed. This seems like a settled naming — "adaptive system" is exactly what the segment defines. 

The segment name itself is "scope-adaptive-system" — the role-prefix "scope" plus subject-noun "adaptive-system." The subject-noun is good.

## What I notice about the scope structure

The Discussion's exclusion of "pure computation" (mathematical proof engines) is philosophically interesting — this is a real boundary case that shows the theory knows its limits. AAD doesn't apply to systems with no agent-environment boundary.

## Forward prediction

scope-agency will narrow to binary choice + causal effect. Then we'll move through the postulates.
