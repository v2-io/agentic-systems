---
slug: def-adaptive-tempo
type: definition
status: conditional
depends:
  - emp-update-gain
  - form-event-driven-dynamics
stage: claims-verified
---

# Definition: Adaptive Tempo

The framework's central capacity quantity: **adaptive tempo** $\mathcal{T}$, the effective rate at which an agent acquires useful information from its environment, given as the sum across all observation channels of *channel event rate times optimal update gain* on that channel. In the single-channel case, tempo is simply event-rate times gain. A tensor extension is defined: in the Fisher-local regime where gains are matrix-valued, tempo becomes a matrix sum whose eigenvalues give per-direction adaptation rates; the scalar form is recovered in the shared-eigenbasis collapse (always in 1-D; under (PI)/Čencov along the natural-gradient direction in higher dimensions).

Tempo combines two factors that are *both* load-bearing and *substitutable*: loop **speed** (event rate, how fast the agent cycles) and update **quality** (gain, how well it extracts information per event). An agent can achieve a given tempo via a fast noisy loop or a slower calibrated one; improvements to both factors compound multiplicatively. Tempo is the load-bearing capacity variable for the rest of the framework — it appears on the left-hand side of the persistence condition ( #result-persistence-condition), determines adversarial advantage ( #result-adversarial-tempo-advantage), and connects to code quality as observation infrastructure in the software-domain instantiation. The Discussion below treats the speed-quality substitutability, the observation-noise gating that grounds Boyd's emphasis on Orient quality over raw OODA speed, and the two scope caveats (independent-channel assumption, isotropy) that the tensor form and the per-dimension persistence result address.

## Formal Expression

*[Definition (adaptive-tempo)]*

$$\mathcal{T} = \sum_k \nu^{(k)} \cdot \eta^{(k)*}$$

where:
- $k$ indexes the agent's distinct observation channels
- $\nu^{(k)}$ is the event rate on channel $k$
- $\eta^{(k)\ast}$ is the optimal update gain on channel $k$ ( #emp-update-gain)

Single-channel special case: $\mathcal{T} = \nu \cdot \eta^\ast$.

### Tensor extension under Fisher-local invariance regime

*[Definition (tensor-adaptive-tempo)]*

Under the Fisher-local invariance regime ( #deriv-fisher-local-update-gain), the optimal update gain on channel $k$ is matrix-valued: $K^{(k)} = (H_M + H_L^{(k)})^{-1} H_L^{(k)}$, with $H_M = U_M^{-1}$ the prior precision and $H_L^{(k)} = (U_o^{(k)})^{-1}$ the channel-$k$ observed Fisher information. The tensor adaptive tempo is then

$$\mathcal{T} = \sum_k \nu^{(k)} \cdot K^{(k)}$$

— matrix-valued, with per-direction rates given by the eigenvalues of $\sum_k \nu^{(k)} K^{(k)}$ in the appropriate basis. The scalar form $\mathcal{T} = \sum_k \nu^{(k)} \cdot \eta^{(k)\ast}$ is recovered in the **shared-eigenbasis collapse**: when all $H_M, \{H_L^{(k)}\}$ commute (always in 1-D; under (PI)/Čencov along the natural-gradient direction in higher dimensions), each $K^{(k)}$ acts as the eigenvalue $\eta^{(k)\ast} = U_M/(U_M + U_o^{(k)})$ on the shared natural-gradient direction and the matrix sum collapses to a scalar.

The matrix gain operator $K^{(k)}$ is the per-coordinate primitive: in anisotropic regimes where the prior and likelihoods do not share an eigenbasis (or where different channels pin down different directions), the tensor form preserves the per-direction information that the scalar form averages away.

## Epistemic Status

This is a *definition*, held at `conditional`. It names the quantity that characterizes an agent's total corrective capacity, combining loop speed ($\nu$) and epistemic quality ($\eta^\ast$). The stipulation itself carries no truth-claim, but the definition is advertised as an *operationalization* — $\mathcal{T}$ as the effective rate of useful corrective-information acquisition — and that interpretation holds exactly only under two explicitly named local conditions: **(1) cross-channel noise independence** — independence of the channels' observation noises across channels and events, under which additivity is *derived* exact ( #deriv-tempo-additivity; independence is sufficient but not necessary — equality also holds on a measure-zero cancellation set). When noise independence fails the deviation is signed: the additive form overcounts in the common-source (echo-chamber) regime, where the redundancy penalty is derived in closed form and persistent shared bias forces information saturation, but *under*counts in noise-cancelling (synergistic) configurations — so the additive form is not in general an upper bound (Discussion §Channel noise-independence condition); and **(2) shared eigenbasis / isotropy** for the scalar form — outside it the tensor form above is the correct object (Discussion §Scalar vs. vector tempo). Under both conditions the additive scalar form is the exact operationalization; neither condition is establishable from the global dependency chain — they are properties of the application — so the tier is `conditional` rather than `exact`. The substantive claims are in the results that use it ( #result-persistence-condition, #result-adversarial-tempo-advantage), which inherit the same scope.

**Scope of scalar vs. tensor forms.** The scalar form is exact in the isotropic / shared-eigenbasis / nonredundant-channel case and is what downstream results currently invoke. The tensor form is the natural object under anisotropic gains, Fisher-whitened updates ( #deriv-fisher-whitened-update-rule), LMI causal-IB ( #deriv-causal-ib-lmi), and per-dimension persistence ( #result-per-dimension-persistence) — regimes where scalar tempo overestimates effective adaptation along weak dimensions. Downstream results that invoke scalar $\mathcal{T}$ implicitly assume scalar / isotropic / nonredundant-channel scope; promoting them to the tensor form under the appropriate anisotropic regime is a follow-on cycle item flagged in `TODO.md`.

## Discussion

**Speed-quality substitutability.** An agent can achieve the same tempo via a fast noisy loop (high $\nu$, low $\eta^\ast$) or a slower calibrated one (low $\nu$, high $\eta^\ast$). The product structure means improvements to *both* factors compound multiplicatively.

**Observation noise gating.** Because $\eta^\ast = U_M / (U_M + U_o)$, high observation noise ($U_o$) depresses gain and collapses tempo regardless of loop speed. You cannot outrun a bad observation channel by iterating faster. This grounds Boyd's emphasis on Orient quality over raw OODA speed.

**Centrality.** Tempo is AAT's core capacity metric. It appears on the left side of the persistence condition ( #result-persistence-condition), determines adversarial advantage ( #result-adversarial-tempo-advantage), and connects to code quality as observation infrastructure ( #der-code-quality-as-observation-infrastructure — cross-component reference, see `02-tst-core/`) in the software domain. The strategic analog $\mathcal{T}_\Sigma$ ( #def-strategic-tempo) extends the same structure to strategy-edge revision, with the key difference that strategic edge rates are endogenous (depend on action policy and upstream success).

**Temporal nesting.** Adaptive processes stratify by timescale, with convergence constraints between levels ( #der-temporal-nesting).

**Mismatch dynamics.** The evolution of mismatch over time is governed by the balance between correction (via tempo) and disturbance ($\rho$) ( #hyp-mismatch-dynamics).

**Channel noise-independence condition.** The additive formula is exact when the channels' observation noises are independent across channels and events — Fisher information over conditionally independent observations is exactly additive ( #deriv-tempo-additivity). When channel noises are dependent, the deviation from additivity is **signed**: in the *common-source* regime (a shared persistent bias contaminating several channels — overlapping sensors, repeated teammate reports drawing on one upstream source, redundant telemetry) the additive formula overcounts, with a closed-form redundancy penalty and, under persistent shared bias, outright *saturation* — no number of common-source channels buys information past the shared-bias floor; but correlated noise can also be *cancelled* (a correlated reference channel, anti-correlated errors), in which case the additive formula **under**counts, so the additive form is not a conservative bound in general. No scalar dependence measure — in particular the conditional mutual information $I(e^{(1)}; e^{(2)} \mid M_{\tau^-})$ — can carry the exact correction (it is sign-blind; it survives as a witness of potential non-additivity), and no convention-free per-channel attribution of the deviation exists for three or more channels; the exact joint object and the derivations are in #deriv-tempo-additivity. Since tempo is the core capacity variable (persistence condition, adversarial dynamics, composition), mis-modeled channel structure inflates margins in the common-source direction — and hides genuine triangulation capacity in the synergistic direction. Multi-agent composition ( #der-team-persistence) inherits both directions: communication tempo overcounts when allies report from a shared source and is super-additive when allies' errors anti-correlate.

**Scalar vs. vector tempo.** The scalar $\mathcal{T}$ assumes isotropic correction capacity. When the agent corrects some dimensions faster than others, scalar tempo overestimates effective adaptation along weak dimensions. *[Empirical Claim]* Simulation confirms: in an anisotropic 3D system (gain varying 5:1), scalar $\rho/\mathcal{T}$ overestimated by 72%, with the weak dimension accounting for 84% of total mismatch ( #obs-section-i-validation-simulations). The correct formulation is per-dimension: $\mathcal{T}_k \gt \rho_k / \delta_{\text{critical},k}$ ( #result-per-dimension-persistence). The tensor extension above ( #deriv-fisher-local-update-gain) gives the per-coordinate primitive $K^{(k)}$ that the per-dimension persistence result invokes — the matrix gain operator on each channel — making the per-dimension formulation a direct consequence of the tensor tempo definition rather than a separate generalization. Under cross-dimensional correction (off-diagonal $\mathcal{T}$ in the coordinate basis of $D_\delta$), the matrix-Loewner persistence condition `#deriv-matrix-persistence-condition` is the canonical form: $\Sigma_\infty \prec D_\delta = \mathrm{diag}(\delta_{\text{critical},k}^2)$ in the strict positive-definite order, with $\Sigma_\infty$ solving the continuous Lyapunov equation $\mathcal{T}\Sigma_\infty + \Sigma_\infty\mathcal{T}^T = \Sigma_w$. The per-coordinate form is its diagonal-axis-aligned special case; matrix-Loewner is strictly sharper (the per-coordinate form is *unsafe* when $\mathcal{T}$'s eigenbasis misaligns with the coordinate axes — `#deriv-matrix-persistence-condition` §"Where per-coordinate is unsafe").

## Working Notes

### Incidental audit gold (lift 2026-05-30)

Cross-audit "wandering thoughts" / §14-ideation harvested from the de-novo auditors' working dirs, deduplicated across substrates and attributed by substrate + audit. Orthogonal pedagogical/generative material, kept separate from certified theory-fix findings (handled elsewhere). **Coverage:** 11 of the 14 contributing audit dirs reached a digested reflection on this segment (193847, 266847, 361742, 384279, 471203, 526815, 584721, 742613, 773921, 829314, 849201) plus the batched 963715 (19–23 batch) and 451729 (batch-05); 472913, 613842 did not file a dedicated note here. Substrate attribution inferred from voice where not explicit.

#### Candidate Brief prose / pre-prose

- **"You cannot outrun a bad observation channel by iterating faster."** The segment's own sentence, flagged by nearly every substrate as the standout pedagogy: because $\eta^\ast = U_M/(U_M+U_o)$, high $U_o$ collapses $\eta^\ast$ and therefore $\mathcal{T} \to 0$ regardless of $\nu$ — Boyd's "Orient quality dominates raw OODA speed," made derivable rather than heuristic (Claude, AUDIT-WORKING-193847; Claude, AUDIT-WORKING-849201; Claude, AUDIT-WORKING-829314; Claude, AUDIT-WORKING-471203; Codex/Claude, AUDIT-WORKING-384279). Already in the body; flagged as the chapter's strongest Brief/README anchor.
- **Tempo = speed $\times$ quality, the agent's "metabolism for learning" / "power metric."** $\mathcal{T} = \sum_k \nu^{(k)}\eta^{(k)\ast}$ "unifies hardware speed ($\nu$) with epistemic quality ($\eta^\ast$), allowing apples-to-apples comparison between completely different agent architectures" — an RL agent (fast, low gain), a human expert (slow, high gain), a dev team (mixed) (Claude, AUDIT-WORKING-193847; Claude, AUDIT-WORKING-829314 — "the agent's metabolism for learning").

#### Candidate Discussion

- **The agile-sprint critique (TST concrete anchor).** "A devastating critique of naive Agile: if a team's code quality is terrible (high $U_o$ — flaky tests, hard-to-reproduce bugs), $\eta^\ast$ collapses toward zero, so no matter how many two-week sprints ($\nu$) they run, effective tempo $\mathcal{T}$ stays near zero — they iterate fast but learn nothing. The framework proves you must fix the observation channel (reliable tests, telemetry) *before* increasing sprint cadence, because $\nu$ is multiplied by $\eta^\ast$" (Claude, AUDIT-WORKING-829314). Candidate worked-example anchor for the observation-noise-gating Discussion.
- **The CEO-echo-chamber gloss for the redundancy penalty (organizational anchor).** "If a CEO asks three VPs for a report and all three draw from the same broken dashboard, they have three high-frequency channels that are perfectly correlated; the additive formula $\sum \nu\eta^\ast$ would falsely report excellent tempo. The redundancy penalty formally captures organizational echo chambers — you need structurally independent channels (automated telemetry + anonymous surveys + customer interviews) to legitimately sum their tempos" (Claude, AUDIT-WORKING-829314; Claude, AUDIT-WORKING-266847 — "diversity of observation perspective matters, not just count of observers").

#### Follow-up items

- **Status `exact` vs the segment's own overcounting caveats.** Several substrates note the tension: frontmatter/Formal-Expression present the additive scalar $\mathcal{T}$ unconditionally as `exact`, yet the segment's *own* Discussion states the conditions (channel independence; isotropy) under which it overcounts. Candidate repairs: make the Formal Expression conditional ("under channel independence and scalar/isotropic reduction"), define an upper-bound $\mathcal{T}_{\text{add}}$, or move caveats to Epistemic Status and set `conditional` (Codex/Claude, AUDIT-WORKING-742613 "candidate finding I"; Codex/Claude, AUDIT-WORKING-526815 — separately flags that "rate of *useful information acquisition*" is loose wording since $\eta^\ast$ is a correction fraction not an information payload; "quality-adjusted correction event rate" may be more accurate). The segment's defense ("the definition is not a truth-claim") is noted but several push back that an advertised "effective rate" that overcounts is a *conditional operationalization*, not just a naming choice. *Resolved 2026-07-14 (adjudication):* status set to `conditional` with both conditions (channel independence; shared-eigenbasis/isotropy) named in Epistemic Status — matching `def-strategic-tempo` and the `conditional` tier definition (conditions are application properties, not establishable from the depends chain). The upper-bound restatement ($\mathcal T_{\text{add}}$ with $\mathcal{T} \leq \mathcal T_{\text{add}}$ as the exact Formal Expression) was considered and deferred, not rejected. *Resolved 2026-07-15 (derivation cycle, `#deriv-tempo-additivity`):* the deferral hardened into a refutation — the inequality is **false in general** (the deviation from additivity is signed; noise-cancelling configurations make the additive form an *under*count), so the $\mathcal T_{\text{add}}$-as-upper-bound re-symboling is closed as a non-move and the recorded "upgrade path back to `exact` for the bound form" is closed as refuted. The bound *is* exact on the common-source (echo-chamber) scope with a closed-form penalty; condition (1) is now derived rather than asserted; the definition's honest tier remains `conditional` (the conditions are application properties).
- **Formalize the redundancy penalty, don't just bound it.** (Claude, AUDIT-WORKING-471203; Claude, AUDIT-WORKING-829314.) *Discharged 2026-07-15:* derived in `#deriv-tempo-additivity` — exact signed deviation $\Delta$ via joint Fisher information; closed-form penalty $R_s$ on the common-source scope; saturation theorem under persistent shared bias; plus two no-gos (the CMI-penalty form is impossible — sign-blind measures cannot carry the signed correction; no convention-free per-channel attribution for $n \geq 3$, the PID obstruction). Spike trail: `spikes/spike-adaptive-tempo-redundancy-penalty-2026-07-15.md`. Ripple sites recorded there, not yet executed: `disc-independence-audit` §3 still states the refuted general inequality; `der-team-persistence` and `def-strategic-tempo` inherit the corrected two-sided reading.
- **Speed-quality substitutability is regime-bounded.** "Same tempo via fast-noisy or slow-calibrated" holds only within the same information-direction / channel-independence regime — a fast *redundant* channel cannot substitute for a slow *non-redundant* one, and a high-gain channel in the wrong dimension cannot substitute for low gain in the bottleneck dimension (Codex/Claude, AUDIT-WORKING-742613; Claude, AUDIT-WORKING-584721 — watch downstream claims that treat the substitution as full equivalence, since *persistence* may not be invariant to which side you're on). Also: "(Descended from TF-11.)" is the recurring diff-voice lineage tag (Claude, AUDIT-WORKING-471203; Claude, AUDIT-WORKING-829314) — certified-track cleanup.

#### Readers often ask / wonder

- "Does the redundancy penalty force agents to seek *diverse, uncorrelated* allies to maximize composite tempo, rather than just more allies?" (Claude, AUDIT-WORKING-193847 — multi-agent design consequence; Claude, AUDIT-WORKING-584721 ties this to `#der-team-persistence` overcounting).
- "How is $\rho$ (environmental change rate) formally defined, and does it have units compatible with $\mathcal{T}$ for the persistence condition to make dimensional sense?" — a near-universal next-question at this segment, answered in `#hyp-mismatch-dynamics` / the persistence segments (Claude, AUDIT-WORKING-829314; Claude, AUDIT-WORKING-849201; Claude, AUDIT-WORKING-773921).

#### Candidate figures

- **A capacity-funnel diagram**: raw channel rates $\nu_k$ enter; gain $\eta_k$ gates each; a redundancy stage subtracts overlap ($I(e^{(i)};e^{(j)} \mid M_{\tau^-})$) *before* summing to effective tempo; then a split into scalar vs tensor outputs shows why a single number can overstate the weakest direction (Codex/Claude, AUDIT-WORKING-526815). The figure should make "more telemetry $\neq$ more adaptation" visible. *(Promotion caution, 2026-07-16: the subtract-CMI-overlap stage as drawn is refuted by #deriv-tempo-additivity no-go N1 — sign-blind measures cannot carry the correction, and the deviation can be negative. A promoted figure needs the joint-covariance form, or restriction to the common-source regime where subtraction is directionally right.)*

#### Belongs elsewhere

- **The tensor/scalar-anisotropy concern for the headline persistence theorem (points at `#result-persistence-condition` / `#result-per-dimension-persistence`).** Multiple substrates flag that if the core persistence theorems are stated/proven only for scalar $\mathcal{T}$, they may be "dangerously over-optimistic for real-world agents with weak dimensions" — a company fast at UI bugs but slow at DB-schema changes dies when $\rho$ hits the schema even if *average* scalar tempo looks healthy (Claude, AUDIT-WORKING-829314; Claude, AUDIT-WORKING-742613; Claude, AUDIT-WORKING-584721). Watch-item routed to the persistence segments, not this definition.
- **Two-evolutionary-paths reach.** Speed-quality substitutability suggests two survival strategies in the same environment — fast/cheap/noisy reactive (insect) vs slow/expensive/calibrated deliberate (human) — but observation-noise gating puts a hard ceiling on the reactive path: above some $U_o$ no $\nu$ suffices, so some environments *require* deep deliberation and high-quality sensors. For raising an ELI's tempo, the lever is lowering $U_o$ (better tools/data) and managing $U_M$ (humility), not just more inference compute (Gemini, AUDIT-WORKING-193847). Aspirational reach; points at `04-eli-core/` infrastructure design.
#### Candidate experiment (2026-08-12, agent-proposed during an incident-response & forensics applications read)

The common-source saturation result ( #deriv-tempo-additivity) has a direct oversight-engineering reading: a monitoring stack whose feeds share an upstream source saturates at the shared-bias floor — no feed count buys past it, and the partially-correlated middle carries the closed-form redundancy penalty rather than collapsing to one channel's worth. A candidate experiment: instrument a multi-channel monitor over a drifting target with controlled channel correlation, and measure effective tempo vs the additive prediction across the independent / common-source / synergistic regimes — turning the signed-deviation theory into calibration guidance for oversight-channel independence. Agent-proposed forward pointer.
